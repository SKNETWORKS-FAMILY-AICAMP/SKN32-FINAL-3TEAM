# 산출물 현황

> **작성자** 오한빈 (팀장)
> **작성** 2026-08-14 · **최종 갱신** 2026-09-21 KST

> 🔄 **2026-09-18 — `docs/ohb/` 가 09-12 에서 멈춰 있었다.** 코드 행은 09-17 까지 왔는데
> 인계 다섯(09-13·09-14·09-14 밤·09-16·09-16 밤)·검토·판정정리·D초안 둘·라벨링 지시서 둘·
> 남은것 한 장이 **이름조차 없었다** — 머리말의 기준(「다른 사람이 찾아 읽을 것이면 올린다」)을
> 적어 놓고 지키지 않은 것이다. 아래 「2026-09-13~18 묶음」으로 한꺼번에 올린다.
> 🚨 **또 낡는다** — 손으로 맞추는 칸이다 (D-177). ⬜ 게이트 후보: `docs/ohb/*.md` ↔ 이 표의 대조.

> 🔄 **2026-09-12 밤 — 이 줄이 09-03 에 멈춰 있었다.** 본문에는 09-12 행이 둘 있었다.
> 집행계약 머리말에서 09-12 오전에 고친 것과 **같은 병**이 여기 남아 있던 것이다.
> 🚨 **손으로 맞추는 칸이라 또 낡는다** — 낡으면 「이 표가 지금 것인가」를 못 묻는다 (D-177).

> 🚨 **무엇을 이 표에 올리는가** (2026-09-12 밤 신설). 종전에는 기준이 없어서
> 「등재 누락」과 「등재 대상이 아님」이 구별되지 않았다 — `docs/ohb/` 23개 중 14개가
> 이름조차 없었다. **기준 — 다른 사람이 찾아 읽을 것이면 올린다.** 인계·검토·결정요청·
> 초안·계약·스키마·산출 스크립트가 그것이고, **내 작업 메모는 안 올린다.**
> ⬜ 마이그레이션은 **묶음 단위**로 올린다(`0005~0007` 처럼) — 파일마다 올리면 표가 이력이 된다.

> 제출본은 저장소가 아니라 **빌드 결과물**이다 (D-53).
> 🚨 **`launcher.py package` 는 아직 없다** — D-53 이 계획한 명령이고 미구현이다. 지금은 `python launcher.py pdf <문서경로>` 로 한 건씩 뽑는다.
> 상태: `현행` / `작업중` / `역사기록(소급수정 안 함)` / `예정`

## 개정 규칙

★ **버전은 「고칠 때」가 아니라 「PDF를 발행한 뒤」 올린다.**

```
마크다운 수정 … 수정 … 수정      -> 버전 그대로
PDF 발행 (요청 시)               -> 그 시점의 마크다운이 해당 버전의 확정본
그 뒤에 수정이 생기면            -> 머리말 버전을 올린다  v1.0 -> v1.1
```

- 작업 중에는 **버전을 올리지 않는다.** 초안 단계에서 번호가 늘면 어느 것이 확정본인지 흐려진다.
- **PDF 발행이 버전을 고정하는 사건**이다. 발행 후 수정은 새 버전에서 한다.
- 🚨 **PDF는 개정마다 다시 뽑지 않는다.** 제출본은 빌드 결과물이므로(D-53)
  **요청이 있을 때만** `scripts/build_pdf.py` 로 생성한다.
- `dist/` 는 `.gitignore` 대상이다. PDF를 커밋하지 않는다.

### 🔄 2026-09-01 — 버전이 사는 곳을 바꿨다 (팀장 결정)

🚨 **버전은 문서 머리말 한 곳에만 둔다. 파일명에는 붙이지 않는다.**

파일명에 붙이면 갱신할 때 두 곳을 손대야 하고, 실제로 어긋났다 —
`수집전처리_기획_v1.2.md` 의 내용이 **v1.3** 이었다. 같은 사실이 두 곳에 있으면
갈라지고, 갈라지면 어느 쪽이 맞는지 알 수 없다 (D-99 와 같은 형태다).

| | 원본 (`docs/**`) | 발행물 (`dist/*.pdf`) |
|---|---|---|
| 성격 | **계속 고쳐진다** | **그 시점에서 얼어붙는다** |
| 파일명 | 🚨 버전 **없음** — 경로가 고정돼야 참조가 안 깨진다 | ✅ 버전 **있음** — `03_작업일정_v1.0.pdf` |
| 버전 출처 | 머리말 (H1 또는 문서 정보 표) | 원본 머리말에서 자동으로 읽는다 |

- 이전 **발행물**은 지우지 않는다. 기획서가 v3.8까지 온 방식이 여기에 산다 —
  다만 그 대상은 마크다운 원본이 아니라 **PDF** 다.
- 버전을 읽는 방법도 한 곳에 둔다 — `scripts/docmeta.py`.
  `sync_project.py` 와 `build_pdf.py` 가 각자 정규식을 들면 그 둘이 또 갈라진다.

🚨 **버전이 없는 문서가 있다** — 사실원장 · 설계결정기록 · 산출물현황 · 검토표 ·
판정매트릭스처럼 **계속 고쳐지는 원장**이다. 이들에게는 **커밋 해시가 판(版)이다.**
없는 것에 억지로 `v1.0` 을 붙이지 않는다.

> **현재 상태**
> - `03_작업일정.md` — v1.0 PDF 발행 완료 → 🔄 **2026-09-03 `v1.1`** (Phase 0 재설정 · 배정 개정)
> - 🔄 `01_주제확정_및_실행일정.md` — **2026-09-03 `v1.2`** (공식 주차 `1W~9W`·게이트 날짜 일원화 · 역할 분담 개정)
> - 🔄 `01_기획/02_프로젝트기획서.md` — **`v3.9` 제출본 PDF 발행 완료 (2026-09-03)**. **→ 🔄 같은 날 `v4.0`** (과금 경계 D-69 정정 · 세 진입점)
>   발행물 `dist/제출/01_기획/CopyLane_프로젝트기획서_v3.9.pdf` (105쪽) · 제출 묶음 `dist/제출/CopyLane_기획단계_산출물_20260903.zip`

| 경로 | 문서 | 상태 | 제출 포함 |
|---|---|:-:|:-:|
| `docs/00_사실원장.md` | 사실 원장 (SSOT) | 현행 | ✅ |
| `docs/00_거버넌스_집행계약.md` | ★ **「코드로 강제한다」의 지도** — 저장소 게이트 **30개 (pytest 37건)** · doctor 검사 · DB CHECK 6 · `collect.py` 레코드 계약 · 수집 시점 필드. 🚨 **새 검사를 어디에 넣을지의 판단 기준**(D-89)이 여기 있다 | 현행 | ✅ |
| `docs/00_설계결정기록.md` | 설계 결정 기록 🔄 **D-01~D-207** (**결정의 단일 원장** — ADR 개별 파일 없음, D-58). 🚨 **범위를 여기 적지 않는 편이 낫다** — 2026-09-12 밤까지 「D-01~D-140」으로 **67건 낡아 있었다**(D-54 가 막으려던 사본 드리프트). 최신 번호는 결정기록 색인이 정본이다 | 현행 | ✅ |
| `docs/01_기획/01_주제확정_및_실행일정.md` | 주제 확정 · 10/26 역산 일정 | 현행 | ✅ |
| `docs/01_기획/02_프로젝트기획서.md` | 기획서 본문 (설계 전체) | 현행 | ✅ |
| `docs/01_기획/03_작업일정.md` | **공식 WBS 기준 작업일 산정 · 주차 배치 · 마일스톤** | 현행 | ✅ |
| 🔄 `dist/제출/01_기획/CopyLane_요구사항정의서.xlsx` | ★ **요구사항 정의서** — 기능 803(사용자 41화면·관리자 77화면) · 비기능 32 · **MVP 범위 판정**(핵심 34/부가 27/범위 밖 60) · 기획 문서 대응 · 0903 신규. 🚨 **원본이 `dist/`(gitignore)에 있다 — 위치 결정 필요**(D-53·D-90) | **작업중** | ✅ |
| 🔄 `dist/제출/01_기획/CopyLane_WBS.xlsx` | ★ **WBS** — 작업 53건 · 실명 소유자 · 시작/마감일 · 작업일 기준 갠트. 🚨 위 항목과 같은 위치 문제 | 현행 | ✅ |
| 🔄 `dist/제출/01_기획/CopyLane_팀역할분담표.xlsx` | ★ **팀 역할분담표** — 실명 배정 · 트랙 T1~T5 · 부하 곡선 · 이관 규칙(D-50 · D-94). 단일 출처는 사실원장 「팀 (5인)」 | 현행 | ✅ |
| `assets/brand/` | 로고·브랜드 가이드 (SVG 원본 + PNG) | 현행 | ✅ |
| 🔄 `launcher.py` | **작업 진입점 — Typer 이중 인터페이스 (D-51)**. 얇은 껍데기, 로직은 전부 밖에. 🔄 **2026-09-11 전면 개편 (D-189)** — 번호 메뉴 **33개 · 그룹 7 · `0` = 뒤로** · ⚠ 는 확인을 묻고 → 는 값을 묻는다. 🔴 **메뉴가 `subprocess` 로 자기 CLI 를 부른다** — 종전에는 `@app.command()` 함수를 파이썬에서 직접 불렀고, `typer.Option(False, "--write")` **객체가 참**이라 「보기만 한다」 버튼이 **매번 `--write` 를 실행해 파생물을 덮어썼다.** 경로가 하나면 갈릴 자리가 없다. `onboard`·`inventory`·`status` 는 있는데 메뉴에 없어 아무도 못 부르던 명령이라 신규 등재. 🚨 게이트가 아니라 임포트 시 자가검사(`_check_menu()`)다 — 번호 중복·이름 불일치를 `SystemExit` 로 세운다 | 현행 | — |
| `setup.bat` · `setup.ps1` | **부트스트랩 — 더블클릭 1회로 환경 완성.** uv·PATH·PYTHONUTF8·의존성·훅·`.env`·DB·게이트 | 현행 | — |
| `pyproject.toml` · `uv.lock` · `.python-version` | **5인 환경 동일성의 실체** — 파이썬 패치 버전까지 고정 | 현행 | — |
| `docker-compose.yml` · `scripts/db/init/` | 로컬 DB — pgvector 컨테이너. `127.0.0.1` 만 바인딩 (P3-14) | 현행 | — |
| `tests/test_governance_layout.py` | 🔄 **수를 여기 적지 않는다** (2026-09-12 밤 · D-54). 종전 「게이트 40건(09-09 밤)」은 **함수 수를 케이스 단위로** 적은 것이라 집행계약·원장과 세는 대상이 달랐다 — 분모·분자는 `docs/00_사실원장.md` 「거버넌스 게이트」 행이, 목록은 `docs/00_거버넌스_집행계약.md` §1 이 단일 출처다 (🆕 서명↔원장 일치 · **판정 문언 단일 출처**(masking·attribution·license) — 생성물 손편집으로 마스킹 정책 10줄이 사라진 사고를 검사로 바꿨다. 🚨 공백을 지우고 비교한다 — 줄바꿈 위치가 결과를 바꾸면 문언 검사가 아니라 서식 검사다) (🚨 목록의 단일 출처는 `00_거버넌스_집행계약.md` §1) — 저장소 구조·등급·레지스트리를 코드가 검사 (D-19 · D-15 · D-23 · D-71 · D-89). 🚨 등급↔용도 상한 · NOREDIST 정합 · 수집 시작 시 2인 확인 · 모델 배포용도 · **17 raw 소비 금지 · 18 manifest 원장 · 19 판정 근거 존재**(D-100). 🔄 `.gitignore` 게이트를 **동작 검사**로 바꿨다 — 패턴 문자열을 보면 실패할 수 없는 단언이 된다 (D-170) | 현행 | — |
| 🔄 `app/models.py` | ★ **런타임 층 ORM — 7표** — `work_doc` · `copy_sentence` · `batch_run` · `judgment`(**다형 참조**) · `upload_blob` · `slot_assignment` · `app_account` (D-103 · D-104 · D-105 · D-213). 🔄 **2026-09-16 — 「6표」가 두 곳에 낡아 있었다**: `0001` docstring 이 6, 여기 열거가 5(`upload_blob`·`app_account` 누락). 실측은 **7** 이다 (`__tablename__` 전수 · `app_account` 는 0012 에서 늘었다). 🚨 **수는 여기 적지 않는 것이 원칙이나**(D-54) 층의 구성은 목록이므로 목록을 정본으로 둔다 | 현행 | — |
| 🔄 `alembic/` · `alembic.ini` | **마이그레이션.** 🚨 `env.py` 의 `ischema_names['vector']` 가 이 디렉터리의 존재 이유다 (D-84 ⑪ · 8-4 체크리스트 11) · `include_object` 가 autogenerate 의 DROP 을 막는다. 🔄 **2026-09-16 — `0001`~`0015` 다. 그리고 `0001` 의 입력이 **동결본** `db/schema_0001.sql` 로 고정됐다 (D-225) — 종전에는 실행 시점에 `db/schema.sql` 을 읽어 「0003 이 도는 DB 의 모양」이 사람마다 달랐다. 🚨 **스키마를 바꿀 때 고치는 곳은 둘이다** — `db/schema.sql` + 새 마이그레이션. 갈리는 것은 `launcher.py db-drift` 가 잰다.** 🔄 **2026-09-12 밤 — 상태를 고쳤다: 「초기 리비전 미생성」은 `0001` 이 선 뒤로 참이 아니었다.** 아래 `db/migrations/0005~0007`·`0008~0010`·`0011` 행과 같은 사실을 한 문서 안에서 다르게 말하고 있었다 | 현행 | — |
| `.pre-commit-config.yaml` | 커밋 훅 — ruff · gitleaks · 기본 검사 (P0-2) | 현행 | — |
| `scripts/build_pdf.py` | 문서 → 제출용 PDF 빌드 (D-53). 발행물 이름에 버전이 붙는다(`docmeta`). 🚨 **의존성은 `docs` 그룹** — `uv sync --group docs` + `uv run playwright install chromium` 이 선행. 🔄 **2026-09-17 — `inline_images()` 추가.** 임시 HTML 이 `dist/` 에 쓰이므로 문서의 상대 경로 그림이 **조용히 빈칸**이 됐다(에러도 안 났다). 문서 위치 기준으로 찾아 data URI 로 박고, **없으면 멈춘다** (D-72). 도면이 든 첫 문서에서 드러났다 | 현행 | — |
| `scripts/collect.py` | 제재 사례 레코드 계약 · **D-65 범위 판정 집계** (`report`) | 현행 (수집 어댑터는 T1) | — |
| `scripts/doctor.py` | 환경·거버넌스 진단 (D-51 · D-89) — **기기 검사 12종**(9·10·12·13은 pytest로 이관) + 신규 17~19 | **작업중** — 검사 목록만 있고 본문 미구현. 🔄 런처에서 `@stub` 처리해 눌러도 죽지 않는다 | — |
| `collect/` | **수집기 공통 모듈** — `registry`(게이트) · `store`(원장·무손상 저장) · `http`(간격·재시도) · `env`(🚨 `.env` 를 읽는 유일한 곳). 🔄 `store.VOLATILE`·`identity_sha256()` — **동일성 판정은 정규화문, 저장은 원문** (D-168). `mfds_press` 는 `jsessionid` 때문에 원문 해시로는 같은지 물을 수 없다 | 현행 | — |
| 🔄 `collect/law_api.py` | **법제처 OPEN API 수집기 (S1-01 · S1-02)** — 법령 9 · 고시 11 · 판례 · 재결례. ⛔ **「`LAW_OC_KEY` 대기」는 낡은 표기였다** — 승인 2026-08-20 · 수집 2026-09-02 완료인데 9월 17일까지 남아 있었다. 🆕 **`--all`** (2026-09-17) — 질의 없이 전량을 받아 사건명 필터(`_in_domain`)만 건다. `decc` 전량 35,213 중 **392**(1.1%)가 통과하고 본문 272건이 약 2.3분이라 **규칙 ②(감당 가능하면 전량)가 성립**한다. 🚨 빈 질의는 사건명·본문 검색이 같은 전량을 주므로 `by_body` 를 비운다(페이징 353장을 두 번 돌지 않는다 · 규약 5). 🚨 질의 경로는 남긴다 — **`prec` 은 아직 전량 규모를 안 쟀다** | 현행 | — |
| `collect/ftc_body.py` | ★ **공정위 결정문 본문 수집기 (1층 본체)** — 목록(`lawSearch`) → 본문(`lawService`) 2단. 1,087건 확보. 🚨 12건은 원천에 결정일자가 없어 `{번호}.xml` 로 저장한다 (D-115 ③이 경계한 고정 문자열 충돌과 다르다) | 현행 | — |
| `collect/mfds_press.py` | **식약처 보도자료 수집기 (평가 홀드아웃)** — probe → index → 본문 3단. 🚨 `use="U3"` (U1 deny) · 공공누리 미부착 격리 · 색인 이어받기 | 🔄 **현행** — 2026-09-09 본문 105 · 첨부 PDF 105 확보 (D-132 로 공공누리 해소) | — |
| `collect/law_annex.py` | 🔄 **[별표] 파서** — 처분 축 분해 · 조문 상속. **5법령 · 별표 24 · 파싱 243행** (2026-09-09 실측). 🔴 서식은 기본으로 받지 않는다 — 화장품법 시행규칙이 별표 88행 · **서식 157행(64%)** 이라 2인 대조 목록을 덮었다. `--forms` 로 연다 (D-72). 🔄 `main()` 끝에서 **파싱 감사를 부른다** — 「나중에 돌린다」는 안 돈다 (D-98 개정 · D-146) | 현행 | — |
| `preprocess/` | ★ **전처리 층 (D-116)** — `RAW_READERS` 가 처음부터 가리키던 자리. 🚨 `scripts/` 는 `data/raw/` 를 읽지 않는다 (D-19 · D-92) | 현행 | — |
| `preprocess/text.py` | ★ **문자 정규화 공용 모듈 (D-117)** — `sep_norm()` 이 나열 구분자를 `·` 로 편다. 🚨 같은 함정을 세 번 밟고 만들었다. **매칭은 정규화문, 보관은 원문**. 🔄 `sheet_lengths()`·`write_sheet()` — 표본 길이를 보이게 하고 **사람이 채운 검증셋을 덮어쓰지 않는다** (D-169) | 현행 | — |
| 🆕 `preprocess/split.py` | ★ **골든셋 분할 [P12]** — `doc_id` 단위 출처 분리. `ftc` 를 문서 단위로 봉인해 평가로 돌린다. 🔴 `unmeasurable`·`same_source` 를 산출에 박는다 (D-171) | 현행 | — |
| 🆕 `preprocess/dictionary.py` | ★ **[P6] 금지 표현 사전** — 669종. 조문이 항목을 고른다. 🔴 `적법중첩` 23종은 단독 판정 금지 (D-156) | 현행 | — |
| 🆕 `preprocess/inject.py` | ★ **[P10] 결함 주입** — 1,375건. 규칙이 곧 라벨. 🔴 전부 `split: train` · 틀 다양성을 함께 잰다 (D-173) | 현행 | — |
| 🆕 `preprocess/golden.py` | ★ **골든셋 물질화 (D-143)** — 문장·라벨을 한 파일로. 🔴 문구 단위 2차 필터 — 문서를 갈라도 문구는 겹친다 (D-174) | 현행 | — |
| 🆕 `scripts/eval_rule.py` | ★ **판정기 B + 첫 실측 지표** — 룰 기반 사전 매칭. 인코더와 학습을 공유하지 않아 오류가 독립이다 (기획문서 6-2). 🚨 낮은 것이 정상 | 현행 | — |
| 🆕 `scripts/data_status.py` → `docs/03_데이터/데이터현황판.md` | ★ **데이터 현황판 (생성물)** — 「무엇을 쓰기로 했고 무엇을 안 쓰기로 했나」의 단일 출처. 쓴다 31 · 수기 3 · 보류 13 · 안 쓴다 13 · 배제 10. 🚨 **손으로 적으면 갈린다** — 2026-09-03 판이 그렇게 낡았다 (D-54). 🚨 「받았다」의 근거는 **파일과 원장**이지 `collected_at` 이 아니다 — 둘을 함께 낸다 | 현행 | — |
| 🆕 `docs/03_데이터/전처리_결과서.md` | ★ **전처리 결과서 (3W 제출)** — 원문 → 문구 → 골든셋 변환 사슬 12절. 원천 4갈래 · ftc 8,253 → 4,725행 · 골든셋 6,626 · 사전 536 · 평가 197. **§10 알고 있는 결함 여섯을 크기 순으로** 놓는다 — 1번이 중복 1,249행(18.8%). 🚨 **수는 전부 사실원장 09-17 절 참조이고 이 문서는 옮겨 적은 것이다** — ⬜ 4W 에 생성기를 붙인다 (D-90 · D-54) | 현행 | ✅ |
| 🆕 `docs/03_데이터/_그림/전처리_흐름.svg` | **도면 원천** — 원천·전처리·파생물 3밴드 + 회수 깔때기. 🚨 **손으로 그렸다** — 수가 바뀌면 여기도 고친다. `build_pdf.py` 가 data URI 로 박아 PDF 에 넣는다 | 현행 | — |
| 🆕 `scripts/load_db.py` | ★ **거버넌스 DB 적재** — 2026-09-09 까지 **적재 코드가 0줄**이었다. fail-closed(모르는 열거값이면 멈춘다 · **D-220** — 종전 주석의 `D-72` 는 오기였다). 🚨 CHECK 둘을 못 지나는 소스는 **넣지 않고 이름을 낸다** — 실측 **통과 32 · 막힘 15**(2인 확인 미완 10 + attribution 없음 5 · 2026-09-13). 🔄 **2026-09-13 — 「멱등(`ON CONFLICT`)」은 넣는 쪽만 맞는 말이었다** (D-223): 키가 바뀌면 옛 행이 남는다. `sweep_golden()` 을 붙였고 `load_sources()` 가 **거둔 것·남긴 것**까지 돌려준다. 실측 `golden_sample` **2,812 → 1,910** · `source` **33 → 32**. ⛔ **하위가 있으면 이름만 내고 멈춘다.** ✅ 골든셋은 들어간다(옛 「못 넣는다」는 해소됐다) | 현행 | — |
| 🆕 `preprocess/chunk.py` | ★ **[P5] 청킹** — 이름표만 있고 코드가 0줄이었다. **청크 2,585개** 🔴 **`chunk_id` 유일성을 게이트로 건다** — 첫 적재에서 2,594 를 보내고 2,297 만 들어갔다(`ON CONFLICT` 가 297행을 덮었다 · D-149). 네 번 고친 끝에 **원천의 `조문키`** 를 쓴다. (일반 763 · 화장품 951 · 건기식 450 · 식품 430). 🚨 **조문 단위로 자른다** — 임의 길이로 자르면 한 청크가 두 호에 걸쳐 「어느 호를 인용하는지」를 못 말한다. 길어서 쪼갠 121개는 `part` 가 원 조문을 가리킨다. ⚠️ 토큰 수는 글자 상한 700 으로 어림했다 — 실측은 W1 의 남은 일 | 현행 | — |
| 🆕 `scripts/embed.py` | ★ **KURE-v1 임베딩 → pgvector** — 임베딩 코드가 0줄이었다. 🚨 `--check` 가 **`vector(1024)` 와 모델 차원이 맞는지** 잰다(DB 불필요) — 아무도 안 재 봤다. 어긋나면 고칠 곳 셋을 이름으로 낸다 | 현행 (🚨 `--check` 미실행) | — |
| 🆕 `app/api.py` | ★ **FastAPI 골격** (D-42 · D-135 · D-56) — `/health`(층별 행 수) · `/search`(🚨 아직 벡터가 아니라 부분일치) · `/judge`(🚨 **501** — 가짜 200 을 내지 않는다). 🔴 거버넌스가 API 에도 걸린다 — `/search` 는 `source_use.U2_rag` 가 열린 것만 돌려준다 | 현행 | — |
| 🆕 `docs/ohb/미수집_18건_진행계획_2026-09-09.md` | **미수집 18건을 셋으로 가른다** — 명령 한 줄(1) · 수집기 필요(1) · 사람이 받아야(13) · 파생 산출(1). 🚨 **판정을 실제로 막는 것은 둘뿐** | 현행 | — |
| 🔄 `preprocess/hf_api.py` | ★ **건기식 API 2종 — 🚨 축이 둘이다** (D-185). 정본 축 I-0050 → `hf_api_labels.jsonl`(2층 적법라벨 · 지위: 인정) · 관측 축 I-0040 → `hf_display_claims.jsonl`(표시 문구 관측 · **지위를 안 붙인다**). ⛔ 종전에는 둘을 한 파일에 「지위: 인정」으로 담아 **위반 예시가 적법 근거로 적재**돼 있었다(2024-19). 🔴 짝짓기는 **(인정번호, 원료명)** — 인정번호는 I-0040 에서 외래키가 아니다(773행/고유 768 · 중복 4건이 서로 다른 원료). 경계 사례 후보 **122건**(단정형 7). 🔴 「새로 얻는 것 316」 재검 → **정본 축만 세면 44** (D-178) | 현행 | — |
| 🆕 `tests/test_hf_api.py` | **축이 둘이라는 것을 코드가 계속 기억하는가** — 16건(`gate` 6). 축 분리 · 관측 행에 `지위` 없음 · 원료 불일치는 짝짓지 않음 · 대상 한정이 빠지면 「좁음」. 🚨 둘 다 문자열이라 어디서도 안 터졌다 — **테스트가 없으면 되돌아간다** | 현행 | — |
| 🆕 `preprocess/law_article.py` | ★ **3층 판단규범 — 조문 본문** (2,216 노드 · 법령 20건). 🔴 **조문 형식이 아닌 행정규칙 6건**은 `<조문내용>` 하나에 전문이 온다(10K~23K자) — 공정위 심사지침 5종 + 부당표시광고 유형 지정고시가 **1행으로 나오고 있었다**. 마커 파서를 붙여 열었다. 🔴 `admrul_34650`(건강기능식품의 기준 및 규격)은 **원천이 본문 대신 첨부를 준다** — 표시한다 | 현행 | — |
| 🔄 `preprocess/law_case.py` | **5층 반례 — 판례 84 · 재결례 392**(2026-09-17 · 120 → 392). 🚨 필드가 달라 한 파일로 합치지 않는다 (판결요지/재결요지 · 법원명/재결청). 🔴 판례 84 중 **판결요지가 없는 것이 43건** | 현행 | — |
| 🆕 `preprocess/decc_extract.py` | ★ **재결례 → 광고 문구 + 근거 조문** (2026-09-17 · 5판). 「4. 관계법령」→ 조문 · 「인정사실」·「피청구인 주장」→ 문구. 🚨 재결서는 **같은 문구를 3개 절에 반복**해 절을 안 가르면 수가 3배로 부푼다. **392건 실측 — 문구 543 · 조문 회수 373 · 우리 법 19건 70문구**(인용/취소 3 · 기각 16) → `decc_phrases.jsonl`. 🔴 **결함 셋을 연달아 잡았고 셋 다 그럴듯한 틀린 수를 냈다** — ① 「관계법령」 절만 봐서 **22건을 놓쳤다**(재결서가 조문 제목을 절 이름으로 쓴다) ② `ART` 가 중첩 수량자라 전문에 걸면 **111초**로 폭주(문자 클래스로 **0.05초**) ③ 이름을 통째로 버려 **진짜가 4건 죽었다**(19→15 · `_CANON` 으로 잘라내기로 회복). 🚨 전문 경로는 신뢰도가 낮아 `조문출처` 를 함께 낸다 (D-178) | 현행 | — |
| 🆕 `data/derived/decc_phrases.jsonl` | **생성물 70행** — `확정유형`·`후보유형`은 **비어 있다. 사람이 채운다** (D-66 · D-172). `data/**` 라 미커밋 | 현행 | — |
| 🔄 `preprocess/guide_label.py` | ★ **해설서 위반문구 → 평가셋** (2026-09-17 · **2판** · D-240 초안). 🔴 **1판은 축이 틀렸다** — 내가 쓴 별표1 앵커를 정본으로 삼아 라벨률이 **127/1,834 = 6.9%** 였다. 그런데 원천은 **1,834행 전부에 라벨이 있다**(결측 0). 떨어진 1,707행은 **원천이 아니라 앵커의 한계**였다. ★ 2판은 정본 축을 **원천라벨 3분류**로 되돌린다(라벨률 100%) — 거짓·과장·기만 1,145 → 제4·5호 · 질병/의약품/건기식 558 → 제1·2·3호 · 비교·비방 131 → 제6·7호. **제8조 7/8호를 덮고 제8호(사행심·음란)만 미포함**(제정 때 신설). 🚨 레코드마다 **`판정지위: 심의지적`** — 행정처분·판결 확정이 아니다 (D-237·D-240). ⛔ 앵커는 `--anchors` 로 갈라 낸다 — **평가셋이 아니다** (D-160). 게이트 셋: 원천라벨 결측 fail-closed · 매핑 없는 라벨 정지 · `CANDIDATES`↔`LABEL_TO_ARTICLE8` 키 대조 (D-99) | 현행 | — |
| 🔄 `data/derived/evalset_by_statute.jsonl` | **1,834행 · 867,592 B · sha `5b70b638c1a3`** — 원천라벨 축. `확정유형`은 **전부 비어 있다**(사람 몫 · D-66·D-172). 시트는 `scripts/label_sheet.py export … --who <이름>` 이 그대로 뽑는다(`후보유형` 칸을 읽는다). 🚨 **컨테이너에서 돌린 것**이라 클론 B 재실행 대조가 남았다 (D-146). `data/**` 라 미커밋 | 현행 | — |
| 🆕 `data/derived/guide_anchor_candidates.jsonl` | **1,834행 · 382,409 B · sha `11f7ece18986`** — ⛔ **평가셋이 아니다.** 앵커 걸림 127 / 없음 1,707. 🔴 **`의약품_오인` 3건 중 2건이 오탐**(「무**농약**」·「시간**절약**」이 `[가-힣]{2,}약(?![가-힣])` 에 걸린다). 수를 그대로 인용하지 않는다 (D-191). `data/**` 라 미커밋 | 현행 | — |
| 🆕 `tests/test_guide_label.py` | **게이트 10건** — 두 표(`CANDIDATES`↔`LABEL_TO_ARTICLE8`) 키 일치 · 가리키는 호가 이름표에 있음 · **제8호는 아무 라벨도 안 가리킴**(승계 근거 ㉰가 바뀌면 여기서 터진다) · 원천라벨 결측 fail-closed · 모르는 라벨 정지 · `확정유형` 빈 채로 나옴 · `판정지위` 전 행 부착 · `후보유형` 칸 이름 불변(`label_sheet` 가 읽는다) · 앵커가 평가셋 행에 안 들어감 · 🔴 **앵커 오탐이 실재함을 못박는다**(「무농약」·「시간절약」 — 고쳐지면 원장·D-240 을 같이 고치라고 터진다) | 현행 | — |
| 🆕 `data/derived/casebook2021_labelsheet.jsonl` | **사례집 2021판 전사 250행** — 광고사례(캡처 실문구) **208** · 위반사례(식약처 요약문) 42(고유 어휘 118종). 원본 54쪽을 눈으로 보고 옮겼다 — **OCR 은 후보 제시용**이다(「불면증→북며증」·「건망증→건망긍」 오독 실측 · D-133 ⚠️). 🚨 40자 초과 42건은 자르지 않고 `초과40` 으로 표시만 했다 (D-133 ②). ⛔ 자리표시자 틀 2건은 `비고` 로 격리. ⛔ 호 9(상호·상표)는 **문구 0** — 그림으로만 성립해 범위 밖 (D-192). `data/**` 라 미커밋 | 현행 | — |
| 🆕 `preprocess/cosmetic.py` | **제품 사실 — 화장품 원료 53,088행** (성분 21,897 · 사용제한 31,191 · 금지 23,452/한도 5,422/한도금지 2,317). 🚨 **판정 경로가 아직 없다** — 제품 지위 확인이 붙을 때 쓴다 | 현행 | — |
| 🆕 `preprocess/law_norm.py` | ★ **[별표] 산문 → 3층 규범 노드** — `1. → 가. → 1)` 계층을 자른다. 표 0행 별표 12건 · 노드 **300개**. ★ 013453 [별표 1]이 **위법 유형의 법령상 정의 그 자체**(본문 8호). 🔴 **구역을 가른다** — 「비고」의 적용 제외 2호가 위법 유형 1·2호로 읽히던 것을 막는다 (D-153·D-156). 🚨 `text` 는 매칭용이고 화면은 `lines` 를 쓴다 — 고정폭 접힘은 복원이 애매하나 `norm()` 이 공백을 지우므로 매칭에는 영향이 없다 (D-98·D-117) | 현행 | — |
| 🆕 `tests/test_law_norm.py` | **게이트 5건** — 비고 구역 분리 · 구역 내 `path` 유일 · 이어지는 줄이 노드를 만들지 않음 · 깊이는 마커 모양이 정함 · **구역 이름이 「비고」를 먹지 않음**(`endswith("고")` 로 걸렀다가 실제로 먹었다) | 현행 | — |
| 🆕 `docs/ohb/결정요청_2026-09-09_밤_5건.md` | **판정 대기 5건** — 판정 C(robots) · D-132 준용 5건 단서 4호 · attribution 조립 위치 · 소스 라이선스 게이트 · 위법 유형 제8호 | 현행 | — |
| 🆕 `scripts/annex_audit.py` | ★ **[별표] 파싱 자동 감사 (D-98 개정)** — 사람이 하던 전량 대조(243행 × 2인 = **3.4인시**)를 산수로 바꾼다. 🔴 **신호는 별표의 성격으로 가른다** — 「못 읽은 처분 칸」 54행이 전부 과징금·과태료 별표라 전 별표에 걸면 **오탐 54건으로 시작**한다 (D-167). 🔴 **기준선이 본체다** — 지금 값이 깨끗해 0 을 내므로, 행 수가 줄면 실패한다 (D-170) | 현행 | — |
| 🆕 `scripts/annex_baseline.json` | **생성물** — 별표별 행 수 기준선 12건. 법 개정 시 `--update` 로 사람이 갱신한다 | 현행 | — |
| 🆕 `tests/test_annex_audit.py` | **게이트 6건** — 감사가 **무너지는지**를 본다(못 읽은 처분 칸 · 조문 없는 처분 행 · 행 감소 · 미등재 별표) + **오탐하지 않는지**(빈 처분 칸 · 과징금 별표). 🚨 통과를 확인하는 파일이 아니다 (D-170) | 현행 | — |
| 🆕 `scripts/label_merge.py` | **라벨 취합 · Cohen's κ** — 갈린 항목을 다수결로 덮지 않고 목록으로 낸다 | 현행 | — |
| `preprocess/ftc_triage.py` | ★ **1층 라벨 선별 + 회피 표기 계수** — 737/1,087건. 🚨 기준은 법조가 아니라 **사건명 행위유형** (D-116) | 현행 | — |
| `scripts/extract_rationale.py` | ★ **판정 근거 추출기** — `판정매트릭스.html` 🔄 77 엔트리의 `why`·용도별 근거·URL 을 뽑는다. 🚨 `ALIAS` 표가 매트릭스 id ↔ 레지스트리 키를 잇는다 (D-99 · D-100) | 현행 | — |
| `scripts/registry_rationale.yaml` | **생성물** — 검토용 판정 근거 사본. 손으로 고치지 않는다 (원본은 판정매트릭스) | 현행 | — |
| `scripts/review_sheet.py` | ★ **S0-14 검토표 생성기** — 위험도 A/B/C 구간 · 판정 근거 · 근거 URL · 이견 절차. 🚨 미확인 근거는 단독으로 A 구간 (D-101) | 현행 | — |
| `scripts/sync_project.py` | ★ **프로젝트 사본 생성기** — `MAP` 이 레포↔프로젝트 대응의 단일 출처. 사본 첫 줄에 `버전 · 갱신일 · 그 문서를 마지막으로 바꾼 커밋` 스탬프를 찍는다 | 현행 | — |
| `scripts/docmeta.py` | **버전을 읽는 단 하나의 방법** — H1 과 문서 정보 표에서만 읽는다. `sync_project` 와 `build_pdf` 가 공유한다 | 현행 | — |
| `docs/02_설계/청크_스키마.md` | RAG 청크 계약 | **작업중 (W1)** | 예정 |
| `docs/02_설계/DB_스키마.md` | DB 설계 초안 | **작업중 (W1)** | 예정 |
| `docs/02_설계/LangGraph_상태_스키마.md` | 그래프 상태 계약. 🔄 2026-09-10 — TODO 셋이 닫혔다(Pydantic 계약 · 버전 핀 1.2.11 · 스텁 배선). ⬜ 남은 것: 체크포인터 TTL(D-129) · 페르소나 팬아웃 reducer · 위법유형→불가사유 매핑표(D-59) | **작업중 (W1)** | 예정 |
| `docs/03_데이터/수집전처리_기획.md` | **수집·전처리 실행 사양** — 역산 · 인벤토리 · 수집기 12종 · P1~P12. 🔄 레지스트리 스키마를 실물과 일치시킴 (D-90) | 현행 | ✅ |
| `docs/03_데이터/수집리스트.md` | **착수 순서 45건** (S0 선행 ~ S5). 🔄 S0-14(2인 확인 — 용도가 열린 소스 전건 31, 🔄 미서명 **12건** 남음 · 사실원장 참조)·S0-15·S1-11 추가 | 현행 | ✅ |
| `docs/03_데이터/소스분석.md` | **소스 73종 판정 근거 · 페르소나 설계.** 🔄 2-0-3절 레지스트리 반영 (D-90) | 현행 | ✅ |
| `docs/03_데이터/판정매트릭스.html` | **소스 🔄 77종 × 판정 10축 한눈보기** (등급·제약·비용·가치·용도) | 현행 | ✅ |
| `docs/02_설계/거버넌스데이터층_DDL.md` | **거버넌스·데이터 층 18테이블 DDL 실물.** 🚨 인증·런타임은 범위 밖 — `DB_스키마.md` 와 합쳐야 WBS 산출물이 완성된다 | 현행 | ✅ |
| `docs/03_데이터/전처리_사양.md` | `TRANSFORM_RULES` 양방향 단일 규칙표 · 측정 축 3종 (D-74) · **문자 수준 정규화 층과 회피 표기 축 (D-84 ①)** | 현행 | ✅ |
| `docs/01_기획/경제성_근거.md` | 채점 D축 근거 — 자체 산출 · 시장 규모 · 대체 비용 (D-75). 🔶 **대체 비용 부분 확보**(협회 수수료 2건) · 소요일 미확보 | **작업중** | ✅ |
| `docs/02_설계/평가_지표.md` | 평가 지표 4층 — L1 단계별 품질 · L2 통합(oracle ablation) · L3 운영(응답시간 예산·원가) · L4 거버넌스 (D-77) | **예정** | 예정 |
| 🔄 `docs/ohb/인계_2026-09-03_1층확보.md` | **인계 — 1층 본체 1,087건 · 선별 737건 · 전처리 층 개설.** 🚨 `mfds_press` 공공누리 판정에서 멈춰 있다 (D-118 ⑤) | 현행 | — |
| 🔄 `docs/ohb/인계_2026-09-07.md` | **인계 — 2인 확인 판정 3건(쟁점 A·B·E) · 서명 13 · 등재 3 · ftc 마스킹 2패스 · 2층 655건 수집.** 🚨 §0-1 이 초판 오류를 적는다 — **09-06 인계를 안 읽고 작업해** 셋을 「신규」로 잘못 적었고 폐기된 수(737)를 썼다. §6 조용한 실패 6건 · §7 D-141 후보(휘발성 필드) | 현행 | — |
| 🔄 `docs/ohb/공공누리_판정근거_2026-09-06.md` | **공공누리·저작권 판정 근거표 (2인 확인용)** — 저작권법 제24조의2·제36조·제37조 원문 · 식약처 정책 3문장 · robots 3종. 🚨 **쟁점 A·B·E 결론 칸이 비어 있다 — 자료이지 판정이 아니다.** 🔄 09-07 증보: 쟁점 D·§4-2 **해소**(세 도메인이 같은 정책 페이지를 쓴다) · §3-2 `nedrug` · 쟁점 E(단서 3호·`공개종료일자`) 신설. **이 표 하나가 `mfds_press`·`foodsafetykorea`·`nedrug` 셋을 동시에 막고 있다** | 현행 (판정 대기) | — |
| 🔄 `docs/ohb/결정요청_13건_2026-09-03.md` | **결정 요청 13건** — 위험도 값 · 9/11 게이트 · 홀드아웃·공공누리 · 2인 확인 분배 · MVP 승인 · 검증 ⑥⑦ · 리소스·슬롯 · P4·D7·O10 · DDL v1.2 · 임의값 4 · 이월 5. 맥락·대안·트레이드오프·★추천 | 현행 (채택 전 = 결정 아님) | — |
| 🔄 `docs/ohb/데이터현황판_2026-09-03.md` | **데이터 현황판 (팀원 작업 지시용)** — 층별 충족도 · 소스 40 현황 · 작업 묶음 A~F (수집기 5+8 · 2인 확인 12 · 다운로드 11 · 판정 4 · 수기 1). 🚨 스냅샷 — 상태는 레지스트리·사실원장이 원본 | 현행 | — |
| 🔄 `docs/ohb/정합성검토_2026-09-03.md` | **문서 정합성 검토·정정 기록 17건** — 레지스트리 실물 40 · 게이트 30/37 · Phase 0 9/9 · 배정 개정 전파 · 사본 동기화 절차 | 현행 | — |
| 🔄 `docs/ohb/설계검토_아키텍처_2026-09-03.md` | **설계 논리 검토** — 🚨 지금 결정할 것 R1~R8(보류 처리 · K 소진 종착 · 위험도 R0 · 근거 불일치 정의 · D-96↔D-122 · K 의미 · 9/11 게이트 · 홀드아웃) + 파이프라인 17 · 데이터 13 · 운영 12 | 현행 (채택 전 = 결정 아님) | — |
| 🔄 `docs/ohb/회신_2026-09-02_배치검수_제안검토.md` | 박수진·소성민 제안 검토 회신 — **용어 대응표 · 채택/기각 · 다음 할 일** (D-105) | 현행 | — |
| `docs/04_보안/보안점검.md` | **보안 점검 — P0~P3 19건 · 법적 의무 · 구조적 강점 8건** | 현행 | ✅ |
| `docs/05_배포/배포계획.md` | **AWS·Ubuntu·MobaXterm 배포 계획 — 아키텍처 · 비용 · 절차 · 일정 (D-79)** | 현행 | ✅ |
| 🆕 `README.md` | ★ **공개 저장소 첫 화면** — 문제 · 진입점 셋 · 판정 코어 불변식 넷 · 거버넌스 6 · 정직함(막힌 구간·측정 불가) · 실측 상태표. 도면 15장 embed · `<details>` 6 · 로고 테마 전환. 🚨 수치는 사본이고 원본은 사실원장이다 (D-54) | 현행 | ✅ |
| 🆕 `DISCLAIMER.md` | **고지 및 유효 범위** (169→216줄). 🔄 09-11 증보 — §1-1 진입점 셋 · §2-2 판정 **4값** · §4 외부 전송 금지가 구조임(D-184)·금액 환산 안 함(D-182) · 🆕 **§6-1 저장소를 클론한 사람에게**(AS-IS · 데이터 미포함 · 원천 이용 조건 직접 확인 · 라이선스 부재=모든 권리 유보) | 현행 | ✅ |
| 🆕 `발표자료/중간발표_2026-09-15/` | **심사위원용 제출 폴더** — 발표 PDF 25쪽 + 기획 4 · 화면 2 · 설계 4 + 안내 README. 12파일 14MB. 🚨 **zip 은 커밋하지 않는다** — 훅(5MB)에 막히고 GitHub 에서 미리보기가 안 된다. 중복(도면집·PNG·MD)을 빼 22.8→14MB | 현행 | — |
| 🆕 `assets/diagrams/` | **도면 21장** — 번호(P/U/W/A/D/E/G/S)가 식별자. 파일명은 ASCII, 번호↔제목 대응표는 폴더 README. 🚨 도면의 수치는 그린 시점 실측이고 정본은 사실원장이다 | 현행 | — |
| 🆕 `.github/workflows/gate.yml` | ★ **CI 게이트 (2026-09-11)** — `tests`(windows-latest: `uv sync --frozen` → `ruff check` → `pytest -m gate -rs` → 전체) · `hooks`(ubuntu-latest: `pre-commit run --all-files`). 🚨 **`paths-ignore` 를 넣지 않는다** — 문서만 고쳐도 게이트가 돈다(`test_governance_layout` 이 문서를 읽는다). 🔴 **176 중 7 은 여기서 안 돈다** — `data/**` 파생물이 있어야 한다(D-19). ⬜ Ubuntu 축은 배포 리허설(9/22) 전 | 현행 | ✅ |
| 🆕 `.github/workflows/secrets.yml` | **비밀 스캔 — 이력 전체** (`fetch-depth: 0`) · 주 1회 + PR. 🚨 `gitleaks-action` 은 **org 계정에 라이선스를 요구**해 컨테이너로 직접 돈다. 🔴 **D-188 을 대체하지 않는다** — 하한선이지 점검표가 아니다 | 현행 | ✅ |
| 🔄 `assets/brand/` | **브랜드 자산 12종** — 그라디언트 판 (D-186). 🔴 **라스터가 정본**(벡터 원본 없음) — 인쇄·대형 불가 · 파비콘 16px 뭉갬. 옛 파랑 판 22개는 `legacy/` 에 보존 (2주차 제출본이 그 로고를 쓴다) | 현행 | — |
| 🔄 `docs/ohb/인계_2026-09-10.md` | **인계 — Phase 0 마감 · 클론 A → 클론 B.** walking skeleton 한 바퀴 · DB 층 충족(golden 1,908 · chunk 2,585) · 별표 243행 · 게이트 102→170 · D-175~184. 🚨 §0.5 에 **클론 B 에서 처음 겪을 새 동작 둘**(판 규칙 D-177 · doctor 셋째 축)이 있다 | 현행 | — |
| 🔄 `docs/ohb/인계_2026-09-11.md` | **인계 — 클론 B · 건기식 API 축 분리 (D-185).** 인정번호가 외래키로 안 쓰인다 · 판 두 개 판정 · 2인 확인 재개 | 현행 | — |
| 🔄 `docs/ohb/인계_2026-09-11_저녁.md` | **인계 — 공개 전환 (D-186~188).** 원격 정본이 `origin`=SKN 으로 바뀌었다 · 브랜드 그라디언트 전환 · 🔴 **09-17 안건의 전제가 틀렸다**(별표1 제5호 아목). 🚨 §0.5 를 먼저 읽는다 | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-11_밤.md` | **인계 — CI 도입 (D-189).** 🔴 **게이트 통과가 팀장 기기에서만 참이었다** · 초록불이 안 보는 7개 · PR→브랜치 보호 순서 · 집 기기에서 놀랄 것 넷 · **D-189 본문 초안(§7)** | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-12.md` | **인계 — 「바뀌지 않았다」가 답이었다.** 승인문구 **178 정본 확정**(재수집 655건 전량 스킵이 증거) · 「낡았다」의 **네 번째 축**(갈린 이력) · 문서 주장과 사실이 갈린 자리 여섯 · 게이트 **함수 40 · 케이스 47** 실측 | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-12_오후.md` | **인계 — 한 층으로 다섯 층을 쟀다 (D-193~198).** 🔴 **벡터 단독 순위가 어휘 겹침 순서 그대로**(6위/19위/50위 밖) · 어휘 갈래 + **RRF** 로 W1 결단 항목을 닫았다 · 제목뿐인 조 청크 제외 · 고아 거두기. 🚨 §3 이 **미실행**이고 예측을 먼저 적어 뒀다 (D-146 · D-191) | 현행 | — |
| 🆕 `docs/ohb/hf_api_축분리_2026-09-11.md` | **건기식 API 축 분리 분석 전문** (D-185) — 열이 말하는 것 · 인정번호가 외래키가 아닌 근거(773행/고유 768 · 중복 4건이 서로 다른 원료) · 대조 7분류 · 경계 사례 후보 122. 🚨 **판 두 개의 판정 기록 원본**이 여기 있다 — `data/_판보류/` 는 `.gitignore` 가 먹어 다른 클론에 안 간다 | 현행 | — |
| 🆕 `docs/ohb/결정요청_2026-09-11_인정범위초과_라벨.md` | ⛔ **전제가 틀려 대체됨** — `violation_article_초안_2026-09-11.md` §1 을 본다. 「좁음 39 + 넓음 10 = 49」를 한 덩이로 물었는데, 좁음은 **덜 주장한 것**이라 위반 방향이 아니고, 새 enum 없이 **별표1 제5호 아목**에 이미 자리가 있다 | 역사기록 | — |
| 🆕 `docs/ohb/violation_article_초안_2026-09-11.md` | **라벨 ↔ 조문 초안 매핑** — `violation_t` 11값 전부 · 식품(013453 별표1) ↔ 화장품(008741 별표5) **평행 축** · 🔴 확정 필요 다섯. ★ **09-17 안건의 전제를 뒤집었다** | 현행 | — |
| 🔄 `docs/ohb/D177_179_본문_2026-09-10.md` | **D-177·178·179 본문 초안** — 대안 표 · 「무엇과 맞바꿨나」 · 게이트. 원장으로 이관 완료(2026-09-10), 이 파일은 이관 전 원본으로 남는다 | 현행 | — |
| `docs/{ohb,ksr,lse,psj,ssm}/` | **개인 작업 문서** — 이니셜 = 브랜치명. 확정 시 정규 위치로 이동 후 이 표에 등재 | 현행 | — |
| `scripts/gen_registry.py` + `scripts/registry_head.yaml` · `registry_tail.yaml` | ★ **레지스트리 생성기 (D-90)** — 판정(`_matrix/data.js`) → 집행(`data_sources.yaml`). 🚨 **손으로 양쪽을 고치면 반드시 갈린다.** head/tail 은 **손으로 쓴 입력**이다 — 판정 사유 문구는 매트릭스에서 복원되지 않는다 | 현행 | — |
| `build/registry_body.yaml` | 중간 산출물 — 매 실행마다 덮어써진다. `.gitignore` 대상 | 현행 | — |
| `app/contracts.py` | 🆕 **판정 코어 출력 계약 (D-124 ①)** — Pydantic v2. 축 넷을 나눠 담는다(판정 상태 · 위험도 · 불가 사유 · 종착). 불변식은 DB 제약과 **같은 문장**으로 건다. 🚨 **진입점 A 만 덮는다** — B(생성)·C(AI 광고 생성) 계약은 미작성 (D-181) | **작업중** | — |
| `tests/fixtures/judge/*.json` | 🆕 **골든 픽스처 9건 (D-124 ③)** — 통과·프론티어 / 보류 2종 / 근거불일치 / 증명서 A·C / 탐색실패 B / 근거없음 / 미판정. 화면·BFF 가 분기를 그리는 재료. `GET /judge/fixtures/{name}` 로도 나간다 | 현행 | — |
| `app/graph.py` | 🆕 **walking skeleton (D-124 ② · Phase 0)** — 스텁 노드 11 · 라우터 2 · 계측 데코레이터. langgraph 없이 도는 `run_stub()` 과 컴파일본이 **같은 순서·종착**을 낸다(게이트가 대조). 🔴 `build_graph()` 가 LangSmith 추적이 켜져 있으면 멈춘다 (D-184) | 현행 | — |
| `tests/test_contracts.py` · `tests/test_graph_routers.py` | 🆕 **계약·그래프 게이트 43건** — 절반이 「실패할 수 있는 단언」(D-170). 리듀서 키는 선언과 **실제 append 를 둘 다** 본다 | 현행 | — |
| `db/migrations/0005~0007` + `alembic/versions/` | 🆕 **ENUM 후보 등재(기능성화장품_오인 · D-65) · 널 우회 제약 차단(D-170) · 그 되돌리기(0007)**. 🚨 0007 은 「이미 돈 마이그레이션은 마이그레이션으로만 거둔다」의 사례다 (D-99) | 현행 | — |
| 🆕 `app/retrieve.py` | 🆕 **조문 검색 코어 (2026-09-12)** — 갈래 셋(`by_vector` 뜻 · `by_lexical` 어휘 · `by_literal` 기호)과 **RRF 결합**(`fuse`·`search` · D-193). 거버넌스를 **게이트가 아니라 WHERE 절로** 건다(`source_use.allowed AND use_code='U2_rag'` + `v_current_chunk`). `citation()` 이 「제8조제1항제1호」를 조립하고 **확신 없으면 `None`** — 부분 인용을 내지 않는다 (D-100). 🚨 모델 이름을 상수로 안 든다 — `chunk_embedding.model_id` 를 DB 에서 읽어 **저장된 벡터를 만든 그 모델**을 로드한다 | **작업중** | — |
| 🆕 `db/migrations/0008~0010` + `alembic/versions/` | 🆕 **0008** 자립 텍스트(`chunk.context`) · 항 서수(`paragraph_no`) · 임베딩 입력 지문(`input_sha256` · D-176) — 🆕 **0009** `v_current_chunk` 재생성: `SELECT c.*` 는 생성 시점에 열 목록으로 고정된다(0008 이 이걸 안 해서 검색이 503) — 🆕 **0010** 어휘 색인 `chunk.tsv` 생성열 + GIN (D-193 · D-194). 🚨 **`chunk` 에 열을 더하면 같은 파일에서 뷰를 다시 만든다** (D-197) | **작업중** | — |
| 🆕 `db/migrations/0011_chunk_part.sql` + `alembic/versions/` | 🆕 **쪼갠 조각(`part_no`·`part_total`)과 모델 입력 토큰(`input_token_count`)** — `preprocess/chunk.py` 가 만들어 두고 **아무도 안 읽던 값**을 `_SELECT`→`Hit`→`SearchHit` 까지 이었다 (D-199). `ck_chunk_tokens` 는 그대로 두고 **주석만 정직하게** 고쳤다 — 재는 축(`text`)과 쓰는 축(`context+text`)이 0008 이후 갈렸다 (D-200). ✅ **재적재 완료 (09-12 밤)** — 청크 2,400 · 쪼갠 조각 121 · 입력 토큰 최대 422 · 512 초과 0행 | 현행 | — |
| 🆕 `scripts/search_probe.py` | 🆕 **검색 순위 탐침** — 원장 §3-1 의 수를 만든 도구가 커밋되지 않아 **재현 불가**였던 것을 세운다 (D-204). 범주 넷을 다 돌고, 후보 폭 기본값은 `retrieve.POOL`(기획서 5-6)에서 읽으며, 30건 미만이면 D-40 으로 「측정 불가」를 찍는다. 🚨 `collect/probe.py`(소스 탐침 · D-109)와 **다른 물건**이라 이름을 갈랐다 | 현행 | — |
| 🆕 `app/settings.py` | 🆕 **설정을 읽는 한 곳** (D-99). `dsn()` 네 벌을 한 벌로 모았고, 🔴 **네 곳 다 `.env` 를 안 읽고 있던 것**을 같이 고쳤다 — `collect/env.py` 가 「읽는 유일한 곳」인데 DB 경로가 그것을 안 지났다. `mysql://` 는 이름을 대고 막는다 (D-95 · D-51). 게이트 5 | 현행 | — |
| 🆕 `scripts/experiment.py` · `experiments/README.md` | 🆕 **T2·T3 분배 단위의 형식** (D-94). 두 층 — `harness.yaml`(팀장 고정) · `<이니셜>_<번호>.yaml`(팀원). 🔴 **고정분을 팀원 설정이 못 덮는 것**을 검증기가 막는다. 팀 명단은 `docs/<이니셜>/` 이 정본이라 코드에 두 벌로 안 적는다. 게이트 9. ⬜ `harness.yaml` 값은 **팀장만** — D-94 마감 2W 말 | **작업중** — 값 대기 | — |
| 🆕 `tests/test_contract_surface.py` + `tests/contract_surface.json` | 🆕 **계약이 조용히 바뀌면 잡는다** (D-124). `openapi.json` 통째가 아니라 **enum 값·필드와 필수 여부·라우트**만 — 판 버전 잡음이 계약 변경으로 안 읽히게. 🚨 FastAPI 내장 문서 라우트는 뺐다. 음성 픽스처 넷으로 **잡는다는 것을 쟀다** (D-203) | 현행 | — |
| 🆕 `tests/test_fixture_coverage.py` | 🆕 **픽스처가 화면 분기를 다 덮는가.** 판정 축 넷 + 위험도. 못 덮는 값은 **사유와 함께** `KNOWN_GAPS` 에 적고, **덮이면 예외를 지우게** 만든다 (D-170). 지금 예외 둘 — `HoldReason.gap2`(인코더 뒤) · `Risk.R0`(SCR 미정) | 현행 | — |
| 🆕 `docs/00_병렬작업_계약.md` | 🆕 **5인이 서로를 기다리지 않게 하는 경계** — 경계 여섯의 현황(넷은 서 있고 **설정·파생물·실험 셋이 `dict`**) · 병목 셋(데이터·LangGraph·파인튜닝)을 **끊는 법** · **Pydantic 을 어디에 넣고 어디에 안 넣나**(그래프 상태는 안 넣는다 — LangGraph 공식 한계 넷) · 파일 소유권과 충돌 지점 · 계약 축(OpenAPI 스냅샷) 제안 · 착수 순서 8. 🚨 **수는 안 적는다 — 원장이 정본** (D-54) | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-12_밤.md` | **인계 — 「만들어 놓고 안 읽은 값」.** 전수 검토 22건 반영 · 치명 둘(`part` 미배선 · 대칭을 안 재는 대칭 게이트) · D-199~204 · 🚨 §0.5 에 **재적재와 게이트 예측 213** 이 있다 · §4 에 **기획서를 펴서 새로 나온 셋**(W1 #10 미결 · `by_literal` 진입점 없음 · 청크 계약 드리프트) | 현행 | — |
| 🆕 `docs/ohb/검토_2026-09-12_전수.md` | **전수 검토 — 코드 축 + 문서·원장 정합성.** 치명 2(`part` 미배선 · 대칭을 안 재는 대칭 게이트) · 중요 5 · 사소 4 · 문서 9. 🚨 **전부 정적**이다(셸이 폴더에 못 붙었다 · D-146) | 현행 | — |
| 🆕 `docs/ohb/작업현황_2026-09-12.md` | **팀장 작업 현황 실측** — 커밋 **266 중 265 = 팀장**(나머지 1은 `user.name` 미설정) · 변경 **125,520줄** · 폴더별 손길(**`docs/` 44.1 % > 코드 41.5 %** · 🔴 **`app/` 은 코드 중 최소 34**) · 작업일 13일 · 층별 인벤토리 · 🔴 **팀원 코드 저장소 0줄**(산출물은 `발표자료/` 에 있다). 🚨 **D-207 배정 판정의 근거 보강** — 이양이 싼 자리는 「있는 것 나누기」가 아니라 「없는 것 짓기」 셋뿐이다 | 현행 | — |
| 🆕 `app/settings.py` | ★ **접속과 판정 파라미터의 단일 출처** (D-209) — `PARAMS` 15개(검색·청크·판정·표본·입구 상한·적재)에 **출처 태그**가 붙어 있고 `[설계]`·`[임의]` 는 **바꾸는 조건**을 같이 든다. 🔴 한 값에서 두 형태를 낸다 — `dsn()`(psycopg)·`sqlalchemy_url()`(alembic). ⛔ 판정 파라미터를 `.env` 로 열지 않는다 (D-205 — 바뀐 사실이 안 남는다) | 현행 | — |
| 🆕 `app/logging_conf.py` | 🔴 **문구가 로그에 안 실리게** (보안점검 P1-4 · D-210) — `RedactFilter`(질의 문자열 통째로 · `record.args` 도) · `mask()`(길이+지문) · `setup_logging()` 을 `app/api.py` 가 **import 시점에** 부른다. 🚨 넘치게 지운다 (D-72) | 현행 | — |
| 🆕 `app/templating.py` | Jinja2 를 **만드는 자리 하나** — 라우터마다 만들면 자동 이스케이프 설정이 사람마다 갈린다. 런타임 `assert` + 게이트 (P2-9) | 현행 | — |
| 🆕 `app/routers/` | **화면·BFF 를 사람별 파일로** (D-208) — `user.py`(ksr·lse) · `admin.py`(ssm·psj). 🚨 **DB 없이도 `/u/`·`/admin/` 이 뜬다**(실행 확인). ⛔ 판정 로직을 여기 쓰지 않는다 (D-119) | 현행 | — |
| 🆕 `app/templates/` · `app/static/` | `base.html`(팀장 · **CSP** `script-src 'self'`) · `user/` · `admin/` · `base.css` · `vendor/README.md`(외부 스크립트는 **받아서 커밋** · P0-3). ⬜ HTMX 미도입 — 없어도 화면은 돈다 | 현행 | — |
| 🆕 `app/auth.py` · `app/routers/auth.py` · `app/templates/auth/` | 🔴 **`governor` 로그인** (D-66 구현 · D-213) — **Argon2id**(OWASP m=19456·t=2·p=1 · PHC 문자열) · 세션 서명 쿠키(새 의존성 없이 `hmac`) · CSRF 이중 제출 · **시도 제한**(P2-11) · 접속기록 로거(⬜ 테이블은 배포 전). 🚨 가입 화면 없음 — 계정은 `launcher.py admin-add` 로만 | 현행 | — |
| 🆕 `scripts/admin_account.py` | 계정 주입 — `getpass` 로만 받고 **`docs/<이니셜>/` 이 없으면 거부**한다(D-99). ⛔ 기본 계정을 시드하지 않는다 | 현행 | — |
| 🆕 `scripts/db_fresh_check.py` | 🔴 **빈 DB 에서 `alembic upgrade head` 가 도는가** (2026-09-13 · D-221 · 이서은 제보) — 옆에 임시 DB 를 만들어 돌리고 지운다. ⛔ 정적 검사로는 못 잡는다: 어느 표가 어느 타입을 붙잡는지는 PostgreSQL 만 안다. 🚨 진짜 DB 이름과 같으면 아무것도 안 하고 멈춘다 (D-220). `launcher.py db-fresh` · 메뉴 39 | 현행 | — |
| 🆕 `scripts/fontcheck.py` | 🔴 **뽑기 전에 이 기기에 글꼴이 있는지 잰다** (2026-09-13 · D-220 · D-206) — `build_diagram.py` 와 `build_pdf.py` 가 **같은 한 벌**을 쓴다 (D-99). ⛔ `document.fonts.check()` 를 안 쓴다(없는 글꼴에도 `True`). ★ generic **셋**과 폭을 견준다 — 하나라도 다르면 있다. **양성·음성 둘 다 실측** | 현행 | — |
| 🆕 `db/migrations/0013_risk_lookup_four_eyes.sql` · `alembic/versions/…0013_*.py` | 🔴 **`ck_sanction_four_eyes` 가 둘 다 NULL 이면 통과했다** (D-66 · D-170) — `sanction_rule` 은 `collect/registry.py` 를 안 지나므로 강제 지점이 **없었다.** ★ 막는 자리를 **읽는 쪽**(`v_risk_lookup`)으로 옮겼다 — 적재는 되고 서명 전에는 위험도 하한이 안 잡힌다. ✅ **2026-09-13 적용 확인** — `doctor --env` 가 `alembic 0014_judgment_check_reassert` 를 낸다. ⛔ `downgrade()` 는 `NotImplementedError` — 2인 확인을 여는 것이라 사람이 판정한다 | 현행 | — |
| 🆕 `alembic/versions/…0012_app_account.py` | `app_account` — **런타임 층**이라 `db/schema.sql` 을 안 건드린다(D-89). 🔴 `CHECK` 가 **PHC 접두어를 강제**한다 — 약한 해시가 들어오는 길을 DB 가 막는다 | 현행 | — |
| 🆕 `alembic/versions/…0014_judgment_check_reassert.py` | 🔴 **`stamp` 로 `0006` 을 건너뛴 DB 를 낫게 한다** (2026-09-13 · D-221 · D-131). 🚨 `0006` 은 거버넌스 마이그레이션인데 **런타임 표 `judgment` 에 제약을 건다** — `db/schema.sql` 은 런타임 층을 안 담으므로(D-89) 그 부분만 새 DB 에 없다. 실측: 약한 판이라 **`risk_floor` 가 NULL 이면 통과**했다(코드 하한 없이 최종 위험도를 적을 수 있다). ★ **채팅으로 절차를 알려 주지 않는다 — `git pull` + `migrate` 하나로 낫게 한다** (D-117). 멱등 · 깨는 행이 있으면 먼저 세서 멈춘다 | 현행 | — |
| 🆕 `tests/test_diagram_provenance.py` | 🔴 **C2PA 공개 문단이 사라지지 않는가** (2026-09-13 · D-222) — `assets/diagrams/README.md` 와 `docs/00_사실원장.md` 둘 다. ⛔ **PNG 에 `caBX` 가 있는지는 안 본다** — 기기에서 뽑은 장은 없는 것이 정상이라 갈래 (b)를 막는다. **정상인 것을 빨갛게 만드는 게이트는 곧 꺼진다** (D-170). D-216 의 「가명 규칙 문단이 살아 있다」와 **같은 어법** · 반대 대조 하나 | 현행 | — |
| 🆕 `tests/test_competitor_anonymity.py` | 🔴 **경쟁사 실명이 저장소로 돌아오지 않는가** (2026-09-12 · D-216) — 가명 「플랫폼 A」·「심의 도구 B」. 🚨 대응표는 저장소 밖이다. ⬜ **이력에는 남는다** — 재작성은 판정 사항. 🔴 09-13 에 **한 곳이 더 걸렸다**(`docs/01_기획/01_주제확정_및_실행일정.md` 140·214행) — 09-12 masking 이 안 훑은 파일이다 | 현행 | — |
| 🆕 `tests/test_auth_boundary.py` | 🔴 **인증이 없는 동안 쓰기를 못 만들게** (D-212) — 관리자 라우터에 POST/PUT/PATCH/DELETE 금지 · 기본 바인딩 `127.0.0.1` · **모든 응답에 보안 헤더**(응답을 받아 본다). 🔴 **인증이 서면 첫 검사를 지운다** | 현행 | — |
| 🆕 `tests/test_ci_pinning.py` | 🔴 **CI 를 지키는 게이트** (D-215) — `uses:` 커밋 SHA 핀 · 판번호 주석 · `pull_request_target` 금지 · `run:` 셸 주입 금지 · `permissions` 명시 · 🆕 **컨테이너 이미지도 다이제스트 핀**(태그는 남긴다 · D-99). **음성 픽스처 있음** | 현행 | — |
| 🆕 `tests/test_templates.py` · `test_logging_redaction.py` | 🔴 **화면과 로그를 지키는 게이트** — 이스케이프·CSP·만드는 자리 하나 / 문구 마스킹·`uvicorn.access` 부착 확인. **둘 다 음성 픽스처를 갖는다** (D-203). 수는 원장이 정본 (D-54) | 현행 | — |
| 🆕 `docs/ohb/검토_2026-09-12_배운것_적용.md` | **앞 프로젝트 교훈 20개 대조표** — 이미 하는 것 8 · 진짜 갭 5 · 손볼 여지 3. D-190~192 의 초안이 여기서 나왔다. **판정이 아니라 대조표다** | 현행 | — |
| 🆕 `docs/ohb/sanction_rule_초안_2026-09-12.md` | **`sanction_rule` 적재 초안** — 별표7 72행 중 **광고 4행을 실측으로 찾았다.** D-09 래칫의 「코드 하한」이 지금 0행이다 | 현행 (판정 대기) | — |
| 🔄 `preprocess/chunk.py` · `scripts/embed.py` | 🔄 **2026-09-12** — 청킹이 **제목뿐인 조 머리 행을 담지 않고**(D-195 · `_TITLE_ONLY`), 적재가 **선언에 없는 청크를 거둔다**(D-196 · `sweep_orphans`). ⛔ `--limit` 실행은 거두지 않는다 — 연습 한 번이 표를 비운다. 🚨 `CHUNK_COLS` 한 줄이 INSERT 칸·VALUES·`DO UPDATE SET`·값 튜플을 **전부** 낸다 (D-99 — 종전 네 벌이었고 실제로 갈렸다) | **작업중** | — |
| `scripts/law_api_smoke.py` | 법제처 OC 키 스모크 — 인증 · 대상 법령 6종 · 🚨 **[별표] 조회 가능 여부**(4층 위험도 전체가 여기 달렸다) | 현행 | — |
| `scripts/build_matrix.py` | ★ **판정 매트릭스 빌드 (D-90)** — `data.js` → `sources.json` + `판정매트릭스.html`. `--check` 로 어긋남만 확인. 🚨 node 를 쓰지 않는다 (D-87) | 현행 | — |
| `docs/03_데이터/_matrix/` | **판정 매트릭스 원본** — `data.js`(판정 🔄 77건) · `shell.html`(화면) · `sources.json`(생성기 입력) + 재빌드 절차 | 현행 | — |
| `data_sources.yaml` | **소스 레지스트리 — 🔄 등재 43 · 배제 10 · 미채택 11 · 모델 6** (2026-09-03 저녁 · D-132 · 🔄 **2026-09-07 재생성 — 수치는 동일하나 파일은 바뀌었다**: 오늘 수집으로 `mark_collected()` 가 `scripts/registry_review.yaml` 에 `collected_at: 2026-09-07` **7건**을 찍었고, 그 원장이 이 파일의 **입력**이라 함께 갱신됐다 — **비결정성이 아니라 입력 변경이다.** 🚨 `registry_review.yaml` 은 사람이 서명하는 원장인데 **수집기도 쓴다**(규약 3 · 게이트 15) — 두 손이 한 파일에 든다). 🚨 수치의 단일 출처는 사실원장 「레지스트리 현황」이다. 등재 단위는 데이터셋이 아니라 **이용조건**. 🚨 **생성물이다 — 직접 고치지 않는다** (D-90) | 현행 (생성물) | ✅ |

| 🆕 `docs/ohb/인계_2026-09-13.md` | **인계 — 글꼴·도면·두 기기 (D-220).** 되받아 대조의 단위가 파일 종류로 갈린다는 진단을 **정정**했다(재인코딩이 아니라 C2PA 청크 +5,770B). 🚨 대조는 **파일까지이고 커밋까지가 아니다** | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-14.md` · `인계_2026-09-14_밤.md` | **인계 — 검색 좌표와 평가축.** 🔴 좌표가 서는 값이 **38.1%** 라는 실측이 여기서 나왔다(`chunks.jsonl` 09-14 00:37 판) | 현행 | — |
| 🆕 `docs/ohb/인계_2026-09-16.md` · `인계_2026-09-16_밤.md` | **인계 — 사전과 평가셋 (D-227~D-231).** 🔴 **`소비자_기만` 사전 규칙 0개** · **train 100% 다중 / test 100% 단독** · 「묶으면 셋이 풀린다」 **철회**(micro F1 0.013→0.026). §10 에 **대조 기록**과 D-218 스테이징 사고, D-230 경위가 있다 | 현행 | — |
| 🆕 `docs/ohb/검토_2026-09-16_사전과_평가셋.md` | **이 날 잰 수의 원본** — 다시 재지 말라고 인계가 가리킨다. 🔴 첫 판이 업체·상표명 11곳을 자국 없이 내보냈다(→ D-230 · 현재 파일은 `[업체]`·`[제품]`) | 현행 | — |
| 🆕 `docs/ohb/판정정리_2026-09-16.md` · `D초안_2026-09-16_위험도_척도.md` · `D초안_2026-09-16_판정E_양자화.md` | **남은 판정 목록 + D 초안 둘** — 위험도 네 단계(→ D-227) · 판정 E 양자화 | 현행 (판정 대기) | — |
| 🆕 `docs/ohb/sanction_rule_초안_2026-09-16.md` | **`sanction_rule` 식품 축 5판** — 별표7 업종 블록 **넷 전수** · 1차 처분 · 🔴 식품접객업은 1호·3호가 **법으로 적용 제외** · **2·3차를 맞대야 R3 경로가 보인다** | 현행 (판정 대기) | — |
| 🆕 `docs/ohb/라벨링_지시서_2026-09-10.md` · `라벨링_지시서_보충_2026-09-17.md` | **사람이 라벨을 붙이는 절차** — 유형 8종 정의·경계·판단 예. 보충판이 「대상 표현」·「범위 밖」의 선을 그었다. 🚨 이 지시서로 붙인 248행이 **평가셋의 원천**이다 (D-243) | 현행 | — |
| 🆕 `docs/ohb/남은것_2026-09-17.md` | **데이터가 아직 못 하는 것 한 장** — 유형 8종이 두 법에서 온다 · 막힌 것 넷 · 3W/4W. 🔄 **09-18 에 ①이 부분 해소**(평가 197→360 · 측정 가능 2→4종)이므로 **표의 「평가(봉인)」 칸은 낡았다** — 정본은 사실원장 09-18 절 ③ | 🔄 **일부 낡음** | — |
| 🆕 `docs/ohb/결정요청_2026-09-17_화장품축.md` | ★ **화장품 축 결정요청 2판 (판정 7건)** — 지침서 별표 **금지 44 · 허용 45 · 입증 7** 전수 · A/B/A+B 단독판정 변화 실측(-2/-1/**-3**) · 🔴 **「폐지 제도」·「뒤집힘 4건」 철회** · 등재 초안 · 의약외품 보류. §0 에 **PDF 를 디스크에 안 남겨 잃은 경위**를 적었다 (D-188) | 현행 (판정 대기) | — |
| 🆕 `docs/ohb/화장품_별표2013_전사.jsonl` · `화장품_별표2013_전사.py` | **별표 손 전사와 그 생성기** — PDF 텍스트 층이 **0자**라 손으로 옮겼다. 🚨 **검수 대상이다** — 전사 오류가 그대로 사전이 된다 | 현행 (검수 대기) | — |
| 🆕 `docs/ohb/인계_2026-09-18.md` | ★ **인계 — 화장품 축 등재 · 평가셋 개통 · 파생물 원장 · 🔴 사고 둘 (D-243~D-246).** §4 에 `register` 사고(**334노드가 게이트 337 초록 아래 사라졌다**)와 **D-218 재발**이 있다. §5 미실행 10 · §6 3단 전송 · §7 내가 틀린 것 여섯 · §10 대조 기록 10건. 🚨 §1 의 앞 커밋 해시는 **비어 있다** — `git log` 출력을 못 받았다 (D-188) | 현행 | — |
| 🆕 `docs/ohb/기기간_동등성_2026-09-17.md` | ★ **기기 사이 동등성 방침** — 「파생물만으로 되는가」의 답(된다 · `RAW_READERS`) · **3단 전송** · 부류 넷 · 🔴 **원문캐시 정정** · `doctor --data --hash` 세 축 정정 (D-244) | 현행 | — |
| 🆕 `scripts/derived_manifest.py` · `tests/test_derived_manifest.py` | ★ **파생물 원장 (D-244)** — 151개 · 53.0 MB 를 **부류 넷**(원천 1 · 표본 4 · 생성물 41 · **원문캐시 105**)으로 가르고 sha256 을 적는다. `--write`·`--check`·`--export-check`(법인 표기 유출 검사). 🚨 **부류 이름은 `KIND_RULES` 한 곳에서만** — 손으로 박아 두었다가 「원문캐시」를 더하는 순간 내 게이트가 걸렸다 (D-99). 게이트 5 + 비게이트 1 | 현행 | — |
| 🆕 `preprocess/labels.py` · `tests/test_label_path.py` | ★ **사람 라벨 저장소 (D-243)** — `files()`·`consensus()`·`out_of_scope()`·`docs()`. 🔴 **248행이 `split.inputs()` 에 없어 어디로도 안 가고 있었다.** 평가 **197 → 360** · 측정 가능 유형 **2 → 4종**. 🚨 「범위 밖」 85행은 **넣지 않는다** — fail-closed (D-72) · 지위는 D-242 안건. `scripts/label_merge.py` 가 같은 저장소를 쓴다 | 현행 | — |
| 🆕 `scripts/casebook2021_sheet.py` | **2021 사례집 라벨 시트 생성기 278행** — `--dump` 가 `casebook2021_labelsheet.jsonl` 을 **바이트까지 재현**한다(161,601 B · `999710e20ac3`). 🚨 시트는 **표본**이라 다시 뽑으면 라벨이 갈린다 (D-244) | 현행 | — |
| 🔄 `collect/ingest.py` · 🆕 `tests/test_adopt.py` | **사람이 받아 온 파일을 원장에 올린다** — `count`(읽기만) · `register`(2인 확인 필수) · 🆕 **`adopt`** (D-246 · 판을 원본 자리로 올리고 **원장에 새 행**). 🔴 **2026-09-18 사고** — `register` 가 `raw_dir(소스id)` 를 써서 `law_go_kr` 파일이 `data/raw/law_go_kr/` 로 갔고 **화장품법 334노드가 초록불 아래 사라졌다**(2,207→1,873). 매핑을 `collect/store.py` `FAMILY_OF` 한 곳으로 모으고 게이트 3을 세웠다 (D-245) | 현행 | — |

> 🔄 **2026-09-21 (클론 A 검토) — 2026-09-19~21 묶음.** 09-18 판 이후 들어온 모듈 일곱과 `docs/ohb/` 여덟이
> 이름이 없었다. ⬜ 이 날 **고친** 파일(마스킹·로그·저장소 받기 등)은 새 행을 만들지 않았다 — 인계 09-21 클론A 2판이 목록이다.

| 파일 | 내용 | 상태 | 게이트 |
|---|---|---|---|
| 🆕 `scripts/data_store.py` | ★ **파생물을 공유 저장소로 옮긴다 (D-247)** — 정본 `data-publish` · 사본 `data-sync` · `data-setup`(`.env` 에 역할·폴더·별칭). 무엇을 받을지는 git 의 파생물 원장이 정한다. 🔄 09-21 — 전부 임시 파일로 받아 sha 를 맞춘 뒤에만 바꿔 끼운다 · dry-run 도 저장소 준비를 본다 | 현행 | ✅ |
| 🆕 `scripts/raw_inbox.py` | ★ **팀원 원문 → 받은편지함 → 정본 (D-250)** — `raw-publish` · `raw-import` · 합치지 않은 원문이 남으면 정본의 추출·올리기가 멈춘다 | 현행 | ✅ |
| 🆕 `collect/missing.py` | **없는 원문을 「왜 없는지」 8갈래로 (D-253)** — `doctor --data` 와 `inventory` 가 같은 함수를 쓴다. 이 기기가 받았는데 없는 것만 🔴 | 현행 | ✅ |
| 🆕 `scripts/label_round.py` | **라벨 한 판 (D-252)** — 겹침 · 검증 행 · 블라인드 재검 · 갈린 것 판정. `plan`·`decide` 는 정본 전용 | 현행 | ✅ |
| 🆕 `preprocess/endorse_scan.py` | **공정위 추천·보증 사건 계측 (D-255)** — 세기만 한다. `--candidates` 파일은 🔄 09-21 부터 피심인 상호·실명을 가린다(앵커 · D-248) | 현행 | ✅ |
| 🆕 `preprocess/lineage.py` | **골든셋 행의 계보 → (프래그먼트, 재배포) 한 표 (D-249)** — `golden` 과 `load_db` 가 같이 읽는다 | 현행 | — |
| 🔄 `scripts/db_reset.py` | **DB 를 선언 상태로 다시 만든다** — 🔄 D-254 역할별 `--data` · 🔄 09-21 지우기 전 사전검사가 `load_db.LOAD_INPUTS` 전부를 본다 | 현행 | ✅ |
| 🆕 `docs/ohb/인계_2026-09-18_클론A.md` · `인계_2026-09-21_클론A.md` | **클론 A 인계 둘** — 09-21 판 §2 가 「A 에서 되는 것과 막히는 것」. 🔄 09-21 A 검토에서 그 표의 두 칸(`extract --sheet` · `eval`)을 고쳤다 | 현행 | — |
| 🆕 `docs/ohb/검토_2026-09-19_기기간_동등성_자동화.md` | **CI 빨강 · 파생물 전달 · 어느 기기에서든 수집** — D-247 의 근거 | 현행 | — |
| 🆕 `docs/ohb/검토요청_2026-09-18_mfds_cgm_expc.md` · `검토요청_2026-09-19_마스킹정책_law_go_kr.md` | **2인 확인 검토요청 · 마스킹 정책 검토요청** | 현행 (판정 대기 포함) | — |
| 🆕 `docs/ohb/결정요청_2026-09-20_골든셋_재배포표시.md` | **골든셋 `redistributable` 이 D-133 과 어긋난다** — 공개 범위가 걸려 초안까지 | 현행 (판정 대기) | — |
| 🆕 `docs/ohb/라벨링_지시서_보충_2026-09-20.md` | **라벨링 지시서 보충 2 · 2차 판** — 정본은 09-10 지시서 | 현행 | — |
| 🆕 `docs/ohb/런처_전수감사_2026-09-20.md` | **런처 전수 감사** — D-254 의 근거 | 현행 | — |
| 🆕 `docs/ohb/결정요청_2026-09-21_클론A.md` | **클론 A 검토에서 나온 판정 대기 안건** — 초안까지 | 현행 (판정 대기) | — |

## 참고 — claude.ai 프로젝트에만 있는 역사 기록 (레포 미포함)

GlobalValueGuard v1~v4 · 애드체크 v1 · 신용장 검토 · 에이전트 실패추적 평가 · 후보 비교 문서들 — 폐기/보류 기록이므로 소급 수정하지 않는다.

> 🔄 **2026-08-31 — 데이터 문서 5건을 레포 정규 위치로 옮겼다.** 수집리스트·소스분석·판정매트릭스·수집전처리 기획·DDL 이
> claude.ai 프로젝트에만 있어 **레포판과 갈라지는 사고가 실제로 났다**(사실원장이 양쪽에서 서로 다른 절을 갖고 있었다).
> 🚨 **레포가 원본이고, 프로젝트는 사본이다.** 사본은 레포에서 덮어쓰며 반대 방향으로는 고치지 않는다.
>
> 🔄 **2026-09-01 — 동기화를 상시로 한다 (팀장 지시).** 레포에 커밋이 들어가면 **그 직후 프로젝트 컨텍스트를 맞춘다.**
> 그동안은 요청이 있을 때만 맞췄고, 그 사이에 사실원장이 양쪽에서 서로 다른 절을 갖는 사고가 났다.
>
> 🚨 **대응표는 여기에 두지 않는다.** `scripts/sync_project.py` 의 `MAP` 이 단일 출처다 —
> 표를 문서에도 두면 또 두 벌이 되고, 실제로 그렇게 어긋났다 (D-99).
>
> ```bash
> uv run python scripts/sync_project.py   # build/project_sync/ 에 스탬프 찍힌 사본 생성
> ```
>
> 🔄 **2026-09-01 — 버전은 문서 머리말 한 곳에만 둔다 (팀장 결정).** 파일명에서 뺐다.
> 파일명에 붙이면 갱신할 때 두 곳을 손대야 하고, 실제로 어긋났다 —
> `수집전처리_기획_v1.2.md` 의 내용이 **v1.3** 이었다.
> 프로젝트 사본 경로도 버전을 뺀다 — 버전이 오르면 경로가 바뀌고, 그러면
> **새 사본이 생기고 옛 사본이 유령으로 남는다.**
>
> 🚨 **사본 첫 줄에 스탬프가 찍힌다** — `버전 · 갱신일 · 그 문서를 마지막으로 바꾼 커밋`.
> 버전 번호만으로는 **어제의 v1.3 과 오늘의 v1.3 을 구분하지 못한다.** 최신 여부는
> `git log -1 --format=%h -- <원본경로>` 와 스탬프의 해시를 비교해 판별한다.
>
> 🚨 **프로젝트에만 있는 것** — `인계_2026-08-31.md`(세션 인계) · `설계스키마_3종.md`(02_설계 합본, 손으로 만든다) · `소스레지스트리.md`(`data_sources.yaml` 에 머리말을 붙인 사본).
> 🔄 **2026-09-10 — `기획문서_v3.5.md` 를 프로젝트에서 지웠다.** 「역사 기록」으로 뒀었는데
> 진입점 셋(D-119·D-164·D-181)이 확정된 뒤로는 **두 진입점 기술이 살아 있는 사본**이 되어
> 검색에 함께 걸렸다. 역사는 `docs/00_설계결정기록.md` 가 든다 — **사본이 들지 않는다.**
>
> 🚨 **프로젝트에 올리지 않는 것** — `docker-compose.yml`·`pyproject.toml`·`launcher.py`.
> 값은 이미 사실원장 「스택 핀」이 갖고 있고, 파일을 또 올리면 **또 두 벌이 된다.**
