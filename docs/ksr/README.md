# 권소라 작업 문서

> 작업 브랜치 `ksr` · 이 폴더에 개인 작업 문서를 둔다.

## 규칙

- 🚨 **수치·일정·파라미터는 여기 옮겨 적지 않는다.** 단일 출처는 [`../00_사실원장.md`](../00_사실원장.md) 이다 (D-54).
- 🚨 **설계 결정은 여기서 하지 않는다.** 결정은 [`../00_설계결정기록.md`](../00_설계결정기록.md) 에 `D-XX` 로만 등재한다 (D-58).
- 여기 있는 문서는 **작업 메모·초안**이다. 확정되면 `01_기획/`·`02_설계/` 등 정규 위치로 옮기고 `00_산출물현황.md` 에 등재한다.
