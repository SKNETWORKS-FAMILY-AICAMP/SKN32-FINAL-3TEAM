# 청크 스키마 (RAG 계약) — W1 확정 대상

> **작성자** 오한빈 (팀장)
> **작성** 2026-08-16 *(추정)* · **최종 갱신** 2026-09-24 KST — 🔄 `category` → `law`(0019 · W6)

> 담당: RAG 트랙 · 기획서 4-5 · 5-6절 참조. 확정 시 이 문서가 계약이다.
> 🚨 **원본은 `db/schema.sql` 이고 이 문서는 그것을 읽는 자리다** (D-54 · D-90).
> 열을 더할 때는 파일을 먼저 고치고 이 표를 맞춘다 — 반대로 하면 두 벌이 된다.

## 필수 메타데이터 (기획서 4-5 + 실제 스키마)

🔄 **2026-09-12 밤 — 이 표가 실제 스키마와 갈려 있었다.** 08-31 이후 `chunk` 에 일곱 칸이
늘었는데(`paragraph`·`item`·`paragraph_no`·`context`·`token_count`·`tsv`·`doc_type`) 표는
그대로였고, 그 사이 **하나는 만들어 놓고 아무도 안 읽는 값**이 되었다(`part` · D-199).
계약 문서가 낡으면 「없는 칸」과 「안 적은 칸」이 구별되지 않는다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `chunk_id` | text PK | 🚨 원천이 주는 유일 키로 만든다 — 조립하면 겹친다 (D-149) |
| `fragment_id` | fk | 🚨 필수 — 캐스케이드 삭제 경로 (D-20) |
| `doc_id` | fk | 원문서 |
| `law_id` | str | 법령/고시 식별자 |
| `article` | str | 조 — 「제8조」·「제8조의3」 |
| `paragraph` | str | **항 — 원문 표기(「①」)**. 없을 수 있다 (D-167 · 2026-09-12) |
| `item` | str | **호 — 「1.」** |
| `paragraph_no` | smallint | 항 서수 — 원문에 항번호가 없어도 우리가 센 것. 호의 31%(283/909)가 그렇다 (D-117 · 0008) |
| `context` | text | **자립 텍스트** = 조 제목 + 항 본문. 임베딩 입력이자 화면 문맥 (0008). 🔄 **별표는 별표 제목 + 상위 항목**(2026-09-24 · 짧은 목록 청크가 벡터 상위를 점령해서). 🚨 NULL=미적재 · ''=붙일 문맥 없음 |
| `part_no` / `part_total` | smallint | 🆕 **쪼갠 조각 번호/총수** (0011 · D-199). `part_total > 1` 이면 **이 근거는 조문의 일부**다 |
| `doc_type` | str | 법령 / 별표 |
| `law` | text | 🔄 **법 축** — 표시광고법 / 식품표시광고법 / 화장품법 / 건강기능식품법. **법 ID 로 정한다** (0019 · W6 · D-271 ①). 대응표 `collect/law_map.py`. ⛔ 종전 `category text[]`(일반 / 식품 / 건기식 / 화장품)는 낱말로 정해 265청크가 「일반」으로 떨어졌다 |
| `text` | text | **인용 단위** — 한 글자도 안 바꾼다 (D-158) |
| `token_count` | int | `text` 의 토큰. `ck_chunk_tokens(512)` 가 보는 축 |
| `input_token_count` | smallint | 🆕 **`embed_input()` = context + text 의 토큰** (0011 · D-200). **상한 CHECK 없음** |
| `tsv` | tsvector | 생성열 — 어휘 색인. `context + text` (0010 · D-193) |
| `effective_date` | date | 🚨 개정일 — 현행 기준 필터 (2026.7.1 과징금 고시 사례) |
| `superseded_at` | date | 폐지 — 삭제가 아니라 필터 |
| `embedding` | vector(1024) | `chunk_embedding` 에 별도 표. `model_id`·`input_sha256` 동반 (D-176) |

⬜ `source_grade` 는 **이 표에 두지 않는다** — `fragment` → `source` 조인으로 읽는다.
08-31 표는 「FRAGMENT 에서 상속된 실제 등급」이라 적었는데, 값을 복사해 두면 원천 등급이
바뀔 때 두 벌이 된다 (D-99). 거버넌스는 **질의로** 건다 (`app/retrieve.py` `_JOINS`).

## TODO (W1)

- [x] **청크 크기·오버랩 정책 (조문 단위 vs 항 단위)** — 조문 단위로 자르고, 넘으면 항 단위로,
      그래도 넘으면 문장 경계 (D-158 · `preprocess/chunk.py`). **오버랩은 두지 않는다** —
      인용 단위가 겹치면 「어느 호가 걸렸나」를 못 말한다.
- [ ] 🔴 **BM25 인덱스 필드** — 기획서 8-4 W1 결단 **#10 이 아직 안 닫혔다.**
      결단 항목은 `rank_bm25` / `pg_textsearch` / `pg_search` 셋 중 고르라고 했는데,
      0010 이 고른 것은 **넷째(Postgres 코어 `tsvector` + `ts_rank_cd`)**이고
      🚨 **기획서 7-3 이 이미 *"Postgres 코어 tsvector 는 BM25 가 아닙니다(IDF·길이 정규화 없음)"* 라
      적어 두었다.** 09-12 오후에 그것을 실측으로 「발견」한 것은 기획서를 안 편 것이다 (D-100).
      ⬜ `kiwipiepy` 형태소 기준은 **보류** — 이미지 핀 교체와 함께 간다 (D-194).
      ★ 다시 고를 자리 — **골든셋 30건 뒤에** (D-40 · D-190).
- [x] **리랭커 512 토큰 제한 대응 (긴 조문 분할 규칙)** — 700자 상한으로 문장 경계 분할,
      `part_no`/`part_total` 로 원 조문을 가리킨다 (0011 · D-199).
      🚨 **상한 자체는 아직 안 걸렸다** — `input_token_count` 를 재기만 한다.
      리랭커 모델 선정(기획서 7-3 · v2-m3 0.6B vs base 0.3B)이 그 수를 정한다 (D-200).
- [x] **`expired` 청크 처리** — 삭제가 아니라 필터. `superseded_at` + `v_current_chunk`.
      🚨 `chunk` 에 열을 더하면 **그 뷰를 같은 파일에서 다시 만든다** (0009 · D-197).

## 검색 갈래 (2026-09-12 · D-167 · D-193)

기획서 5-6 의 파이프라인이 정본이다.

```
문장 -> [BM25 + 벡터 하이브리드] 상위 50      <- POOL. **잠정값**이다 (D-205)
              v
      [cross-encoder 리랭커] 상위 5           <- 미착수. 모델 미선정
              v
      [인용 검증: 코드]  존재가 아니라 **일치**까지
```

| 갈래 | 뜻 | 진입점 |
|---|---|---|
| `by_vector` | 뜻이 가깝다 | `/search` · `graph.retrieve` — 🔄 둘 다 `search()` → RRF |
| `by_lexical` | 어휘가 겹친다 (조사 깎기 + 접두어) | `/search` · `graph.retrieve` — 🔄 둘 다 `search()` → RRF (2026-09-21 대조 · `app/graph.py` `retrieve`) |
| `by_literal` | 글자가 그대로 있다 — 「제5호 아목」 | ⬜ **없다** |

🚨 **「상위 50」은 고정값이 아니라 잠정값이다** (D-205). 기획서 8-4 #10 이
*"W1에 실제 청크 수를 보고 정합니다"*, 7-3 이 *"응답시간으로 실측해서"* 라 적었다.
**바꾸는 조건 둘** — ① 골든셋 30건 (D-40) ② 리랭커 선정 후 응답시간 <500ms 실측.
정본은 원장 「판정 경로의 출처 태그」 표다.

🔴 **`by_literal` 에 진입점이 없다.** 기획서 5-6 은 *"하이브리드 필수: 조문 번호·법령명은
정확 매칭이 중요해 BM25가 있어야 합니다"* 라 적었는데, 지금 하이브리드는 벡터+어휘 둘뿐이고
기호 검색은 코어에만 있다. **빠뜨린 것이 아니라 아직 안 정한 것**이다 — 광고 문구 입력에는
「제5호 아목」이 안 오지만, **사람이 조문을 찾는 화면**에는 온다 (SCR 미정).
