# 관리자 콘솔 — 승인 대기 중 할 수 있는 작업 메모 (psj, 2026-09-16)

팀장 승인(`source_grade_proposal`/`source_grade_history` 테이블) 대기하는 동안
DB 스키마 변경 없이 할 수 있는 작업 목록. 우선순위 순서 아님 — 생각날 때마다 추가.

## 1. `/admin/sources`, `/admin/dict-entries` 화면 보강
- 검색/필터: 등급별(L1~L4), 검증 여부별 — 쿼리 파라미터로 처리, 스키마 변경 없음
- 정렬 기능 (현재는 고정 정렬만 있음)
- 페이지네이션 — 지금은 전체 행을 한 번에 긁어옴, 데이터 늘면 느려질 수 있음

## 2. A+B+C 흐름 화면 뼈대 미리 구현 (완료 — 2026-09-16)
- `/admin/sources/{source_id}` 상세 페이지 추가
- 등급 변경 제안 / 2인 확인 / 이력 섹션을 비활성(disabled) 상태로 먼저 배치
- 승인 나오면 POST 라우트만 연결하면 되게 뼈대만 미리 준비
- 관련: `app/routers/admin.py`(`_get_source`, `source_detail`),
  `app/templates/admin/source_detail.html`

## 3. L1~L4 판정 기준표를 화면에 반영
- `docs/psj/판정근거_등급기준_초안.md` 체크리스트를 상세 화면에 참고 텍스트로 노출
- → 2번 작업하면서 상세 화면에 같이 넣음 (완료)

## 4. 에러/빈 상태 UX 다듬기
- DB 연결 실패 시 지금은 빈 값만 반환 (`_table_counts()`, `_list_*()` 패턴)
- 화면에 "일시적으로 불러올 수 없습니다" 류의 사용자 친화적 안내 문구가
  제대로 노출되고 있는지 4개 화면 전부 다시 점검 필요 (지금은 `db-up` 명령어
  안내 정도만 있음 — 최종 사용자향 문구로는 부족할 수 있음)

## 5. tickets/notices/faq 요구사항 정리 (스키마 설계 전 단계)
- 스키마 자체는 ksr/lse 랑 논의 후 결정 (별도 잠금 아님, 그냥 아직 안 만난 것)
- 논의 전에 psj 쪽에서 필요한 요구사항만 먼저 정리해두면 미팅이 빨라짐
- 아직 작성 안 함 — 다음에 착수
