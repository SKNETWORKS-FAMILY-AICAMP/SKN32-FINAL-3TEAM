# S0-14 · 2인 확인 검토표

> 🚨 **생성물입니다. 이 파일에 결과를 적지 마십시오 — 재생성하면 사라집니다.**
> 검토 결과는 `scripts/registry_review.yaml` 에 적습니다 (하단 「기입 방법」).

## 무엇이 검토 대상인가

| 집합 | 건수 |
|---|:-:|
| 레지스트리 전체 | 43 |
| 용도가 하나라도 열린 것 — **게이트가 통과시킬 수 있는 것** | **33** |
| `status: collect` + `manual` — 작업 계획에 있는 것 | 31 |
| 둘 다인 것 | 29 |
| 아직 `reviewed_by` 가 비어 있어 **이 표에 실린 것** | **3** |

> 🚨 앞의 두 숫자는 **같은 집합이 아닙니다** — 값이 우연히 가까워도 마찬가지입니다. 검토는 「수집할 것」이 아니라 **「게이트가 통과시킬 수 있는 것」**을 대상으로 합니다 — 수집 계획이 없어도 용도가 열려 있으면 누군가 부르면 나갑니다.

> `status` 는 **가져오는가**이고 `use` 는 **가져온 것을 쓸 수 있는가**입니다 (D-108). `collect` 는 자동 수집기가 도는 것, `manual` 은 🚨 **자동 수집이 약관 위반이라 사람이 수기로만** 옮기는 것, `hold` 는 이번 범위 밖입니다.

## 이 검토가 막으려는 것

> **동의만 찍는 검토는 게이트가 아닙니다** (D-66).
> 그래서 아래에 **판정 근거(왜 이 등급인가)** 와 **근거 URL** 을 함께 실었습니다.
> 검토자는 판정을 **재현**해 보십시오. 재현이 안 되면 그것이 이견입니다.

> ⚠️ **「판정 근거」와 「활용 주의」는 다릅니다.** 앞은 등급을 그렇게 본 이유이고, 뒤는 쓸 때 조심할 것입니다. 서명의 대상은 **앞** 입니다.

**확인하는 것 다섯**

1. **등급이 맞는가** — G3(원문 자유) · G2(사실만·원문 미보관) · G0(미표기 → fail-closed)
2. **열린 용도가 등급 상한을 넘지 않는가** — G2 에 U2(원문 색인)·U3(화면 인용)가 열리면 정의 위반
3. **재배포 축을 등급과 혼동하지 않았는가** — 🚨 등급 G3 인데 재배포 불가인 것이 있습니다 (D-71)
4. **근거 URL 이 실제로 그 조건을 말하는가** — 링크를 열어 보십시오. 🔄 **⚠️ 접근 URL 로 대체됨** 표시가 붙은 것은 **아직 근거 페이지가 없는 것**입니다 — 그 소스는 링크를 열어도 조건이 안 나오므로, 확인이 안 되면 그것이 이견입니다 (역검토 v1.3 §2)
5. 🔄 **상업 이용이 확인됐는가** — 🚨 이 축은 `use`(U1~U4)에 **없습니다.** 게이트가 집행하지 않지만 **우리 제품이 상업적**이라 실제로는 가장 자주 걸립니다. 채점이 이 축의 미확인에 +4 를 주므로, 확인 항목에도 있어야 합니다 (역검토 v1.3 §8).

> 🚨 **3번의 「재배포」가 무엇을 가리키는지** — `redistributable` 은 **우리가 만든 파생 데이터셋을 공개 배포할 수 있는가**입니다. 원문 열람·이용 가부가 아닙니다 (D-71). 골든셋에 `false` 인 소스가 **한 문장이라도** 섞이면 그 골든셋 전체를 공개할 수 없고, `SA` 가 섞이면 공개는 되지만 **골든셋 전체가 그 라이선스로 전염**됩니다 (D-60). 표의 「재배포」 칸이 그 둘을 갈라서 표시합니다.

## 이견이 나오면

| 단계 | 하는 일 |
|:-:|---|
| 1 | `reviewed_by` 를 **비워 둡니다.** 채우면 게이트가 열립니다 |
| 1-2 | 🔄 **`reviewed_at` 은 찍고 `dissent_note` 를 남깁니다** — 「보고 반대함」과 「아직 안 봄」이 둘 다 빈칸이면 진척과 이견이 같은 모양으로 보입니다. 게이트는 그대로 `reviewed_by` 만 봅니다 |
| 2 | 이견 내용을 팀장에게 전달 — 무엇이 어떤 근거로 다른지 |
| 3 | 팀장이 재판정하고 `decided_at` 을 갱신 |
| 4 | 재검토 후 동의하면 그때 `reviewed_by` 기입 |

> 🚨 **이견이 하나라도 있으면 그 소스는 수집하지 않습니다.** 나머지 소스는 영향받지 않습니다.

---

## A. 자세히 봐야 하는 것 (2건)

> **아래 사유의 합계가 4점 이상**인 소스입니다. **여기에 시간을 씁니다.**

| 사유 | 점수 |
|---|:-:|
| 근거가 스스로 「미확인」이라 말함 | **+4** |
| 재배포 불가 (등급과 다른 축 · D-71) | **+4** |
| G2 등급 (사실만 · 원문 미보관) | +3 |
| G3 아닌데 U2·U3 가 열림 (등급 상한 초과 의심) | +3 |
| 판정 변경 이력 (「정정」·🔄) | +2 |
| 묶음 등재 (서명 한 번이 N종에 걸림 · D-90) | +2 |
| 위험 플래그 (GATED·TOS·PREAPPROVAL·NOSTORE·QUERYLOG·PII·NC·SA) | 개당 +1 |

> 🚨 **정정 이력(+2)만으로는 A 가 되지 않습니다.** B 에 있는 소스에도 정정 이력이 있을 수 있으니, B 의 판정 근거를 건너뛰지 마십시오.

### `kcia_guideline` — 대한화장품협회 광고자문 기준 해설서 2025 ★2025판

| 등급 | **G2** | 열린 용도 | U1 U4 |
|---|---|---|---|
| 제약 | BY | 재배포 | 가능 · 출처표시 |
| 판정 | 오한빈 (2026-08-20) | 규모 | 금지 79 / 허용 47 / 실증 19범주 (실측) |

**왜 자세히 봐야 하는가**

- **G2 — 사실만 추출 · 원문 미보관.** U2·U3 가 열려 있으면 정의 위반
- **판정이 한 번 바뀐 이력**이 있다 — 무엇이 왜 바뀌었는지 확인

**판정 근거** (판정매트릭스 `why`)

> 🔄 2026-09-02 정정 — 종전 근거 「공지에 "저작권은 대한화장품협회에 귀속" 명시 확인」은 재현되지 않았다(권소라). 공지 본문과 첨부 해설서 PDF 전문(35p·28,233자)에 저작권·귀속·무단·전재·복제·배포·Copyright 이 0건이다. 🚨 없는 문장을 근거로 적고 있었다. 그러나 G2 판정은 유지한다 — 저작권은 표기 없이도 발생하므로(저작권법 제10조 무방식주의) 표기 부재는 「자유 이용 가능」이 아니다. 즉 근거가 「명시적 귀속 표기」에서 「표기 부재 + 무방식주의」로 바뀌며, 그것이 오히려 정확하다. G3 승격은 협회 회신이 있어야만 가능하다.

용도별 근거
- **U1 학습** — ⚠️ 표현 목록(문자열)만. 해설 문장은 투입 금지
- **U3 인용** — ❌ 해설서를 인용하지 않는다. 화장품법·고시·시행규칙 별표로 소급
- **U4 배포** — ⚠️ 어휘 사전 형태로만
- **상업** — ⚠️ 사실은 저작권 대상이 아니므로 사실 한정으로만
- **원문** — ❌ 해설 문장·편집·배열은 협회 저작물

**활용 주의** — 판정 근거가 아니라 쓸 때 조심할 것입니다.

> 🚨 기획서의 2023판 링크는 폐기. 2025-11-10 개정판이 최신이고 「천연·유기농」·「비건」 표현 안내가 추가됐다.

**근거 확인** → <https://kcia.or.kr/ad/cs/notice.php?type=view&no=17039> ⚠️ 접근 URL 로 대체됨

접근 방법: 공식 다운로드

### `klue_dataset` — KLUE 벤치마크 데이터셋 (모델 아님)

| 등급 | **G3** | 열린 용도 | U1 U2 U3 U4 |
|---|---|---|---|
| 제약 | BY, SA | 재배포 | 가능 · 🚨 **SA 전염** (골든셋 전체가 동일 라이선스 · D-60) |
| 판정 | 오한빈 (2026-08-20) | 규모 | 205k+ (ynat/nli/sts/ner/re/dp/mrc/wos) |

**왜 자세히 봐야 하는가**

- 제약 SA — 각각이 별도 확인 대상
- 🚨 **판정 근거가 스스로 「미확인」이라고 적고 있다** — 걸린 축: **U4 배포**. 등급은 맞을 수 있으나 **확인된 적이 없다** — 확인되지 않은 것은 자명할 수 없다. **근거 URL 을 열어 이용허락범위를 눈으로 확인**하고, 확인되면 팀장에게 근거 갱신을 요청하고, 열리지 않으면 그것이 이견이다

**판정 근거** (판정매트릭스 `why`)

> GitHub·HF 양쪽 CC BY-SA 4.0 일치 확인. 🚨 **SA 파급은 kmhas 와 같은 미결 사안이다** — 데이터셋 이용은 되고 **학습 산출물의 라이선스 파급이 D-60 을 좌우한다** (D-71 · W5 결정).

용도별 근거
- **U1 학습** — CC BY-SA 4.0
- **U3 인용** — 출처표시
- **U4 배포** — ⚠️ ShareAlike — 데이터셋 개작·배포는 동일 라이선스. 🚨 **학습 산출물로의 파급은 미확인** (D-71 · D-60 선결 · W5 결정)
- **상업** — ✅ 가능
- **원문** — 제한 없음

**활용 주의** — 판정 근거가 아니라 쓸 때 조심할 것입니다.

> ⚠️ SA 전염성 — 데이터셋을 파생·배포하지 않고 모델 학습에만 쓰면 통상 문제되지 않는다. 🚨 데이터셋의 CC BY-SA가 KLUE-RoBERTa 「모델」에 적용된다고 가정하면 안 된다(아래 모델 항목 참조).

**근거 확인** → <https://github.com/KLUE-benchmark/KLUE> ⚠️ 접근 URL 로 대체됨

접근 방법: 자유 다운로드

---

## B. 확인만 하면 되는 것 (1건)

> 제약이 하나둘 붙어 있으나 등급은 명확합니다. 근거를 읽고 링크를 열어 확인하십시오.

**`self_sanction_stat`** — ★ 자체 산출 — 부당광고 제재 실적 (행정처분 API + 의결서)  
**G3** · 열린 용도 U1 U2 U3 U4 · 제약 없음 · 재배포 가능 · [근거 확인](../01_기획/경제성_근거.md)

> 🔄 2026-09-02 정정(권소라 §6-7) — 원천은 셋이다: 공정위 의결문(ftc_decisions) + 식약처 행정처분 API(mfds_sanctions) + 과징금 고시(law_go_kr · penalty_notice). 종전 근거는 둘만 적었다. 셋 다 G3 이므로 파생 통계도 G3 라는 결론은 유지된다. 🚨 라이선스 페이지가 존재하지 않는 자체 산출물이라 근거가 URL 이 아니라 산출 근거 문서다 — docs/01_기획/경제성_근거.md 가 산출 방법과 원천을 적는다(D-75 · D-99). 원천 등급이 내려가면 이 파생 통계도 함께 내려간다.

> ⚠️ **활용 주의** — ★★ 인용한 숫자보다 산출한 숫자가 강하다. "MarketsandMarkets에 따르면"보다 "우리가 수집한 처분 레코드에서 계산하면"이 심사에서 훨씬 잘 먹힌다. / 게다가 이 계산 자체가 제품의 일부다 — 위험도 산정의 「예상 제재 금액」이 바로 이 분포에서 나온다. 하나를 만들어 두 번 쓴다. 🚨 원천이 재판정되면 이 통계도 재판정 대상이다 — 근거가 남의 약관이 아니라 우리 원천이기 때문이다. 🚨 2026-09-02 — 파생물이 원천보다 먼저 서명됐다(권소라 §6-8): 이 소스는 서명됐는데 원천인 law_go_kr 은 아직…

용도별 근거
- **U1 학습** — 파생 통계
- **U3 인용** — ★ 발표의 핵심 근거
- **U4 배포** — 제한 없음
- **상업** — 원천이 전부 공공저작물
- **원문** — 우리가 산출한 값

---

## C. 거의 자명한 것 (0건)

> 공공기관 공식 API·공표 자료로 제약이 없습니다. 훑어보고 넘어가도 됩니다.

| 소스 | 이름 | 등급 | 열린 용도 | 제약 | 재배포 | 근거 |
|---|---|:-:|---|---|:-:|:-:|

---

## 작업 계획에는 있으나 게이트가 닫아 둔 것 (2건) — 검토 불필요

> **전 용도 deny** 입니다. 게이트가 어떤 용도로도 내보내지 않으므로 2인 확인의 대상이 아닙니다. 여는 것은 **재판정**이지 검토가 아닙니다.

> 🚨 **`status: collect` 는 여기 올 수 없습니다** — 게이트 22 가 막습니다 (D-108). 가져오는데 쓸 곳이 없는 자동 수집은 판정 없이 원문을 손에 쥐는 일입니다. 여기 남는 것은 **수기 경로(`manual`)** 뿐이고, 그 용도 축은 아직 미결입니다.

| 소스 | 이름 | 등급 | 닫아 둔 이유 |
|---|---|:-:|---|
| `meta_adlibrary` | Meta 광고 라이브러리 (UI) ★사용자제공 | **G2** | 🚨 UI 열람만 · 자동 수집은 약관 위반 · 🚨 수기 경로 |
| `google_atc` | Google 광고 투명성 센터 ★사용자제공 | **G2** | 🚨 UI 열람만 · 공식 API 없음 · 🚨 수기 경로 |

---

## 기입 방법

```yaml
# scripts/registry_review.yaml  ← 여기에 적습니다
ftc_decisions_api:          # 🚨 예시는 C 구간의 자명한 소스로 둡니다
  reviewed_by: 권소라       # 🚨 판정자(decided_by)와 달라야 합니다 (ck_source_four_eyes)
  reviewed_at: 2026-09-03
  # 🔄 이견이면 reviewed_by 는 비우고 reviewed_at 과 dissent_note 만 적습니다
  # dissent_note: 근거 URL 이 이용허락범위를 말하지 않음
```

> 🚨 **검토자가 적는 것은 위 셋뿐입니다** — `reviewed_by` · `reviewed_at` · (이견 시) `dissent_note`. `decided_by` · `decided_at` 은 **판정자의 기록**이라 손대지 않고, `collected_at` 은 **수집기가 찍습니다.** 예시에 판정자 필드를 함께 두면 복붙으로 덮어쓰게 됩니다 (역검토 v1.3 §11).

기입 후 `uv run python scripts/gen_registry.py` → `uv run pytest -m gate` 로 확인합니다.
