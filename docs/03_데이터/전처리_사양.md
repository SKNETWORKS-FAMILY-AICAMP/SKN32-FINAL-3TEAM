# 전처리 사양 — `TRANSFORM_RULES` 단일 규칙표 · 문자 수준 정규화 층

> **작성자** 오한빈 (팀장)
> **작성** 2026-08-31 · **최종 갱신** 2026-08-31

> 담당: 데이터·거버넌스 트랙. 근거는 **D-17 · D-18 · D-20 · D-71 · D-74 · D-83 · D-84**.
> 🚨 **확정 시 이 문서가 계약이다** — 수집기·학습·평가가 모두 여기 정의된 필드 이름을 그대로 쓴다.
>
> 값은 옮겨 적지 않는다. 파라미터는 [`00_사실원장.md`](../00_사실원장.md), 결정은 `D-XX` 로 참조한다 (D-54 · D-58).

---

## 0. 파이프라인 — 무엇이 무엇을 만드는가

```
   원문 파일                     [P1] 파싱
        │                            │
        ▼                            ▼
   ★ [P3] 마스킹 ─────────── 등급 무관 · 수집 직후 즉시 (D-17)
        │                     업체명·상표·대표자명. 원문 미보관
        ▼
   ★ [P2] 정규화 층 ──────── 🚨 비파괴. 원문·정규화문·오프셋 맵을 함께 보관 (D-84 ①)
        │                     회피 표기 플래그를 여기서 딴다
        ▼
     [P4] 문장 분할 ───────── 광고 텍스트는 마침표가 없다
        │
        ├──► [P5] 청킹 ─────► RAG 인덱스 (청크_스키마.md 가 계약)
        ├──► [P6] 사전 구축 ─► 코드 판정기 (이질적 판정기 B)
        └──► [P9] 라벨 스키마
                 │
                 ▼
         ★ [P10] TRANSFORM_RULES ── 정방향(주입) / 역방향(시정) 양방향 (D-74)
                 │
                 ├──► ★ [P11] 표면 변환 축 ── 회피 표기 (D-84 ①) · 🚨 직교 축
                 │
                 ▼
           [P12] 골든셋 분할 ── 🚨 출처 분리. 주입본은 test_holdout 에 넣지 않는다
```

🚨 **[P3] 마스킹이 [P2] 정규화보다 먼저다.** 정규화는 오프셋 맵을 만드는데, 마스킹이 뒤에 오면 그 맵이 전부 어긋난다. 그리고 마스킹은 **등급과 무관하게** 수집 직후 즉시다 (D-17) — 나중으로 미루면 그 사이의 백업·중간 파일에 원문이 남는다.

---

## 1. [P2] 문자 수준 정규화 층 — D-84 ①

### 1-1. 왜 층이 필요한가

Aho-Corasick 은 **정확 문자열 매칭**이다. `치료` 는 잡아도 아래는 못 잡는다.

```
치 료   ·   치·료   ·   치+료   ·   ㅊl료   ·   治療   ·   ｃｕｒｅ
```

🚨 **온라인 광고에서 필터 회피 표기는 일상적 관행이다.** D-82 에서 경쟁 도구의 사전 매칭 한계를 지적했는데, **우리 룰 층도 같은 기술이다.** 정규화가 없으면 같은 방식으로 뚫리고, 그러면 *"선행 도구와 무엇이 다른가"* 에 답할 수 없다.

### 1-2. 🚨 비파괴가 핵심 — 파괴하면 둘이 깨진다

```
① 원문 오프셋이 깨진다   ->  하이라이트 UI · 주장 스팬 BIO 라벨이 전부 어긋난다
② 회피 표기 자체가 신호다 ->  정규화로 지워버리면 그 신호를 잃는다
```

②가 덜 알려진 쪽이다. **회피 표기가 있다는 것은 작성자가 위법성을 인지하고 있었다는 뜻**이다. 지우면 그 정보가 사라진다.

### 1-3. 계약 — `NormalizedText`

```python
@dataclass(frozen=True)
class NormalizedText:
    raw: str                  # 원문. 표시·오프셋의 기준이다
    norm: str                 # 정규화문. 매칭의 기준이다
    offset_map: list[int]     # norm[i] -> raw 의 시작 오프셋 (len == len(norm))
    evasion_flags: list[str]  # 적용된 정규화 규칙 ID — ["JAMO_SPLIT", "SEP_INSERT"]
```

🚨 **`offset_map` 은 선택 사항이 아니다.** 보안 점검 P2-9 가 스팬을 `(offset, length)` 구조로 넘기라고 요구하고, 주장 스팬 BIO 라벨(D-30)과 하이라이트 UI 가 전부 이 좌표 위에 선다. **매칭은 `norm` 에서 하고, 결과는 `offset_map` 으로 `raw` 좌표로 되돌려 보고한다.**

### 1-4. 규칙표

| # | 플래그 | 규칙 | 예 |
|:-:|---|---|---|
| N1 | `FULLWIDTH` | 유니코드 **NFKC** 정규화 | `ｄｉｅｔ` → `diet` · `⑴` → `(1)` |
| N2 | `ZWSP` | 제어문자 · zero-width 제거 | `치​료` → `치료` |
| N3 | `JAMO_SPLIT` | **자모 분리 복원** | `다ㅇㅣ어트` → `다이어트` · `ㅊl료` → `치료` |
| N4 | `SEP_INSERT` | **구분자 삽입 제거** | `치·료` · `치.료` · `치+료` · `치 료` → `치료` |
| N5 | `REPEAT` | 반복 문자 축약 (3회 이상 → 2회) | `대박!!!!!` → `대박!!` |
| N6 | `SPACE` | 공백 정규화 (연속 공백 → 1칸) | |
| N7 | `HANJA` | **한자 → 한글 음차** (사전 기반 · 대상 어휘 한정) | `治療` → `치료` |
| — | — | 🚨 **이모지는 제거하지 않는다** | 문장 경계 신호로 쓴다 ([P4]) |
| — | — | 🚨 **대소문자 · 숫자는 건드리지 않는다** | 수치가 판정 대상이다 |

⚠️ **N4 는 과잉 적용 위험이 있다.** `비타민 C` 의 공백까지 지우면 정상 표현이 깨진다. **N4 는 사전 어휘의 문자 사이에서만** 적용한다 — 후보 어휘를 자모 단위로 펼쳐 정규식을 만들고(`치[^가-힣A-Za-z0-9]{0,2}료`), 매칭된 구간에만 적용한다. 전역 치환이 아니다.

### 1-5. `evasion_flags` 는 부산물이 아니라 피처다

★ 위험도 인코더의 **입력 피처**로 넣고 **가산 근거로 기록**한다. 안전 방향으로만 움직이므로(올리기만 한다) 기획서 3-5절의 단조성 원칙과 정합한다.

> **화면 문구 예시** — *"필터 회피로 보이는 표기(`치·료`)가 있어 위험도를 한 단계 올렸습니다."*
> 이것은 사람이 검증할 수 있는 근거다. 점수만 올리고 이유를 못 대면 D-59 의 판정 축이 흐려진다.

---

## 2. ★ [P11] 표면 변환 축 — 회피 표기 골든셋

### 2-1. 🚨 이것을 `TRANSFORM_RULES` 의 T11 로 넣으면 안 된다

D-84 는 *"골든셋에 「회피 표기」 축을 추가한다"* 고만 했다. **규칙표에 한 줄 더 넣는 것으로 읽으면 두 가지가 깨진다.**

| 깨지는 것 | 이유 |
|---|---|
| **주입 규약 1번** (1문장당 1규칙만) | 회피 표기는 **위법성을 만들지 않는다.** `치료` 를 `치·료` 로 바꿔도 위법 유형은 그대로 V2 다. 규칙표에 넣으면 한 문장에 T4 + T11 이 동시에 붙어 규약이 깨진다 |
| **유형별 Recall 분리 보고** (D-39 지표 1) | `rule_id` 로 규칙별 검출률을 보는데, 표면 변형이 같은 축에 섞이면 *"V2 를 못 잡은 것"* 과 *"표기를 못 뚫은 것"* 이 한 숫자가 된다. **정확히 구분해야 할 두 실패다** |

★ **의미 변환(위법성 부여)과 표면 변환(탐지 회피)은 직교한다.** 그러므로 **축을 따로 둔다.**

```
  [적법 문구] ──T4(의약품 어휘 삽입)──> [위법 문구: "치료 효과"]
                                              │
                                              ├── surface: none   →  "치료 효과"
                                              ├── surface: S2     →  "치·료 효과"
                                              └── surface: S3     →  "ㅊl료 효과"

  ★ 같은 rule_id(T4) · 같은 위법유형(V2) · 다른 surface_variant
     → 세 건의 Recall 차이가 곧 정규화 층의 성능이다
```

### 2-2. 표면 변환 축 `SURFACE_VARIANTS`

| ID | 변환 | 대응 정규화 규칙 | 실재성 |
|:-:|---|:-:|---|
| `S0` | 없음 (원형) | — | 기준선 |
| `S1` | 전각 · 유사 문자 치환 | N1 | 흔함 |
| `S2` | 구분자 삽입 (`·` `.` `+` 공백) | N4 | **가장 흔함** |
| `S3` | 자모 분리 (`ㅊl료`) | N3 | 흔함 |
| `S4` | zero-width 삽입 | N2 | 자동화 도구 산출물 |
| `S5` | 한자 · 음차 (`治療`) | N7 | 드묾 |

### 2-3. 골든셋 필드 추가

```
golden_sample:
    surface_variant : enum  S0 | S1 | S2 | S3 | S4 | S5     ← 신규
    surface_of      : fk    S0 원형 샘플의 id (S0 이면 null) ← 신규
```

**주입 규약에 두 줄을 더한다.**

| # | 규약 | 이유 |
|:-:|---|---|
| 9 | **표면 변형은 S0 원형과 짝으로만 만든다** (`surface_of` 필수) | 짝이 없으면 델타를 못 잰다. 이 축의 존재 이유가 델타다 |
| 10 | 🚨 **표면 변형본과 그 S0 원형은 반드시 같은 split 에 넣는다** | 원형이 train, 변형이 test 면 그것은 누수다 — [P12] 출처 분리와 같은 축 |

### 2-4. 측정 — 이 축이 답하는 질문 하나

```
정규화 층 이득 = Recall(surface = S0)  −  Recall(surface ∈ {S1..S5})
```

★ **0 에 가까울수록 정규화 층이 제 몫을 한 것**이다. D-77 L1-3(Span F1)에 축을 하나 더하는 형태이고, **정규화 코드를 끄고 한 번 더 돌리면 「층이 있어서 얼마나 벌었는가」가 그대로 나온다.**

### 2-5. 🚨 실제 회피 표기는 어디서 오는가

**합성만으로는 어떤 표기가 실재하는지 모른다.** 우리가 상상한 패턴만 방어하게 된다.

| 출처 | 내용 | 상태 |
|---|---|:-:|
| **식약처 부당광고 점검 보도자료** | 적발 문구 예시에 회피 표기가 그대로 실린다 | 수집 대상 (`mfds_press`) |
| **공정위 의결서** | 광고 원문 인용 구간 | 수집 대상 (`ftc_decisions`) |
| 대한화장품협회 해설서 2025 | 회피 사례 언급 여부 미확인 | 확인 필요 |

→ **수집 항목을 새로 만들 필요는 없다.** 이미 받는 자료에서 **회피 표기 실사례를 별도로 표시**하면 된다. 다만 **표시하지 않으면 그대로 흘러가므로**, `mfds_press`·`ftc_decisions` 파서에 회피 표기 탐지를 넣고 `evasion_observed` 카운트를 수집 보고서에 낸다.

⚠️ **실사례가 30건에 못 미치면 「측정 불가」로 보고한다** (D-40). 합성 변형만으로 낸 숫자를 실사례 성능처럼 말하지 않는다.

### 2-6. 🆕 회피는 문자에만 있지 않다 — **시각적 회피 표기** <sub>(2026-09-03 · UI v6.7 흡수 · D-119)</sub>

지금까지 이 장은 **문자 축**만 다뤘다 — `치·료` · `ㅊl료`. 그런데 **가장 흔한 회피는 문자가 아니다.**

```
  필수 병기 문구를 본문의 1/5 크기로 넣는다
  → 텍스트는 정상이다. 정규화로도 잡히지 않는다. 렌더 결과에만 나타난다.
```

🚨 **이 축은 전처리에서 잡을 수 없다.** 문자열이 온전하므로 `sep_norm()` 도 회피 표기 계수도 반응하지 않는다.
**막는 자리는 템플릿 층(진입점 C)이고, 방법은 탐지가 아니라 강제다.**

| | 문자 회피 표기 (2-1~2-5) | **시각적 회피 표기 (신설)** |
|---|---|---|
| 어디에 나타나는가 | 문자열 | **렌더 결과** |
| 우리 대응 | 정규화 + 계수 (탐지) | 🚨 **크기 하한 강제** (구조) |
| 규칙 | `sep_norm()` 나열 구분자 전개 | **`max(본문 × 70%, 13px)`** — 법정 기준 70% 와 KRDS 접근성 하한 13px 중 **큰 쪽** |
| 집행 | 전처리 층 | 템플릿 에디터가 **하한 밑으로 못 내려가게 막는다** |

★ **탐지가 아니라 강제인 이유** — 렌더 결과를 검사하려면 스크린샷을 판정해야 하고, 그것은 **D-104(이미지는 판정하지 않는다)** 에 정면으로 걸린다.
**만들 때 못 만들게 하면 검사할 필요가 없다.** 필수 표기 섹션의 「삭제 불가 · 항상 마지막」도 같은 형태다.

> ★ **발표에서 쓸 수 있다** — *"금지 표현만 막는 것이 아니라, **고지를 숨기는 방법 자체를 구조로 막습니다.**"*

⚠️ **골든셋 축에는 넣지 않는다.** 문자 축(`surface_variant`)과 달리 **텍스트로 표본을 만들 수 없다.** 측정은 산출물 렌더 검증(REQ-N-011)으로 하고, D-40 의 30건 기준 대상이 아니다.

---

## 3. [P10] `TRANSFORM_RULES` — 양방향 단일 규칙표 (D-74)

> 기획서 5-4절(결함 주입)과 5-7절(변환 규칙 R1~R3)은 **같은 규칙표의 두 방향**이다. 하나를 만들어 두 번 쓴다.

```
     [적법 문구]  ──── 정방향(주입) ────>  [위법 문구]
          ^                                     │
          └──────── 역방향(시정) ───────────────┘

  정방향 산출 : 골든셋 (정답을 아는 위법 샘플)      -> 인코더 학습·평가
  역방향 산출 : (위법, 시정) 페어                   -> sLLM SFT
  ★ 부산물   : 변형 구간 좌표 = 주장 스팬 BIO 라벨  -> 라벨링 비용 0
```

| ID | 정방향 (주입) | 역방향 (시정) | 라벨 | 대상 |
|:-:|---|---|:-:|---|
| **T1** | 완화구 삭제 (`도움을 줄 수 있다고 알려진` 제거) | **단정 → 조건부** (R2) | V5 / R2 | 인정 기능성 문구 |
| **T2** | 기능성 표현 → **질병명 치환** (`눈 건강`→`백내장 예방`) | 질병어 → 지향어 | V1 / R4 | 전 카테고리 |
| **T3** | 제품유형을 일반식품으로 바꾸고 기능성 표현 유지 | 유형에 맞는 표현으로 | V3·V4 / R3 | 건기식 문구 |
| **T4** | **의약품 어휘 삽입** (`치료제`·`처방`·`특효`) | 어휘 제거 | V2 / R3 | 전 카테고리 |
| **T5** | **최상급·절대 표현 삽입** (`100%`·`완치`·`유일한`) | 표현 완화 | V5 / R3 | 전 카테고리 |
| **T6** | 후기·체험기 형식 변환 + 대가성 미표시 | 대가성 표시 **병기 추가** | V6 / R3 | 전 카테고리 |
| **T7** | 경쟁사 비교·비방 삽입 | 비교 대상 제거 | V7 / R2 | 전 카테고리 |
| **T8** | 수치 삽입 후 **산정 근거 제거** | ★ 산정 근거 **병기 의무** (R3) | V5·V6 / R3 | 수치 포함 문구 |
| **T9** | **실적 표현화** (`~하셨어요`·`~되찾아가셨어요`) | ★ **실적 → 예상** (R1) | V5 / R2 | 성과 언급 문구 |
| **T10** | 화장품에 **세포·재생·흉터 어휘 삽입** | 어휘 제거 | V2 / R3 | 화장품 문구 |

★ **T8 · T9 의 역방향이 기획서 5-7절의 R3 · R1 과 정확히 일치한다.** 의결서 시정 페어에서 귀납한 규칙이 규칙표에 그대로 들어앉는다.

### 주입 규약

| # | 규약 | 이유 |
|:-:|---|---|
| 1 | **1문장당 의미 규칙 1개만** | 다중 라벨이면 유형별 Recall 분리 보고가 흐려진다 |
| 2 | **변형 전/후 오프셋을 반드시 기록** | ★ 주장 스팬 BIO 라벨이 여기서 나온다 |
| 3 | 🚨 **원본 문구도 `V0` 라벨로 함께 넣는다** | 안 넣으면 골든셋이 전부 위법 → 클래스 불균형 재발 |
| 4 | 유형별 목표량을 먼저 정하고 역산 배분 | 유형당 **최소 150건** (train 120 / dev 30) |
| 5 | 🚨 **주입본은 절대 `test_holdout` 에 넣지 않는다** | 평가는 실사례 홀드아웃으로만 |
| 6 | 변형 규칙 ID 를 샘플에 기록 (`rule_id`) | 규칙별 검출률 분리 보고 |
| 7 | **주입과 동시에 측정 축 3종을 채운다** (아래 4장) | T9 → `시제=과거`, T1 → `확실성=확실`. **규칙이 곧 라벨** |
| 8 | 🚨 **`provenance` · `redistributable` 을 모든 행에 기록** | 5장 참조 — 나중에 되돌릴 수 없다 |
| 9 | **표면 변형은 S0 원형과 짝으로만** (`surface_of` 필수) | 짝이 없으면 델타를 못 잰다 |
| 10 | 🚨 **표면 변형본과 S0 원형은 같은 split** | 누수 방지 |

---

## 4. 측정 축 3종 — 변환이 실제로 일어났는지를 잰다 (D-74)

**「재판정 통과율」은 결과를 재지만, R1·R2 가 실제로 작동했는지는 재지 못한다.** AI Hub 71486(문장 유형 판단 데이터)의 라벨 축이 우리 변환 규칙과 정확히 맞물린다.

| 축 | 값 | 대응 변환 |
|---|---|---|
| **확실성** | 확실 / 불확실 | **R2 단정 → 조건부** (T1 역방향) |
| **시제** | 과거 / 현재 / 미래 | **R1 실적 → 예상** (T9 역방향) |
| **문장유형** | 사실 / 추론 / 예측 / 대화 | **B 실증형 판정** (D-59) |

★ **발표 지표 2번(재판정 통과율 + 내용 보존율)을 규칙별로 분해해 함께 보고한다** (D-74 · 사실원장).

🚨 **AI Hub 71486 은 `NOREDIST` 다.** 이 축의 라벨러를 학습시키는 것은 되고(U1 allow · U4 allow), **그 데이터를 우리 골든셋에 섞어 공개하는 것은 안 된다.** 아래 5장이 그 경계를 다룬다.

---

## 5. 🚨 `provenance` · `redistributable` — 수집 시점에만 기록할 수 있다

D-71 의 전파 규칙은 이것이다.

```
데이터셋 공개 = { f | f 에 NOREDIST 가 없음 }
배포 모델 학습 = { f | f.U1 AND f.U4 }        ← 다른 축이다
```

**골든셋에 `redistributable: false` 소스에서 온 문장이 한 줄이라도 섞이면 그 골든셋 전체를 공개 배포할 수 없다.** 그리고 **섞인 뒤에는 어느 줄이 어디서 왔는지 되돌릴 수 없다.**

| 필드 | 위치 | 값 |
|---|---|---|
| `fragment_id` | 모든 문장·청크·학습샘플 | FK 필수 (D-20) |
| `provenance` | 골든셋 · 학습샘플 행 | 유래 소스 키 (`aihub_71486` 등) |
| `redistributable` | 골든셋 · 학습샘플 행 | `fragment → source` 에서 상속. **행 단위로 복사해 둔다** |

⚠️ **왜 상속값을 행에 복사하는가** — 조인으로 매번 계산하면 공개 시점에 소스가 이미 지워졌거나(D-20 캐스케이드) 등급이 바뀐 경우 답이 달라진다. **공개 판정은 그 행이 만들어진 시점의 사실로 한다.**

★ **데이터셋 공개용 뷰는 이미 있다** — `v_publishable_golden` (`redistributable = true` 필터).

---

## 6. 나머지 단계 — 계약만 적고 상세는 참조

| 단계 | 계약 | 상세 |
|---|---|---|
| **[P1] 파싱** | HWP·PDF·HTML → `document.raw_text` + `page_no` | 의결서 `<그림 N>` 캡처는 **추출하지 않는다** (G1 · D-18) |
| **[P3] 마스킹** | 업체명·상표·대표자명 → `[업체]`·`[상표]`·`[대표]`. **원문 미보관** | 🚨 제품 **유형명과 수치는 보존**한다 — 판정 대상이다 (D-17) |
| **[P4] 문장 분할** | 광고 텍스트는 마침표가 없다. 줄바꿈·이모지·해시태그를 경계로 쓴다 | D-77 L1-1 이 분할 정확도를 잰다 |
| **[P5] 청킹** | 조문 단위 · 리랭커 512 토큰 상한 | [`청크_스키마.md`](../02_설계/청크_스키마.md) 가 계약 |
| **[P6] 사전** | 금지/허용 표현 · 완화어휘 금지 사전 | 🚨 **N4 의 후보 어휘가 여기서 나온다** — 사전과 정규화 층은 같은 어휘 목록을 쓴다 |
| **[P12] 분할** | 🚨 **출처 분리.** 같은 의결서 문구가 train/test 에 섞이면 F1 이 부풀려진다 | 주입본은 `test_holdout` 금지 |

---

## 7. 미해결

| # | 항목 | 기한 |
|:-:|---|---|
| 1 | 🚨 **N7(한자 음차) 대상 어휘 목록** — 전역 변환은 위험하다. 사전 어휘로 한정하되 그 목록이 아직 없다 | [P6] 사전 구축과 동시 |
| 2 | **N4 의 구분자 허용 폭** (`{0,2}` 가 맞는가) — 넓히면 오탐, 좁히면 회피 | 회피 표기 실사례 확보 후 |
| 3 | **회피 표기 실사례 건수** — 30건 미만이면 「측정 불가」 (D-40) | 2W 수집 후 |
| 4 | 표면 변환 축을 **위험도 인코더 피처로 넣을 때의 가중** | Phase 2 |

---

## 근거

D-17(마스킹) · D-18(프래그먼트 단위 등급) · D-20(캐스케이드 삭제) · D-30(주장 원장) · D-39(발표 지표 3) · D-40(30건 미만 측정 불가) · D-59(불가 사유 A/B/C) · D-71(NOREDIST) · D-74(양방향 규칙표 · 측정 축 3종) · D-77(평가 지표 4층) · D-82 · D-83(문맥 5층) · **D-84 ①(문자 수준 정규화 층)** · 🔄 **D-119**(진입점 C — 시각적 회피 표기를 막는 자리) · 🔄 **D-120**(입력 형태가 문서 경계를 선언한다 — 층 3 적용 범위) · 🔄 **D-122**(`NOTRAIN`) · 보안 점검 P2-9
