# 거버넌스 집행 계약 — 「코드로 강제한다」가 실제로 어디에 박혀 있는가

> **작성자** 오한빈 (팀장) · **2026-08-31** · 최종 갱신 **2026-09-21** · 근거 **D-15 · D-19 · D-20 · D-51 · D-65 · D-66 · D-71 · D-73 · D-78 · D-89 · D-90 · D-91 · 🔄 D-122**

> 기획서 4-1절이 *"거버넌스는 문서가 아니라 코드로 강제한다"* 고 했고, 차별점 ⑤가 그것이다.
> **그런데 「어디에 어떻게」가 네 곳에 흩어져 있다.** 이 문서가 그 지도다 — 새 검사를 넣을 때
> **어디에 넣어야 하는지**부터 정한다.

---

## 0. 🚨 어디에 넣을 것인가 — 판단 기준

| 질문 | 넣는 곳 | 왜 |
|---|---|---|
| **기기마다 답이 다른가?** | `scripts/doctor.py` | 내 PC에 postgres가 떠 있는지는 저장소가 답할 수 없다 |
| **저장소에서 답이 하나인가?** | `tests/test_governance_layout.py` | 🚨 **CI·PR에서 도는 쪽.** doctor에만 두면 **아무도 안 돌린 PR이 그대로 병합된다** — 🔄 **2026-09-11 부터 실제로 돈다**: `.github/workflows/gate.yml` `tests` 잡 |
| **행마다 답이 다른가?** | **DB `CHECK` 제약** | 코드를 우회해 INSERT해도 막힌다 |
| **되돌릴 수 없는가?** | **수집 시점 필드** | `provenance`·`redistributable` 은 나중에 복원 불가 |

★ 위 세 줄이 **D-89** 이고, 네 번째 줄이 **D-71**이다.

> ⛔ **2026-09-11 정정 — 둘째 줄은 그날까지 거짓이었다.** *"CI·PR 에서 도는 쪽"* 이라고
> 근거를 세워 놓고 **CI 가 없었다**(`.github/` 자체가 없었다). 검사는 로컬 pre-commit 훅뿐이었고
> 훅은 각자 `uv run pre-commit install` 을 쳐야 붙으며, **pytest 를 돌지 않는다.**
> 즉 계약이 경고한 *"아무도 안 돌린 PR 이 그대로 병합된다"* 가 **계약 자신에게** 일어나 있었다 —
> D-170(실패할 수 없는 단언을 적지 않는다)의 문서판이다.
>
> ★ **그날 밤 CI 를 세우면서 이 줄이 처음으로 참이 됐다.** 워크플로 둘 —
>
> | 파일 | 잡 | 무엇 |
> |---|---|---|
> | `.github/workflows/gate.yml` | `tests` (windows-latest) | `uv sync --frozen` → `ruff check` → `pytest -m gate -rs` → 전체 |
> | | `hooks` (ubuntu-latest) | `pre-commit run --all-files` |
> | `.github/workflows/secrets.yml` | `history` (ubuntu-latest) | gitleaks **이력 전체** (`fetch-depth: 0`) · 주 1회 + PR |
>
> 🚨 **`paths-ignore` 를 넣지 않았다** — 문서만 고쳐도 게이트가 돈다. `test_governance_layout.py`
> 가 **문서를 읽어** D-54 단일 출처를 검사하기 때문이다.
> 🔴 **그래도 게이트 일곱은 CI 에서 안 돈다** — `data/**` 파생물이 있어야 돌고 그것은
> 커밋하지 않는다(D-19). 그 일곱은 **데이터가 있는 기기에서 `launcher.py gate`** 가 증명한다.
> `-rs` 가 건너뛴 이름을 로그에 남기는 이유가 이것이다 (D-188).
>
> 🚨 **전체 게이트 수를 여기 적지 않는다** (D-54). ⛔ 09-11 에 「176 중 7」이라 적었고
> **하루 만에 194 가 되면서 그 줄이 낡았다.** 분자만 사실이고 분모는 매일 바뀐다 —
> 분모는 `docs/00_사실원장.md` 「거버넌스 게이트」 행이 단일 출처다.
> 🔄 **2026-09-12 밤** — 옮기고 보니 **원장 안에도 낡은 분모가 현재형으로 남아 있었다**
> (「게이트 176 중 7」 · §④). 원장 쪽도 분모를 뺐다. **분자 7 은 여전히 사실이다.**

---

## 1. `tests/test_governance_layout.py` — 저장소 게이트 🔄 **46개** (`def test_` 전수 · 2026-09-21 실측 · 전부 `@pytest.mark.gate`)

> 🔄 **2026-09-21 (같은 날 둘째)** — 45 → **46**. `test_G2_는_삭제_경로가_붙기_전에는_받지_않는다` (D-92 · 아래 G2 상자). `def test_` 46 · `@pytest.mark.gate` 46 (grep).

> 🔄 **2026-09-21 (클론 A 검토)** — 43 → **45**. `def test_` 45 · `@pytest.mark.gate` 45 (grep 으로 셌다). 09-16 이후 둘이 늘었는데
> 이 머리글이 안 따라왔다. ⬜ 아래 표(1~34)와 맞추는 일은 여전히 안 했다 — 위 「표가 6 적다」 문단이 그대로다.

> ⛔ 종전에 「**40개**(pytest **47건** · 2026-09-12 실측)」이라 적혀 있었다. 함수 수는 **43** 이다.
> ⬜ **pytest 건수는 미측정이다** — 이 파일에 `parametrize` 가 셋 있어 함수 수보다 크고,
>    `-m gate` 전체(326)에서 이 파일 몫만 떼어 재지 않았다 (D-146).
> 🚨 09-14 인계 §3-1 이 「43 → 45 함수」라 적었는데 **거기 적힌 게이트 둘은 파일에 있고
>    총계는 43** 이다 — 41 → 43 이었고 인계의 수가 틀렸다. 손으로 적은 수가 또 낡은 자리다.

> 🔄 **2026-09-12 밤 — 실측일을 09-11 에서 09-12 로 고쳤다.** 같은 수(함수 40 · 케이스 47)를
> 원장 `:108` 은 「2026-09-12 실측」이라 적고 여기는 「2026-09-11」이라 적어 **날짜가 두 벌**이었다.
> 세는 작업은 09-12 세션에서 했다(인계 09-12 §2 「게이트 수 — 세었다」). ⛔ 수는 같아도
> **날짜가 다르면 어느 판을 센 것인지 못 고정한다** (D-176 — 재현의 단위는 커밋이다).
>
> ✅ **2026-09-12 — 세었다.** 이 문단이 요구한 확인이다.
>
>     def test_                  40개    ← 검사 함수
>     @pytest.mark.gate          40개    ← 전부 붙어 있다. 안 붙은 함수 0
>     파라미터화 확장 포함        47건    ← pytest 가 실제로 세는 수
>       test_수집_url_은_ascii_로_인코딩된다   ×4
>       test_수동보강_표에_중복_키가_없다      ×5
>
> 🚨 **세 수가 전부 낡아 있었다** — 헤더 30 · pytest 38 · 아래 표 34. 셋 다 2026-09-07 이전 값이다.
> 🔴 **넷째 벌이 있었다** — `docs/00_산출물현황.md` 가 같은 칸을 「게이트 **40건**(2026-09-09 밤 실측)」
> 이라 들고 있었다. **함수 수(40)를 케이스 단위(건)로** 적은 것이라, 09-12 에 D-167 로 이름 붙인
> 축 혼동이 그대로 남아 있던 자리다. 2026-09-12 밤에 **수를 지우고 원장을 가리키게** 고쳤다 (D-54).
> 🔴 **아래 표(1~34)가 실제 검사보다 6 적다.** 표는 게이트의 **사본**이고 정본은 코드다 —
>    표에 없는 검사 6개가 무엇인지는 아직 안 맞춰 봤다. **모른다는 것을 여기 적어 둔다** (D-170).
> ★ 교차 검증 — `pytest -m gate` 진행률에서 이 파일이 32%→59% 구간이고, 앞선 58건과 뒤따르는
>   `test_graph_routers` 31건이 77% 에 떨어지려면 이 파일이 **47** 이어야 한다. 46 이면 76% 다.

> 🚨 **검사 37종이 아니라 pytest 케이스 37건이다.** 검사 함수는 **30개**이고, 1번(등급 디렉터리)이 5종으로 파라미터화돼 있어 `1~5` 로 센다.
>
> 🔄 **2026-09-02~03 — 키 유출 방지 5건(30~34)이 붙어 35→37 이 됐다.** 아래 마지막 절.

```bash
uv run pytest -m gate        # 또는  python launcher.py gate
```

### 형태 검사 11건 (D-19 · D-15 · D-23)

| # | 검사 |
|:-:|---|
| 1~5 | 등급 디렉터리 5종 존재 — `g3` `g2_facts` `g2_norepub` `quarantine` `.g1_blocked` |
| 6 | `.g1_blocked/` 가 비어 있다 — 파일이 있으면 즉시 실패 |
| 7 | `.env` 가 git에 추적되지 않는다 |
| 8 | 모든 소스에 등급과 용도 4축이 있다 |
| 9 | G1 소스는 전 용도 deny |
| 10 | `blocked` 는 전부 G1 이고 사유가 있다 |
| 11 | 모든 모델에 라이선스와 등급이 있다 |

### 🔄 의미 검사 5건 (2026-08-31 추가 · D-90 ④)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 12 | **등급이 허용하는 용도 상한** | 🚨 **G0 에 학습·원문색인이 열려 있던 실제 결함** (`kfia_approved_list`) |
| 13 | 제약 플래그가 정의된 집합 안 | `NOREDIST` 오타 하나가 조용히 제약을 없애는 것 |
| 14 | ★ `NOREDIST` ↔ `redistributable` 정합 | **D-71 전파 규칙을 문서에서 코드로** |
| 15 | ★ `collected_at` 이 있으면 2인 확인 완료 | **「수집 전에 확인」이 뒤로 밀리는 것** |
| 16 | 모델에 `use.U4` 필드 | KLUE-RoBERTa 「배포 금지」가 **주석**으로만 있던 것 |

> ★ **15번의 형태가 핵심이다.** 「수집 전에 하라」는 지시는 반드시 밀린다.
> **시점을 못 박으면** — 수집을 시작하는 순간이 마지노선이고 그때는 이미 되돌릴 수 없다 — **지시가 게이트가 된다.**
>
> ★ **검증 방법** — 새 게이트를 **이전 레지스트리에 돌려 4건이 실패하는 것**을 먼저 확인했다.
> **통과만 하는 게이트는 게이트가 아니다.**

### 🔄 배치 검사 3건 (2026-08-31 추가 · D-92 · D-100)

> 🚨 **원문은 등급 디렉터리 밖(`data/raw/`)에 둔다.** D-18(판정 단위는 FRAGMENT)과
> D-19(위치가 곧 게이트)를 동시에 만족시키는 유일한 배치다 — 한 원문 파일 안에서
> 조각의 등급이 갈리면 그 파일은 **어느 등급 디렉터리에도 놓을 수 없다.**
>
> 그 대가로 `raw/` 안에서는 등급이 섞인다. **구조가 못 막는 자리를 아래 둘이 대신한다.**

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 17 | ★ `data/raw` 참조가 `collect/`·`preprocess/` 밖에 있으면 실패 | 🚨 **학습 스크립트가 `data/raw/**` 를 글롭하는 것** — 하나만 있어도 G2·G0 원문이 학습에 들어간다 |
| 18 | ★ `manifest.jsonl` 의 `source_id` 가 레지스트리에 있고 **G1 이 아닐 것** | G1 이 raw 에 들어오는 것 |
| 19 | ★ **용도가 열린 소스는 모두 판정 근거(`why`)를 가질 것** | 🚨 **근거가 조용히 사라진 채 서명이 들어가는 것** — 매트릭스 id 와 레지스트리 키가 어긋나면 검토표에 「판정 근거 없음」만 뜨고, 근거가 없어서인지 이름이 어긋나서인지 검토자는 구분할 수 없다 |

> ★ **17번은 doctor 검사 16번(판정 경로 외부 네트워크 요청 = 0)과 같은 형태다.** 「읽지 마라」는 규약은
> 코드에 박아야 지켜진다. `raw/` 를 읽어도 되는 곳은 **수집기와 전처리기뿐**이다.
>
> ★ **19번은 2인 확인(D-66)의 전제를 지킨다.** 2인 확인은 동의를 받는 절차가 아니라
> 판정을 **재현**하는 절차이므로, 재현할 근거가 없으면 절차 자체가 성립하지 않는다.
>
> 🚨 근거의 원본은 `docs/03_데이터/판정매트릭스.html` 이고, 검토용 사본은
> `scripts/registry_rationale.yaml` 이다 (`scripts/extract_rationale.py` 가 뽑는다).
> 둘의 단위가 다르다 — 매트릭스는 **판정 근거** 단위 🔄 77건(D-132 까지), 레지스트리는 **이용조건**
> 단위 🔄 43건이다 (D-90 · 2026-09-03 저녁 실물). 그 사이를 잇는 것이 `ALIAS` 표이고, **19번이 지키는 것은
> 사실상 이 표가 낡지 않는 것**이다.
>
> 🚨 근거는 `data_sources.yaml` 에 넣지 않는다. 그 파일은 **수집기가 읽는 집행 목록**이고
> 판정 근거는 **사람이 읽는 검토 자료**다. 집행 산출물에 산문을 섞으면 게이트가
> 산문을 검사하게 된다.

### 🔄 절차·구조 검사 3건 (2026-09-02 추가 · D-103 · D-108)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 20 | ★ 크롤링형 소스는 `robots_checked_at` 을 가질 것 | 🚨 **게이트는 초록불인데 수집기 첫 줄에서 죽던 자리** — 규약 6 이 요구하는 필드를 만드는 코드가 어디에도 없어 `mfds_hf_ingredient_board`(568건)가 영구 수집 불가였다 |
| 21 | ★ `judgment.subject_type` 이 CHECK 로 고정될 것 | 다형 참조는 FK 무결성이 DB 수준에서 안 걸린다 — **오타 하나가 조용히 새 종류의 판정을 만드는 것** (D-103) |
| 22 | ★ **`status: collect` 는 용도가 하나 이상 열려 있을 것** | 🚨 **G0 를 fail-closed 로 닫아 둔 이유(D-72)가 수집 단계에서 우회되는 것** — 등급은 「쓸 수 있는가」를 막았는데 「가져오는가」는 막지 않았다 (D-108) |

> ★ **22번이 규칙을 늘리지 않고 잡는다.** G0 는 정의상 전 용도 `deny` 이므로 **등급을 따로 검사하지 않아도** 자동으로 `collect` 가 될 수 없다. 이미 있는 축(`use`)으로 새 축(`status`)을 묶는다.
>
> 🚨 **역은 걸지 않는다.** `hold` 인데 용도가 열린 것은 「판정 끝, 착수만 남음」이며 정상이다. `status` 는 **가져오는가**, `use` 는 **가져온 것을 쓸 수 있는가** — 한쪽을 다른 쪽에서 유도하려 들면 상태값이 다시 뭉개진다.
>
> ★ **검증 방법** — 20번과 22번 모두 **이전 레지스트리에 돌려 실패하는 것**을 먼저 확인했다 (22번은 `kfia_approved_list` 1건). **통과만 하는 게이트는 게이트가 아니다.**

### 🔄 탐침 검사 2건 (2026-09-02 추가 · D-109)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 23 | ★ **탐침이 저장 경로를 부를 수 없을 것** | 🚨 **게이트 전체의 우회로** — 탐침은 `reviewed_by` 를 면제받으므로 여기 쓰기가 한 줄 생기면 「일단 받아두고 나중에 판정한다」가 된다 (규약 1 의 경고) |
| 24 | ★ 탐침 게이트가 **G1 · `manual` · `GATED` · robots 미확인**을 막고 **G0 는 통과시킬 것** | 면제 하나(`reviewed_by`)가 넷을 데려가는 것 · 반대로 G0 를 막아 **확인 수단이 판정 뒤로 밀리는 것** |

> ★ **23번은 「약속」이 아니라 「구조」를 검사한다** (D-107 조건 1 과 같은 형태). 셋을 본다 —
> ① **AST** 로 `store` import·저장 호출 부재 ② `write_text` 가 정확히 한 자리이고 `docs/` 일 것
> ③ 🚨 **런타임** — 새 프로세스에서 `import collect.probe` 후 `sys.modules` 에 `collect.store` 가 없을 것.
>
> 🚨 **①을 문자열로 하면 설명이 위반으로 읽힌다** — 탐침 docstring 의 *"`store` 를 import 하지 않는다"* 라는
> **문장 자체**에 걸렸다. 게이트가 산문을 검사하기 시작하면 사람이 주석을 지우게 된다.
>
> 🚨 **③이 실제 결함을 잡았다.** `collect/__init__.py` 의 재수출 때문에 **탐침 프로세스에 저장 코드가
> 로드**되고 있었고, ①·② 는 둘 다 통과했다. **「안 부른다」와 「부를 수 없다」의 차이**가 거기서 갈렸다.

### 🔄 탐침 1회전이 만든 검사 2건 (2026-09-02 · D-109)

> ★ **둘 다 탐침을 돌리다 실제로 부딪힌 결함이다.** 게이트를 먼저 상상해서 만든 것이 아니다.

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 25 | ★ 수집 URL 이 **ASCII 로 인코딩될 것** (3건 파라미터화) | 🚨 한글 경로 URL 에서 `UnicodeEncodeError` 가 **`FetchError` 가 아니라 재시도 루프 밖에서** 터져 한 소스가 전체 실행을 멈추던 것. 나스미디어 2건(`/정기보고서/…`) |
| 26 | ★ `gen_registry.py` 의 `EXTRA`·`STATUS` 에 **중복 키가 없을 것** | 🚨 dict 리터럴의 중복 키는 **조용히 뒤엣것이 이긴다** — 탐침 소견을 넣다가 `masking`·`fragment_note` 를 통째로 날렸다. 파이썬도 ruff 도 잡지 않고 실행은 성공한다 |

> 🚨 **25번은 탐침이 아니라 수집기의 버그였다.** `collect/http.py` 는 둘의 공용이라 같은 URL 로
> 수집기를 돌렸어도 똑같이 죽었다. **탐침이 먼저 돈 덕에 수집 착수 전에 나왔다** — D-109 가 노린 효과다.
>
> 🚨 **26번은 이 저장소의 반복 결함과 같은 형태다** — 값이 두 칸 사이에서 사라지고,
> 사라진 자리를 검사하는 것이 없다 (역검토 v1.3 부록 · 게이트 19 · 23-③ 과 한 묶음).

### 🔄 승인 기록 검사 1건 (2026-09-02 · D-109)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 27 | ★ **`GATED` 소스는 `approved_at` 없이 열리지 않을 것** — 탐침과 수집기 **양쪽에서** | 🚨 **`require()` 가 `GATED` 를 아예 보지 않고 있었다** — 탐침이 막던 조건을 정작 받아 오는 쪽은 검사하지 않아 **승인 없이 받는 경로**가 열려 있었다 |

> 🚨 **탐침을 만들지 않았으면 드러나지 않았을 자리다.** 게이트 25(한글 URL 버그)에 이어
> **탐침이 수집기의 결함을 잡은 두 번째 사례**다 — 같은 조건을 두 경로가 따로 구현하면 갈린다.
>
> 🚨 `approved_at` 은 **「신청했다」가 아니라 「승인됐다」**의 날짜다. 둘을 같은 칸에 적으면
> 승인 대기 중인 소스가 승인된 것으로 읽히고, **그 오독은 약관 위반으로 끝난다.**
> `approved_by` 를 짝으로 요구하는 이유도 같다 — AI Hub 는 **내국인 한정** 데이터셋이 있어
> 「누가 신청했는가」가 조건의 일부다.

### 🔄 등록 경로 검사 1건 (2026-09-02 · D-109)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 28 | ★ `ingest.register` 는 **`require()` 로 시작**하고 탐침의 `probe()` 를 쓰지 않을 것 · `manifest_append`·`mark_collected` 를 부를 것 · **`count` 는 아무것도 바꾸지 않을 것** | 🚨 사람이 받아 온 파일이 **2인 확인을 건너뛰고 원장에 오르는 것** · provenance 없이 raw 에 들어오는 것 |

> ★ **게이트 23 의 정확한 반대다.** 23 은 탐침이 **저장하지 못하게** 하고, 28 은 등록이
> **문을 건너뛰지 못하게** 한다. 둘 다 「약속」이 아니라 **호출 그래프**를 본다.
>
> 🚨 **사람이 손으로 받아 왔다는 사실이 2인 확인을 면제하지 않는다.** 수집기가 돌지 않는
> 소스에서는 이 문이 **유일하게 남은 게이트**이고, 탐침의 느슨한 문(`probe`)을 물려주면 사라진다.

### 🔄 파생 소스 검사 1건 (2026-09-02 · 권소라 2인확인 §6-8)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 29 | ★ 파생 소스는 **원천보다 넓게 열리지 않고**, 원천보다 **먼저 서명되지 않을 것** | 🚨 원천이 재판정으로 닫혀도 **파생물은 열린 채 남는 것** · 원천을 확인하지 않은 사람이 파생물 등급에 서명하는 것 |

> ★ **검토자가 예측하고 게이트가 확인한 첫 사례다.** 권소라가 2인 확인 §6-8 에서
> *"파생물이 원천보다 먼저 서명되는 순서가 된다"* 고 적었고, 게이트를 만들자
> **`self_sanction_stat` 이 서명됐는데 원천 `law_go_kr` 이 미검토**라며 그대로 빨간불이 됐다.
>
> 🚨 해소는 **서명을 원천 뒤로 미루는 것**이다 — 확인은 끝났으므로 **이견이 아니라 순서 대기**이고,
> 그래서 `dissent_note` 를 쓰지 않는다. `derived_from` 을 **필드로** 둔 것이 이 검사의 전제다:
> 「어디서 나왔는가」가 산문에만 있으면 아무도 그 연결을 보지 않는다.

### 🆕 사용자 업로드물 검사 1건 — **예정 · 미구현** (2026-09-03 · D-122)

| # | 검사 | 잡는 것 |
|:-:|---|---|
| 35 (예정) | 🚧 **예정** — ★ `UPLOAD_ROOT`(= `NOTRAIN` 경로 상수 · D-128)를 학습·색인 코드가 **`consent_train = true` 필터 없이 참조하면 실패**. 경로는 `upload_blob.path` 한 곳(D-129) | 🚨 **사용자가 올린 이미지·파일·입력 문구가 학습에 들어가는 것.** 게이트 17 이 `data/raw/` 를 지키는데 **업로드물은 다른 경로로 들어와 어느 게이트도 막지 않는다** |

> 🚨 **아직 코드가 없다.** 이 표에 있다는 사실이 집행을 뜻하지 않으므로 **`예정`을 지우지 않는다** —
> 게이트 수(`pytest -m gate`)에도 포함하지 않는다. 구현은 WBS 2.10 (9/10~9/14).
>
> ★ **자리는 D-73 이 이미 정해 뒀다.** 등급(G0~G3)이 아니라 **「막는 지점」 축**이다 —
> 수집(`NOSTORE`) / 재배포(`NOREDIST`) / 공개(`PREAPPROVAL`) / 런타임(`QUERYLOG`)에 이어
> **학습(`NOTRAIN`)이 그 축의 다섯째**다. **등급표를 건드리지 않는다** (D-122).
> 형태는 **게이트 17 과 같다** — 경로 참조를 정적으로 검사한다.

### 🔄 키 유출 방지 검사 5건 (2026-09-02~03 · D-111 · D-112)

> 🚨 **D-111 이 센 경로는 셋이었다 — 화면 입력 · 셸 기록 · 키 이름. 넷째가 있었다.**
> 2026-09-02 `cosmetic_*` 두 건이 403 으로 실패했고 `FetchError` 가 요청 URL 을 통째로 찍어
> `serviceKey` 값이 터미널·스크롤백·**대화 기록**에 남았다. 키를 재발급했다.
> 앞의 셋과 성질이 다르다 — **사람이 실수해야 새는 것이 아니라 코드가 정상 동작할 때 샌다.**

| # | 검사 | 막는 것 |
|:-:|---|---|
| 30 | ★ **`.env.example` 에 실제 값이 없을 것** | 🚨 `.env` 와 `.env.example` 은 **한 글자 차이**이고 나란히 있다. 잘못 여는 것은 드문 실수가 아니라 **예상되는 실수**다. gitleaks 는 키 **모양**을 보므로 모양이 평범한 키는 지나간다 — 여기서는 모양이 아니라 **자리**를 본다 |
| 31 | ★ **키 입력 경로가 값을 인자로 받지 않을 것** | 값이 인자로 지나가면 **셸 기록에 남아** 마스킹 입력을 만든 뜻이 사라진다. 🚨 「급하니까 이번만」인 `--key` 가 커밋되면 그 뒤로는 그것이 표준 사용법이 된다. 검사는 **AST** 로 한다 |
| 32 | ★ **설정파일이 이름을 바꿔 새지 않을 것** (`.env.*` + `!.env.example`) | `.gitignore` 의 `.env` 한 줄로는 모자라다 — 편집기·OS 가 `.env~` `.env.bak` `.env.save` `.env.local` 을 만든다. **안에 든 것은 똑같은 키인데 이름이 달라 안 걸린다.** 🚨 두 줄의 **순서**가 뒤바뀌면 예제가 커밋되지 않아 팀원이 키 이름을 알 수 없다 |
| 33 | ★ **오류 메시지가 키를 그대로 찍지 않을 것** (D-112) | 🚨 **코드가 정상 동작할 때 새는 경로.** `FetchError` 의 f-string 에 맨 `url` 이 들어가는 것을 AST 로 막고, 인코딩 4형태 + 경로형 키가 실제로 가려지는지 **행동으로** 확인한다. **과잉 마스킹도 결함이다** — `/api/sample/I-0040/…` 은 건드리면 안 된다 |
| 34 | ★ **키를 읽는 곳이 가릴 것을 등록할 것** (`env.get` → `http.register_secret`) | 가리기는 **값으로** 해야 강하다 — 쿼리든 경로든 인코딩이 어떻든 잡힌다. 값을 쥐는 곳은 `env.get()` **하나뿐**인데, 그 자리가 등록을 빠뜨리면 값 기반 마스킹이 통째로 꺼지고 **이름 그물만 남는다** — 이름을 모르는 API 에서는 그물이 없다. 🚨 의존 방향(`env → http`)도 함께 검사한다 |

---

> 🚨 **G2 소스의 raw 는 사실 추출 후 삭제한다** — manifest 의 `sha256` 만 남기고,
> 재실행은 보관본이 아니라 **재수집**으로 한다 (**D-92** — D-17 「원문 미보관」과 규약 2 의 화해).
> 🔄 **2026-09-21 — 코드가 집행한다.** 삭제 함수 `store.drop_raw_for_g2` 를 부르는 곳이 **없어서**, 그 호출부가
> `collect/registry.py` `G2_DROP_WIRED` 에 적히기 전까지 `registry.require()` 가 **G2 를 거부**한다(팀장 판정 (나)).
> 게이트 `test_G2_는_삭제_경로가_붙기_전에는_받지_않는다` 가 목록 ↔ 실제 호출부를 **양방향**으로 대조한다 — 플래그만 켜면 빨강.

---

## 🆕 1-2. 화면·로그·설정 게이트 (2026-09-12 밤 · D-208 · D-209 · D-210)

> 🚨 **파일이 셋으로 갈려 있다** — 세는 축이 다르기 때문이다 (D-167). 수는 원장이 정본이다.

| 파일 | 무엇을 지키나 | 근거 |
|---|---|---|
| `tests/test_templates.py` | 🔴 **화면이 XSS 를 못 만들게** — `\|safe`·`innerHTML` 금지 · 자동 이스케이프 켜짐 · `Jinja2Templates` 를 **만드는 자리가 하나** · 🔄 **템플릿이 CSP 를 따로 안 든다**(정책은 미들웨어 한 곳 · D-212) · 화면 폴더가 사람별로 갈려 있음 | 보안점검 **P2-9** · D-208 |
| `tests/test_logging_redaction.py` | 🔴 **문구가 로그에 안 실리게** — 접근 로그·`text=`·`prompt:` 를 가린다 · 가린 값은 되돌릴 수 없고 같은지는 물을 수 있다 · `uvicorn.access` 에 필터가 **실제로 붙어 있는가** · `record.args` 도 본다 · 🆕 09-22 **트레이스백의 예외 메시지**(원인 연쇄 포함)를 가린다 · 한 레코드는 **한 번만** 가린다 · **음성 픽스처** | 보안점검 **P1-4** · D-76 · D-210 |
| 🆕 `tests/test_ci_pinning.py` | 🔴 **CI 가 남의 태그를 안 믿게** — `uses:` 는 전부 **커밋 SHA 핀**(옆에 `# vX.Y.Z` 판번호) · `pull_request_target` 금지 · `run:` 안에 `${{ github.event.* }}` 펼치기 금지(셸 주입) · 모든 워크플로가 `permissions:` 를 스스로 적음 · 🆕 **컨테이너 이미지도 다이제스트 핀**(태그는 지우지 않는다 — `.pre-commit-config.yaml` `rev` 대조가 읽는다 · D-99) · **음성 픽스처** | **D-215** · D-117 · D-188 · D-211 |
| 🔄 `tests/test_auth_boundary.py` | 🔴 **거버넌스 콘솔의 경계가 실제로 막는가** — 로그인 없이 관리자 화면이 안 뜬다(읽기도) · CSRF 없는 로그인 거부 · 해시가 **Argon2id + OWASP 파라미터** · 🔴 **클라우드 에디션에서 관리자·로그인이 안 붙는다**(404) · 시도 제한이 잠근다 · 기본 바인딩 `127.0.0.1` · **모든 응답에 보안 헤더** · 🔄 09-22 **관리자 쓰기는 허용 목록(공지·약관·문의)과 필수 호출(`require_governor`·`csrf_ok`·`audit`) 안에서만** — `app/routers/admin*.py` 전부를 구문 트리로 읽는다(종전 `admin.py` 한 파일). 🚨 **막는 이유가 낡지 않았는지를 게이트가 스스로 본다** (D-170) | 보안점검 **P1-5·P1-7·P2-9·P2-10·P2-11** · D-66 · D-212 · **D-213** · **D-260 ⑦** |
| `tests/test_settings.py` | 🔴 **설정과 파라미터가 한 곳** — `dsn()` 정의 하나 · 기본 DSN 문자열 하나 · `.env` 읽는 자리 하나 · 얼려 있음 · postgres 가 아니면 이름을 대고 막음 · 경로 비교가 OS 에 안 흔들림 · **판정 파라미터에 숫자를 다시 물리지 않음** · **출처 태그** · **`.env.example` 의 기본값이 실제로 통과** | D-99 · D-206 · **D-209** |

> 🔴 **`.env.example` 대조 게이트가 왜 필요했나** — 09-12 밤 한때 검증기가 그 파일이
> 「기본값 · 그대로 두면 됩니다」라 적어 둔 값을 **거부**했다. 문서와 코드가 갈렸고,
> **그대로 복사한 사람이 전부 밟는 자리**였다 (D-209).

> ⬜ **화면 라우트(`/u/**`·`/admin/**`)는 계약 표면 스냅숏에 안 들어간다** — 의도한 것이다.
> 계약(팀장)과 화면(팀원)은 바뀌는 속도가 다르고, 화면을 스냅숏에 넣으면 팀원이 화면을 더할
> 때마다 팀장 승인이 필요해진다. **계약만 잠근다** (D-192 — 범위 밖 판정을 자리에 박는다).

## 🆕 1-3. 게이트 파일 색인 (2026-09-21 · D-54 · D-170)

> ⛔ **무엇이 있었나** — 위 §1·§1-2 가 이름을 댄 게이트 파일은 **여섯**이었다. `@pytest.mark.gate` 가 붙은
> 파일은 2026-09-21 에 **마흔넷**이다(이 색인의 대조 파일을 더하기 전 43 · `grep -l` 실측). 나머지 서른일곱은 이 문서에서
> 찾을 수 없었다 — 「어디에 박혀 있는가」의 지도(머리말)가 지도 노릇을 못 했다.
> ★ 그래서 **색인만** 둔다. 무엇을 지키는지의 설명은 각 파일 머리말이 정본이다 — 아래 칸은 그 첫 줄을
> 옮긴 것이고, 옮긴 것이 낡아도 게이트는 안 잡는다(이름만 대조한다).
> 🔴 `tests/test_contract_index.py` 가 **양방향으로** 본다 — 게이트 파일이 이 문서에 없으면 빨강,
> 이 문서가 없는 파일을 대면 빨강. 새 게이트 파일을 만들면 **같은 커밋에** 여기 한 줄 보탠다.
> 🚨 수는 적지 않는다 — 원장 「거버넌스 게이트」 행이 정본이다 (D-54).

| 파일 | 머리말 첫 줄 |
|---|---|
| `tests/test_adopt.py` | 판 채택과 **원문 폴더 이름** (2026-09-18 · D-143 · D-99). |
| `tests/test_annex_audit.py` | [별표] 파싱 감사가 **실패할 수 있는지**를 본다 (S2-04 · D-98 개정 · D-170). |
| `tests/test_chunk_ratchet.py` | `preprocess/chunk.py` 하한 래칫 (2026-09-20 · D-254 · 감사 §1-8). |
| `tests/test_competitor_anonymity.py` | 경쟁사 실명이 **저장소로 돌아오지 못하게** 막는다 (2026-09-12 · D-216). |
| `tests/test_contract_index.py` | 게이트 파일 ↔ 이 문서 §1-3 색인 대조 (2026-09-21). |
| `tests/test_contract_surface.py` | 계약 표면 스냅샷 — **계약이 조용히 바뀌면 잡는다** (2026-09-12 밤 · D-124 · 병렬작업 계약 §6). |
| `tests/test_contracts.py` | 판정 계약 게이트 (D-124 ④). |
| `tests/test_data_store.py` | 공유 저장소로 파생물을 옮기는 길 (2026-09-19 · D-247 · 검토 2026-09-19 §5). |
| `tests/test_db_reset_role.py` | `scripts/db_reset.py --data` — 역할을 **지우기 전에** 본다 (2026-09-20 · D-254 · 감사 §1-5). |
| `tests/test_db_schema.py` | 거버넌스 층 스키마 — `db/schema.sql` 이 원본이다 (2026-09-09). |
| `tests/test_decision_map.py` | `dmap` 폐기·대체 판정 — 상태 칸의 **첫 낱말**만 본다. 개정 메모의 낱말이 살아 있는 결정을 폐기로 세지 않게 (2026-09-23). |
| `tests/test_derived_manifest.py` | 파생물 원장 — 기기 사이 동등성의 게이트 (D-19 의 짝 · 2026-09-17). |
| `tests/test_diagram_provenance.py` | 도면 PNG 의 C2PA 공개 문단이 살아 있는가 (2026-09-13 · D-222). |
| `tests/test_dictionary_leak.py` | 🔴 **평가셋이 판정 규칙을 고르지 못하게** — D-175 (2026-09-10). |
| `tests/test_embed_sweep.py` | `scripts/embed.py` `sweep_orphans` 의 가드 (2026-09-20 · D-254 · 감사 §1-8). |
| `tests/test_embed_upsert.py` | `chunk` upsert 가 **넣는 칸을 전부 갱신하는가** (2026-09-12). |
| `tests/test_endorse_scan.py` | 공정위 추천·보증 사건 계측 (D-255 ⬜ · `preprocess/endorse_scan.py`). |
| `tests/test_eval_groups.py` | 보고용 묶음 지표 (D-255 · `scripts/eval_rule.py`). |
| `tests/test_evasion_policy.py` | `preprocess/evasion_scan.py` — 폴더 이름이 아니라 **원천 id** 로 마스킹 정책을 찾는다 (D-254). |
| `tests/test_experiment.py` | 실험 분배 단위 — **팀장이 고정한 것을 팀원이 못 바꾸는가** (2026-09-12 밤 · D-94). |
| `tests/test_fixture_coverage.py` | 골든 픽스처가 **화면이 그려야 할 분기를 다 덮는가** (2026-09-12 밤 · D-124 · 병렬작업 계약 §2-1). |
| `tests/test_graph_routers.py` | walking skeleton 게이트 (D-124 · Phase 0). |
| `tests/test_hf_api.py` | `preprocess.hf_api` — **축이 둘이라는 것**을 코드가 계속 기억하는가 (D-185). |
| `tests/test_input_fingerprint.py` | 🔴 **입력 지문** — 같은 커밋·같은 seed 로도 결과가 갈린다 (D-176 · 2026-09-10). |
| `tests/test_label_merge.py` | 🔴 **라벨 취합** — 사람 시간이 들어간 자리라 되돌리는 비용이 가장 크다 (D-66 · D-170). |
| `tests/test_label_path.py` | 사람이 붙인 라벨이 **골든셋까지 닿는가** (2026-09-17 · D-172 · D-99). |
| `tests/test_label_round.py` | 라벨 한 판 — 짜기 · 검증 행 · 재검 · 갈린 것 · 판정 (2026-09-20 · `scripts/label_round.py`). |
| `tests/test_label_sheet.py` | 🔴 **사람은 번호만 채운다** — 라벨 시트 ↔ CSV 왕복 (2026-09-10). |
| `tests/test_launcher_automation.py` | 런처 자동화 검토 (2026-09-20) — 정본 전용 · 원문캐시 · `data-refresh` · 수집 뒤 안내 · 병합 전 요약. |
| `tests/test_launcher_fix_audit.py` | 런처 전수 감사(2026-09-20) 고침 — D-254. |
| `tests/test_law_norm.py` | [별표] 산문 파서가 **틀릴 수 있는지**를 본다 (D-170). |
| `tests/test_load_db_sweep.py` | `scripts/load_db.py` — 사전 거두기 · 수집 원장 계약 (2026-09-20 · D-254 · 감사 §2 load). |
| `tests/test_missing.py` | 원장에 있고 디스크에 없는 것을 **왜 없는지**로 가른다 (D-253 · `collect/missing.py`). |
| `tests/test_preview.py` | 추출 미리보기 — 임시 폴더에 돌려 지금 파생물과 맞댄다 (2026-09-21 · D-256 · `preprocess/preview.py`). |
| `tests/test_raw_dirs_fix_d.py` | 추출기가 읽는 원문 폴더 ⊂ `store.families(원천)` (2026-09-20 · D-254 · 감사 §1-7). |
| `tests/test_raw_inbox.py` | 팀원 수집 → 원문 받은편지함 → 정본 (2026-09-20 · D-250). |
| `tests/test_raw_mirror.py` | 정본 원문 거울 — 팀장 기기 읽기용 (2026-09-21 · D-256 · `scripts/raw_mirror.py`). |
| `tests/test_review_fix_ssm.py` | 소성민 코드 리뷰(2026-09-21) 고침 — 🔄 2026-09-24 **#8 통과 문턱(R0 · D-273)만 게이트**. 나머지는 게이트가 아니다. |
| `tests/test_retrieve.py` | 검색 코어의 계약 — DB 없이 검사할 수 있는 것만 (2026-09-12). |
| `tests/test_split.py` | `preprocess.split` — 골든셋 분할 [P12] · 🚨 **출처 분리** (2026-09-09). |
| `tests/test_split_seal.py` | `preprocess/split.py --write` — 봉인 평가셋이 줄면 멈춘다 (2026-09-20 · D-254 · 감사 §2 golden). |
| `tests/test_store_read_editions.py` | 🔴 **읽는 쪽의 판 규칙** — D-177 (2026-09-10). |
| `tests/test_user_db_down.py` | 사용자 화면이 DB 없이 뜨는가 · 프론티어 축 — 🆕 2026-09-22 (ksr 병합 흡수). |
| `tests/test_error_log.py` | 오류 로그 표(`app_error_log`)와 기록기 — 🆕 2026-09-22 (ssm 요청). 가린 문장·예외 클래스 이름만 · 상한 = 표의 CHECK · 받지 않는 로거·기록기 스레드 · `emit()` 이 DB 를 안 기다린다 · 죽으면 쉬었다 다시 · 버린 수를 센다 · 켜는 조건(cloud 는 끔) · `psycopg.connect` 는 `app/db.py` 한 곳 · **음성 픽스처** | 보안점검 **P1-4**·**P1-6** · D-72 · D-99 · D-213 |
| `tests/test_ops_tables.py` | 운영 표와 사용자 세션 — 🆕 2026-09-22 (D-260). 표의 `CHECK` 값 = 모델 목록 · 문의 상한 = 설정값 · 🔴 **넣지 않기로 한 칸이 없다**(회원 유형·역할·채널·팔로워·요금제 · 문의의 판정·문장 칸) · 🔴 **만들지 않기로 한 표가 없다**(`enterprise`·`signup_review`·`signup_review_doc`) · `work_doc.owner_id` → `user_account` · 🔴 **사용자 세션으로 관리자 문을 못 연다**(응답으로 잰다) | **D-260** · D-66 · D-68 · D-69 · D-76 · D-107 · D-180 |
| `tests/test_label_multilabel.py` | 다중 라벨의 합의는 유형별로 — 🆕 2026-09-22 (D-262). {8}·{5,8} 은 부분합의 · 범위밖 ↔ 유형은 갈림 · 🔴 **부분합의는 판정 전까지 평가로 안 나간다** · 유형별 κ 는 부분합의를 공유 유형 일치로 센다(묶음 κ 반대 대조) · `;` 구분자 · 엑셀이 깬 지문도 문구가 같으면 받고 문구를 고치면 멈춘다 | **D-262** · D-252 · D-40 · D-160 |

---

## 2. `scripts/doctor.py` — 기기 검사 (본문 미구현 · W1)

```
일반 (기획서 7-5)
   1  Python 3.11 · uv.lock 동기화        5  LangGraph 버전 == 핀
   2  .env 가 git 에 추적되지 않는가       6  모델 캐시 (인코더·리랭커·GGUF)
   3  postgres 접속 + pgvector 확장        7  GPU 가용 → 없으면 CPU 폴백 안내
   4  Alembic head == DB revision          8  kiwipiepy 사전 로드

거버넌스
   9·10·12·13  →  🚨 pytest 로 이관 (D-89)
   11  quarantine/ 방치 일수 경고
   14  g2_norepub/ 가 「배포 스크립트」에서 참조되는가        (D-71 — 학습이 아니라 배포)
   15  클라우드 반출 대상에 g3/·g2_facts/ 외 등급이 섞였는가  (D-78 ③ — AWS는 제3자 계정)
   16  판정 런타임 경로에 외부 API 호출이 있는가 → 즉시 실패  (D-73 · D-78)
   17  golden/*.jsonl 전 행에 provenance · redistributable   (D-71)
   18  manifest.jsonl 의 source_id 가 레지스트리에 있는가
   19  raw/ 가 derived/ 없이 방치돼 있지 않은가
   🔄 20  만료 지난 work_doc · 고아 LangGraph checkpoint · 잔존 job 원문 = 0   (D-129 — 「기본 미보관」의 DB 층)
```

🔄 **2026-09-12 밤 — `--env` 로 넷이 실제로 돌기 시작했다** (D-208).
`launcher.py doctor` 가 「무엇을 볼까」를 물어 `--env` 를 붙인다. **데이터 없이 돈다** —
새 클론에서 제일 먼저 부르는 자리다.

```
✅ 1  Python 버전 ↔ .python-version 핀
🆕 🔴 git 신원 (user.name · user.email)   ← 목록에 없던 검사. 없으면 커밋이 기기 계정 이름으로
      들어가고, 실제로 커밋 266건 중 1건이 그렇게 남았다. 5인이 붙으면 저자를 못 가른다
✅ 3  postgres 접속 + pgvector 확장
✅ 4  Alembic revision (테이블이 없으면 migrate 를 안내한다)
✅    .env 존재 · 화면 골격(base.html · base.css) 존재
⬜ 나머지(uv.lock 동기화 · 모델 캐시 · GPU · kiwipiepy)는 여전히 자리표시자다 (D-188)
```

> 🚨 **16번이 차별점 ④의 집행이다.** D-77 L4의 *「판정 경로의 외부 네트워크 요청 수 = 0」* 이고,
> **GPT API 크레딧이 있어도 판정·생성 경로에는 들어갈 수 없다** (D-78 ②).
> 0이 아니면 *"출시 전 문구를 밖으로 보내지 않는다"* 가 **말로만 남는다.**

---

## 3. DB `CHECK` 제약 — 행 단위 (PostgreSQL 16 실증 완료)

| 제약 | 막는 것 | 근거 |
|---|---|:-:|
| `ck_source_four_eyes` — `grade_decided_by <> grade_reviewed_by` | 혼자 판정하고 혼자 검토하는 것 | D-66 |
| `ck_golden_injected_not_holdout` | 🚨 **주입본이 평가셋에 들어가는 것** — 자기가 만든 문제를 자기가 푸는 것 | 기획서 6-2 |
| `ck_surface_pairing` | 표면 변형이 S0 원형 없이 존재하는 것 — **짝이 없으면 델타를 못 잰다** | D-91 |
| `ck_segment_k_anon` — `member_count >= K_MIN` | k-익명성 미달 세그먼트 | 기획서 4-2 |
| `ck_chunk_tokens` — `token_count <= 512` | 리랭커가 삼키지 못하는 청크 | D-84 ③ |
| `ck_judgment_raise_needs_evidence` | 근거 스팬 없이 위험도를 하한 위로 올리는 것 | D-131 |
| 전 테이블 `fragment_id ... ON DELETE CASCADE` | 소스를 지웠는데 파생물이 남는 것 | D-20 |

> 🔴 **위 표가 맞았고 코드가 틀렸다** (2026-09-10 · 마이그레이션 0006).
> `ck_chunk_tokens` 의 실제 식은 `token_count IS NULL OR token_count <= 512` 였고,
> `token_count` 가 널 허용이라 **칸을 안 채우면 무제한**이었다. `ck_judgment_raise_needs_evidence`
> 도 하한만 비우면 통과했다. postgres 16 에 같은 식으로 세워 넣어 보고 확인했다 —
> 900자 청크와 「하한 없는 R4」가 둘 다 들어갔다.
> ★ **널이면 안 되는 것은 CHECK 가 아니라 NOT NULL 로 막는다.** `ck_source_four_eyes` 와
> `ck_segment_k_anon` 이 처음부터 그 모양이었다 — 옳은 자리가 같은 스키마 안에 있었다.
> ⛔ `ck_sanction_four_eyes` 도 같이 조였다가 **되돌렸다**(0007). 그 자리는 종전에도 서명 없이
> 들어갔으므로 NOT NULL 은 고치는 것이 아니라 **없던 차단을 새로 만드는 것**이었다.
> 2인 확인의 강제 지점은 `collect/registry.py` 하나다.

> ★ **캐스케이드는 걸어 두는 것과 실제로 지워지는 것이 다른 문제**다.
> `DELETE FROM source` 를 실제로 돌려 **9개 테이블이 0이 되는 것**을 확인했다. D-77 L4의 세 번째 지표가 이것이다.

---

## 4. `scripts/collect.py` — 레코드 계약 (D-65)

```bash
python scripts/collect.py schema     # 계약 출력
python scripts/collect.py validate   # 수집물이 계약을 지키는지
python scripts/collect.py report     # ★ 9/17 범위 판정 집계
```

**필수 필드**

| 필드 | 비고 |
|---|---|
| `case_id` · `source` · `source_url` · `decided_date` | `source` 는 **`data_sources.yaml` 의 키** |
| `fragment_id` | 🚨 **필수** — 캐스케이드 삭제 경로 (D-20) |
| `law` · `category` | 표시광고법 / 식품표시광고법 / 화장품법 × 일반·식품·건기식·화장품 |
| `violation_types` | 다중 라벨 |
| `has_ad_text` | 🚨 **광고 문구 원문이 인용돼 있는가** — 없으면 실질 표본이 아니다 |
| `ad_text_before` · `ad_text_after` | **마스킹 후** (D-17 · D-257). 시정 페어의 원천 |

**위법 유형 6종** — 질병_예방치료_표방 · 건강기능식품_오인 · 의약품_오인 · 거짓_과장 · 소비자_기만 · 후기_체험기_기만
**편입 후보 3종** — 추천_보증_뒷광고 · 부당_비교광고 · 실증책임_위반 *(새 법령이 아니라 표시광고법 안의 유형이라 라우팅이 안 바뀐다)*

> 🚨 **`MIN_SAMPLES = 30`** (D-40) — **유형별 실질 표본이 30건 미만이면 「측정 불가」**이고,
> 하나라도 미달이면 **범위 확장 금지**다 (D-65 · 판정일 **9/17**).
> ★ **계약만 지키면 9/17 판정이 `report` 출력 하나로 끝난다** — 그것이 이 파일의 존재 이유다.

---

## 5. 수집 시점에만 남길 수 있는 것 (D-71)

**되돌릴 수 없으므로 게이트보다 앞선다.**

| 필드 | 어디에 | 왜 나중에 못 하는가 |
|---|---|---|
| `provenance` | 골든셋·학습샘플 **행마다** | 섞인 뒤에는 **어느 줄이 어디서 왔는지 알 수 없다** |
| `redistributable` | 같음 (상속값을 **행에 복사**) | 소스가 캐스케이드로 지워지거나 등급이 바뀌면 조인의 답이 달라진다 |
| `raw` / `norm` / `offset_map` | 문장·청크 | 정규화를 파괴적으로 하면 **원문 좌표가 영구히 소실**된다 (D-84 ①) |
| `surface_variant` / `surface_of` | 골든셋 | 나중에 붙이려면 **골든셋 전체를 재생성**해야 한다 (D-91) |

> 🚨 **골든셋에 `redistributable: false` 소스에서 온 문장이 한 줄이라도 섞이면 그 골든셋 전체를 공개할 수 없다.**
> AI Hub 6종이 정확히 그 함정이다 — **등급은 G3 인데 데이터셋 재배포는 막혀 있다.** 등급과 재배포는 다른 축이다.

---

## 6. 생성 경로 — 손으로 고치지 않는 것

```
docs/03_데이터/_matrix/data.js      판정 🔄 85건 (등급·제약·비용·가치·근거)  ← 손으로 고치는 곳
        │
        └── scripts/build_matrix.py
                ├──> docs/03_데이터/판정매트릭스.html          (한눈보기)
                └──> docs/03_데이터/_matrix/sources.json
                          │
                          ├── scripts/gen_registry.py    ──> data_sources.yaml  (집행 🔄 47건)
                          └── scripts/extract_rationale.py ──> registry_rationale.yaml
                                    │                              (ALIAS · D-100)
                                    └── scripts/review_sheet.py ──> S0-14 검토표
                                                  │
                                           uv run pytest -m gate

data_sources.yaml + 두 원장 ──> scripts/data_status.py --write ──> docs/03_데이터/데이터현황판.md
```

🆕 **2026-09-21 — CI 가 매트릭스 대조를 돈다** (`.github/workflows/gate.yml` 「판정매트릭스 대조」 = `build_matrix.py --check`).
⛔ 종전에는 런처 `check`·`rebuild`·`registry`·`review` 에만 있어 `data.js` 만 고치고 `rebuild` 를 안 친 커밋이 CI 를 지났다.
⬜ `data_sources.yaml`·`데이터현황판.md` 의 최신성은 여전히 대조가 없다 — `gen_registry.py`·`data_status.py` 에 대조 모드가 없다.

🚨 **판정을 고칠 때는 `data.js` 를 고친다.** 매트릭스나 레지스트리를 직접 고치면
다음 빌드에서 날아가고, **그 사이에 두 벌이 갈린다** — 실제로 사실원장이 그렇게 갈렸다 (D-90 트레이드오프).

★ **재현 확인** — 재빌드한 HTML이 현재 파일과 **바이트 단위로 같고**, 재생성한 레지스트리의 **md5가 같다.**

### 🆕 스키마 축 — 같은 규칙이 여기에도 있다 (2026-09-12)

```
🧊 db/schema_0001.sql   ← 동결본. alembic 0001 이 읽는 유일한 파일 (D-225)
        ↑ 2026-09-14 에 그때의 db/schema.sql 본문을 그대로 떼어 굳혔다. 한 글자도 바뀌지 않는다

db/schema.sql                                  ← 거버넌스 층의 「현재 상태 선언」. 사람이 읽는 정본
        ├──> docs/02_설계/거버넌스데이터층_DDL.md 「부록」 sql 블록   ← 사본. 손으로 맞춘다
        └──> db/migrations/NNNN_*.sql                        ← 앞으로 나아가는 방향만
                    └── alembic/versions/  ... 파일을 **읽어 실행만** 한다 (DDL 복사 금지 · D-99)
                          ⛔ 단 0001 은 db/schema_0001.sql 을 읽는다 — schema.sql 이 아니다
```

🔴 **0번의 입력은 동결돼 있고 게이트 둘이 그것을 본다** (2026-09-16 등재 · D-225).

| 게이트 | 무엇을 보나 |
|---|---|
| `test_0001_이_읽는_파일은_동결본이다` | `SCHEMA =` **대입문만** 정규식으로 본다 — docstring 의 언급이 오탐되지 않게 |
| `test_동결본이_바뀌지_않았다` | 정규화 해시 핀 `63f32c85018cda6d…` 과 비교한다 |

⛔ **종전에는 `0001` 이 실행 시점에 `db/schema.sql` 을 읽었고, 그 파일이 09-09 이후 여덟 번
바뀌면서 「0003 이 도는 DB 의 모양」이 사람마다 달랐다** — 09-13(이서은)·09-14(박수진) 이틀
연속 같은 자리에서 막힌 원인이다. 버전형 체인의 불변식은 「0번이 고정」이다.

★ **스키마를 바꿀 때 고치는 곳은 둘이다** — `db/schema.sql` **+ 새 마이그레이션**.
  하나만 고치면 두 벌이 조용히 갈리고, 그것을 잡는 것은 사람이 아니라 `launcher.py db-drift` 다.
  (실측 2026-09-16 · 동결 전후 차이 **0** · 빈 볼륨에서 `0001` → `0015` 완주)

🚨 **부록은 사본이고 파일이 원본이다.** 게이트 `test_문서_부록과_schema_sql_이_같다` 가
**한 글자도 다르지 않은지** 본다 — 09-12 에 실제로 이 게이트가 커밋을 한 번 막았다.

🔴 **`chunk` 에 열을 더하는 마이그레이션은 `v_current_chunk` 를 같이 다시 만든다.**
`SELECT c.*` 는 뷰를 만들 때 **열 목록으로 전개돼 고정**되기 때문이다. 0008 이 이것을 안 해서
검색이 통째로 503 이 났다. 게이트 `test_chunk_에_열을_더하면_뷰도_다시_만든다` 가 순서를 본다 —
**같은 파일의 `ADD COLUMN` 뒤**에 두는 쪽이 낫다(한 트랜잭션이라 반만 적용될 수 없다 · D-197).
🔄 **2026-09-12 밤 — 그 게이트가 실제로 잡는지를 음성 픽스처로 쟀다** (D-203).
D-197 은 *"종전 규칙이 잡던 것을 새 규칙도 잡는다는 것을 같은 커밋에서 보여야 한다"* 고 적었는데
**논증으로만 보였다.** 검사 본문을 `view_refresh_violation()` 으로 빼고 일부러 어긴 임시 폴더로
다섯 경우를 잰다 — 0008 형·0009 형·0010 형·순서 뒤집기·대상 없음.
★ **규칙으로 올린다 — 게이트를 고치거나 새로 만들면, 그것이 잡는다고 주장하는 입력을
같은 커밋에서 실제로 떨어뜨려 보인다.** 「통과만 하는 게이트는 게이트가 아니다」를
**게이트 자신에게** 건다.
