# 문서 정합성 검토 · 정정 기록 — 2026-09-03 (저녁)

> 오한빈 · 브랜치 `ohb` · 검토 기준 HEAD `e00dbf4`
> 방법 — 문서가 말하는 수치를 **실물**(`data_sources.yaml` · `tests/` · `data/` · `docs/**`)로 되세었다. 옮겨 적힌 값은 전부 사실원장(D-54)으로 되돌렸다.
> 짝 문서 — `설계검토_아키텍처_2026-09-03.md` (설계 논리 검토는 그쪽)

---

## 0. 실물 계수 (2026-09-03 저녁)

| 무엇 | 값 | 어디서 |
|---|---:|---|
| 레지스트리 등재 | **40** (collect 28 · hold 10 · manual 2) | `data_sources.yaml` |
| `blocked` / `not_adopted` / `models` | **10 / 11 / 6** | 〃 |
| `reviewed_by` 서명 / 용도 열림 / 작업계획 / 미서명 | **19 / 31 / 30 / 12** | 〃 · `review_sheet.py` |
| 판정 매트릭스 | **74** (`ftc_decisions_body` 추가 후) | `_matrix/sources.json` |
| 거버넌스 게이트 | 검사 함수 **30** | `tests/test_governance_layout.py` |
| 설계결정 | **D-01~D-124** 본문 누락 없음 | `00_설계결정기록.md` |
| `mfds_press` 색인 / manifest / 공정위 본문 | **4,349 / 1,166 / 1,087** | `data/` |

---

## 1. 어긋나 있던 것과 고친 것

| # | 어긋남 | 고친 자리 |
|:-:|---|---|
| 1 | **등재 소스 수가 네 벌** — 41 · 42/9/10 · 43/9/10 · 43 | 사실원장(40·10·11) · 산출물현황 · 집행계약 §1·§6 · 수집전처리_기획 §현황 · 수집리스트·소스분석 머리말(08-31 값 옆에 09-03 실물 병기) |
| 2 | **게이트 수** — 산출물현황 「19건」 · 수집전처리_기획 「16건」 | → 30개 / pytest 37건 (단일 출처 집행계약 §1) |
| 3 | 집행계약 **번호 충돌** — NOTRAIN 예정 검사가 `30`, 키 유출 검사도 `30~34` | 예정 검사를 `35 (예정)` 로 |
| 4 | 설계결정기록 **색인에 D-112~D-118 없음** · 산출물현황 「D-01~D-102」 | 색인 7행 추가 · 「D-01~D-124」 |
| 5 | 사실원장 모델 표 「KLUE-RoBERTa · KoELECTRA 비교군 G0」 ↔ `models.klue_roberta` **G1 배제**(D-106) · 비교군은 **KcELECTRA**(D-84 ③) | 사실원장 모델 표 |
| 6 | **Phase 0 게이트** — 사실원장 일정 표·작업일정 마일스톤·수집리스트 S1/크리티컬 패스가 「9/2」인데 같은 사실원장 아래는 「9/2 미달성 → 9/9」 | 셋 다 9/9 (재) · 9/2 는 취소선 |
| 7 | **주제확정 v1.1** — `W1~W10` 자체 달력 · 게이트 8/30·9/13·9/27·10/11 · 전환 9/13 · 「기획문서 v2.1이 유일 설계 문서」 | **v1.2** — 2-1 공식 `1W~9W` 표로 교체 · 2-2 위험 구간 셋 · 2-3 게이트 9/9·9/11·9/28·10/13 · 2-4 전환 9/11 · §4 역할 분담을 09-03 배정으로 교체 · §7 요약 · 관련 문서 |
| 8 | **09-03 배정 개정 미전파** — 작업일정 §3 「화면설계서 T1 (W6부터 이관)」·「웹앱 T1·T5」 | 작업일정 **v1.1** — 팀원 4인(화면+BFF) + 팀장(판정 코어 API) · 이관 규칙 폐기 명기 |
| 9 | **D-119 삼원화 미전파** — 기획서 본문 두 곳 「이원화」 · 주제확정 관련문서 표 | 「진입점 분리(2-5절 · 삼원화)」 |
| 10 | **머리말 갱신일 낡음** — 사실원장·산출물현황(08-31 14:30) · 전처리_사양(08-31) · 배포계획(08-31) 이 전부 09-03 커밋됨 | 넷 다 09-03 |
| 11 | **생성물 재생성 누락** — `registry_head.yaml` 의 `NOTRAIN` 주석이 `data_sources.yaml` 에 없음 | `gen_registry.py` 재실행 — 차이는 주석 2행뿐 · `extract_rationale`·`review_sheet` 산출은 **변화 없음** |
| 12 | **판정 매트릭스 73 → 74** — 집행계약·산출물현황이 73 | 74 (사실원장 「레지스트리 현황」에 행 추가) |
| 13 | `mfds_press` 색인 수 세 벌 (4,349 / 4,348 / 4,347) · manifest 1,154 ↔ 1,166 | 사실원장 1,166 으로. **4,348(인계·D-117·D-118) · 4,347(yaml probe_note) 은 당시 값이라 그대로 둠** |
| 14 | **S0-14 산정** — 수집리스트 09-02 시간표(A13·B7·C11 ≈2h36) · 산출물현황 「31건」 | `review_sheet.py` 출력(12건 · A9·B3·C0 · 1h29)만 단일 출처라고 적고 표 제거 |
| 15 | 산출물현황 `law_api_smoke.py` **중복 등재** | 한 행 삭제 |
| 16 | **전처리 사양 2-5** 회피 표기 출처에 `ftc_decisions` 잔존 (실사례 0건 · D-116 ③) · 수집리스트 S1-11 도 같음 | 취소선 + 사유 · 미해결 #3 을 「`mfds_press` 공공누리 판정 선결」로 |
| 18 | **기획서 8-5 이관 규칙 ①·리스크표 3** — 「T1 이 W6 부터 화면·배포 이관」이 배정 개정 뒤에도 살아 있음 | 취소선 + 사유 · 점검일 「W5(9/14)」→ 3W 말(9/17) — ⚠️ 9/17 은 범위 확정일에 맞춘 **제안값**, 팀장 확인 |
| 19 | **산출물현황** 에 오늘 문서 2건 미등재 | `정합성검토` · `설계검토_아키텍처` 행 추가 |
| 17 | **수집리스트 S2-01·S2-02** — 「파일데이터 5,870건 `ftc_bulk.py`」가 아직 크리티컬 패스에 살아 있음 (실제는 본문 API 경로로 해소 · D-115) | 취소선 + 해소 표기 · S2-07 에 공공누리 미결 표기 |

### 그대로 둔 것 (역사 기록)

- 설계결정기록 본문의 당시 수치(D-110 「31→29」, D-117 「4,348」 등) — 결정 시점의 값이라 **소급 수정하지 않는다** (0.2 기록 규칙).
- 수집리스트·소스분석 머리말의 「08-31: 43·9·10」 — 그날의 값. 옆에 09-03 실물을 병기만 했다.
- 기획서 부록 C 개정 이력의 「이원화」 — 이력이다.

---

## 2. 프로젝트(claude.ai) 사본

사본은 전부 `b279d6c` 이하인데 레포는 그 뒤 7커밋 + 오늘 정정이 들어갔다. 낡은 사본 — 사실원장 · 산출물현황 · 집행계약 · 설계결정기록(D-119~124 없음) · **기획문서(v3.8 ↔ v4.0)** · 수집전처리_기획 · 전처리_사양 · DDL · 배포계획 · 주제확정 · 작업일정.

🚨 **`sync_project.py` 는 커밋 뒤에 돌린다** — 스탬프가 「그 문서를 마지막으로 바꾼 커밋」이라 커밋 전에 돌리면 낡은 해시가 찍힌다.

```powershell
uv run pytest -m gate                               # 정정 후 초록 확인
git add -A ; git commit -m "docs: 정합성 정정 17건 — 레지스트리 실물 40 · Phase 0 9/9 · 배정 개정 전파 · 주제확정 v1.2 · 작업일정 v1.1"
uv run python scripts/sync_project.py               # → build/project_sync/ 를 프로젝트에 올린다
```

---

## 3. 이번에 드러난 반복 형태 — 다음에 같은 사고를 막으려면

| 형태 | 사례 | 처방 |
|---|---|---|
| **수치를 옮겨 적음** (D-54 위반) | 등재 소스 수 4벌 · 게이트 수 3벌 · 매트릭스 73/74 | 원장 밖 문서는 「사실원장 참조」만. 특히 **산출물현황 표의 설명 칸**이 수치를 가장 많이 옮겨 적는다 |
| **결정은 바뀌었는데 본문이 안 따라감** | D-62(9/11) · D-106(KLUE) · D-119(삼원) · D-124(9/9) · 09-03 배정 | 결정기록 「영향」 칸에 적힌 문서를 **같은 커밋에서** 고친다. 색인 누락(D-112~118)도 같은 형태 |
| **머리말이 본문보다 늦음** | 갱신일 4건 | `docmeta.py` 가 읽는 자리라 낡으면 스탬프가 거짓말한다 — 커밋 훅에서 `최종 갱신` 과 커밋 날짜 대조 검사 후보 |
| **생성물 재생성 누락** | NOTRAIN 주석 | `launcher.py check` 에 `build_matrix --check` + `gen_registry` md5 대조를 넣는 것이 D-90 의 마무리다 |
