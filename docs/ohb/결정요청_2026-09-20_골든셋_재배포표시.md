# 결정요청 2026-09-20 — 골든셋 행의 `redistributable` 이 D-133 과 어긋난다

> 작성 Claude · **판정 대기** (공개 범위가 걸린 안건이라 초안까지만 만들고 멈췄다 — 코드는 안 바꿨다)
> 앞 문서 → `docs/ohb/검토_2026-09-19_기기간_동등성_자동화.md` §22

## 무엇이 어긋났나 [코드 확인 · 클론 B 생성물 2026-09-20]

- **D-133 ①** (확정 2026-09-04) — 광고 카피 조각 = **`G2` + `NOREDIST`**. *「`NOREDIST` 로 골든셋 공개에서도 빠진다 (D-71 전파)」*
- `scripts/load_db.py` `load_fragments()` 는 그 판정대로 인용 문구 프래그먼트를 **G2** 로 만든다 —
  `ftc_decisions_body:golden` · `mfds_casebook:golden` · `mfds_special_use_guide:golden`(09-19 추가)
- 🔴 그런데 **행** 에는 `preprocess/golden.py` 가 `"redistributable": True` 를 **상수로** 박는다(주문 L119 · 이유 L150).
  원천·등급을 보지 않는다. DB `golden_sample.redistributable` 도 그 값을 그대로 받는다

| 계보 (provenance · origin) | 행 | 무엇 | D-133 상 | 지금 행 값 |
|---|---:|---|---|---|
| ftc_decisions_body · real | 5,325 | 의결서가 인용한 광고 문구 | NOREDIST | `true` |
| mfds_casebook · real | 313 | 사례집이 인용한 광고 문구 | NOREDIST | `true` |
| mfds_special_use_guide · real | 163 | 해설서가 인용한 광고 문구 (사람 라벨) | NOREDIST | `true` |
| mfds_hf_ingredient_board · approved | 296 | 식약처 승인 기능성 문구 (G3 · 기관 저작물) | 재배포 가능 | `true` |
| mfds_hf_ingredient_board · injected | 784 | 승인 문구에 규칙으로 결함을 넣은 **우리 합성문** | 재배포 가능 | `true` |

★ **지금 새고 있지는 않다** — 골든셋을 외부에 공개하는 명령이 아직 없다. `data-publish` 는 팀 내부 저장소이고(D-78 ③)
  별도로 「NOREDIST 원천을 받은 적 있으면 막는다」를 건다. 🚨 그러나 **공개 단계에서 이 칸을 믿으면 5,801행이 샌다.**
  칸이 있는데 틀린 값이 들어 있는 것이, 칸이 없는 것보다 나쁘다 — 읽는 쪽이 검사를 안 한다.

## 대안

| | 얻는 것 | 떠안는 것 |
|---|---|---|
| **(가) ★ D-133 대로 — 인용 문구 행은 `false`** · 값은 계보 표 하나에서 (`GOLDEN_FRAGMENT` 의 등급과 한 표 · D-99) | 원장과 코드가 같은 말을 한다 · 공개 단계에서 거를 칸이 맞다 | 공개 가능한 골든셋이 **1,080행(승인·합성)** 으로 준다 — **평가셋 실문장 293행은 공개 불가**라 외부가 우리 점수를 재현 못 한다 |
| (나) 지금대로 두고 D-133 을 고친다 (인용 문구도 재배포 가능) | 공개 골든셋이 크다 | D-133 이 막은 위험(광고주 저작물 전재)을 되연다 · 팀장 판정 「최대한 안전한 결정」과 반대 |
| (다) 칸을 없애고 공개 시점에 프래그먼트 등급으로 거른다 | 값이 두 벌이 안 된다 | `doctor` 17번 · D-71 규약 7(「행마다 provenance · redistributable」)을 고쳐야 한다 |

**추천 (가).** D-133 은 확정이고 이것은 **코드가 판정을 안 따른 것**이다. 공개 범위를 넓히려면 (나)로 판정을 바꾸는 것이 순서다.
(가)로 가면 바뀌는 것 — `preprocess/golden.py`(계보별 값) · `scripts/load_db.py`(같은 표를 읽는다) · 테스트 1 ·
재생성(`golden --write` → `load`). 평가 수치는 안 바뀐다(행과 라벨이 같다).

## 확인할 사람

판정 — 오한빈 (공개 범위) · D-133 을 고치는 (나)라면 2인 확인

---

## 🆕 2차 탐색 (같은 날) — D-133 은 **둘** 다 코드에 안 걸려 있다

D-133 결정 표 1·2행 — ① 광고 카피 조각 = G2 + NOREDIST ② 추출 문구 **길이 상한 40자 · 게이트로 강제**.
①은 위와 같고, 🔴 **②는 게이트가 없다** — `tests/` 에 40자 검사가 0개, 추출기(`ftc_extract.phrases_in`)에 상한이 없다.
레지스트리 문언(`mfds_casebook_2021` · `collect/mfds_board.py`)만 「40자 상한」을 말한다. D-232·D-234(이유 회수)도 이 상한을 안 다뤘다.

[클론 B 골든셋 실측 2026-09-20] 실문장(`real`) 중 **40자 초과 737행**

| 계보 · split · 구역 | 40자 초과 | 최대 |
|---|---:|---:|
| ftc · train · 이유 | 677 / 4,816 | 119 |
| ftc · train · 주문 | 13 | 101 |
| ftc · test_sentence · 주문 | 8 | — |
| 해설서 · test_sentence · 주문 | 39 / 163 | 243 |
| 사례집 | 0 / 313 | 22 |

🚨 **상한을 지금 걸면 평가셋 유형이 무너진다** — test_sentence 에서 40자 초과를 빼면

| 유형 | 지금 | 뺀 뒤 | D-40 (30) |
|---|---:|---:|---|
| 거짓_과장 | 108 | 87 | ✅ |
| 소비자_기만 | 70 | 66 | ✅ |
| 건강기능식품_오인 | 53 | 46 | ✅ |
| 🔴 **부당_비교광고** | **35** | **27** | ✅ → **측정 불가** |

### ② 의 대안

| | 얻는 것 | 떠안는 것 |
|---|---|---|
| (ㄱ) 상한을 그대로 걸고 초과 행을 뺀다 | D-133 문언대로 | 부당_비교광고 평가 불가 · 이유 학습행 677 손실(14%) |
| (ㄴ) 40자로 자른다 | 행 수 유지 | 🔴 **문장이 잘려 라벨이 안 맞는다** — 쓰지 않는다 |
| (ㄷ) ★ **D-133 이 허용한 길** — *「올릴 때 이유를 결정기록에 남긴다」* · 상한을 실측 최대(해설서 243)가 아니라 **문장 한 개 길이**로 올리고 게이트를 건다 | 지금 데이터가 선다 · 게이트가 생긴다 | 광고주 문구를 더 길게 갖는다 — ①(NOREDIST)이 공개를 막으므로 **보관 범위**만 넓어진다 |

## 판정안 (클로드 추천)

1. **① (가)** — 인용 문구 행은 `redistributable: false`. 계보 표 하나에서 등급과 함께 정한다 (D-99)
2. **② (ㄷ)** — 상한을 올리되 **숫자는 사람이 정한다**. 클로드 제안: 120자(ftc 이유 최대 119 · 해설서 초과 39행은 따로 본다)
3. 두 결정 모두 **D-133 개정**으로 기록한다 — 확정 결정의 문언을 바꾸는 것이라 2인 확인 대상인지 팀장이 정한다

⚠️ 내가 틀릴 수 있는 곳 — 10~30자 광고 문구는 판례상 **저작물성이 부정되는 경우가 많아** ①이 과잉일 수 있다(대안 (나)의 근거).
그러나 D-133 이 「최대한 안전」으로 이미 택한 방향이고, 나는 법률가가 아니다. 공개를 실제로 할 때 법률 확인을 받는 것이 순서다.

---

## 🔄 결과 (2026-09-20) — 팀장 판정 · **D-249** 로 기록

*「120자로 할거고 이미 올라간 이력을 지우기 보다 앞으로 git 에 올라가지 않게 하면 될 것 같은데 2인확인은 필요없고」*

- ① (가) — 인용 문구 행 `redistributable: false` · 값은 `preprocess/lineage.py` 한 표
- ② (ㄷ) — 보관 상한 **120자** · 넘으면 버린다(해설서 2행) · 게이트
- git — 라벨·라벨 시트 4개를 git 에서 빼고 팀 비공개 저장소가 옮긴다 · **이력은 지우지 않는다**
- 2인 확인 — 면제(팀장)
