# 인계 — 2026-09-14 (다섯 번 「이미 있었다」 · 게이트 2건 · 판정 이력 절 · 🔴 평가 홀드아웃 후보 0)

> **앞 문서** — [`인계_2026-09-13.md`](인계_2026-09-13.md)
> 🚨 **이 문서는 원장이 아니다.** 수의 정본은 [`../00_사실원장.md`](../00_사실원장.md) (D-54),
> 판정의 정본은 [`../00_설계결정기록.md`](../00_설계결정기록.md) (D 번호)다.
> 여기 적힌 것은 **오늘 한 것과 남긴 것**이다.
>
> 🔴 **이 세션은 claude.ai 대화에서 진행됐고, 기기 셸이 붙지 않았다.** 모든 실행·커밋은 팀장이
> 직접 했고, 나는 파일을 쓰고 **되받아 바이트로 대조**했다. 실행 출력은 팀장이 돌려준 것이다.

---

## 0. 일곱 줄

| | 무엇 | 어디 |
|:-:|---|---|
| 1 | 🔴 **다섯 번 「새로 발견했다」고 보고했는데 전부 이미 판정돼 있었다** — D-19·D-92 / D-60 / D-155 / D-161 / 데이터현황판 | §2 |
| 2 | **게이트 2건 추가** — 미채택 판정이 되살아나는 길을 막는다. 음성 확인 3건 | §1 · §3 |
| 3 | **README 실측 반영** — 게이트 **286 → 297** · 전체 **704 → 716** · 결정 뱃지 **D-219 → D-223** | §3 |
| 4 | **fail-closed 인용 20곳을 D-220 으로** (원장이 든 15 + 내가 찾은 5) | §3 |
| 5 | 🆕 **현황판에 「판정 이력」 절** — 원장을 소스 이름으로 훑는다. 매트릭스가 든 D **66 → 171**(+156) | §4 |
| 6 | 🔴 **평가 홀드아웃 후보 넷이 전부 닫혔다.** D-155 가 「최상위 리스크」라 적은 칸에 **지금 후보가 0개**다 | §5 |
| 7 | ⬜ **판정 대기 7건** — 하나는 D-92 미집행, 하나는 원장에 없는 규칙이 47건의 용도축을 정하고 있는 것 | §6 |

🚨 **커밋 넷** — `a0ad61f` · `382d809` · `59286de` · `ec00616`. 전부 `origin/ohb` 에 올라갔다.

---

## 1. 커밋 넷

| 해시 | 무엇 |
|---|---|
| `a0ad61f` | 게이트: 미채택 판정이 되살아나지 않는지 검사 **2건** (D-90 ② 집행) |
| `382d809` | README: 게이트 297 · 전체 716 실측 반영 · 결정 뱃지 D-223 · fail-closed 인용을 D-220 으로 |
| `59286de` | 곁가지: fail-closed 인용 **17곳**을 D-72 → D-220 (13개 파일) |
| `ec00616` | 현황판 「판정 이력」 절 + `split.py` 표기 갱신 + 곁가지 셋 (8개 파일 · +177/−9) |

⛔ `ec00616` 은 **한 덩이에 곁가지가 섞여 있다.** 처음에 `git commit -m … --amend --no-edit` 을
냈다가 `-m` 이 앞 커밋 제목을 덮었고, push 전에 제목을 고쳤다. **`--no-edit` 은 `-m` 을 못 이긴다.**
🔶 커밋 **본문**의 `—` 가 PowerShell 인코딩으로 `?` 가 됐다. 제목은 멀쩡하고 push 후라 두었다.

---

## 2. 🔴 다섯 번 「이미 판정돼 있었다」 — 이 세션의 가장 큰 사실

| 내가 「발견」한 것 | 이미 있던 판정 | 언제 |
|---|---|---|
| 소비 시점에 용도축 게이트가 없다 | **D-19 · D-92** | 08-14 · 08-31 |
| 파생물을 저장소에 올려도 되나 | **D-60**(09-11 개정) | 09-11 |
| 골든셋 313행이 정본과 어긋난다 | **D-155** | 09-08 |
| `mfds_sanctions` 를 평가 후보로 | **D-161**(`sanctions_scan.py` docstring 이 인용) | — |
| 소스별 판정을 한 곳에 모으자 | **데이터현황판이 이미 그 문서였다** | — |

★ **D-155 가 그 함정을 본문에 적어 두었다** — *「`U1: deny` 를 보고 「학습 데이터가 아니니」로 넘길
뻔했다 … 용도 축에 「평가」가 없어 평가 전용 원천이 `U1 deny` 로 표기되고, **실제로 나를 한 번
속였다**」*. **나는 그 다음 희생자였다.**

🚨 **원인은 순서였다.** 나는 실측부터 시작하고 원장을 나중에 읽었다. 작업 지침이 *「**D 번호를
인용하기 전에** 원장 본문을 읽는다」* 였는데, **「탐색 중이니까」라고 생각해 안 읽었다 — 조건절이
좁았다.**

⛔ **그리고 이 문제 자체가 이미 판정돼 있다 — D-117** *「같은 문자 함정을 세 번 밟았다 —
**기록으로는 안 막히고 코드로만 막힌다**」*. 원장 223건 중 **53건(24%)**이 본문에 「같은 일이 또
났다」를 적고 있다. ⛔ **이 24%는 느슨한 정규식(「두 번째」·「또 밟」·「같은 모양」)이 잡은 것이라
과대 추정일 수 있다 — 하나씩 읽어 세지 않았다.**

---

## 3. 게이트 2건 · README · fail-closed 20곳

### 3-1. 새 게이트 둘 (`tests/test_governance_layout.py` · 43 → 45 함수)

```
test_미채택_키가_등재에도_생성기_목록에도_없다
    ① not_adopted 키 ∩ sources             = ∅
    ② not_adopted 키 ∩ gen_registry.ORDER  = ∅   ← ast 로 읽는다
test_서명이_끝난_키가_정본에_없으면_미채택_기록이_있다
    검토표의 2인 서명 완료 키 − sources − not_adopted 키 = ∅
```

🔴 **막으려는 것** — `foodsafety_admin_measure` 를 미채택으로 내린 **실제 수단이
`gen_registry.py:665` 의 `ORDER` 주석 한 줄**이었다. `NOT_ADOPTED_IDS`(:780)는 **생성을 막지 않고**
「미등재」 진단 출력에만 쓰인다. 그리고 그 키의 **완료된 2인 서명이 `registry_review.yaml` 에 남아
있어서**, 주석을 지우면 `ck_source_four_eyes` 가 **처음부터 통과**한다. 인계 §8-7(09-13)에서 DB 에
미채택 소스가 「2인 확인 완료」로 앉아 있던 그 모양이 **재발 경로로 열려 있었다.**

**음성 확인 3건 — 전부 의도대로 빨갛다.** ⓐ 미채택 키를 `sources` 에 되살림 → ① 실패
ⓑ `ORDER` 주석만 되살림 → ① 실패 ⓒ `not_adopted` 에서 키를 바꿈 → ② 실패.
⛔ **ⓒ 는 첫 판에서 초록이 나왔다** — 내 고장 주입이 틀렸다(`- key:` 로 찾았는데 실제는 `- {key:`).
고쳐서 빨간 것을 확인했다. **「실패할 수 없는 단언」을 만들 뻔했다** (D-170).

### 3-2. README — 손으로 맞추는 칸이 셋 낡아 있었다

| 자리 | 전 | 후 |
|---|---:|---:|
| 게이트 | 286 | **297** |
| 전체 | 704 | **716** |
| 결정 뱃지 | D-219 | **D-223** |

★ 게이트는 **+11 인데 내가 더한 건 2건**이다 — 나머지 9는 그 전부터 낡아 있었다.
🔴 뱃지는 찾으려던 게 아니라 걸린 것이다 — **같은 README 의 다른 줄이 `D-01 ~ D-223 · 빠진 번호 0`**
이라 적고 있었다. 원장을 직접 세어 확인했다: **헤딩 223 · 1~223 · 빠진 번호 0.**
⛔ `test_readme_drift.py` 는 **명령 이름과 폴더 구조만** 본다 — 수는 검사 대상이 아니다.

### 3-3. fail-closed 인용 — 원장이 든 15 + 내가 찾은 5 = **20곳**

`59286de` 17곳(원장 15 + `preprocess/chunk.py:218` + `tests/…:1532`) · `ec00616` 3곳
(`.env.example:88` · `collect/law_annex.py:293` · `collect/openapi.py:228`).

- 🔴 원장이 `app/graph.py:392` 라 적었는데 실제는 **:449** 였다 — 파일이 움직였다. 낱말로 찾아 걸렸다.
- 🟢 **안 건드린 것** — `collect/registry.py:112·124·151` · `collect/probe.py:6` 은 **진짜 D-72**(보류의
  뜻 · 「확인 후 2인 판정으로 승격」)다. 원장이 *「낱말이 같다고 바꾸면 맞는 인용을 틀리게 만든다」*
  고 경고한 자리다.
- ⬜ **남은 것** — `data_sources.yaml` 8곳(**생성물** · 원천은 `registry_head.yaml`) · `00_사실원장.md`
  7곳(**D-220 을 세운 기록 그 자체**라 두는 게 맞다) · 도면 원천 3장(§7) · 🔴 **같은 문장이 세 벌**
  (`data_sources.yaml:48` · `집행계약:146` · `tests/…:609` — 「G0 를 fail-closed 로 닫아 둔 이유(D-72)」).

---

## 4. 🆕 현황판 「판정 이력」 절 (`ec00616`)

**새 명령도 새 문서도 만들지 않았다.** `docs/03_데이터/데이터현황판.md` 가 이미 등재·보류·수기·
미채택·배제를 한 문서에 모으고 있었고, **없는 것은 D 번호 칸 하나**였다.

```
scripts/decision_map.py   load_ledger() 가 돌려주는 결정에 body 한 칸 추가 (파서를 두 벌 두지 않는다)
scripts/data_status.py    _decisions_by_source() + 「📜 판정 이력」 절
                          별칭은 extract_rationale.ALIAS 를 쓴다 (매트릭스 id ↔ 레지스트리 키의 정본)
```

**실측 — 색인이 보태는 것**

| | |
|---|---:|
| 매트릭스(`data.js`)가 지금 인용한 D (전 소스 합) | **66** |
| 원장을 소스 이름으로 훑어 나오는 D | **171** |
| 🟢 색인이 새로 보태는 D | **156** (51개 소스) |
| 🔴 매트릭스 인용이 0인데 색인은 나오는 소스 | **28개** |

`mfds_press` 2→19 · `mfds_sanctions` **0→12** · `mfds_casebook` 4→9 · `law_go_kr` 0→16(ALIAS 가
법제처 12종을 이어 준다) · `kcia_guideline` 0→5.

### 🔴 필터를 넣었더니 손해였다 — 그 실측을 코드에 박았다

표본 28건(`mfds_press` 19 + `mfds_casebook` 9)을 손으로 갈랐다 — **판정 19 · 단순 언급 9 = 정밀도 68%.**
명령줄(`launcher.py …`)·파일경로(`preprocess/<id>.py`)를 거르는 필터를 붙여 다시 쟀다:

| | 정밀도 | 걸러낸 노이즈 | 놓친 판정 |
|---|---:|---:|---|
| `mfds_press` | 71% *(무필터 74%)* | **0 / 5** | 🔴 D-136 · D-167 |
| `mfds_casebook` | 57% *(무필터 56%)* | 1 / 4 | 🔴 D-158 |

★ **왜 안 먹혔는지가 값이다 — 두 가지를 거꾸로 알고 있었다.** ⓐ 노이즈는 산문이다(재료 나열·비유·
계기). ⓑ 🔴 **파일명·명령줄에 이름이 나오는 것이 오히려 판정의 표시였다** — D-136 의 유일한 언급
줄이 `launcher.py probe mfds_press`(robots 확인 판정)이고 D-167·D-158 은 *「`preprocess/<id>.py` 를
만든다」* 가 **결정문 본문**이다. **그래서 거르지 않는다.** 이 수와 이유를
`_decisions_by_source()` docstring 에 박았다 — 다음 사람이 같은 필터를 또 만들지 않게.

⛔ **68%는 두 소스 표본이다.** D 가 가장 많은 소스라 노이즈가 가장 많을 조건이었다. 그리고 내
분류가 정답이라는 보장이 없다 — 경계선 셋(D-91·D-99·D-160)을 다르게 세면 **64~75%** 사이에서 움직인다.

---

## 5. 🔴 평가 홀드아웃 — 후보 넷이 전부 닫혔다

D-155 가 적어 둔 것: *「**문장 단위 평가 홀드아웃은 여전히 빈칸이다 — 그것이 지금 프로젝트의
최상위 리스크다.** 학습 입력은 2,461건 확보됐는데 **그것을 채점할 데이터가 없다**」*

| 후보 | 닫힌 이유 |
|---|---|
| `mfds_casebook` | **D-155** — 서로 다른 문구 237 · **평균 5.3자**. 문장이 아니다 → 「금지 표현 사전」으로 성격 변경 |
| `mfds_press` | **D-155** — 107파일 · 인용 516 중 대부분 제목·정책명, 본문은 **이미지 749개** |
| `mfds_sanctions` | **D-161** — 조문명에 표시·광고가 든 행 **57/5,122**, 제8조는 **28행**, 그마저 라벨 표시 위반 (D-40 의 30건 미달) |
| `foodsafety_ad_monitor` | 🔴 **API 가 안 돈다** — 우리 키·sample 키 둘 다 `ERROR-310`(09-09). **오늘 탐침도 API 를 못 불렀다**(아래) |

### 탐침 결과 — 「✅ 열림」이 「API 가 돈다」가 아니다

`docs/03_데이터/실측_2026-09-14.md`(`ec00616` 동봉) —

```
## foodsafety_ad_monitor — G3 · 가치 A · ✅ 열림
- URL: …/api/openApiInfo.do?…svc_no=I0500…   ⚠️ 접근 URL 로 대체됨
- 응답: 197,643 bytes
- 저작권 … 503 09시~19시에는 서비스가 제한됩니다 … ERROR-601 SQL 문장 오류입니다 …
```

⛔ 탐침이 부른 것은 **포털 안내 페이지**(`openApiInfo.do`)다. `evidence_url` 이 없어 `access` URL 로
대체했다는 표시(`⚠️ 접근 URL 로 대체됨`)가 그 뜻이고, **API 자체는 한 번도 안 불렀다.** 긁힌
「이용조건 문구」도 페이지 푸터와 **오류 코드 목록**이 섞여 나왔다.

★ **탐침이 「열림」으로 찍은 것을 「쓸 수 있다」로 읽으면 안 된다.** D-149 의 모양이다.

---

## 6. ⬜ 판정 대기 — 다음 사람이 이어받을 순서

| # | 무엇 | 왜 지금 | 이미 있는 판정 |
|:-:|---|---|---|
| **1** | 🔴 **`derived/` → 등급 디렉터리 집행** | 등급 디렉터리 **넷이 전부 비어 있고** 파생물이 전부 `derived/` 에 있다. 지금은 수집분 13건이 전부 G3 라 위험이 **우연히** 안 터진다. 🚨 미수집에 **G2 가 둘**(`kcia_guideline` 2층·가치 A · `khff_survey`) — 받는 날 터진다 | **D-19 · D-92** — **새 판정이 아니라 미집행**이다. doctor 14·15·19 가 집행계약에 「본문 미구현·W1」로 올라 있다 |
| **2** | 🔴 **`cond` 변환 규칙을 원장에 등재** | `gen_registry.py:110` 이 `cond`(조건부)를 **G2 면 allow · G3 면 deny** 로 가른다. 등급이 높을수록 더 세게 닫히는 **역전**이고, 열린 쪽 3건은 조건(「해설 문장 투입 금지」)이 **코드에 안 닿는다.** ⛔ **원장 223건에 `cond` 가 0건** — 47건 전부의 용도축을 정하는 규칙이 판정으로 등재된 적이 없다 | 없다. D-100 이 *「근거 없이 판정을 적지 않는다」* |
| **3** | 🔴 **평가 홀드아웃 — 후보 0** | §5. D-155 의 열린 결정 ②가 우회로 없이 정면에 있다 | **D-155** 열린 결정 ①②가 그대로 |
| **4** | **「안 읽어서 생기는 재발」 3안** | ㉮ 지침 조건절을 「무엇이든 하기 전에」로 ㉯ `launcher.py check` 가 **건드린 파일에 걸린 D 를 띄운다** ㉰ 저장소에 `CLAUDE.md`(지금 **없다** — 작업 지침이 claude.ai 에만 있어 저장소를 연 세션은 지침이 0이다) | **D-117** — *「기록으로는 안 막히고 코드로만 막힌다」*. ㉯가 그 처방 |
| **5** | **읽는 순서 표를 README 첫머리에** | 실측: 오늘 5건 중 **현황판이 2건 · `dmap --open` 이 2건**을 잡는다. 🔴 **README 가 데이터현황판을 안 가리킨다** | — |
| **6** | **파일 색인을 docstring 으로 생성** | `00_산출물현황.md` 가 이미 그 문서인데 **커버리지 65%**(105 중 68)이고 본문이 낡았다(`D-01~D-207`·`게이트 30개(pytest 37건)`). 🟢 모듈 docstring 첫 줄 **32/33**, 형식도 통일 | `launcher.py summary()` 가 같은 판단을 이미 했다 — *「설명을 표에 따로 적으면 두 벌이 되고 한쪽만 갱신된다」* |
| **7** | 🔴 **`status` 파생물 절의 기기 축 혼입** | `data_status.py` 머리말이 *「이 현황판은 「팀 축」이다 — **두 기기가 같은 답을 낸다**」* 라 선언하는데, 마지막 절이 `DERIVED.rglob` 로 **이 기기의 실물**을 훑는다. §7 | **D-19 · D-89** 가 갈라 둔 축을 이 절이 어긴다 |

### ⬜ 갈래별 세부 (필요할 때만)

- **1번** — (가) 그대로 (나) **청킹 입구에만 자물쇠**(지금 통과 대상이 `law_go_kr` 하나라 회귀 0) (다) 파생물 행에 출처·등급을 달고 게이트 (라) D-92 원안 완전 집행. ★ 권고는 **(나) 지금 → (다) 뒤에 → (라) 보류**. ⛔ (나)는 **부분 방어**다 — U1·U4 축은 그대로이고, `train` 이 스텁이라 지금은 방어할 대상이 없을 뿐이다.
- **2번** — (가) 그대로 (나) 전부 deny (다) ★ **지금 동작을 D 로 등재하고 조건 문구를 필드로** (라) 규칙부터 고친다. ★ 권고 **(다)** — D-220 이 *「새 규칙이 아니다 — 이미 집행되던 것에 번호를 준다」* 로 한 것과 같은 자리다.
- **6번** — ⛔ **`.sql`·`.yaml` 에는 docstring 이 없다.** 마이그레이션 10 · 설정 yaml 9 의 첫 줄 주석 형식이 통일돼 있는지 **안 재 봤다.** 그것부터 재고 붙인다.

---

## 7. 🚨 회사기기로 옮길 때 — 미리 읽어야 하는 위험

| | 같나 | 넘어갔을 때 |
|---|:-:|---|
| 코드 · 문서 · `uv.lock` · 게이트 297 | ✅ | 그대로 |
| **`data/` 실물** | ❌ | 🔴 **`status --write` 를 바로 돌리지 않는다.** 파생물 절이 그 기기의 `data/derived/` 를 훑는다 — 이 클론에서 뽑힌 목록에 `mfds_press_pdf/tables/*.json` **100개 이상**이 들어 있어, 없는 기기에서 돌리면 **100줄 넘게 사라진 diff** 가 뜬다. **판정 6번-7이 그것이다** |
| `data/manifest.jsonl` | ✅ | 커밋되므로 「원장에 오름 13」은 두 기기가 같다 |
| **도면 · PDF** | ❌ | `Noto Sans CJK KR` 이 없으면 `diagram`·`pdf` 가 **설계대로 막힌다**(09-13 §7-1). 회사기기 권한은 모른다 |
| PNG `caBX` | ❌ | 「그림이 바뀐 장만 넘긴다」 (D-222) |
| **DB** | ❌ | 🔴 **`0013`·`0014` 이전이면** `v_risk_lookup` 이 서명 없는 행을 내보내고 `ck_judgment_raise_needs_evidence` 가 약한 판이다. **`git pull` + `onboard` 가 그 자리다** |
| 🆕 **「판정 이력」 절** | ✅ | 원장만 읽는다 — 두 기기가 같은 답을 낸다 |

```
git pull
uv run python launcher.py onboard
uv run python -m pytest -m gate -q
```

---

## 8. 오늘 내가 틀렸던 것

| # | 무엇 | 어떻게 알았나 |
|:-:|---|---|
| 1 | 🔴 **다섯 번 「새로 발견했다」** — 전부 원장에 있었다 | 뒤늦게 원장을 읽었다. §2 |
| 2 | 🔴 **「수집 13건이 전부 G3 라 위험 미실현」** — 등급 상한과 소스별 판정을 섞었다 | 재 보니 **등급 상한보다 좁게 판정된 소스가 18건**이고, 수집분에도 U1·U2 deny 가 둘(`mfds_casebook`·`mfds_press`)이었다 |
| 3 | 🔴 **「정본과 골든셋이 어긋나 있다」** — 313행을 위반으로 읽었다 | `split.py:272` 가 `train = … + term` 으로 **D-155 를 정확히 집행**하고 있었다. 어긋난 것은 데이터가 아니라 **같은 파일의 머리말**이었다 |
| 4 | 🔴 **노이즈 필터를 만들면 정밀도가 오를 것이라 단정했다** | 재 보니 71%/57% 로 **안 오르고** 판정 셋을 놓쳤다. §4 |
| 5 | 🔴 **음성 확인 ⓒ 가 초록으로 나왔는데 게이트 탓인 줄 알았다** | 내 고장 주입의 정규식이 틀렸다(`- key:` vs `- {key:`). 고쳐서 빨간 것을 확인했다 |
| 6 | 🔴 **`--amend --no-edit` 에 `-m` 을 같이 줬다** | 앞 커밋의 제목이 곁가지 제목으로 덮였다. push 전에 고쳤다 |
| 7 | 🔴 **바이트 대조에서 두 번, 쓴 것이 반영 안 된 채로 돌아왔다** | `split.py`·`data_status.py` 둘 다 첫 쓰기 뒤 옛 판이 staged 됐다. 다시 써서 일치를 확인했다 — **쓰기 도구의 성공 응답을 증거로 쓰지 않는다**(D-149 파일판)가 실제로 걸린 자리다 |
| 8 | 🔶 **`data_status.py` 파일 끝 개행을 날렸다** | 내 정리 스크립트에서 `t.split("\\n")` 이 **리터럴 백슬래시-n** 이라 `rstrip()` 이 파일 전체에 걸렸다. `ruff format` 훅이 잡아 커밋이 멈췄다 |

---

## 9. 대조 기록 · 안 본 것 (D-188)

**바이트 대조 ✅** — `tests/test_governance_layout.py`(89,262B) · `README.md`(18,224B) · 13개 파일
(D-220 인용) · `.env.example`·`law_annex.py`·`openapi.py` · `preprocess/split.py`(20,243B) ·
`scripts/decision_map.py`(10,692B) · `scripts/data_status.py`(12,000B).

**실행 확인 ✅**(팀장 기기 · 팀장이 돌린 출력) — `pytest -m gate` **297건 전부 초록**(네 번) ·
`dmap` 결정 223(폐기 7) · 살아 있는 것 216 · 인용 173 · 🔴 인용 0건 43 · ⬜ 열린 결정 65 ·
`data_status --write` 19,378자 · pre-commit 8종.

**⬜ 안 본 것 — 「없는 것」이 아니라 「내가 안 본 것」이다**

- `preprocess/mask.py`(70KB) · `app/api.py` · `app/contracts.py` · `scripts/doctor.py` — **안 열었다.**
- `.sql`·`.yaml`·`.github/` 의 `D-72` 인용 — 전수는 봤으나 **문맥으로 가르지 않았다**(생성물·원장이라 손대지 않기로 한 것과 별개).
- **골든셋 `sizes.train 문구 802` 와 행 수의 대사** — 단계가 달라(주입·사전이 뒤에 더해진다) **못 맞췄다.** 313 은 양쪽에서 정확히 일치한다.
- **`train`·`eval` 이 `derived/` 를 직접 읽는지** — 둘 다 스텁이라 **없는 것**이지만, 생기면 판정 1번의 자물쇠가 같이 필요하다.
- **매트릭스 D 인용 34/82 의 정밀도** — 소스→D 색인은 68%를 쟀지만 **매트릭스 쪽은 안 쟀다.**
- **원장 재발 53건(24%)** — 정규식이 잡은 수다. **하나씩 읽어 확인하지 않았다.**

### 🔴 이 문서가 인용한 D 중 **원장 본문을 읽은 것과 안 읽은 것**

★ **오늘의 사고가 이것이었으므로 이 문서 자신부터 가른다** (작업 지침 — *「D 번호를 인용하기 전에
원장 본문을 읽는다. 색인 표만 보고 인용하지 않는다」*).

```
본문을 읽고 인용    D-19 · D-60 · D-72 · D-90 · D-92 · D-117 · D-155 · D-220 · D-223
                   (덤으로 읽은 것 — D-17 · D-18 · D-71 · D-108 · D-110)

🔴 본문을 안 읽고    D-40 · D-54 · D-89 · D-91 · D-99 · D-100 · D-136 · D-149 · D-158
   인용             D-160 · D-161 · D-167 · D-170 · D-188 · D-222
   ← 코드 주석 · 게이트 docstring · 앞 인계 · README 가 인용한 것을 **재인용**했다
```

⛔ **뒤의 15는 「그 결정이 그런 뜻이다」의 근거가 못 된다.** 특히 **D-161** 은
`sanctions_scan.py` docstring 만 보고 「기각됐다」로 썼다 — 판정 3번을 실제로 움직이기 전에
**원장 본문을 열어야 한다.** D-220 이 *「번호를 지어내면 원장을 안 읽고도 인용이 되고, 그 인용이
다음 사람에게는 근거로 보인다」* 고 적은 자리가 정확히 여기다.
