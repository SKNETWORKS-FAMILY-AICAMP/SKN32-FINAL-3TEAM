# 인계 2026-09-22 — 클론 B(집 · 정본) → 클론 A(회사) · A 에서 할 일 한 장

> 작성 Claude · 클론 B 세션 · **앞 문서** `인계_2026-09-21_클론A_3판.md`
> 🚨 **A 에 앉으면 §0 만 순서대로 친다.** 3판 §0(B 에서 할 것)은 이 날 **전부 끝났다** — 무엇을 왜 했는지는 사실원장 「2026-09-22 · 클론 B」 절이 정본이다.
> 기준 커밋 — 파생물 원장 `5d4533e` (push 됨) · 이 문서가 든 문서 커밋은 그 뒤다.
> 🚨 이 문서에는 사람 이름·상호 원값을 적지 않는다. 원값 표는 B 의 레포 밖 `C:\CopyLane_private\pii_triage_0922f.tsv` 에만 있다.

## 지금 상태 — 한 줄씩

- B 게이트 — **648 passed** (`c92cc97` 뒤 · skip 0) [사용자 실행 · B]
- 마스킹 — 개인 실명은 피심인이 아니어도 가린다(**D-258** · ftc · law_go_kr · 2인 확인 권소라) · 두 번 적힌 피심인 · 등록번호 일부 노출꼴 · 띄어 쓴 이름까지 → 파생물 반영
- 반출 검사 — 개인 식별 **0** · 법인 잔여 **0** · 넓은 거름 🟡 161 (「그 밖」 11 — 흔한 말 9 + 보도자료 공무원 2)
- 골든셋 — **6,741행** (train 6,389 · test 352) · 🔴 **봉인 17개 교체**(거짓_과장 · 팀장 판정 (가) · `--allow-shrink` · D-254) — 이 날 전후의 거짓_과장 지표는 **다른 시험지**다
- 공유 저장소 — 새 판 **7개** 올림(원장 sha `e37dc970…`) · 가리기 전 옛 판 **7개** 지움(53 → **46** · 드라이브 휴지통)
- 판정 — ① → D-258 · ② 보도자료 공무원 실명은 **남긴다 → D-259** · `결정요청_2026-09-22_사람_마스킹_범위.md` 닫힘

## §0 🔴 A 에서 할 것 — 순서대로 · 각 단계 출력을 클로드에게

**① 받고 게이트**

```
git pull origin ohb
uv run python launcher.py check
```

기대 — **게이트 648 이상 passed · skip 0** (이 인계 커밋은 문서뿐이라 수가 늘지 않는다고 **예상** — 안 쟀다).

**② 파생물 받기** — B 가 올린 판

```
uv run python launcher.py data-sync --dry-run
uv run python launcher.py data-sync
uv run python scripts/derived_manifest.py --export-check
```

- `--dry-run` — 받을 것 **7개 안팎**(`banned_terms` · `ftc_layer1_phrases` · `ftc_layer1_triage` · `ftc_reason_sample` · `ftc_stage` · `golden/golden.jsonl` · `law_decc`)이면 기대대로
- 🚨 `pull` 없이 옛 커밋으로 받으면 「저장소에 없음」으로 **멈춘다** — 옛 판 객체 7개를 지웠다. 정상이다. `pull` 부터 한다
- `--export-check` — 개인 식별 0 · 법인 잔여 0 이어야 한다

**③ 🔴 옛 판 백업 정리** — 🚨 **삭제다. 목록을 먼저 준다**

`data-sync` 는 바뀌는 파일의 옛 판을 레포 밖 `CopyLane_backup\<시각>\` 에 **복사**해 둔다. 이번에 바뀌는 7개의 옛 판은
**가리기 전 이름·상호가 든 판**이라 A 디스크에 남기지 않는다.

```
Get-ChildItem ..\CopyLane_backup -Directory | Select-Object Name, LastWriteTime
Get-ChildItem ..\CopyLane_backup -Recurse -File | Select-Object @{n="dir";e={$_.Directory.Name}}, Name, Length
```

- 경로는 A 기준으로 바꾼다(09-21 A 는 `CopyLane_backup\20260921-112416` 이었다)
- 목록을 주면 지울 것(오늘 판 · 09-21 판 중 위 7 파일)과 남길 것을 가려 명령을 드린다

**④ 원문 거울 받기** (D-256 · 3판 §0 ⑧)

```
uv run python launcher.py raw-mirror-sync --dry-run
uv run python launcher.py raw-mirror-sync
uv run python launcher.py extract ftc_decisions_body --preview
```

- `--preview` 는 거울 원문으로 추출을 **미리보기만** 한다 — 파생물은 여전히 정본(B)만 만든다

**⑤ 드라이브 휴지통 영구삭제** — 기기 무관 · drive.google.com

- 저장소 옛 객체 **10개** — 09-22 의 7(`6d356a56` · `9d15f38f` · `2911cc38` · `c8269300` · `ff30fbdc` · `19fb4959` · `bb734e93`) + 앞서 넣은 `02597a40` · `07918338` · `c72a396a`
- 🚨 공유 폴더라 **소유자 휴지통**에 들어간다 — 소유자 계정에서 확인한 뒤 비운다. 비우면 되돌릴 수 없다

## §1 판정 대기 — 기기 무관

| # | 무엇 | 어디 | 비고 |
|:-:|---|---|---|
| 1 | 🔴 **단계 관문** — 철수 조건 판단 · 권고: 조건 7 을 앞당겨 쓴다(팀 브랜치에 변환 코드가 없다) | 커밋 `c416459` §4 | **시한 9/28** |
| 2 | 봉인 선택을 후보 증감에 안 흔들리게 — 문서 번호 해시 순 | D-176 · D-254 개정 | 바꾸는 순간 봉인이 한 번 크게 바뀐다 — 1차 지표 전후로 시점을 고른다 |
| 3 | `--export-check` 넓은 거름(🟡)을 엄격으로 올릴지 (3판 §1 #6) | 09-22 수 — 🟡 161 중 「그 밖」 11 이 전부 이름 아님(흔한 말 9 · D-259 예외 2) | **권고: 올리지 않는다** — 올리면 흔한 말 9 로 반출이 막힌다 |
| 4 | 3판 §1 의 나머지 — 행정심판 계보 · `/search` 재배포 축 · DISCLAIMER 세 줄 · 레지스트리 `masking:` 문언 10곳(D-257 뜻) · `/search` 카테고리 타입 · 병렬작업 계약 담당 표 | 3판 §1 #1 · #3~#5 · #7 · #8 | 3판 #2(클로드가 옮겨 적은 2인 서명 둘)는 **닫혔다** — 권소라 확인 · 옮겨 적음 주석 제거(`f5e1a92`) |

## §2 클로드 몫 — 다음 세션

- **곁가지(안 고쳤다 · 커밋 나눠 「곁가지」)**
  - `data_refresh` 끝 줄에 `[green]…[/green]` 마크업이 글자로 찍힌다
  - `derived_manifest` 가 `law_decc.jsonl` 의 만든 이를 `decc_extract` 로 적는다 — 실제 작성기는 `law_case`
  - `data-refresh` `EXTRACTORS` 에 `law_case` · `ftc_extract --stage` 가 없다 — 이 날 손으로 돌렸다
  - `ftc_reason_sample` · `ftc_layer1_triage` 는 `data-refresh` 로 다시 안 나온다(`ftc_reason_probe --dump` · `ftc_triage --dump` 를 손으로)
  - `mfds_press` 첨부 단계에 `already_have` 가 없다
  - `_TITLED_PERSON` 이 목록꼴(「A, B 대표」)에서 조사를 잃는다
  - `banned_terms` 에 NFD 자모 3 (사전 · 사람 아님)
  - `_COMMON_WORDS` 에 「소유」·「전체」 — law_decc 「그 밖」 9 를 닫는다
  - 저장소 고아 객체 거두기 명령(오늘 손으로 한 것 — 저장소 목록 × `git log` 원장 판 대조)을 `data_store` 에
- ⬜ (선택) `--pii-triage` 가 D-259 예외 2건을 「기록된 예외」로 따로 세게 — 팀장이 원하면
- 3판 §2 의 나머지 — 도면 A-04 · S-01 · W-01(`retrieve` 배선) · S0-14 「이견」 절 한 줄 · 전처리 결과서 §2 괄호 수 · 담당자 알림 · CI 매트릭스 대조 단계 — **이 날 손 안 댔다**

## §3 3판에서 틀렸던 것 — 다음 인계에서 되풀이하지 않는다

- 3판 §0 ③ — `data-refresh ftc_decisions_body` 만으로는 `law_decc`(작성기 `law_case`)와 `ftc_stage`(`--stage` 필요)가 **다시 안 나온다**
- 3판 §0 ④ — `preprocess.golden --write` 는 없는 옵션이다(`--dump` 뿐). 골든셋은 `data-refresh` 가 돌린다
- 3판 §0 ⑦ — `mask --survey` 는 사람 잔여를 **안 잰다**. 잰 것은 `derived_manifest.py --pii-triage <레포 밖 파일>` 이다

## §4 안 본 것 · 깔고 간 가정

- **A 디스크** — 이 날 한 번도 안 봤다. §0 의 수는 A 에서 처음 재는 것이다 (D-188)
- 골든셋 6,837 → 6,741(−96)의 행별 내역 — 봉인 교체에 따른 이유 행 이동으로 **추정**만 했다
- 서비스 화면이 보도자료 제목을 보여 주는지 — D-259 의 다시 볼 조건인데 안 봤다
- `CopyLane_backup` 에 이번 7 파일의 옛 판이 **실제로** 몇 벌 쌓였는지 — A 에서 처음 본다
- B 의 기기 셸 연결(클로드 쪽 `device_bash`)은 이 날 오후부터 붙지 않았다 — 파일 쓰기·읽기는 스테이징으로 했고, 쓴 파일은 되받아 바이트로 대조했다
