# 인계 2026-09-25 — 클론 B(집 · 정본) → 다음 자리 · 한 장

> 클론 B 세션 · **앞 문서** `인계_2026-09-23_클론A.md`
> 🚨 **다음 자리에 앉으면 §0 을 순서대로 친다.** 무엇을 왜 정했는지는 원장 본문이 정본이다 — D-272 개정 · D-285 개정 2~4 · D-286 개정 3~4 · D-289 · 사실원장 「2026-09-25」 ⑧~⑱. 여기는 요약이다
> 기준 커밋 — **`299b547`**(push 됨 · 파생물 원장 · 공유 저장소 판 sha `a1a3aed5…`) 뒤에 사실원장 ⑱ 재측정 문서 커밋 1개(push 됨 · 해시는 이 문서에 안 적었다)
> 🚨 앞 인계(09-23 A → B) §0 ①~⑦ 중 **이 세션이 한 것은 ④ 의 조건 칸뿐**이다(D-285 개정 4 · 품목 칸은 아니다). 나머지(0018 migrate · Q4 대응표 → W6 · 사전 재생성 뒤 E3 · W1 · Q1·Q2)는 이 세션에서 **보지 않았다** — 끝났는지 모른다 (D-188)

## 지금 상태 — 한 줄씩

- B 게이트 — **847 passed · 675 deselected · skip 0** (`299b547` 커밋 앞 `check`) [사용자 실행 · B]
- 해설서 조문·조건 판 — 전체 **1,834 · 채택 1,834 · 대기 0** → 평가에 들어갔다. 판독 = 독립판독_합의 1,716 · **팀장판정 118**(`decisions.jsonl` · 원천)
  - 채택 조건 — C 698 · B 415 · A 269 · D 271 · M 181 · 근거_후보 72
- 골든셋 **8,432행** — train 6,425 · test_sentence **2,007**(해설서 1,816 · 공정위 131 · 적법 60) · 평가 조건 칸 A 268 · B 404 · C 692 · D 271 · M 181 · 분할 문서 1,977/2,042 (`split_manifest.json` git 추적)
- 판정기 B 기준선 — 채점 1,555(뺀 M·D 452) · 해설서 식품 1호 P 0.917 R 0.471 · 3호 0.774/0.052 · 5호 0.923/0.055 · 4·6·7호 적중 0 · 공정위 칸 거의 0 · 적법 오탐 3/60(5.0%) — 사실원장 ⑰ · ⑱
  - 🚨 **이 점수를 제품 성능으로 읽지 않는다** — 판정기 B 는 대조군이다. 평가의 **90% 가 해설서 한 원천**이라 합친 점수는 해설서 점수다 → **원천별로만 보고한다**(`eval_rule` 이 그렇게 낸다)
  - 식품 1호 R 0.471 은 **누수 아님**(⑰ — 해설서는 train 0행 · 사전 원천에 해설서 없음 · 같은 문구 0 · 적중은 병 이름 9개)
  - 🔴 표시 「사전에 항목 0 — 구조적으로 못 맞힌다」 — 식품 6·7·8호 · 표시광고법 2·3·4호 · 비방 · 비교 · 후기. 이 칸의 0 은 성능이 아니다
- 공유 저장소(구글 드라이브 `J:\내 드라이브\CopyLane_store`) — `decisions.jsonl`(118) · `readings.jsonl` · 분할 판을 올렸다 · 원장이 HEAD 와 같다

### 이 날의 판정 — 한 줄씩 (본문은 원장)

| D | 무엇 |
|---|---|
| D-272 개정 (나) | 주 근거는 **「사항」**으로 가른다 — 시행령 제2조 사항은 식품표시광고법 · 가격·할인·환불·거래 조건·대가 관계·출처는 표시광고법 · 식품 행의 표시광고법 인용은 채점하지 않는다 · ⬜ 제3조의 배제 여부는 법률 확인 |
| D-285 개정 2 | 기대 응답이 같은 갈림은 규칙으로 닫는다 — 호만 다름 → `근거_후보`(어느 쪽이든 정답) · D↔M → M · 시트 81행 |
| D-285 개정 3 | 지문을 **base32** 로(`gs:[a-z2-7]{12}` — 휴대전화 꼴 오탐 제거) · `adopted.jsonl` 은 생성물(`rebuild`) |
| D-285 개정 4 | 해설서는 **대기 0 일 때만** 평가에 들어간다 · DB 는 조건 칸 행을 안 넣는다 · 팀장 판정 가져오기 경로(`import-decisions`) · 거래 조건 행은 대기 |
| D-286 개정 3 | 조건은 법이 적법 여부를 가르는 **결정 요인**으로 — 표현 그 자체 C · 조성·지위 A · 사실·입증 B · 표시 방식은 B + 표시요건 |
| D-286 개정 4 (+보정) | 경계 묶음 판정을 규칙으로 — 평가어 셋 **(다)**(객관 속성·우량 용어 B / 주관적 인상 M) · G1~G8 · 무첨가는 뺀 것이 없어도 A·7.나 · 원천 설명문 D·원천결손(④ 확장) · 전문가 관계를 드러낸 제목은 ⑨(④″) · 개인 맞춤은 사실 주장 B · 심의 준수 강조는 C·7.나 |
| D-289 (나″) | 표시요건은 조건 칸이 아니라 **근거 단위 속성** · 효과는 출처 문언(법령·고시 = 확정 · 공정위 지침 = 분기) · 방식은 D-93 자리 · ③④ 계약·층 3 은 초안(`D초안_2026-09-25_표시요건_계약.md`) |

## §0 🔴 다음 자리에서 할 것 — 순서대로 · 각 단계 출력을 세션에 붙인다

**① 받고 게이트** (클론 A 라면)

```
git pull origin ohb
uv run python launcher.py data-sync --dry-run
uv run python launcher.py data-sync
uv run python launcher.py check
```

- 기대 — `--dry-run` 에 `labels/guide_statute/decisions.jsonl` · `readings.jsonl` · 골든셋·사전 등 · 게이트 **847 이상 passed · skip 0**. 🟡 A 의 수는 처음 재는 것이다
- 🚨 판정·분할은 **정본(B)에서만** 바꾼다 — A 에서 `import-decisions` · `golden --write` 를 치지 않는다

**② 앞 인계(09-23) §0 의 남은 것** — 끝났는지부터 확인 (`migrate` · Q4 대응표 → W6 · 사전 재생성 뒤 E3 · Q1 · Q2)

**③ 합의 채택 행 사람 감사** — 이 날의 점검(⑱)은 **팀장 판정 96행과 모양이 같은 행만** 봤다(후보 32 → 검토 22 → 반영). 합의 1,716행의 정확도는 **재지 않았다** — 무작위 표본을 사람이 본다

**④ W1 평가 도구** — M(보류) · D(판정 대상 아님) 행을 채점하는 것은 W1 뿐이다(판정기 B 는 뺀다 · 452행). 이 날 바뀐 기대 응답 26행(위반↔보류 15 · 위반↔대상아님 10 · 보류↔대상아님 1)의 효과는 W1 에서 보인다

## §1 판정 대기 · 열린 것

| # | 무엇 | 어디 | 비고 |
|:-:|---|---|---|
| 1 | 식품표시광고법 제3조(「다른 법률에 우선」)가 표시광고법을 **배제**하는지 | D-272 ⬜ · 사실원장 ⑭ | 법률 확인 · 공정위 의결 계측은 「공존」 쪽 근거(배제 미확인 · 2020 뒤 식품 집행 적음) · 결정은 답과 무관하게 서게 짰다 |
| 2 | 사항 판별 규칙 — 문장이 거래 조건인가(낱말 · 분류기) | D-272 개정 ⬜ · W4 `merge_laws` | 해설서 거래 조건 12행은 판정 끝(D 9 · 식품 사항으로 판정 3 — 「절대품질 절대가격」 · 「한달에 이만원 …」 · 「효과가 없다면 … 환불」) |
| 3 | 표시요건 계약 — `EvidenceArticle.disclosure` · 증명서 가드 · 층 3 발동 규칙 | `docs/ohb/D초안_2026-09-25_표시요건_계약.md` | **W3 계약 판에 묶는다**(잠금 1급 · ksr · lse 화면) |
| 4 | 연락처 **지우는** 축이 `mask.POLICY` 에 없다(유선번호 검사도 안 본다) | D-285 개정 3 ⬜ | 별도 안건 · 먼저 잰다 |
| 5 | 수집 초안 두 건 — 레지스트리 · 2인 확인 | `docs/ohb/수집초안_2026-09-25_식품등의_표시기준.md` · `…_기만적인_표시광고_심사지침.md` | 2인 확인은 사람 몫 |
| 6 | 경계 규칙을 검사 스크립트로 저장소에 둘지 · 낱말 목록은 예시 | D-286 개정 4 ⬜ | 이 날 점검은 작업공간 스크립트(저장소 밖 · 남지 않는다) |
| 7 | 심의 고지 B·4.마 · 면책 문구 M 은 팀장 판정대로 두었다 — C·2(의약품 인식)로 읽을 여지는 ⑱ 에 적음 | 사실원장 ⑱ | 다시 볼 때 |

## §2 이 날 만든 것 — 레포 밖(`build/`) · 정본 기기에만

- `build/labels/guide_statute__팀장판정도구.html` — 93행 판정 도구(오프라인 · 광고 문구가 들어 있어 외부에 올리지 않는다 · D-133)
- `build/labels/guide_statute__팀장판정표*.csv` — 판정표 · 채움 · 정합 · 개정4(팀장 4행 · 합의 후보) — 가져온 결과는 `decisions.jsonl` 에 있다
- `build/ftc_food_scan.csv` — 공정위 의결 식품 사건 계측(마스킹 뒤)

## §3 이 날 틀렸던 것 — 되풀이하지 않는다

- `preprocess.split` 을 `--write` 없이 드려 분할이 옛것으로 남았다 — 게이트 `test_분할_산출물이_지문을_들고_있다` 가 잡았다. **분할을 바꿀 때는 `launcher.py golden --write`**
- 기기 쓰기(`device_commit_files`)가 같은 출력 경로의 **옛 판을 쓴 적이 두 번** 있었다 — 쓰기 성공 응답을 믿지 않고 되받아 바이트로 대조해서 잡았다 · **매번 새 출력 경로에서 쓴다**
- 문서 끝 빈 줄 두 개로 pre-commit(end-of-file-fixer)이 커밋을 멈췄다 — 덧붙일 때 끝을 한 줄로 맞춘다
- 기대 응답 변화 수를 잘못 셌다(11 · 8 → 바로 15 · 10) · [별표 1] 7.나 를 「안 쓴 원재료」로만 읽어 「광고심의를 준수합니다」 C·7.나 를 틀렸다고 했다 — 7.나 본문에 「직접적인 관련이 적은 내용 … 강조」가 있다. **목의 뜻은 원문으로 읽는다**
- 한쪽 기기(컨테이너)의 실측을 기기 사실로 쓴 적 — 수에는 늘 「어느 기기 · 어느 게이트 뒤」를 붙인다 (D-178)

## §4 안 본 것 · 깔고 간 가정 (D-188)

- **클론 A 디스크** — 이 날 한 번도 안 봤다
- 합의 채택 1,716행의 정확도 — §0 ③
- 공정위 「부당한 표시·광고행위의 유형 및 기준 지정고시」의 「주관적인 판단 … 오인성이 존재한다고 보기 어렵다」를 식품 사항에 빌려 쓴 것은 해석이다(D-286 개정 4) — 식품 고시에는 같은 문장이 없다
- 해설서는 2017 안내서(식품표시광고법 시행 전)다 — 원천 라벨과 현행 조문의 대응은 판독과 팀장 판정이 붙였다
- 기기 셸(`device_bash`)은 이 날도 붙지 않았다 — 파일은 스테이징으로 읽고 쓰고 되받아 대조했다
