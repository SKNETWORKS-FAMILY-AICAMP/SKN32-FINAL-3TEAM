# 인계 2026-09-22 (2판) — 클론 A(회사) → 클론 B(집 · 정본) · B 에서 할 일 한 장

> 작성 Claude · 클론 A 세션 · **앞 문서** `인계_2026-09-22_클론B.md` · 🔄 **2026-09-23 §1 B · §2 개정** — 1판의 §1 B 표는 폐기다
> 🚨 **검수는 대체 문구를 내지 않는다**(팀장 판정 2026-09-23) — `D초안_2026-09-23_검수는_대체문구를_내지_않는다.md` 가 정본이다. 계약 검증기 둘은 **안 고친다**
> 🚨 **B 에 앉으면 §0 을 순서대로 친다.** 이 날 A 에서 무엇을 왜 정했는지는 원장 D-260 ~ D-264 본문이 정본이다 — 여기는 요약이다.
> 기준 — `ohb` 마지막 push(D-242 확정 · D-262~D-264 · 계약 초안 문서 커밋). 커밋 번호는 사용자가 push 했고 클로드는 출력을 못 받았다.

## 지금 상태 — 한 줄씩

- A 게이트 — **695 passed**(`43eb9c0` 뒤) [사용자 실행 · A]. 그 뒤 라벨 묶음(새 게이트 `tests/test_label_multilabel.py` 18개)은 **A 실기기 `check` 출력을 못 받았다** — 기대 713 안팎(**추정**)
- A 에서 끝낸 것 — 오류 기록(D-261 · `d6aac71` · `8982452`) · 운영 테이블·사용자 세션(D-260 · `43eb9c0`) · 원장 문체 정리(`4e71585`) · 다중 라벨 유형별 합의(D-262) · 조건 칸 확정(D-242) · 분기 시뮬레이터(D-263) · 전제에 묶인 생성(D-264) · 계약 초안
- 🔴 **2차 라벨은 A 에서 가져오지 않았다** — 라벨·시트는 정본에서만 들인다(D-249). A 에서는 CSV 4개로 `compare` 를 **건식(dry)** 으로만 돌렸다
- 🔴 **`app/contracts.py` 는 안 고쳤다**(잠금 1급) — §1 판정이 먼저다

## §0 🔴 B 에서 할 것 — 순서대로 · 각 단계 출력을 클로드에게

**① 받고 게이트**

```
git pull origin ohb
uv run python launcher.py migrate
uv run python launcher.py check
```

- 기대 — B 는 648(09-22 오전) 이후 A 의 코드 넷(오류 기록 · 운영 테이블 · 라벨 합의)을 처음 받는다. 수는 **처음 재는 것**이다
- `migrate` — 0016(`app_error_log`) · 0017(운영 테이블) 이 B DB 에 처음 붙는다. 0017 은 `work_doc.owner_id` 에 값이 있으면 **멈춘다**(정상 — 값 목록을 준다)

**② 2차 라벨 CSV 가 A 에서 본 판과 같은지**

A 에서 본 네 판(사용자가 대화에 올린 파일 · sha256 앞 12자 · 행 196씩):

| 붙인이 | sha256 앞 12 | 바이트 |
|---|---|---|
| 권소라 | `e0fd80d57151` | 66,004 |
| 박수진 | `c070bbab703e` | 65,596 |
| 소성민 | `fc3be67c922f` | 65,485 |
| 이서은 | `7abc93f1ade5` | 70,598 |

```
Get-ChildItem build\labels\*__round2_labelsheet.csv | Get-FileHash -Algorithm SHA256 | Select-Object @{n="f";e={Split-Path $_.Path -Leaf}}, @{n="sha";e={$_.Hash.Substring(0,12).ToLower()}}
```

- 다르면 멈추고 알려 준다 — 어느 판이 최신인지는 사람이 정한다
- B 에 없으면 대화에 올린 파일을 `build\labels\<이름>__round2_labelsheet.csv` 로 둔다
- 팀장 본인 CSV(37행) 가 있으면 같이 둔다 — A 에서는 **못 봤다**(없는 것인지 안 받은 것인지 모른다 · D-188)

**③ 가져오기 → 비교** (정본에서만)

```
uv run python scripts/label_sheet.py import build/labels/권소라__round2_labelsheet.csv --sheet data/derived/label_rounds/round2_labelsheet.jsonl
uv run python scripts/label_sheet.py import build/labels/박수진__round2_labelsheet.csv --sheet data/derived/label_rounds/round2_labelsheet.jsonl
uv run python scripts/label_sheet.py import build/labels/소성민__round2_labelsheet.csv --sheet data/derived/label_rounds/round2_labelsheet.jsonl
uv run python scripts/label_sheet.py import build/labels/이서은__round2_labelsheet.csv --sheet data/derived/label_rounds/round2_labelsheet.jsonl
uv run python scripts/label_round.py compare --sheet data/derived/label_rounds/round2_labelsheet.jsonl build/labels/권소라__round2_labelsheet.csv build/labels/박수진__round2_labelsheet.csv build/labels/소성민__round2_labelsheet.csv build/labels/이서은__round2_labelsheet.csv
uv run python scripts/label_merge.py data/derived/labels/권소라__round2_labelsheet.jsonl data/derived/labels/박수진__round2_labelsheet.jsonl data/derived/labels/소성민__round2_labelsheet.jsonl data/derived/labels/이서은__round2_labelsheet.jsonl
```

- `import` — `;` 구분자 · 엑셀이 지수로 바꾼 지문(`7.69762E+11`)은 이제 **문구가 같으면 받는다**. 문구를 고친 행은 여전히 멈춘다(정상)
- `compare` 기대(A 건식 실측) — 2인 이상 **316** = 합의 **103** · 부분합의 **74** · 갈림 **139** · 판정표 **213** 행. 판정표에 「합의 유형」·「갈린 유형」 칸이 새로 선다
- `label_merge` — PowerShell 은 `*` 를 안 펴 주므로 파일을 넷 다 적었다 · 유형별 κ(A 건식 실측: 질병 0.69 · 건기식 0.62 · 후기 0.62 · 부당비교 0.49 · 거짓과장 0.43 · 비방 0.38 · 의약품 0.14 · 소비자기만 0.08)
- 🚨 `decide` 는 **팀장이 `최종번호` 를 채운 뒤**에만 — 클로드는 판정표까지 만들고 멈춘다

**④ §1 판정** — 아래 표. 정해지면 클로드가 `contracts.py` · 픽스처 · 게이트를 같은 작업에서 만든다

## §1 판정 대기 — 이번 B 세션의 본안

**A. 계약 초안 §6** (`docs/ohb/계약초안_2026-09-22_분기_시뮬레이터.md`)

| # | 무엇 | 초안의 안 |
|:-:|---|---|
| 1 | `Premise` 값 | 품목 분기 넷(`식품` · `건기식_인정` · `건기식_비인정` · `화장품`) + `실증_있음` · 「일반」 없음 |
| 2 | 초기 판정의 `premise_basis` 기본값 | `건기식_비인정` + 실증 없음 (가장 보수적 · D-263 ①) |
| 3 | 검증기 두 개를 푸는 방향 | 아래 B |
| 4 | `judgment` 테이블 전제부 저장 칸 | 저장 경로(동의 · D-96)를 만들 때 같이 — 지금은 안 만든다 |

**B. 🔄 검증기 둘은 닫혔다** — 팀장 판정(2026-09-23): **검수는 대체 문구를 내지 않는다.** `_no_candidates_for_qualification` · `_outcome_matches_payload` 를 **고치지 않는다.** 근거·개정 범위·새 안건은 `D초안_2026-09-23_검수는_대체문구를_내지_않는다.md`. 대신 B 에서 정할 것이 다섯 늘었다:

| # | 무엇 | 권고 |
|:-:|---|---|
| 5 | `hold` 사유 `premise_unknown` 추가 | 추가 — 계약 + DB enum + 마이그레이션 0018 |
| 6 | 분기 화면으로 넘어갈 신호 | `branches` 가 비지 않으면 넘어간다 |
| 7 | 재검수 사유 `branch_selected` 추가 | 추가 |
| 8 | `Branch` 가 실을 전·후 | `verdict` · `risk` · `sanction` 셋 다 |
| 9 | 🔴 `gap2` 의 뜻이 원장(코드↔모델 등급 차)과 코드 주석(1·2위 격차)에서 갈린다 | 원장 뜻 정본 · 문턱 2 는 `[임의]` 로 `PARAMS` 에 빼고 harness 뒤 확정 · **지금 1 로 내리지 않는다** |

**C. 사용자가 보류한 셋** — 이번에는 안 다룬다: 혼자 붙인 라벨(141행)의 지위 · 박수진이 보기를 전부 고른 44행 · 2차 검증 행 정답률

**D. 앞 인계에서 넘어온 것** — `인계_2026-09-22_클론B.md` §1 그대로(단계 관문 **시한 9/28** · 봉인 선택 해시 순 · 넓은 거름 · 3판 §1 나머지)

## §2 클로드 몫 — B 에서 이어서

1. **§1 판정 뒤** — ① 원장 **D-265** 는 A 에서 적었다(D-125 · D-264 개정 줄 포함 · 색인 포함) ② `contracts.py`(Premise · Branch · SubstBranch · Condition · 칸 추가 · **검증기는 더하기만**) ③ 픽스처 넷(`branch_hf_recognized` 는 후보를 뺀 판) ④ 게이트. D-59 · D-125 · D-263 · D-264 본문은 A 에서 읽었다 — D초안 §0 에 인용 위치가 있다
2. 판정 코어 순서 — 평가 도구 → `split` → `classify` → `judge`(사전 바닥) → 기본 인코더(KURE) 인터페이스(dev 는 train 에서 사건 단위로) → `sanction_rule` 적재기 + 검토표(YAML 권고) → 조건부 `/judge` 501 해제
3. 생성(진입점 B) — 수정 쌍 245 분석 → 치환 사전 → 변환 규칙 → 근접도 평가(표 단위 holdout · D-264). 🔄 245쌍은 **재료**다 — 런타임 입력이 아니다(D-265)
4. 시뮬레이터 기준 문안 초안 — 조문에서(D-263 ②) · 공개 문안이라 초안까지
5. 팀원 알림 — 소성민(`admin_errors` 바뀜) · psj · ssm · ksr · lse(D-260 §7 화면 배선) · 🆕 ksr · lse(`review.html` 「대체 문구 후보」 카드 제거 · 검수 → 생성 이음새)
6. 앞 인계 §2 곁가지 목록 — 그대로 남아 있다

## §3 A 에서 틀렸던 것 · 배운 것

- 기기 쓰기 도구가 성공을 돌려줬는데 **옛 내용**이 써진 적이 한 번 있다 — 바이트 대조로 잡았다. 쓰기 뒤 되받기를 빼지 않는다(D-149)
- psycopg `conn.transaction()` 은 바깥 트랜잭션이 없으면 **커밋한다** — DB 테스트는 바깥 트랜잭션 + `psycopg.Rollback` 으로 감싼다
- `admin_*.py` 를 통째로 복사하다 고친 `admin_errors.py` 를 덮어쓴 적이 있다 — 고친 파일은 복사 대상에서 뺀다

## §4 안 본 것 · 깔고 간 가정

- **B 디스크 · B DB** — 이 날 A 세션은 한 번도 안 봤다. §0 의 수는 B 에서 처음 재는 것이다(D-188)
- 라벨 묶음 뒤 A 실기기 `check` 수 — 못 받았다
- ③ 의 기대 수는 A 컨테이너에서 대화에 올린 CSV 로 잰 **건식** 값이다 — B 의 시트·라벨 파일이 다르면 달라진다
- 🔄 API 초안 · 기획서 · 화면 템플릿은 09-23 에 읽었다(D초안 §0). **실행 출력은 없다** — 전부 읽은 것이다(D-146)
- A 기기 셸(클로드 쪽 `device_bash`)은 이 날 끝에 붙지 않았다(Windows 쪽 공유 문제) — 이 문서는 스테이징으로 쓰고 되받아 대조했다
