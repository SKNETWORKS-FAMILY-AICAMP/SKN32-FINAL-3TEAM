# 인계 2026-09-18 — 클론 A (회사 기기)

앞 문서 → `docs/ohb/인계_2026-09-18.md`

저장 자리 → `docs/ohb/인계_2026-09-18_클론A.md`

🚨 **이 문서의 실행 출력은 전부 사용자가 돌려 준 것이다** (D-146). 클로드는 클론 A 에서 `device_bash` 가
　 뜨지 않았다(「Workspace unavailable」) — **파일 스테이징·쓰기만 살아 있었다.**
★ 스테이징한 파일을 컨테이너에서 읽어 센 수는 **[컨테이너 실측]**, 웹에서 본 것은 **[웹]** 으로 적었다.

📌 **이 날 클론 B 는 쓸 수 없었다.** 그래서 파생물·데이터 작업은 하지 않고 **레지스트리·조사·진단**만 했다 (D-226).

---

## §0 열 줄

1. ✅ **최신화** — `ohb` 를 `origin/ohb`(62커밋) → `origin/main` 으로 fast-forward. `e3e9248`
2. 🔴 **클론 A 의 `data/derived` 는 09-10~09-14 판에 멈춰 있다** — 원장 대비 🔄 27 · ⛔ 10 · 🆕 다수.
   `check` 게이트 2개가 **옛 골든셋 때문에** 빨강이었다. 코드 결함이 아니다
3. ✅ **옛 골든셋을 레포 밖으로 옮겼다** — `..\CopyLane_A_old_derived\golden.jsonl`. `check` **335 passed · 5 skipped**
4. 🔴 **파생물을 읽는 게이트 10개가 기기 축을 모른다** (F1) — A 에선 옛 파일 위의 가짜 초록·빨강, B 에선 파일이 사라져도 초록
5. 🔴 **CI 「전체 테스트」가 빨강이다** (F3) — `test_원장이_디스크와_같다` 가 CI 에 없는 생성물 146개를 「없다」로 잡는다
6. 🔴 **원문캐시 분류가 하나 빠졌다** (F2) — `<소스>/text/*.txt`(PDF 전문 · 마스킹 전)가 「생성물」로 분류돼 묶음에 들어간다
7. 🔴 **`launcher.py rebuild` 단계 순서가 거꾸로다** — `gen_registry` 가 `build_matrix` 보다 먼저 돌아 새 id 에서 `KeyError`
8. ✅ **새 원천 등재 `mfds_cgm_expc`** (법제처 「중앙부처 1차 해석」 — 식약처) · **G0 · hold** · 커밋 `1c092a1` `d680b38`
9. ★ **그 원천의 가치는 「위반 표본」이 아니라 「조건부 적법 · 제품 지위」다** — 「항균」·「가려움 기능성화장품」 본문 2건
10. ⬜ **`foodsafety_ad_monitor` 불채택 조건 충족** — sample 키도 `ERROR-310` (사용자 실행 2026-09-18) · 판정 대기

---

## §0-1 읽는 순서

1. 이 문서 §0 · §3(F1~F3) · §6(미실행)
2. `docs/ohb/결정요청_2026-09-17_화장품축.md` **§11** (이 날 추가)
3. `docs/03_데이터/_matrix/data.js` 의 `mfds_cgm_expc` 레코드 — scale · valueNote · caution
4. `docs/ohb/기기간_동등성_2026-09-17.md` — §5 의 빈칸 다섯이 이 문서 §4 에 있다

---

## §1 커밋

```
d680b38  실측: mfds_cgm_expc 규모와 본문 2건 - 조건부 적법 · 제품 지위 원천으로 가치 근거 교체
1c092a1  등재: mfds_cgm_expc (법제처 중앙부처 1차 해석 - 식약처) G0 hold
e3e9248  (origin/main 병합 — 새 커밋 아님, fast-forward)
```

push 완료 — `70771f2..d680b38  ohb -> ohb` (사용자 실행 출력).

- `1c092a1` 본문의 「S0-14 검토표 48 → 50」 중 **1 은 곁가지**다 — 09-17 `mfds_cosmetic_ad_guide_2013` 등재 뒤
  `review_sheet` 를 안 돌린 몫. [컨테이너 실측] 변경 전 입력으로 재생성해도 49 가 나왔다.

---

## §2 클론 A 의 데이터 상태 — 무엇이 어긋났나

`derived-manifest --check` (사용자 실행) —

| 표시 | 개 | 뜻 |
|---|---:|---|
| ⛔ 이 기기에 없다 | **10** | `decc_phrases` · `law_prec` · `cosmetic_*` · `evalset_by_statute` 등 — B 에만 있다 |
| 🔄 sha256 다름 | **27** | `law_article` · `law_norm/*` · 라벨 3종 · 골든셋 · `banned_terms` 등 |
| 🆕 원장에 없다 | 다수 | `mfds_press` · `mfds_press_pdf` · `mfds_casebook` 아래 텍스트·캐시 |
| 같음 | 114 | 원천·표본 5개(git) · `chunks.jsonl` · 원문캐시 tables 105 등 |

[컨테이너 실측] 클론 A 파일 다섯의 sha 가 **사실원장 09-16 ⑦ 절 「클론 A (09-15)」 칸과 같다** —
`law_article 3da2fbf05ae8` · `banned_terms 33f429054a21` · `golden 22918038cc49`(1,908행) · `chunks 485794a26ec7`.
**망가진 것이 아니라 그 판에 멈춰 있다.** 전부 CRLF 다(D-241 이전 판).

- `preprocess.golden`(쓰지 않는 모드) — **분할 입력 3개가 다르다** — `ftc_layer1_phrases` 는 B 895,977 B ↔ A **195,138 B**
- ⛔ **A 에서 `golden --write` · `derived-manifest --write` 를 치지 않는다** — git 추적 파일(`split_manifest.json` ·
  `derived_manifest.jsonl`)을 A 의 옛 입력으로 덮어쓴다
- ⛔ **A 에서 raw 를 다시 받아 맞추지 않는다** — 판이 하나 더 생기고 D-226 (c) 를 거스른다
- **git 으로는 같아질 수 없다** — 원천·표본 5개(0.81%)만 git 이 옮긴다. 생성물 41개(53 MB)는 **B 에서 받아 와야** 한다

### 옮긴 파일 (되돌릴 수 있다)

```
data/derived/golden/golden.jsonl  →  ..\CopyLane_A_old_derived\golden.jsonl
되돌리기: Move-Item ..\CopyLane_A_old_derived\golden.jsonl data\derived\golden\
```

git 추적 파일이 아니라 `git status` 에 안 뜨고 B 에 영향 없다. **나머지 옛 파생물 36개는 그대로 있다.**

---

## §3 🔴 발견 셋 — 기기 축의 구멍

### F1 · 파생물을 읽는 게이트 10개가 「있다」만 본다 (D-226 2항 ↔ 코드)

| 게이트 | 읽는 것 |
|---|---|
| `test_label_path.py:59 · :82 · :97` | `golden.jsonl` |
| `test_split.py` 픽스처 `plan()` → 5개 · `:70` | `ftc_layer1_phrases` 등 · `banned_terms` + `golden` |
| `test_db_schema.py:365` | `golden.jsonl` |

- 건너뛰는 조건이 **파일 존재 하나**다 → A(옛 파일)에선 옛 판 위의 결과, **B 에서 파일이 사라지면 skip 으로 초록**
- D-226 2항 「`check` 는 어디서든 돈다(파생물을 안 읽는다)」는 **쓰인 날 이미 거짓**이었다 — `test_split.py` 는 09-11 부터 있다
- CI 로그(사용자 제공)의 skip 10 이 정확히 이 목록이다
- 명령들은 멈춘다 — `load_db._jsonl` · `chunk.main` · `embed` · `eval_rule` · `dictionary` · `inject` 가 전부 fail-closed (D-220).
  **새는 곳은 「게이트 초록」 하나다**

**안 네 개** (⬜ 판정 대기 · 설계 결정이 걸린다)

| | 무엇 | B 에서 사라진 생성물 | 걸림 |
|---|---|:-:|---|
| (나) | 원천·표본만 없음 검사 | ❌ | 보호가 준다 |
| (다) | 기기 표시 `CLONE=A/B` | ✅ | 팀원 = 셋째 값 · **틀린 값이면 조용히 꺼진다** · 읽는 쪽 배선 없음 |
| **(라) ★ 권고** | **상태 규칙** — 생성물이 하나라도 있으면 「데이터 기기」: 원장과 전부 같아야 하고 게이트는 없으면 fail. 없으면 「안 봤다」 skip | ✅ (전부 소실만 못 잡음) | D-89 예외 사유를 새 D 에 적는다 |
| (마) | `check` 에서 빼고 `golden --write` 뒤에 붙인다 | △ | 다음 생산 때만 잡는다 |

### F2 · `text/*.txt` 가 원문캐시로 안 잡힌다 (D-244 초안 누락)

- `preprocess/evasion_scan.py:46,84` — PDF 전문을 `data/derived/<소스>/text/*.txt` 로 캐시. 마스킹은 `quotes.json` 에만(`:185`)
- `KIND_RULES` 원문캐시 패턴은 `mfds_press_pdf/` 하나 → `.txt` 는 **「생성물」 = 묶음 대상**
- `leaks()` 는 `.json`/`.jsonl` 만 본다 — `.txt` 를 안 센다
- 지금 B 원장엔 이 파일이 없어 **실제로 새지는 않았다**. B 에서 `scan` 한 번 뒤 `--write` → 묶음이면 샌다
- ⬜ `.txt` 안의 법인 표기 수는 **안 셌다**

### F3 · CI 전체 테스트 빨강 — **확정** (사용자 제공 CI 로그)

```
test_원장이_디스크와_같다  —  원장에 없음 [] · 이 기기에 없음 146개 · sha256 다름 []
```

- 146 = 생성물 41 + 원문캐시 105 — CI 체크아웃엔 원래 없다
- `sha256 다름 []` — **git 이 옮기는 5개는 Windows CI 에서도 원장과 바이트 동일**
- PR #25 는 이 빨강인 채 병합됐다(게이트 단계만 필수로 보인다 · 브랜치 보호 설정은 안 봤다)
- 권고: 「없다」 검사는 원천·표본에만, sha 대조는 있는 파일 전부. ⚠️ 단독으로 하면 F1 (나)와 같은 구멍 — **(라)와 함께**

### F4 · `rebuild` 단계 순서 (곁가지 · 수정 제안만)

`launcher.py:445` `steps` 가 `gen_registry` → `build_matrix`. `gen_registry.py:32` 는 `build_matrix` 가 만드는 `sources.json` 을 읽는다.

- 새 id → `KeyError` (이 날 실제로 났다)
- 🔴 **기존 레코드를 고칠 때는 에러 없이 옛 판정으로 `data_sources.yaml` 을 만든다** — 한 번의 rebuild 가 두 벌을 만든다 (D-90)
- `_matrix/README.md` 는 순서를 맞게 적고 있다 — 문서 ↔ 코드 드리프트
- 지금 커밋된 생성물은 올바른 순서 재생성과 **바이트 동일** [컨테이너 실측] — 입은 피해 0
- **우회**: `uv run python scripts/build_matrix.py` 를 먼저 친다

---

## §4 기기간 동등성 — 문서에 없는 다섯

`기기간_동등성_2026-09-17.md` 를 전문 읽고 대조했다.

1. 기기 상태를 **둘**(raw 가진 팀장 / raw 없는 팀원)로만 본다 — **옛 파생물을 든 기기**(클론 A)가 없다 → F1
2. §6 「팀장 (raw 를 가진 기기)」 — **클론 A 도 raw 를 가졌다.** 문서대로면 A 에서 `extract` 를 쳐도 된다 → **「클론 B」로 박을 것** (D-226)
3. §1 「derived 는 마스킹을 지난 층」 정정이 `mfds_press_pdf/` 하나뿐 → F2
4. CI(생성물 없음)가 빠져 있다 → F3
5. §2 `golden.jsonl` **6,796행은 컨테이너 값** — 정본(클론 B)은 **6,789행** (사실원장 09-18 ③ · D-178)

⬜ **2 · 5 는 원장의 수와 판정이 걸려 B 실측 뒤 고친다.**

### 맞추는 순서 (B 작업)

| 단 | 무엇 | 어디서 |
|:-:|---|---|
| 1 | F2 — 원문캐시에 `text/*.txt` · `leaks()` 가 `.txt` 도 | 코드 (A 가능) |
| 2 | `data-export <경로>` / `data-import <묶음>` — import 는 옛 파일 백업 → 풀기 → `--check` | 코드 (A 가능 · 픽스처로 CI 검증) |
| 3 | 공유 위치 (§7-1) | 🔴 사람 |
| 4 | B `export` → A·팀원 `import` → `--check` 초록 | 🔴 B |
| 5 | F1 (라) · F3 · 문서 정정 | 코드 + 판정 |

---

## §5 부족 원천 조사 — 이 날의 결론

### 모자란 것 (원장 · 클론 B 09-18)

평가 30 미만 넷 — `질병_예방치료_표방` · `의약품_오인` · `후기_체험기_기만` · `비방광고`

### 후보 판정 (전부 판정 전 · [웹] + 사용자 화면)

| 후보 | 결과 |
|---|---|
| **법제처 「중앙부처 1차 해석」 — 식약처** | ✅ **등재 `mfds_cgm_expc` (G0 · hold · 가치 B 잠정)** — 아래 |
| 한국소비자원 실태조사 | ⬜ 공공기관이라 제24조의2 밖 · 공공누리 유형 미확인(사이트 인증서 오류) |
| 자율심의기구 3곳 | ❌ 심의 결과·부적합 문구 **비공개** 확인(건기식협회 · 소비자공익네트워크 「All Right Reserved」) · 식품산업협회 미확인 |
| 방미심위 광고 심의 | ⬜ 의결 공개 위치를 못 찾았다 · 매체가 방송 |
| 공정위 뒷광고 점검 | ❌ 지침·의결서는 이미 있다(`law_go_kr` · `ftc_decisions_body`) · 보도자료에 원문 없음 |
| `foodsafety_ad_monitor` (I0500) | 🔴 **불채택 조건 충족** — 우리 키 · sample 키 둘 다 `ERROR-310` (09-09 · 09-18) · data.go.kr 15064912 **404** |

🔴 **질병(식품) · 비방 · 후기의 새 원천은 못 찾았다.** `남은것_2026-09-17` 의 「원천 없음」을 뒤집을 근거 없음.
★ 다만 **화장품 축의 의약품 오인 · 질병 표방 경계**에는 `mfds_cgm_expc` 가 기관 판단을 준다.

### `mfds_cgm_expc` — 무엇을 확인했나

| | |
|---|---|
| 규모 (사용자 화면) | 전 부처 「광고」 1,735 · 식약처 「광고」 648 · 전 부처 「표시광고」 195(최신 50 중 식약처 외 1 · 농식품부) · 「광고문구」 10(식약처 9 · 전부 2024-12-12) |
| 🚨 검색의 한계 | 토큰 일치라 넓다 — 「표시광고」 50건 중 광고 표현 판정은 제목 기준 **약 7건**, 나머지는 알레르기·당알코올 라벨 |
| 【분류】 | 본문에만 있다(예: `화장품법 > 화장품의 취급 > 표시·광고·취급`) · **화면 필터 없음** → 수집 뒤 분류로 거른다 |
| 탐침 (`probe`) | 근거 URL(식약처 저작권정책) — 「업무상 저작물은 별도 허락 없이 이용 가능 · 공공누리 미부착 자료는 부서와 사전 협의」 = `mfds_press` 가 D-132 로 판정받은 구조와 같다 · robots `Allow:/` |
| 원본 | 식약처 `m_1060` 「자주하는 질문집」 — 2024 식품 안내서-1392-01 · 2024 화장품 1395-01 · 2025 식품 1460-01 · 2025 화장품 1463-01 (2025판 4분야 1,800문항) · **같은 게시판의 `mfds_online_guideline` 이 G3 · D-132 · 2인 확인 완료** |
| 시점 | 화면 안내 「당시 법령 기준 · 폐지·변경된 해석 포함」 → 해석일자 필수 필드 (D-138 성격) |

**본문 2건 (사용자가 옮겨 줌)** — 전문은 `결정요청_2026-09-17_화장품축.md` §11

1. 「항균」 — **인체세정용 제품 + 인체적용시험 입증**일 때만 · 기초화장용 불가 (「화장품 표시ㆍ광고 관리 지침」 [별표 2] — **우리 코퍼스에 없는 규범**)
2. 「피부 장벽 기능 회복 · **가려움** 개선 기능성화장품」 + 「의약품이 아님」 — **심사·보고 효능 그대로면 표시 가능** → 같은 표현이 **제품 지위로 적법 ↔ 위반이 갈린다** (D-242)

⚠️ **사전 충돌 후보** — 클론 A **09-10 판** `banned_terms` 305행(「…가려움증…」 · 거짓_과장) · 506행(「항균 항염…」 · 질병) 이
`단독판정: true` · `적법예시: []`. **정본(B)에서 다시 확인해야 한다.**

---

## §6 🔴 미실행 · 판정 대기

| | 무엇 | 왜 · 누가 |
|:-:|---|---|
| 1 | **`mfds_cgm_expc` 2인 확인** | 🔴 사람 — 등급 G3(D-132 준용) 여부 · 등재 단위(`law_go_kr` covers 로 접을지 · D-90) |
| 2 | **`foodsafety_ad_monitor` → not_adopted** | 🔴 판정 — 원천은 `gen_registry.py` `STATUS` · `NOT_ADOPTED_IDS` · `registry_tail.yaml` |
| 3 | **F1 방식** (라)/(다)/(나)/(마) | 🔴 판정 — D-226 2항 · D-89 예외 |
| 4 | F2 · F3 을 **D-244 초안에 반영**할지 | 🔴 판정 |
| 5 | F4 `rebuild` 순서 수정 + 순서 게이트 | 제안만 · 곁가지 커밋 |
| 6 | 화장품 질문집 **표시·광고 장 본문** | 「광고문구」 10건 중 본문 2건만 봤다 |
| 7 | **공정위 1차 해석이 있는가** | 화면 「부처 선택」에서 확인 요청했으나 답 없음 · API target `ftcCgmExpc` 는 **추정** |
| 8 | 법제처 1차 해석 ⊇ 책자인가 | 대조 안 함 |
| 9 | `.txt` 원문캐시의 법인 표기 수 | 안 셌다 |
| 10 | 동등성 문서 정정 5건 | §4 — 2·5 는 B 실측 뒤 |

---

## §7 ② 소스별 수집 분담 — 선행 조건 (팀장: 「나중에 갈 생각이 있다」)

**분담 방식 셋** — ① 조사·판정·수기·라벨 분담(**지금 가능**) · ② 소스별 수집을 팀원 기기에서, 가공은 B(**준비 후**) ·
③ 수집+가공 분담(❌ D-226 정면 충돌).

②에 필요한 것 —

| | 무엇 | 비고 |
|:-:|---|---|
| 1 | **원문을 B 로 모으는 길** — 공유 위치 → B 가 **`adopt` 흐름으로만** 등록 | D-245 사고 자리 · `register` 금지 |
| 2 | **`data/manifest.jsonl` 충돌 규칙** — 동시 수집 → 같은 파일 끝에서 충돌 | 시작 직전에 정해도 된다 |
| 3 | **같은 소스는 한 사람** — 페이징 밀림 원천은 받을 때마다 판이 는다 | D-149 · D-208 · D-211 |
| 4 | 키·승인은 사람마다 — `setkey` 각자 · AI Hub 는 개인 승인 | ⬜ **AI Hub 원문을 팀원 사이에 넘기는 것이 `NOREDIST` 에 걸리는지 미확인** |

★ **B 작업(§4 표) 때 함께 챙길 것** — `data-export`/`import` 를 **파생물 전용으로 굳히지 않는다**(경로·검사를 인자로) ·
공유 위치를 **양방향**(나눠 주는 곳 / 모으는 곳)으로 정한다.

필요한 판정(시작할 때) — D-226 에 「수집은 소스 담당 기기, 가공은 클론 B」 추가 · 소스별 담당자 표.

✅ 수집기는 이미 막아 준다 — `registry.require()` 가 2인 확인 없거나 hold 인 소스를 **누가 돌리든 거부**한다
([컨테이너 실측] `mfds_cgm_expc` 에 `U1` 요청 → 「G0 에서 열려 있지 않다」).

---

## §8 내가 틀린 것 — 여섯

1. 🔴 **「A 를 B 와 같게 만드는 것은 D-226 과 반대」** — 과잉 해석. D-226 이 막는 것은 A 에서 **만드는** 것이고 **받는** 것은 D-244 의 길이다
2. 🔴 **「런처 경로를 미리 돌려 봤다」** — 컨테이너에서 README 순서로 돌려 `rebuild` 순서 버그(F4)를 못 봤다. 검증한 경로와 사용자가 칠 경로가 달랐다
3. 🔴 **제목만 보고 「가치 C 로 내리자」** — 본문 1건이 판단을 뒤집었다. 「안 본 것」을 「없다」로 쓴 셈 (D-188)
4. 🔴 **「CI 가 1분이면 게이트 단계일 수 있다」** — 실제로는 전체 테스트 단계였다. 추정을 사실처럼 적었다
5. 🔴 **(나)의 비용을 작게 적었다** — B 에서 생성물이 사라지는 위험을 팀장이 짚었다
6. 🔴 **`결정요청_2026-09-14_…_6건.md` 커밋 여부를 물었다** — 09-14 밤 인계 95행에 **팀장 판정(커밋 안 함)**이 이미 있었다. 인계를 안 찾아 봤다

---

## §9 사용자가 칠 명령

클론 B 에서 이어받기 전 —

```
git pull origin ohb
uv run python launcher.py check
uv run python launcher.py derived-manifest --check
```

`rebuild` 를 쓸 때(F4 가 고쳐지기 전) —

```
uv run python scripts/build_matrix.py
uv run python launcher.py rebuild
```

---

## §10 안 본 것 — 「없는 것」과 가른다 (D-188)

- **클론 B 의 실제 상태** — 이 날 B 는 한 번도 안 봤다
- `.txt` 원문캐시 속 법인 표기 · `app/`·`db/`·`scripts/` 비테스트 코드의 파생물 읽기 경로 전수
- 공정위 1차 해석의 존재 · `mfdsCgmExpc` 가 API target 으로 실제 도는지(LBOX 주소에서 본 이름)
- 2025 화장품 질문집 PDF 의 텍스트 층 · 두 해석이 2025판에서 바뀌었는지
- 한국소비자원 공공누리 유형 · 식품산업협회 공개 페이지
- 브랜치 보호 설정(필수 체크가 게이트뿐인지)
- `derived-manifest --check` 의 🆕 항목 — 개수를 세지 않았고, 원문캐시인지 「이 기기에만 있는 파일」인지 가르지 않았다

---

## §11 대조 기록

이 날 기기에 쓴 파일을 **되받아 바이트로 대조**한 결과 (D-218) —

```
✅ docs/03_데이터/_matrix/data.js                  1차 25c051cf2ed5 (160,917 B) → 2차 c38caf56605d (162,854 B)
✅ scripts/gen_registry.py                         040f88170e95 (54,380 B)
✅ docs/ohb/결정요청_2026-09-17_화장품축.md          c94bb076649b (20,532 B)
✅ 생성물 5개(rebuild 1차 뒤) — data_sources.yaml · registry_rationale.yaml · sources.json ·
   판정매트릭스.html · S0-14 검토표  ↔  컨테이너 재생성  바이트 동일 · 전부 LF
```

🔴 **대조의 범위는 파일까지이고 커밋까지가 아니다** — 커밋·push 는 사용자 출력이 유일한 증거다.
