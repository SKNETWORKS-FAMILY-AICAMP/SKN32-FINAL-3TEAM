# 인계 2026-09-23 — 클론 A(회사) → 클론 B(집 · 정본) · B 에서 할 일 한 장

> 작성 Claude · 클론 A 세션 · **앞 문서** `인계_2026-09-22_클론A.md`(2판)
> 🚨 **B 에 앉으면 §0 을 순서대로 친다.** 이 날 A 에서 무엇을 왜 정했는지는 원장 **D-266 ~ D-280** 본문이 정본이다 — 여기는 요약이다
> 기준 — 커밋 **`c626225`**(W3 · push 됨) · 그 앞 `18707bc` · `62ecd5a` · `9e791e9` · `bd7e736` · `6997e69` · `a327eda` · `90d2197` · `f96ed2b`
> 🚨 앞 인계 2판 §0(2차 라벨 import · 비교 · 합치기)을 B 에서 **끝냈는지 A 에서는 모른다** (D-188) — 안 끝났으면 §0 ② 에서 그것부터 한다

## 지금 상태 — 한 줄씩

- A 게이트 — **781 passed · 671 deselected · skip 0**(`c626225` 앞 working tree · 2026-09-23) [사용자 실행 · A]
- A `dmap` — **결정 280**(폐기·대체 8) · 살아 있는 것 272 · 인용된 것 256 · 🔴 인용 0건 16 · ⬜ 열린 결정 119 [사용자 실행 · A]
- A DB — **0018 적용됨** · GUARD 에서 멈추지 않았다(옛 「일반」 · 출처 없는 품목 · 어긋난 확정 행 모두 0) [사용자 실행 · A]
- A 에서 끝낸 것
  - **그래프 재편 W2** — 코어 서브그래프 + 검수·생성 그래프 · 법별 팬아웃 (D-266 · D-267)
  - **W3 계약 재작성** — `app/contracts.py` · `app/models.py` · 0018 · 픽스처 · 검수 화면 라벨 (D-273 ~ D-279)
  - **원장** — D-266 ~ D-280 · 사실원장 「판정 축」 절을 09-03 판에서 갱신
- 🔴 **A 에서 안 한 것** — 2차 라벨 import(D-249 · 정본에서만) · 청크 재생성 · 사전 재생성 · 파생물 publish

### 이 날의 판정 — 한 줄씩 (본문은 원장)

| D | 무엇 |
|---|---|
| D-266 · D-267 | 판정 코어는 서브그래프 하나 · 품목 분기는 **법별 노드 팬아웃**(검색·인코더는 앞에서 한 번) |
| D-268 | 검수에서 확정된 실증형(B) 위반의 종착은 **「지시」** |
| D-269 | 인코더 전, 사전에 안 걸린 문장은 **보류(low_conf)** |
| D-270 | 9/28 게이트 → **10/2 · 규칙 조립 생성 기준** · 페르소나 · 세그먼트 · sLLM 은 설계만 |
| D-271 | 품목 축과 법 축을 가른다 — **「일반」 없음** · 청크 칸 이름 `law` (0019) |
| D-272 | 한 전제에서 두 법이 걸리면 하한은 **높은 쪽** · 근거는 합친다 |
| D-273 | 🔴 **통과 = 확정 ∧ R0** · 확정 문장은 위반 없으면 R0, 있으면 R1 이상 · 확정 위반에는 **불가 사유 필수** |
| D-274 | 종착을 **검수(통과·증명서·지시·보류) / 생성(프론티어·탐색 실패·보류)** 으로 가른다 |
| D-275 | 「판정 대상 아님」(`not_claim`) 칸 · 라벨 조건 칸은 응답에 싣지 않는다 |
| D-276 | 분기 하나 = 품목 전제 하나 · **재검수 때 품목을 늘 묻는다** · 품목 출처를 이력에 |
| D-277 | 전용법 품목은 미검수 고지 + 보류 · 사유 `law_uncovered` · 전용법 데이터는 **채우지 않는다** |
| D-278 · D-279 | 「뺄 구간」(`spans`) 칸 · 검수 화면은 라벨만 고친다 |
| D-280 | R1 의 이름은 **「시정명령 위험」** · 화면 수정 목록 8건(표는 D-280 본문) |

## §0 🔴 B 에서 할 것 — 순서대로 · 각 단계 출력을 클로드에게

**① 받고 게이트**

```
git pull origin ohb
uv run python launcher.py migrate
uv run python launcher.py check
uv run python launcher.py dmap
```

- 🚨 **`migrate` 를 `check` 보다 먼저 친다.** A 에서 반대로 쳐서 1건이 빨갛게 났다 — 홈 화면 게이트가 실제 DB 에 붙는데, 모델에 새 칸(`product_category_source`)이 있고 DB 에는 없었다 (코드 결함 아님)
- `migrate` — 0018(`judgment` 보류 사유 · 품목 출처 칸 · 확정 행 불변식). B 에 0016 · 0017 이 아직이면 함께 붙는다
- 0018 GUARD 가 멈추면 메시지에 **몇 건 · 무엇**이 나온다(옛 품목 값 「일반」 · 출처 없는 품목 · 위반 유무와 위험도가 어긋난 확정 행). **지우지도 고치지도 않고 멈춘다** — 그대로 클로드에게
- 기대 — 게이트 **781 이상** · dmap 결정 280. 🟡 B 의 수는 **처음 재는 것**이다(데이터가 다르면 달라질 수 있다)

**② 앞 인계 2판 §0 이 안 끝났으면 그것부터** — 2차 라벨 CSV 대조 → import → compare → merge (D-249 · 정본에서만)

**③ Q4 — 법 이름 → 법 ID 대응표 (한 곳) → W6**

- 대응표를 **한 곳**에 두고 `collect/law_api.py` `TARGETS` 와 게이트로 잇는다 (D-271 ①). 법률 ID — 표시광고법 `002011` · 식품표시광고법 `013094` · 화장품법 `002015` · 건강기능식품법 `009353` (시행령·시행규칙은 `TARGETS` 그대로)
- 🚨 `005361` 은 표시광고법 **시행령**이다 — 법률 ID 와 헷갈리지 않는다
- 그다음 W6 — `preprocess/chunk.py` 가 법 ID 로 법을 정한다 · 마이그레이션 **0019**(`category` → `law` · `v_current_chunk` 다시 만듦) · `chunk --dump` → `embed` → `derived-manifest --write` → `data-publish` (D-271 ② · D-226)
- 🔴 W6 에서 **지울 것** — `app/graph.py` 의 `_CHUNK_CATEGORY` · `_chunk_category`(W3 에서 둔 임시 연결 — 새 품목 이름 `일반상품` · `전용법_미수록` 을 청크 「일반」으로 보낸다) · 게이트 `test_품목을_청크_범주로_넘기는_다리는_W6_전까지_일반으로_보낸다` · `settings.DEFAULT_CATEGORY`
- 재측정 — 범주 필터로 잰 검색 실측(원장 09-12 · 09-16)을 다시 재서 **전후 병기** (D-271 ⑦ · D-178)

**④ 골든셋 품목 · 조건 칸 (D1)** — `golden.jsonl` 행에 품목 · 조건(없음·C / A / B / M / D) 칸이 없다. W1 평가 도구의 조건부/무조건부 병기가 여기에 막혀 있다

**⑤ 사전 재생성 → E3 다시 잰다** — 라벨이 들어오면 `banned_terms` 가 바뀐다. A 실측(아래 E3)은 09-22 판 사전이다

**⑥ W1 평가 도구** — 그래프를 test_sentence 에 돌려 유형별 P/R/F1 · 보류율(사유별) · 조건부/무조건부. 처음엔 거의 전부 보류라 0 에 가까운 것이 정상 (D-269)

**⑦ 실측을 들고 Q1 · Q2 판정 → W4**

## §1 W4 판정 노드 — A 에서 탐색한 것 (구현 전)

| # | 사실 | 영향 |
|:-:|---|---|
| E1 🔴 | **문장 분할기가 없다.** 설계초안 09-23 §2-2 가 「`preprocess/text.py` 분할기」라 적었지만 그 파일에는 구분자 정규화 · 인용 추출뿐이다. kss 등도 의존성에 없다 | Q3 |
| E2 🔴 | **위험도 하한이 비어 있다** — `sanction_rule` 적재기(W5) 없음 · 2인 서명(D3) 전 · `v_risk_lookup` 0행. 계약상 지시(B)는 실증 분기에 실증 전 위험도가 있어야 서므로 **하한 없이는 지시 종착이 못 선다** | Q1 |
| E3 | 사전 단독판정 **420종 = 유형 넷** — 거짓_과장 295 · 질병_예방치료_표방 60 · 의약품_오인 34 · 건강기능식품_오인 31. 출처 의결서 249(표시광고법) · 사례집 171(식품표시광고법) · 신뢰도 단일 420 · 모호 103 · 적법중첩 19 (09-22 판 `banned_terms.jsonl` · 컨테이너에서 셈) | 불가 사유 ⬜ 셋은 W4 를 안 막는다 · ⑤ 뒤 다시 잰다 |
| E4 | 사전 근거가 **법 이름 문자열**이다(「표시광고법 제3조제1항제1호」) — 계약 근거 조문은 법 ID 가 필요하다 | §0 ③ 대응표 |
| E5 | 사전만으로도 **품목 분기가 갈린다** — 사례집 항목은 식품 전제에서만 · 건강기능식품_오인은 건기식 전제에서 불성립(별표1 3호 본문) · 화장품 전제는 사전 0 | 법별 노드 · `merge_laws` 가 W4 에서 처음 일한다 |
| E6 | 정밀도 위험 — 3자 이하 단독판정 **53**(면역력 · 긴장 · 비만 · 두통 …) · 마스킹된 채 들어간 항목 **2**(「***드림」 · 「○○기관시험의뢰결과」) | 적법 60행 오탐으로 잰다 |
| E7 | 재현율 ≈ 0 예상 — 판정기 B F1 ≈ 0 의 원인이 「본 문구 목록 → 안 본 시험지」(D-231) | W4 뒤 거의 전부 보류가 정상 · 올리는 자리는 W7 |
| E8 | W1 평가 도구 없음 | §0 ⑥ |
| E9 | 인정번호 대조 재료(`product_fact.recognition_no`)는 적재돼 있다 | `classify` 가 「건기식_인정 · 검증됨」 분기를 낼 수 있다(D-263 ⑥). 그 밖의 판별 규칙은 없다 — 대부분 미확정 → 전 분기 |

### 판정 대기 — B 실측 뒤에 정한다

| # | 안건 | A 에서 권했던 것 | B 에서 볼 것 |
|:-:|---|---|---|
| Q1 | 하한(W5) 전 위반 문장의 상태 | (나) 확정 · 위험도 비움 · 「위험도 없는 확정이 있으면 보류」 라우터 규칙 (D-227 ⬜ 를 닫는다) | 사전 재생성 뒤 적중 수 · D3 서명 일정 |
| Q2 | 유형 → 불가 사유(A/B/C) 표 원장 등재 | (가) 사전에 있는 넷만 먼저 — 거짓_과장 B · 질병 C · 의약품 C · 건기식 오인 A (`sanction_rule_초안_2026-09-16.md` §6) | 🔴 **라벨 조건 칸 분포** — 유형별로 A/B/C 가 실제로 어떻게 붙었나(D-242 · import 뒤) |
| Q3 | 분할기 | (가) 규칙 기반 자체 구현 · 원문 좌표 유지 (의존성 추가 없음) | — |
| Q4 | 사전 근거를 판정 근거로 · 대응표 | 사전 근거 = 판정 근거, 검색 결과는 보충 · 대응표는 한 곳 | §0 ③ 에서 선다 |

## §2 알릴 사람

- **ksr · lse** (사용자 화면 소유)
  - 검수 픽스처 이름 변경 — `01_pass` · `04_hold_rd1` · `07_guidance_subst` (옛 `01_pass_frontier` · `04_evidence_mismatch` · `07_search_failed_b` 는 지웠다 · 미리보기 URL 이 바뀐다)
  - 새 픽스처 — 검수 15(전제 선택 필요) · 16(판정 대상 아님) · 17(전용법 미검수) · 생성 15(탐색 실패)
  - 검수 응답 — 종착 「지시」 · 품목 · 미검수 법 · 분기 · 뺄 구간 · 판정 대상 아님 칸 / 생성 응답 — `outcome` 칸(필수)
  - 검수 화면 라벨은 A 가 고쳤다(D-279). **후보 카드 삭제 · 지시 카드 · 분기 카드는 소유자 몫**
  - 🔴 **화면 수정 목록 8건** — 원장 **D-280 「화면에 미치는 것」** 표가 정본(도움말 배지 · 위험도 이름 · 이력 · 홈 통과율 · 랜딩 예시 · 생성 문구 · 「합법」 · 마이페이지)
- **2인 서명** — `sanction_rule` 원천 검토표(D3). W5 가 넣어도 서명 전에는 뷰에 안 나온다

## §3 남은 것 · 깔고 간 가정 (D-188)

- **안 고친 문서** — `docs/02_설계/LangGraph_상태_스키마.md`(W3 칸) · `docs/00_산출물현황.md`(W3 행) · 계약초안 3판 상태 줄(「구현 전」 → 구현됨)
- **계약 안건** — 제재 수위 · 형벌 **설명 줄**을 실을 칸이 계약에 없다(D-280 ⬜ · D-227 「안 나가면 과소 표시」)
- **W4 로 미룬 검증기** — 「품목 미확정이면 분기는 언제나」(D-229 ⑥) — `merge_laws` 가 분기를 만드는 커밋에서 올린다 (D-192)
- **0018 은 컨테이너 Postgres 16 에서 19 시나리오를 돌렸다**(재실행 · 허용/거부 · GUARD 셋). 실제 alembic 체인은 A 기기에서 한 번 돌았다(`0017 → 0018`) — B 는 아직
- **기준 문안(`criteria`) · 실증 자료 종류**는 픽스처 안에서 「(초안)」이다 — 조문 대조 전 (D-263 트레이드오프)
- **가정** — B 의 파생물이 09-22 판 이후 바뀌지 않았다(A 는 09-22 판을 받았다 · 크기만 대조했고 해시는 안 쟀다)
