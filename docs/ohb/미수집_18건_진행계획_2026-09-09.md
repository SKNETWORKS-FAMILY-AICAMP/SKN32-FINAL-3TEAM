# 미수집 18건 — 이 기기 기준 진행 계획 (2026-09-09 밤)

> `status: collect` 31건 중 이 기기에 원문이 있는 것 13, 없는 것 18.
> 🚨 **셋으로 갈린다** — 코드가 받을 수 있는 것 · 사람이 받아야 하는 것 · 파생 산출물.

## 갑. 지금 명령 한 줄로 받는다 (1건)

| 소스 | 층 | 명령 |
|---|---|---|
| `mfds_online_guideline` | **3층 판단규범** | `uv run python -m collect.mfds_board mfds_online_guideline --use U3 --dry-run` → 확인 후 `--dry-run` 빼고 |

수집기(`collect/mfds_board.py`)가 이미 있고, `mfds_casebook`·`mfds_special_use_guide` 를 받은
그 코드다. D-132 준용으로 근거도 오늘 열렸다.

## 을. 수집기를 새로 짜야 한다 (1건)

| 소스 | 층 | 막는 것 |
|---|---|---|
| `foodsafety_admin_measure` | **1층 판정라벨** | `POST /portal/fooddanger/searchAdministMeasureList.do` 의 **요청 본문(파라미터 이름)이 레지스트리에 없다** |

🚨 **짓지 않았다.** 파라미터를 지어내면 오늘 여러 번 밟은 함정과 같은 자리다.
**먼저 할 일** — 브라우저 개발자도구 Network 에서 그 POST 의 요청 본문을 한 번 떠서
`access` 나 `probe_note` 에 적는다. 그러면 수집기는 30분이면 선다.

⚠️ 값이 크지 않다는 것도 함께 본다 — `scale` 이 이미 적어 뒀다:
**전수 86건 중 표시광고법 위반은 13건**, 유형별로 쪼개면 5·2·1·5 로 **D-40 의 30건에 한참 미달**이다.
「연 7건 페이스라 기다려도 30이 되지 않는다」고도 적혀 있다. **1층의 본체는 `ftc` 8,253건이다.**

## 병. 사람이 받아야 한다 (13건)

받은 뒤 `uv run python launcher.py register <소스id> <파일경로>` 로 원장에 올린다
(`collect/ingest.py` — 🚨 2인 확인이 끝나야 통과한다).

### 병-1. AI Hub 5건 — 로그인·신청이 필요하다
`aihub_558` · `aihub_71486` · `aihub_71694` · `aihub_71723` · `aihub_review_corpus`
🚨 **클라우드에 올리지 않는다**(프로젝트 하드 제약). 이 기기에서 받아 이 기기에서 쓴다.
승인은 2026-09-02 에 끝나 있다(`approved_at`).

### 병-2. 자유 다운로드 3건 — GitHub
`nsmc` · `klue_dataset` · `bab2min_shopping`
5층 코퍼스라 **판정 파이프라인을 막지 않는다.**

### 병-3. 웹 다운로드 5건 — 6층 세그먼트·페르소나
`kobaco_mcr`(항목 선택) · `krei_food`(즉시) · `kcc_media`(data.go.kr 파일) ·
`khff_survey`(보도자료) · `kosis`(인증키 자동 발급 → API)
🚨 6층은 **판정이 아니라 페르소나**다. 지금 막고 있는 것이 아니다.

### 병-4. 판정이 걸린 것 2건
- `kcia_guideline` — 🔴 협회 회신 대기. **받아도 `use` 축을 못 연다**(이용조건 없음 · 무방식주의).
- `mfds_production` — 경제성 근거. 급하지 않다.

## 정. 파생 산출물 (1건)

`self_sanction_stat` — 받는 것이 아니라 **계산하는 것**이다
(`mfds_sanctions` + `ftc_decisions_body` + `law_go_kr` 과징금 고시). 원천 셋이 다 있다.

---

## 우선순위 — 판정 파이프라인을 막는 것부터

| # | 일 | 왜 |
|:-:|---|---|
| 1 | `mfds_online_guideline` 수집 | **3층**. 명령 한 줄 |
| 2 | `foodsafety_admin_measure` POST 본문 실측 → 수집기 | **1층**. 다만 13건이라 D-40 미달 |
| 3 | AI Hub 5건 | 5·6층. 승인은 끝나 있다 |
| 4 | 나머지 | 판정을 막지 않는다 |

🚨 **18건 중 판정 파이프라인을 실제로 막는 것은 둘뿐이다**(1·2번). 나머지 16건은
5·6층(코퍼스·페르소나)이거나 판정 대기다. 「18건이 비었다」로 읽으면 실제보다 크게 보인다.
