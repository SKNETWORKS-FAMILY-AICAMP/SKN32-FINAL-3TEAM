# 런처 전수 감사 — 2026-09-20 (클론 B 기준 · `d8d2e55`)

앞 문서 → `docs/ohb/인계_2026-09-18_클론A.md` · 결정 → **D-253** (이 감사의 계기가 된 doctor 결손 가르기)

팀장 — *「런처에 구성된 각 기능도 위와 같은 방법들로 충분히 탐색하고 분석해줘」*

🚨 **읽기 감사다 — 명령을 돌려 재현한 것이 아니다** (D-146). 메뉴 49개를 네 묶음으로 나눠 코드를 따라 읽었다.
　 표시 — **✔** 클로드가 코드를 다시 열어 확인 · **○** 감사 에이전트가 읽은 것(재확인 안 함) · **▶** 컨테이너에서 실행해 봄.
　 이 문서는 **판정이 아니다** — 무엇을 고칠지는 팀장이 정한다.
　 🔄 **같은 날 — 팀장 「전부 고쳐줘」 → §1·§2 전부 고침 (D-254).** 판정 대기 둘(`SHRINK_LIMIT` 5% · 미등재 18건)은 D-254 에.

---

## §0 렌즈 — doctor 에서 드러난 여섯 가지 실패 모양

| # | 모양 | doctor 에서 |
|:-:|---|---|
| 1 | **분류가 현실을 다 덮지 못한다** — 모르는 경우가 초록으로 떨어진다 | 결손 이유 셋 · 실제는 다섯 |
| 2 | **안내가 틀리다** | 「재수집하면 닫힌다」 — 서식은 안 돌아온다 |
| 3 | **새 필드·상태를 안 읽는다** | 기기 칸(D-250)이 있는데 「못 가른다」 |
| 4 | **같은 것을 두 도구가 다르게 부른다** | inventory 🔴 · doctor 🟡 |
| 5 | **초록인데 데이터가 사라진다** | (09-18 register — 334노드 소실, 게이트 337개 초록) |
| 6 | **안전** — 외부 전송·삭제·비밀·역할 가드 | — |

---

## §1 🔴 먼저 볼 것 — 여덟

| # | 명령 | 무엇이 틀렸나 | 실패 장면 | 권고 | |
|:-:|---|---|---|---|:-:|
| 1 | **43 adopt ↔ 48 raw-import** | `raw_inbox._foreign` 이 「다른 기기 행인데 디스크에 없다」만 본다. adopt 가 판(`__c`)을 원래 이름으로 옮기면 판 경로가 사라진다 | 팀원 판을 채택 → `pending` 이 extract·data-refresh·data-publish 를 막는다 → raw-import 가 판을 다시 놓는다 → 추출기가 판 때문에 멈춘다 → 다시 adopt … **끝나지 않는다** | `_foreign` 이 `collect/missing.classify` 의 `moved` 를 빼게 한다 — **D-253 함수를 그대로 쓴다** (D-99) | ✔ |
| 2 | **6 setkey (메뉴)** | 키 이름을 묻는 칸이 「번호, 또는 **값을 그대로**」다. 모르는 값을 이름으로 받는다 | 키 **값**을 붙여 넣으면 `$ uv run python -m collect.setkey <값>` 로 화면에 찍힌다(`launcher.run` 이 명령 줄을 찍는다). 09-18 OC 노출과 같은 부류 | setkey 는 보기에서만 고르게 한다 · 모르는 이름은 되비추지 않는다 | ✔ |
| 3 | **42 derived-manifest `--write`** | 디스크 기준으로 원장을 **통째로 새로 쓴다**. 옛 원장과 비교하지 않는다 | 라벨(원천)·표본 파일이 지워져도 `--check` 의 안내가 「갱신하려면 `--write`」 → 원장에서 조용히 빠진다 → publish 초록. **data-refresh 4단계가 이것을 자동으로 돈다** | `--write` 가 옛 원장 대비 **원천·표본이 빠지거나 바뀌면 멈춘다** (생성물만 자유) | ✔ |
| 4 | **48 raw-import** | `import_` 는 G2 를 안 뺀다(`pending` 은 뺀다) · `--from` 검사는 정본에 이미 있는 경로를 조용히 건너뛴다 | ① 정본이 지운 G2 원문을 되살리거나 「받은편지함에 없음」으로 **전체 거부** ② 같은 경로에 다른 바이트를 올린 브랜치가 「✅ 병합해도 된다」 | G2·NOREDIST 거르기를 `pending` 과 한 함수로 · 같은 경로 다른 sha 는 🔴 | ○ |
| 5 | **41 db-reset `--data`** | 추출 모듈을 **직접** 불러 `only_canonical`·`needs_raw` 를 우회한다. 볼륨을 **먼저** 지운다 | 사본: 빈 DB 가 남는다 / 원문 있는 팀원 사본: 파생물을 덮어쓰고 chunk 에서 멈춰 반쪽 DB / 정본: 파생물 원장이 낡는다 | 역할을 **지우기 전에** 본다 · 사본은 load→embed 만 · 정본은 `data-refresh` 를 부른다 | ○ |
| 6 | **11 registry · 13 review** | 넷 중 하나만 돈다 — `gen_registry` 가 옛 `sources.json` 을 읽는다 | `data.js` 를 고치고 11 을 누르면 **옛 판정으로 집행 파일**(`data_sources.yaml`)이 에러 없이 나온다. 09-18 F4 와 같은 사고 | 11·13 앞에서 `build_matrix --check` · 어긋나면 멈춘다 (또는 메뉴에서 내린다) | ○ |
| 7 | **19 extract · 16 collect · 18 register** | 원문 폴더 상수가 추출기마다 **따로 박혀** 있다(`law_article`·`law_norm`·`mfds_press`·`ftc_extract` …). `store.FAMILY_OF` 를 쓰는 곳은 둘뿐 · 대조 게이트 없음 | `register mfds_press <pdf>` → `mfds_press/` 에 들어가고 추출기는 `mfds_press_pdf/` 를 본다(**09-18 과 같은 모양**) · `collect ftc_decisions_api` 는 `ftc_decisions_api/` 에 쓰는데 표는 `ftc` | 추출기도 `store.families()` 를 쓰게 · 게이트 하나: 「추출기 원문 폴더 ⊂ `families(원천)`」 | ○ |
| 8 | **23 chunk → 24 embed** | **하한 래칫이 없다.** chunk 는 두 층이 다 비었을 때만 멈춘다. embed 는 선언에 없는 청크를 지우고 「✅ 선언과 같다」 | 조문 2,207 → 1,873(09-18) 이 오늘 코드에서도 **초록으로 DB 삭제까지** 간다. `chunks.jsonl` 이 0행이면 표 전체를 거둔다(빈 **파일**은 막지 않는다 — `embed.py:224` 는 파일 유무만 본다) | 이전 판 대비 N% 넘게 줄면 멈추고 `--allow-shrink` 요구 · 빈 선언이면 거두지 않는다. 🚨 **N 은 `[임의]` — 판정이 필요하다** | ✔ |

---

## §2 🟡 틀린 안내 · 낡은 가정 · 두 도구의 어긋남

| 명령 | 무엇 | |
|---|---|:-:|
| **32 gate · 31 check** | `-v` 가 pyproject `-q` 와 상쇄돼 **skip 이름·사유가 안 보인다.** skip 12건이어도 rc 0 (▶ 컨테이너 466 passed · 12 skipped). 「Phase 게이트 판정」이 사본에서 skip 만으로 초록 | ✔ |
| **31 check** | ruff 순서가 pre-commit 과 반대(format → fix) · gitleaks·개행 훅과 비게이트 테스트를 안 돈다 — 「커밋 전 점검」이 커밋 통과를 보장하지 않는다 | ○ |
| **3 doctor `--env`** | DB 리비전이 **head 인지 안 본다** — `alembic_version` 이 있기만 하면 ✅ · DB 실패를 전부 「db-up」 🟡 로 · `DATA_ROLE`·`DATA_STORE`·`DATA_DEVICE` 를 안 본다 | ○ |
| **2 onboard** | 6단계 「inventory 목록대로 `collect … --use U1`」 — 사본은 원문이 필요 없다(D-247·D-250 이전 안내). 7단계 `db-reset --yes --data` 는 §1-5 로 이어진다 | ○ |
| **4 inventory** | `status == collect` 인 소스만 본다 — `manual`(AI Hub 등록)·`hold` 소스의 원장 행이 표에서 빠진다 | ○ |
| **44 data-sync** | 확인창 조건 `data/raw` 존재가 **늘 참**(`.gitkeep` 이 추적된다) → 모든 사본에서 매번 뜨는 확인 = 안 읽히는 확인 | ○ |
| **45 data-publish** | 「커밋 X 의 원장과 같다」의 X 가 **HEAD** — 안내 순서(publish 먼저, 커밋 나중)에서는 거짓. 재배포 제약 검사가 기기 칸을 안 봐서 팀원 한 명이 받으면 정본 publish 가 영구히 막힌다 | ○ |
| **47 raw-publish** | 「이 기기가 받은 원문」이 아니라 「이 기기 디스크에 있고 원장에 있는 것 전부」 — 옛 원문이 있는 사본(클론 A)은 기기 칸 없는 과거분 수천 개를 올린다 | ○ |
| **18 register** | 원장 기준 건너뛰기 없음(D-250 이전) · 원장에 없는 동일 파일을 조용히 건너뜀 · 충돌 시 「새 파일명으로」 안내가 판 규칙(`__c`)을 우회 · 하위 폴더를 펴서 넣는다 | ○ |
| **20 scan** | `scan mfds_press_pdf` 가 폴더 이름을 마스킹 정책 키로 넘겨 **멈춘다**(▶ `MaskPolicyError`) · 첫 줄 「산출물 없음」인데 마스킹 전 전문 캐시를 쓴다 · `needs_raw` 없음 | ○▶ |
| **16 collect** | 수집 뒤 「`data-refresh <원천>`」 안내가 15개 중 **10개**에서 「모르는 원천」으로 거부된다(`law_go_kr`·`mfds_cgm_expc` 포함) | ○ |
| **21 golden** | 메뉴 첫 줄 「사전 → 주입 → 분할 세 단계」 — 실제는 분할이 먼저인 네 단계 · 봉인 평가셋이 줄어도 멈추지 않는다 | ○ |
| **22 load** | `load_dict` 만 옛 행을 안 지운다(빠진 금지 표현이 영구히 남음) · docstring 「골든셋은 아직 못 넣습니다」는 옛말 | ○ |
| **14 status** | 「팀 축 — 두 기기가 같은 답」인데 파생물 절이 **이 기기** `data/derived` 를 훑는다 → 기기마다 diff · 사본에서 줄어든 목록이 초록으로 커밋될 수 있다 | ○ |
| **9 migrate** | 접속 문자열 경로가 둘(`alembic/env.py` 원문 · `settings.sqlalchemy_url()`) — `db-fresh` 초록인데 `migrate` 실패하는 조합 | ○ |
| **40 db-drift** | head 에만 있는 객체를 무조건 면제 — 마이그레이션으로 만든 표를 `schema.sql` 에 안 적어도 초록 · 문구가 동결 이전 | ○ |
| **10 rebuild** | 09-18 F4 순서 버그는 **고쳐졌다** ✔ · 다만 `gen_registry` 「미등재 N」이 rc 0 · `STATUS` 기본값 collect(열리는 쪽) | ○ |
| **37 diagram** | 메뉴에서 도면 이름을 넣으면 **반드시 실패**(위치 인자 vs `--only`) — 위험 확인을 받은 **뒤에** | ○▶ |
| **메뉴 공통** | `_invoke` 가 자식의 종료코드를 버린다 — 메뉴에서 실패해도 「⏎ 계속」뿐 · `_check_menu` 가 묻는 인자와 명령 시그니처를 대조하지 않는다 | ○ |
| **25·26·28 stub** | 「미구현」 패널 뒤 rc 0 · `--help` 에는 정상 명령처럼 보인다 | ○▶ |
| **27 serve** | `--host 0.0.0.0` 을 경고만 하고 연다(인증 0줄) | ○ |
| **35 admin-add** | 이미 있는 계정이면 **비밀번호를 덮고 꺼 둔 계정을 되살린다** — 확인 없이 ✅ | ○ |

---

## §3 결함 없음 (본 범위)

5 keys · 7 db-up · 8 db-down · 17 count · 29 test · 36 admin-list(안내 하나 빼고) · 수집 쪽 비밀(원장 `url` 에 OC·serviceKey 없음 · `redact()` · 해석 본문 OC 섞임 거부) ○ · 추출 5종의 `--dump`·`--sheet` 마스킹 경로 ○

---

## §4 권고 순서

1. **§1-1 adopt ↔ raw-import** — D-253 함수 재사용으로 작다. D-250 흐름이 실제로 막히는 자리
2. **§1-2 setkey** — 비밀. 메뉴 입력 한 곳
3. **§1-3 derived-manifest `--write` 가드** · **§1-8 하한 래칫** — 둘 다 「초록인데 사라진다」. 래칫의 N 은 판정
4. **§2 gate `-rs`** — 한 줄. 건너뛴 것을 보이게
5. 나머지 — 폴더 상수 게이트(§1-7) · db-reset(§1-5) · 11/13(§1-6) 을 한 번에

## §5 깔고 간 가정

- ○ 항목은 에이전트가 코드를 읽은 결과다. 클로드는 §1 의 1·2·3·8 과 gate 만 다시 열어 봤다
- 실패 장면은 코드 경로를 따라간 것이지 기기에서 재현한 것이 아니다
- 컨테이너 `data/` 는 옛 사본이라 수치 근거로 쓰지 않았다
