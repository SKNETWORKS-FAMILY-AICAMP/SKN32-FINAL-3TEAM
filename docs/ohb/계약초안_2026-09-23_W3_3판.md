# 계약 초안 3판 — W3: 종착 「지시」 · 품목 축 · 분기 칸 · 마이그레이션 0018

> 클론 A · **3판 2026-09-23** · 앞 문서 `계약초안_2026-09-22_분기_시뮬레이터.md`(2판) · **상태: 판정 반영 — D-273 ~ D-280 (2026-09-23) · 🔄 구현됨 — W3 `c626225` (A 게이트 781 · 2026-09-23 · B 게이트 781 · 2026-09-24 · 사용자 실행)**
> 잠금 1급(`app/contracts.py` · `app/models.py`)이라 코드보다 이 문서를 먼저 본다 · 화면은 픽스처를 보고 만든다(D-124)
> 입력 — 원장 D-229 · D-242 · D-263 · D-265 · D-266 · D-268 · D-269 · D-271 · D-272 본문 · 2판 §1~§6 · **판정 D-273 ~ D-280** ·
> 코드 실측(A 디스크를 09-23 에 받아 읽음 — 계약 · 모델 · 그래프 · 픽스처 14건 · 화면 `review.html` · 게이트 넷)
> 🚨 **실행한 것이 없다.** 아래 코드 이야기는 전부 읽고 쓴 것이다 (D-146)

---

## 0. 한 장 요약 — 바뀔 계약의 모양

```
JudgeResponse  (검수 · 진입점 A)
 ├─ outcome: Outcome                 pass · certificate · 🆕guidance · hold          ← search_failed 가 빠진다 (D-274)
 ├─ 🆕 category: Category | None      판별된 품목 (D-82 · D-277)
 ├─ 🆕 not_reviewed: list[str]        「○○법 미검수」 — category=전용법_미수록 이면 비지 않는다 (D-271 ⑤ · D-277)
 ├─ sentences[]: SentenceJudgment    = 기록되는 판정 (D-263 ①)
 │    ├─ 🆕 spans: list[Span]         뺄 구간 (D-265 표 · D-278)
 │    ├─ 🆕 not_claim: bool           판정 대상 아님 — 참이면 위반 없음 · R0 · 구간 없음 (D-275)
 │    └─ 🆕 substantiation: SubstBranch | None    실증 분기 — B 실증형에만 (D-263 ④)
 ├─ 🆕 branches[]: Branch             품목 분기 — 문서 단위 (D-263 ④ · D-267)
 │    premise(하나) · verified · outcome · sentences[] · criteria · evidence
 ├─ candidates[]                      ⛔ 검수에서는 늘 빈 목록 (D-265 — 칸과 검증기 둘은 그대로)
 └─ attempt                           ⛔ 검수에서는 늘 0 (D-265)

GenerateResponse (생성 · 진입점 B)
 └─ 🆕 outcome: GenerateOutcome        frontier · search_failed · hold   (D-274)

Category      식품 · 건기식 · 화장품 · 🔄일반상품 · 🆕전용법_미수록      (「일반」 없음 · D-271 ④)
HoldReason    low_conf · gap2 · cat_unknown · rd1 · 🆕premise_unknown · 🆕law_uncovered (D-277)
Premise 🆕    식품 · 건기식_인정 · 건기식_비인정 · 화장품 · 일반상품   (D-276 — 분기 하나 = 전제 하나)
Risk          R0 특이사항 없음 · R1 시정명령 위험 · R2 업무정지 위험 · R3 영업 상실 위험   (이름 D-280 · 값은 그대로)
통과          확정 ∧ R0 — PASS_RISK_MAX = R0 · 확정 문장은 위반 없으면 R0 · 있으면 R1 이상 + 불가 사유 (D-273)
DB            마이그레이션 0018 — 보류 사유 CHECK 넓힘 · 품목 출처 칸 · 확정 행 불변식 CHECK (종착은 DB 에 없다 · F4)
```

---

## 1. 탐색에서 나온 사실

| # | 사실 | 어디서 봤나 | 영향 |
|:-:|---|---|---|
| **F1** 🔴 | **확정 위반이 「통과」로 끝나는 길이 있다.** 통과 = 확정 ∧ 위험도 ≤ R1 (D-125 · D-130), R1 = 시정명령 수준 (D-227 표), 표시광고법은 구조적으로 R1 이 상한 (D-272 맥락 3). 그래서 **일반상품(골든셋 78%)의 확정 위반은 전부 R1 이하**다. 지금 라우터(`route_review`)는 불가 사유를 먼저 봐서 사유가 붙은 위반은 증명서·지시로 간다 — 새는 곳은 **「확정 위반인데 불가 사유가 빈 문장」** 하나다 | 원장 본문 · `app/graph.py` `route_review` · `app/contracts.py` `is_pass` | ✅ **D-273** — 통과 = 확정 ∧ R0 · 불변식 · 확정 위반에 불가 사유 |
| F2 | **판정 픽스처 셋이 D-265 뒤 규칙과 어긋난다.** `01_pass_frontier` 후보 3개 · `04_evidence_mismatch` 근거 불일치 + `attempt=1` · `07_search_failed_b` 탐색 실패 + `attempt=2` — 셋 다 **검수에서 안 나는 값**이다. 그리고 `06_certificate_c` · `compose/14` 가 **R4** 를 쓴다 — 도달 불가 값이다 (D-182) | 픽스처 14건 전수 | 픽스처 고침 (§2-2) · 화면 소유자 알림 |
| F3 | **탐색 실패가 계약 안에서 갈 곳이 없다.** `GenerateResponse` 에 종착 칸이 없다. 게이트 `test_픽스처가_분기를_덮는다` 는 **판정 픽스처만으로** `Outcome` 전부를 덮으라고 요구한다 — 07 을 고치는 순간 깨진다 | `app/contracts.py` · `tests/test_contracts.py` | ✅ **D-274** — 종착을 검수 · 생성으로 가른다 |
| F4 | **종착은 DB 에 없다.** `models.py` · `db/schema.sql` · 마이그레이션 전부에서 `outcome` 0건. D-268 이 「종착 값을 0018 에 묶는다」고 적었는데 **묶을 것이 없다** — 0018 은 보류 사유 CHECK 만 넓힌다. D-268 스스로 「`schema.sql` 은 보지 않았다」고 적어 둔 자리다 | grep | ✅ D-274 맥락 3 · D-268 정정 표시 |
| F5 | **`gap2` 의 뜻이 세 곳에서 틀렸다.** 원장 D-127 = **코드↔모델 2등급 차**. 계약 주석 · 화면 라벨 · `KNOWN_GAPS` 사유 = 「1·2위 격차 부족」. 화면의 `rd1` 라벨 「라운드 1 미해소」 도 D-127(공존 규칙 발동)과 다르다 — 계약 주석은 09-21 에 고쳤고 화면은 안 고쳤다 | 원장 D-127 행 · `review.html` · `test_fixture_coverage.py` | 사실 정정 — 판단 불필요 |
| F6 | **화면은 모르는 값을 틀린 이름으로 그린다.** 종착은 `else` → 「전문가 검토」, 보류 사유는 `else` → 「라운드 1 미해소」. 새 값 셋(`guidance` · `premise_unknown` · 새 사유)이 오면 **오류 없이 틀린 라벨**이 뜬다 | `review.html` 138~168행 | ✅ **D-279** — 라벨만 고친다 · 모르는 값은 「알 수 없는 값」 |
| F7 | **응답에 판별된 품목 칸이 없다.** D-82 는 품목이 「판정 결과에 속한다」, D-229 ⑤ 고지는 「제품 종류」, D-271 ⑤ 는 「○○법 미검수」 고지를 요구한다 — 실을 칸이 없다 | `JudgeResponse` | ✅ **D-277** — `category` · `not_reviewed` · 사유 `law_uncovered` |
| F8 | **「뺄 구간」 칸이 없다.** D-265 표가 `violations[].spans` 를 쓰는데 `violations` 는 유형 이름 목록이다. 구간은 `risk.evidence_span`(상향 근거) 하나뿐이다 | `SentenceJudgment` | ✅ **D-278** — `spans` |
| F9 | **2판의 `Condition` A/B/C 가 `Infeasibility` A/B/C 와 겹친다.** D-242 표 — 「없음·C」= 절대형, A = 지위(자격형), B = 실증. 새로 더하는 것은 M · D 뿐인데 **M 은 D-268 이 `low_conf` 로 보냈다** | 원장 D-242 · D-268 | ✅ **D-275** — 조건 칸은 싣지 않고 `not_claim` 하나 |
| F10 | **「일반」이 두 축에 남아 있다** — 계약 `Category`(품목 · W3)와 청크 범주(법 · W6): `settings.DEFAULT_CATEGORY="일반"` · `/search` · `scripts/search_probe.py`. 🚨 **W3 에서 청크 쪽을 건드리면 검색이 0건**이 된다 — 청크 데이터 값은 정본 B 재생성(0019) 전까지 「일반」이다 | grep | W3 는 계약 쪽만 · 청크 쪽은 W6 |
| F11 | `judgment.hold_reason` 은 `String(16)` — `premise_unknown` 15자라 들어간다. 새 사유 이름도 **16자 이하 ASCII** 여야 한다 | `app/models.py` | 새 사유 이름의 제약 |
| F12 | `Category` 는 **픽스처 어디에도 없다** — 이름을 바꿔도 픽스처는 안 움직인다. 쓰는 곳은 `graph.py` · `test_graph_routers.py` · `scripts/graph_probe.py` 셋 | grep | 이름 변경 범위 |
| F13 | D-229 ⑥ 「품목 미확정이면 분기는 언제나」를 **검증기로 걸면 지금 스텁 그래프 응답이 깨진다** — 스텁은 분기를 못 만든다. 분기는 W4(`merge_laws`)가 만든다 | `graph.py` `to_response` | 검증기는 보류 사유 쪽만 · 나머지는 W4 에 ⬜ 로 박는다 (D-192) |

---

## 2. 구현할 것 — 원장이 정한 대로

| 무엇 | 근거 |
|---|---|
| `Outcome.guidance = "guidance"` 추가 · 그래프 `guidance` 종착이 보류 대신 이것을 낸다 | D-268 |
| `Category.일반` → `일반상품` · `전용법_미수록` 추가 · 주석 「일반 = 판별 결과 일반식품」 삭제 | D-271 ④ · D-229 ② 개정 |
| `HoldReason.premise_unknown` 추가 (`cat_unknown` = 품목을 못 가림 / `premise_unknown` = 품목은 가렸으나 인정 여부로 등급이 갈림) | 2판 §6 #3 · D-263 ② |
| `gap2` 주석 · `KNOWN_GAPS` 사유를 「코드↔모델 2등급 차」로 | D-127 (F5) |
| 조건 M 은 새 사유 없이 `low_conf` | D-268 딸린 결정 |
| 검수 응답의 `candidates` · 검증기 둘(`_outcome_matches_payload` · `_no_candidates_for_qualification`)은 **그대로** | D-265 |
| 청크 쪽 「일반」(`DEFAULT_CATEGORY` · `/search` · `search_probe`)은 **안 건드린다** | D-271 ② · ③ 은 W6 · 0019 (F10) |
| 🆕 `PASS_RISK_MAX = R0` · `is_pass` 주석 · `Risk` 주석의 R1 이름 「시정명령 위험」 | D-273 · D-280 |
| 🆕 검수·생성 종착 enum 분리 — `Outcome`(pass · certificate · guidance · hold) · `GenerateOutcome`(frontier · search_failed · hold) · 생성 응답에 `outcome` | D-274 |
| 🆕 `Premise` · `Branch`(premise 하나 · verified · outcome · sentences · criteria · evidence) · `SubstBranch` | D-276 · D-263 |
| 🆕 `SentenceJudgment.not_claim` · `spans` · `substantiation` | D-275 · D-278 · D-263 ④ |
| 🆕 `JudgeResponse.category` · `not_reviewed` · `HoldReason.law_uncovered` | D-277 |

### 2-1. 검증기 — 더하기만 한다 (2판 §3 을 이것으로 바꾼다)

| # | 검증기 | 규칙 | 근거 |
|:-:|---|---|---|
| V0 | `_confirmed_risk_invariant` | 확정 ∧ 위반 없음 ⇒ 최종 R0 · 확정 ∧ 위반 있음 ⇒ 최종 R1 이상 · `not_claim` 이면 위반 없음 ∧ R0 ∧ 구간 없음 | D-273 결정 2 · D-275 |
| V1 | `_confirmed_violation_has_reason` | 문장이 확정 ∧ 위반 있음 ⇒ `infeasibility` 필수 | D-273 결정 3 · D-59 · D-242 |
| V2 | `_subst_is_for_B` | `substantiation` 있음 ⇒ `infeasibility=B` · 실증 상한은 실증 전 위험도보다 높지 않다(실증은 내리기만) | D-263 ④ · D-61 (2판 `_condition_iff_substantiation` 대체 · D-275) |
| V3 | `_guidance_payload` | `outcome=guidance` ⇒ 전부 확정 ∧ B 가 하나 이상 ∧ A·C 없음 ∧ 확정 B 문장마다 `substantiation` 과 `spans` | D-268 · D-265 표 |
| V4 | `_recorded_is_conservative` | 검증된 분기는 많아야 하나. 없으면 문장마다 **기록된 위험도 ≥ 모든 분기의 위험도**. 기록이 비었는데 분기가 값이 있으면 거부(없음을 낮음으로 세지 않는다) | D-263 ① · D-276 결정 2 · D-72 (2판 `_premise_not_verdict`) |
| V5 | `_branch_hold_has_branches` | 보류 사유 `cat_unknown` · `premise_unknown` 이 하나라도 있으면 `branches` 가 비지 않는다 · 분기 전제는 서로 다르다 · 분기 문장 id 는 응답 문장 id 안에 있다 | D-263 ② · D-229 ⑥ (2판 `_hold_reason_premise`) |
| V6 | `_uncovered_law_notice` | `category=전용법_미수록` ⇔ `not_reviewed` 비지 않음 · 그때 `outcome≠pass` · `law_uncovered` 사유는 그 품목에서만 | D-271 ⑤ · D-277 |

🔜 **W4 에 ⬜ 로 박는 것** (D-192) — 「품목 미확정이면 분기가 언제나 있다」(D-229 ⑥)는 V5 보다 넓다. 스텁 그래프가 분기를 못 만들어서(F13) 지금 걸면 스텁이 깨진다 — `merge_laws` 가 분기를 만드는 커밋에서 검증기로 올린다.

### 2-2. 픽스처 — 고치는 것 다섯 · 새로 넷

| 픽스처 | 지금 | 바뀐 뒤 |
|---|---|---|
| `judge/01_pass_frontier` → **`01_pass`** | 후보 3개 · 확정 · 위반 없음 · **R1** | 후보 없음 · **R0**(D-273 불변식) · 파일 이름 변경(미리보기 URL 이 바뀐다) |
| `generate/11_generate_adapted` | 재검증 문장 둘이 확정 · 위반 없음 · **R1** | **R0** (D-273 불변식) |
| `judge/03_hold_cat_unknown` | 분기 없음 | 분기(식품 · 건기식_비인정 · 화장품 · 일반상품) — V5 |
| `judge/04_evidence_mismatch` → **`04_hold_rd1`** | 근거 불일치 · `attempt=1` | 공존 규칙 보류 · 불일치 없음 · `attempt=0` (근거 불일치는 재생성 이벤트 · D-127) |
| `judge/07_search_failed_b` → **`07_guidance_subst`** | 탐색 실패 · `attempt=2` | 「소비자 선호도 11년 연속 1위」 — 식품 · 거짓·과장 확정 B · 실증 분기 · 뺄 구간 (D-268 예) |
| `judge/06_certificate_c` · `compose/14` | R4 | R3 (D-182 — R4 도달 불가) |
| 🆕 `judge/15_hold_premise_unknown` | — | 「면역력 증진에 탁월한」 — 건기식 · 인정 여부 미상 · 분기 건기식_인정/비인정 · 기준 문안 |
| 🆕 `judge/16_pass_not_claim` | — | 「결혼을 앞둔 여성이나…」 — `not_claim=true` · 위반 없음 · **R0** (D-275) → `KNOWN_GAPS` 의 `Risk.R0` 이 지워진다 |
| 🆕 `judge/17_hold_law_uncovered` | — | 의료기기 문구 · 표시광고법만 판정 · 고지 · `law_uncovered` |
| 🆕 `generate/15_generate_search_failed` | — | `GenerateOutcome.search_failed` 를 덮는다 (D-274). 생성 픽스처 10 · 11 에는 `outcome: frontier` |

🚨 기준 문안(`criteria`)과 실증 자료 종류는 **조문을 인용한 초안**이다 — 문안 확정은 조문 대조 뒤다 (D-263 트레이드오프). 픽스처에 「초안」 표시를 단다.

### 2-3. 마이그레이션 0018

- 파일 `alembic/versions/20260923_0018_hold_reason_values.py` · revision `0018_hold_reason_values` · down `0017_ops_tables`
- 0014 의 모양 그대로 — DDL 직접(런타임층 · `models.py` 가 원본) · `DROP CONSTRAINT IF EXISTS` → `ADD CONSTRAINT` · `downgrade` 는 `NotImplementedError`
- ① 보류 사유 — `low_conf · gap2 · cat_unknown · rd1 · premise_unknown · law_uncovered` (D-277)
- ② 🆕 **품목 출처 칸** — `judgment.product_category_source` · 값 `classified`(판별) · `user_selected`(재검수 선택) · 비면 품목도 비어야 한다 (D-276 결정 6 · 칸 이름은 구현 때 확정)
- ③ 🆕 **확정 행 불변식 CHECK** — `verdict <> 'confirmed' OR risk_final IS NULL OR ((violation_type IS NULL) = (risk_final = 0))` · 계약 V0 와 같은 규칙 (D-273 결정 2 · D-99). 적용 전 GUARD 로 어기는 행을 센다
- GUARD 하나 — **`product_category='일반'` 행이 있으면 멈춘다.** 칸에 CHECK 는 없지만 「일반」은 옛 뜻이 둘이라(D-271 맥락 1) 조용히 새 이름으로 읽으면 안 된다. 넓히기라 보류 사유 쪽 GUARD 는 필요 없다(기존 행이 새 집합을 어길 수 없다)
- `app/models.py` 의 CHECK · 칸 · 주석(190행 사유 목록) 같은 커밋 · ⛔ `judgment` 표는 아직 비어 있다고 코드 주석이 적었지만 **재지 않았다** — GUARD 가 잰다

### 2-4. 게이트 — 고치는 것 · 새로 거는 것

- `test_계약의_판정축이_런타임층_제약과_같다` — 그대로 둔다. 계약 · 모델이 함께 움직이는지를 본다
- `test_픽스처가_분기를_덮는다` · `test_픽스처가_판정_축의_모든_값을_덮는다` — `Outcome` 과 `GenerateOutcome` 을 각각(D-274) · 새 축 `Premise` 추가
- `test_위반을_확정하면_근거_조문이_붙는다` — V1 과 **같이 터지지 않게** 문장에 불가 사유를 넣어 둔다 (한 게이트가 두 이유로 통과하면 한쪽이 죽어도 모른다)
- `test_A_자격형에는_대체_문구를_내지_않는다` — 01 픽스처에서 후보가 빠지므로 후보를 테스트 안에서 만든다
- `test_재생성_라운드는_셋을_넘지_않는다` — 그대로 (검수 응답의 `attempt` 칸은 남는다)
- 🆕 V0~V6 마다 **거부되는 음성 예** 하나씩 (D-170)
- 🆕 `test_통과_종착은_통과가_아닌_문장을_받지_않는다` 에 「확정 · 위반 · R1」 음성 예 추가 — D-273 전에는 통과로 지났다
- 🆕 검수 픽스처 전부 `candidates=[]` · `attempt=0` · `evidence_mismatch=false` (2판 §5 · D-265)
- `tests/test_graph_routers.py` — `Category.일반` → `일반상품` · `전용법_미수록 → ("law_ftc",)` 한 줄 · `guidance` 종착이 `Outcome.guidance` · 생성 종착이 `GenerateOutcome`
- `tests/contract_surface.json` — 스냅샷 갱신(게이트가 변경을 잡는 것이 정상)

---

## 3. 바뀌는 파일과 커밋 나눔

| 커밋 | 파일 |
|:-:|---|
| ① 계약 | `app/contracts.py` · `tests/contract_surface.json` · `tests/test_contracts.py` · `tests/test_fixture_coverage.py` · 픽스처(§2-2) |
| ② DB | `app/models.py` · 마이그레이션 0018 |
| ③ 그래프 | `app/graph.py`(`guidance` · 생성 종착 · 주석) · `tests/test_graph_routers.py` · `scripts/graph_probe.py` |
| ④ 화면 라벨 | `app/templates/user/review.html` — 라벨만 (D-279) · 소유자 알림과 함께 |
| ⑤ 문서 | ✅ 원장 D-273 ~ D-280 · 사실원장 「판정 축」 (bd7e736) · 남은 것: 병렬작업 계약 상태표 · 산출물현황 · `LangGraph_상태_스키마.md` |

①②는 한 번에 들어가야 게이트가 초록이다(계약 ↔ 모델 일치 게이트). 순서는 ① → ② → ③ → ④ 로 쓰고 커밋은 ①+② 를 묶는다.

## 4. 알릴 사람 · B 인계

- **ksr · lse** — 검수 화면: 종착 「지시」 · 보류 사유 둘 · 라벨 정정(gap2 · rd1) · 픽스처 이름 셋 변경(01 · 04 · 07) · 새 픽스처 셋 · 후보 카드는 이제 안 그려진다(삭제는 소유자) · 생성 화면: 응답에 `outcome`
  · 🆕 **화면 수정 목록 여덟**(도움말 배지 · 위험도 이름 · 이력 · 홈 통과율 · 랜딩 예시 · 생성 문구 · 「합법」 · 마이페이지) — 원장 **D-280 「화면에 미치는 것」** 표가 정본
- **클론 B** — `pull` → `migrate`(0018). 0018 은 데이터를 안 바꾼다 · GUARD 가 멈추면 `judgment.product_category='일반'` 행이 있다는 뜻이다

## 5. 안 본 것 · 깔고 간 가정 (D-188)

- **안 본 것** — `docs/02_설계/API_초안` §1 · §2(재검수 사유) · 생성 화면 `generate.html` 의 종착 표시 · 마이그레이션 0015 · 0016 본문(이름만 봤다) · `scripts/schema_drift_check.py` 가 0018 을 어떻게 읽는지
- **가정** — 검수 응답의 `attempt` · `candidates` 칸은 D-265 문언대로 **남긴다**(없애면 검증기 둘이 할 일이 사라진다 — D-265 가 「그대로 둔다」고 적었다)
- **가정** — 조건 D(주장 아님) 문장은 인코더가 붙은 뒤의 모양이다. 인코더 전에는 D-269 대로 `low_conf` 보류다 — 16 픽스처는 **목표 모양**이다
- **범위 밖으로 둔 것** — 생성 요청의 세그먼트 필수 · 전제 · 원료 칸(D-270 ⬜) → W9 · 제재 상세 응답(2판 §6 #5) · `judgment` 에 **선택한 분기 전제** 저장 칸(2판 §6 #6 · D-263 ⑦ — 판정 품목과 그 출처는 D-276 으로 0018 에 들어간다) · 제재 수위·형벌 설명 줄의 계약 칸(D-280 ⬜)
