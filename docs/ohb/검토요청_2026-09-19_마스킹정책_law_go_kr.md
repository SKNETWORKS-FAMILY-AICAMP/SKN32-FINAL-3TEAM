# 검토요청 2026-09-19 — `law_go_kr` 에 사람 마스킹을 켤 것인가 (판례·재결례)

앞 문서 → `docs/ohb/검토_2026-09-19_기기간_동등성_자동화.md` §16~§17

🚨 **2인 확인 대상이다** — 레지스트리 `masking:` 문언과 `preprocess/mask.py` 의 `POLICY` 를 바꾸는 일이라
　 게이트 15 · `tests/test_mask.py` 의 문언 대조가 걸린다. **초안까지만 만들고 멈췄다.** 코드는 바꾸지 않았다.

## 무엇이 드러났나 [클론 B 생성물 · 2026-09-19 반출 검사]

- `data/derived/law_decc.jsonl`(재결례) 한 곳에 **개인 대표자 실명이 가려지지 않은 채** 있다 —
  「법인의 **대표자 손○○**에게 … 확인서를 징구」. 같은 문서의 다른 사람은 원천이 `○○○` 로 가렸다.
  **원천(법제처)이 빠뜨린 자리**다.
- `law_go_kr` 에는 `masking:` 이 **없다**(`None`). 그래서 `preprocess/law_case.py` 는 마스킹을 부르지 않고,
  `apply_policy` 는 이 원천을 모른다.
- 법인 검사에서는 `LEAK_ALLOW` 가 이 파일을 「공표된 판단문」으로 허용한다. 그 사유는 **법인명**에 대한 것이고
  개인 실명에는 맞지 않는다 — 법원·행정심판 스스로 개인을 비실명 처리하기 때문이다.

## 대안

| | 얻는 것 | 떠안는 것 |
|---|---|---|
| **(가) ★ `person` 축만 켠다** — 판례·재결례의 텍스트 칸에 `mask_person` | 원천이 빠뜨린 실명을 우리가 잡는다(D-17 「원천의 정책이지 우리의 보장이 아니다」와 같은 논리) · 업체명·주소는 그대로(공표된 판단문) | 원천이 가린 `○○` 꼴이 `[대표]` 로 **표기 통일**된다 — 문장 뜻은 안 바뀐다. 직함 규칙은 09-19 에 좁혀 오탐 실측 0 |
| (나) 지금처럼 끈다 · 반출 검사가 막게 둔다 | 코드 변경 없음 | `law_decc` 를 영영 못 올린다 — 사본(클론 A·팀원)이 재결례를 못 받는다 |
| (다) `org`·`addr` 까지 켠다 | 가장 보수적 | 판단문의 **처분청·당사자 법인**이 지워져 5층 반례(「누가 무엇을 뒤집었나」)의 값이 준다 |

**추천 (가).**

## (가)를 고르면 바뀌는 것 (초안 — 적용 안 했다)

1. 레지스트리 문언 (`law_go_kr.masking` — 생성 원천에서 고치고 `rebuild`) —

   > 대표자명 등 **개인 실명** 즉시 마스킹. 원천이 판례·재결례의 당사자를 ○○ 로 가려 주지만
   > 빠뜨린 자리가 있다(2026-09-19 실측 1건 · 재결례). 업체명·주소는 공표된 판단문의 일부라 가리지 않는다.

2. `preprocess/mask.py` `POLICY["law_go_kr"] = frozenset({"person"})`
3. `preprocess/law_case.py` — 텍스트 칸(`주문`·`청구취지`·`이유`·`재결요지`·`판시사항`·`판결요지`)에
   `apply_policy(text, "", "law_go_kr")` · 조문(`law_article`)은 **부르지 않는다**(법령에 사람 이름이 없다)
4. 클론 B 에서 `law_case --dump` → `decc_extract --dump` → `derived-manifest --write` → `--export-check`

## 확인할 사람

2인 확인 — 결정 오한빈 · 검토 1인 (`scripts/registry_review.yaml`)

## 🔄 결과 (2026-09-19) — (가) 확정 · 적용함

- 결정 오한빈 · 확인 권소라 (팀장 전달 · 서명 `scripts/registry_review.yaml` `law_go_kr` reviewed_at 2026-09-19)
- 적용 — ① 레지스트리 문언(`scripts/gen_registry.py` EXTRA → `rebuild`) ② `POLICY["law_go_kr"] = {person}`
  ③ `preprocess/law_case.py` 텍스트 칸 `TEXT_FIELDS` 에만 `apply_policy` ④ 가짜 값 테스트 2건(`tests/test_mask.py`)
- 기록 — **D-248**
- ⬜ 남은 것 — 클론 B 에서 `law_case --dump` → `decc_extract --dump` → 반출 검사 0 확인
