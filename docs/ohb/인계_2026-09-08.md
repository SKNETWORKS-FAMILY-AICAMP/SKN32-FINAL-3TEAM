# 인계 — 2026-09-08 · 계측을 세운 하루 (D-141 ~ D-157)

> 클론 A(회사 기기) · 브랜치 `ohb` · 마지막 커밋 `50e1cea`
> 앞 문서: `docs/ohb/인계_2026-09-07.md`

---

## 0. 한 줄

**「세는 쪽」과 「하는 쪽」이 어긋난 자리를 여덟 개 찾아 닫았고, 그 과정에서 1층 라벨이 실제로 늘었다.**

오늘 잡은 결함이 전부 같은 모양이었다 — 검사는 있는데 부르는 길이 없거나, 세는 쪽만 고쳐 두었거나,
지표는 0인데 대상은 남아 있거나. **계측을 세우면 그 다음 것이 나오는 패턴이 하루 종일 이어졌다.**

---

## 1. 오늘의 수치

| | 아침 | 저녁 |
|---|---:|---:|
| `ftc` 1층 문구 | 619 | **627** |
| `ftc` 1층 문서 | 280 | **283** |
| 원장 ↔ 디스크 어긋남 | (안 재고 있었다) → 73 | **20** |
| 등재 원천 중 수집됨 | 10 | **12** |
| 해설서 레코드 | 0 | **2,728** |
| 사례집 (호,문구) 쌍 | 0 | **245** |
| 2층 승인 표현 | 0 | **419레코드 · 167종** |
| 마스킹 정책 선언 원천 | 2 | 2 (**3종 판정 완료 · 서명 대기**) |

**해설서(`mfds_special_use_guide`) 블록 3단** — 미상 0

    삭제      1,834   금지
    근거자료     619   조건부 — 실증자료가 있으면 쓸 수 있다
    수정        245   금지 → 허용 **변환쌍**
    키워드        30   유형별 대표어

★ **이 3단이 CopyLane 의 검수 출력과 그대로 맞는다** — 「금지 / 실증 요구 / 이렇게 고치세요」.

**위반문구 원천라벨별** — `거짓ㆍ과장ㆍ기만` 1,145 · `질병·의약품·건기식 혼동` 558 ·
**`부당한 비교ㆍ비방` 131**.
★ `ftc` 에서 비방 6 · 부당비교 3 으로 D-40 「측정 불가」였던 자리를 **131 이 뚫는다.**
`ftc` 는 이미 전량이라 더 받아도 안 늘었다 — **다른 원천이어야 했고 이것이 그 원천이다.**

---

## 2. 🚨 오늘 나온 「조용한 실패」 **열다섯**

전부 **세는 쪽과 하는 쪽이 어긋난** 자리다. 순서대로 —

| | 무엇 | 어떻게 드러났나 |
|:-:|---|---|
| ① | 한 어절만 잡는 외국 법인격 규칙이 **잔여 11→0 을 만들고도** 「오션스카이 인터넷 인포메이션 [업체]」를 남겼다 | 바뀐 자리를 **눈으로** 보고서야 (D-141) |
| ② | 영문 낱말 경계만 걸었더니 「Summer **Limited** Edition」이 「[업체] Edition」이 됐다 — **마스킹이 학습 입력을 먹었다** | 같은 눈 검사 (D-141) |
| ③ | `NOISE` 가 **마스킹 자국이 든 인용을 통째로 버려** 643→619 문구 · 285→280 문서를 삼키고 있었다 | 과잉삭제를 재 보고 (D-142) |
| ④ | `공정거래위원회`·`위원회` 를 통째로 버려 「… 및 **공정거래위원회 인정**」(기관 사칭형 거짓·과장)이 사라졌다 | **상시 계측기를 세운 첫 실행에서** (D-142 (다)) |
| ⑤ | `_slot_sub` 가 「석정건설**(주)에게**」에서 조사를 회사명으로 먹었다(63건). `residual_orgs` 는 **이미 막고 있었다** | 치환 원장을 켠 첫 실행에서 (D-144) |
| ⑥ | `doctor --data` 는 09-06 부터 돌았는데 `launcher.py` 가 `@stub` 이라 **부르는 길이 없었다.** 73건이 안 보였다 | 수집 상태를 재려다 (D-146) |
| ⑦ | 오류 응답(`ERROR-503`) 146바이트를 **raw 에 저장하고 원장에 「저장」으로 적었다.** 화면엔 「전체 건수: 0」이 찍혀 있었다 | 재수집하다 (D-147) |
| ⑧ | `비방광고` 가 목록에 없는데 `ftc_extract` 는 **이미 뽑고 있었다**(6건) | 라벨 매핑을 짜다 (D-151) |

| ⑨ | `sep_norm` 결과로 문구를 뽑아 **저장된 627문구 중 70건(11.2%)이 망가져 있었다.** 「97.5%」→「97·5%」 · 「1/3」→「1·3」 | 원문과 맞대 보고 (D-152) |
| ⑩ | doctor 가 `law_go_kr` 7건을 「재수집하면 닫힌다」고 안내했다. **그대로 하면 판정 범위 밖 자료가 들어온다** | 질의별 실측과 맞대고 (D-153) |
| ⑪ | doctor 가 갈린 판 56개를 다 뿌려 **화면 60줄이 덮이고** 중요한 셋이 묻혔다. 51,636 B → **146 B** 급감도 안 잡았다 | 출력을 읽으려다 |
| ⑫ | `U1: deny` 를 **「못 쓴다」로 읽을 뻔했다.** 실은 「평가 홀드아웃 전용」이다 — **용도 축에 「평가」가 없다** | 근거를 읽다 (D-155) |
| ⑬ | 사례집 등재 기록 둘이 틀렸다 — **텍스트 레이어가 있어 「수기 전사 반나절」이 불필요**, 「1호·5호」가 아니라 **1·3·4호** | PDF 를 열어 보고 (D-155) |
| ⑭ | 🔴 **금지 표현 사전을 그대로 쓰면 승인된 건기식 표시를 위반으로 잡는다** — 「기억력 개선」이 위반이자 적법이다 | 1층↔2층을 맞대고 (D-156) |
| ⑮ | `POLICY` 에 `person` 축이 있는데 **코드가 안 따랐다** — `org` 를 켜면 딸려 왔다 | 축을 걸어 보다 (D-157) |

**곁가지 여섯** — `sys.path` 가드가 「들어 있나」를 봤다(막으려던 건 **가려짐**인데 검사한 건 **존재**).
`--pages 1` 탐침인데 `mark_collected` 가 찍혔다(`mfds_hf_board.py` 는 같은 규약을 이미 지키고 있었다).
급감 검사 첫 판이 **원장만 보고 🔴 를 냈다**(파일은 이미 지웠는데 원장은 append 라 **영영 운다**).
루프 변수 `a` 가 argparse 네임스페이스를 덮었다 — **출력은 다 찍힌 뒤에 죽어 화면만 보면 정상이었다.**
D-157 을 고치며 **사람 마스킹 순서를 바꿨다**(치환 416→398 · D-143 이 「본문바뀜 8」로 잡음).
🔴 그리고 **`_REDACTED_NAME` 줄을 통째로 날렸다** — 가려진 이름 18건이 안 지워진 채 돌았다.
   결과 문자열 854/854 동일 · 산출물 627 동일 · **치환 원장 개수 하나(416→398)만 달랐다.**

---

## 3. 결정 D-141 ~ D-157

| | 요지 |
|---|---|
| **D-141** | 외국 법인격은 **사전이 아니라 규칙** — 이름은 열린 집합이나 **법인격 표기는 닫힌 집합**. 계측은 **배포면**에서 (전 필드 293 중 247이 「이유」였다) |
| **D-142** | 마스킹은 **가리는 것**이지 지우는 것이 아니다. `[업체]` 는 손실이 아니라 **정보**. + 과잉삭제 상시 계측 |
| **D-143** | 전처리 단계 **물질화** — 차이를 **원천 탓 / 규칙 탓**으로 가른다. 판(규칙 해시·원천 해시)을 줄마다 박는다 |
| **D-144** | **치환 원장** — 「가역」이 아니라 「기록」. 무엇이 지워졌나 · `[업체]`는 어떤 이름이었나 |
| **D-145** | 번호 자리표시자 `[업체1]` **보류** — 효용 21문서인데 **동일성 판정이 없다**(「국제약품」과 「국제약품 주식회사」가 갈린다) |
| **D-146** | **만들어 두고 부르지 않는 검사는 없는 검사다** — `doctor` 를 실행 경로에 |
| **D-147** | **오류 응답은 데이터가 아니다** · **탐침은 수집이 아니다** |
| **D-148** | 「PDF 만」→「PDF 우선, 없으면 hwp」 — 규칙이 막으려던 것은 **중복**이지 hwp 가 아니다 |
| **D-149** | **행 수가 원천 신고와 맞아도 전량이 아니다** — `mfds_sanctions` 5,381 = 5,381 인데 고유는 **5,122** |
| **D-150** | HWP 표를 **셀 구조 그대로** · **원천의 선언(행 수·행별 셀 수)과 대조**해 파싱을 검증 (86/86 일치) |
| **D-151** | 뭉친 라벨은 **후보 유형**으로 — 모른다는 것을 데이터로 쓴다. 확정은 사람이 붙인 검증셋에서만 |
| **D-152** | **분류는 정규화문으로, 저장은 원문으로** — `sep_norm` 결과를 산출물에 넣지 않는다 |
| **D-153** | 「원장에 있는데 없다」의 **세 번째 설명** — 질의 기반 원천에서 판정 필터가 뺀 것. **닫을 필요가 없다** |
| **D-154** | **정규화 계측** — 사슬의 마지막 빈칸. D-117 의 135건을 상시 수치로 |
| **D-155** | 🔴 사례집은 평가 홀드아웃이 아니라 **「금지 표현 사전」**이다 — **문장 단위 평가는 빈칸** |
| **D-156** | 🔴 **같은 표현이 위반이자 적법**이다 — 판정은 「표현 + **제품 지위**」의 함수 |
| **D-157** | `POLICY` 의 `person` 축을 **코드가 따르게** 한다 — 같은 규칙이 원천에 따라 옳고 그르다 |

---

## 4. 🔴 열려 있는 것 — 여기부터 보면 된다

### ① 마스킹 정책 **3종** — 🔄 **판정은 끝났다. 검토 서명만 남았다**

`docs/ohb/검토요청_2026-09-08_마스킹정책_3종.md` — 각 축을 **실제로 걸어 보고** 정했다.

| 원천 | `org` | `person` | `brand` | `addr` | 근거 |
|---|:-:|:-:|:-:|:-:|---|
| `mfds_special_use_guide` | ✅ | 🔴 **끔** | ✅ | ✅ | `person` 오탐 11 · 진짜 0 |
| `mfds_casebook` | ✅ | ✅ | ✅ | ✅ | `person` 1건이 **진짜** |
| `mfds_hf_ingredient_board` | ✅ | ✅ | ✅ | ✅ | 업체명 450 지워짐 · **인정번호 484·기능성내용 432 훼손 0** |

🚨 **자체 검토가 제안 두 개를 뒤집었다.** 「해설서 `brand` 끄기」는 `mask_brand` 가
「영업표지 'X'」 꼴만 잡아 **애초에 0건**이라 무의미했다. 진짜 쟁점은 `person` 이었고
**걸어 보기 전엔 안 보였다.**

⬜ **따로 청한 것 하나** — 해설서 전화번호 1건(`033-332-4000`).
`POLICY` 축에 전화번호가 없어 **지울 수단이 없다.**

**서명되면** 해설서 2,728 · 사례집 245 · 건기식 419 가 한꺼번에 파생으로 나간다.

### ② 검증셋 150건 — `data/derived/mfds_guide_labelsheet.jsonl`

유형별 50건 · `seed=20260908`(재현 조건 · D-54). `확정유형`·`붙인이`·`붙인날` 이 비어 있다.
🚨 **AI 가 채우지 않는다** — 채우면 홀드아웃이 자기 채점이 된다.
채워지면 6종이 실제로 서고, 그때 비로소 키워드 규칙(ⓑ)의 정확도를 잴 수 있다.

### ③ 「빠진 행이 있는가」 — `mfds_sanctions` (D-149)

행 5,381 = 원천 신고 5,381 인데 **고유 5,122**. 중복 259행만큼 **못 받은 행이 있을 수 있다.**
같은 호출을 반복해도 같은 결과일 수 있으므로, **다른 축(기간·기관)으로 나눠 받아 합집합**을 비교해야 확인된다.
⬜ 아직 모른다. **1층·2층에서 이 원천 건수를 쓸 때는 5,122 를 쓴다.**

---

### ④ 🔴 **문장 단위 평가 홀드아웃이 없다 — 지금 최상위 리스크**

학습 입력은 **2,461건**(`ftc` 627 + 해설서 1,834) 확보됐는데 **그것을 채점할 데이터가 없다.**

| 후보 | 실측 | |
|---|---|---|
| `mfds_sanctions` | 5,122 중 **표시·광고 63** | 「전량과 합치면 선다(R8)」가 깨졌다 |
| `mfds_press` | 107파일 · 인용 516 | 대부분 제목·정책명. 본문은 **이미지 749개** |
| `mfds_casebook` | (호,문구) 245쌍 | 🔴 **평균 5.3자** — 사전이지 문장이 아니다 |

★ **음성 표본은 생겼다** — 2층 승인 표현 419건(평균 28.1자, 문장). 그 전까지는
데이터가 전부 위반이라 **「전부 위반」이라 답해도 100%** 였다.
⬜ 남은 것은 **문장 단위 위반 표본을 다른 출처에서** 얻는 일이다
(`ftc` 에서 떼면 같은 기관이라 출처 분리가 깨진다).

---

## 5. 🚨 원천 운영 사실 — 놓치기 쉬운 것

**식품안전나라 `I-0040`(원료별) · `I-0050`(개별인정) 은 KST 19:00 ~ 09:00 에만 응답한다.**
제한 시간대에는 HTTP 200 에 `ERROR-503` 본문이 온다 — **오류가 오류처럼 오지 않는다.**
`mfds_sanctions`(행정처분)는 같은 시각에 정상이다 — **서비스별 제한**이다.

★ 이 사실은 **원장이 이미 증언하고 있었다** — 성공한 수집이 전부 19:07 · 19:29 · 20:02 · 02:17 KST.
「받은 시각」은 재현 조건의 일부다 (D-54). 읽는 사람이 없었을 뿐이다.

---

## 6. 남은 수집 (원장 ↔ 디스크 20건)

| 원천 | 없음 | 어떻게 닫나 |
|---|---:|---|
| `mfds_hf_ingredient` | 8 | **19시 이후** `uv run python -m collect.openapi mfds_hf_ingredient --use U1` |
| `mfds_hf_individual` | 5 | **19시 이후** 같은 방식 |
| `law_go_kr` | 7 | ✅ **닫을 필요가 없다** — `_in_domain` 필터가 뺀 것이다 (D-153) |

🚨 `mfds_hf_ingredient` 의 8 중 하나는 **우리가 지운 오류 파일의 원장 줄**이다 — 정직한 상태다.
★ **실질적으로 열려 있는 것은 13건**이다 (`law_go_kr` 7 은 ㄷ 로 판명).

**미수집 중 「결정 끝 + 수집기 있음」은 21종.** 그중 AI Hub 5종은 `approved_at` 이 비어 승인 신청이 먼저다.
다음 우선순위는 `foodsafety_admin_measure`(1층 · 13건) · `ftc_decisions`(🚨 `ftc_decisions_body` 와 중복 확인 먼저) ·
`kcia_guideline`(협회 회신 대기).

---

## 7. 새로 생긴 명령

    python launcher.py doctor                      # 원장 ↔ 디스크 대조 (--hash 로 전량 해시)
    uv run python -m preprocess.ftc_extract --stage # 단계 물질화 + 지난 판과 비교
    uv run python -m preprocess.hwp <파일> --probe   # HWP 표 구조 확인
    uv run python -m preprocess.hwp <파일> --verify  # 🔴 저작 도구 미리보기와 글자 대조
    uv run python -m preprocess.mfds_guide          # 해설서 레코드 세기
    uv run python -m preprocess.mfds_guide --sheet 50  # 검증셋 생성
    uv run python -m preprocess.mfds_casebook --verify  # 🔴 목차 쪽번호와 대조 (먼저 돌린다)
    uv run python -m preprocess.mfds_casebook --dump    # 사례집 → 1층 판정라벨 190건
    uv run python -m preprocess.mfds_casebook --sheet 30 # 5호 확정을 사람이 붙일 검증셋
    uv run python -m preprocess.mfds_hf --verify        # 분류·필드·모르는 키 계수
    uv run python -m preprocess.mfds_hf --dump          # 건기식 → 2층 적법라벨 465건

`ftc_extract` 는 이제 늘 셋을 찍는다 — ① 마스킹 전/후 ② 차이의 성격 ③ **버린 근거별** 감시표.

---

## 8. ✅ 사슬의 계측이 다 찼다

    수집     manifest.jsonl        ✅ 원장
    정규화   sep_norm              ✅ **D-154 (오늘 채움)**
    분류     classify              ✅ buck
    마스킹   apply_policy          ✅ D-142
    추출     phrases_in            ✅ D-142
    판 비교  ftc_stage.jsonl       ✅ D-143 · D-144

**정규화 계측 실측** — 구분자를 펴지 않으면 분류가 달라지는 문서 **181 / 8,253**.
1층 후보 기준 **들어온 것 135 · 나간 것 0**. 135 는 D-117 이 09-07 에 잰 수와 같다 — **재현됐다.**
★ **나간 것이 0 인 것이 성질이다** — 정규화는 후보를 넣기만 하고 빼지 않는다.
  이 성질이 깨지면(나간 것이 생기면) 정규화 규칙이 과해진 것이다.

**hwp 글자도 검증됐다** (`--verify`) — 저작 도구가 만든 미리보기(`PrvText`)와 **17/17 일치**.
표 구조는 원천 선언과 86/86, 글자는 미리보기와 17/17. 흔적 검사(제어문자·치환문자·고립 자모)도 전부 0.

---

## 9. 하드 제약 (변함없음)

④ AI Hub 원본을 클라우드 컨테이너에 올리지 않는다 · ⑤ AI 가 `reviewed_by`·`확정유형` 을 스스로 채우지 않는다
⑦ API 키 값을 대화·셸 인자에 넣지 않는다 (`launcher.py setkey <이름>`) · 🚨 AI 는 `git` 을 직접 돌리지 않는다

---

## 10. 🔄 이어서 한 것 — 사례집·건기식 추출기 (D-158 · D-159)

    preprocess/mfds_casebook.py   PDF  → 1층 판정라벨 **190건** (조문이 확정 · 5호만 후보)
    preprocess/mfds_hf.py         HTML → 2층 적법라벨 **465건** (인정번호·기능성내용 훼손 0 상시)
    tests/test_mfds_casebook.py   10건
    tests/test_mfds_hf.py         10건

**전체 테스트 348건 통과** (지금까지는 `-m` 으로 추린 38건만 돌렸다 — 아래 ⑤ 참조).

### 이번에도 「조용한 실패」가 넷 나왔다 — 전부 **세는 쪽**이 잡았다

| # | 무엇 | 어떻게 잡았나 |
|:-:|---|---|
| ① | 🔴 사례집 목차 제목으로 본문을 찾으니 **호 다섯이 전부 「1쪽」**에서 잡혔다 (Ⅰ부가 법 §8① 을 통째로 전재해 **글자까지 같다**) | **목차 대조기**. 없었으면 Ⅱ부 대신 법령을 파싱하고 조용히 끝났다 |
| ② | 원천이 사례 블록을 **다섯 이름**으로 부른다 (`[부당광고 사례]`·`[부당광고사례]`·`[광고사례]`·`[위반사례]`) — 하나로 박으면 3·4·5호 사례가 「점검현황」에 붙는데 **예외가 안 난다** | **다섯 쌍 맞는지 세기** |
| ③ | 건기식 정규식이 키를 **「기」 한 글자**로 끊어 465건이 전부 「모르는 키」로 빠졌다 | **버린 것을 세는 계수** |
| ④ | **66건이 한 줄에 키를 둘** 붙여 놨다 — 안 펴면 **일일섭취량이 기능성내용 안에 섞이는데 필드가 비지 않아 계수로도 안 잡힌다** | 인정번호 **399/465** 라는 결손 계수 |
| ⑤ | 🔴 어제 `POLICY` 를 2 → 5종으로 늘리면서 `tests/test_mask.py` 의 `_REGISTRY_SOURCE` 를 **안 늘렸다.** 그날은 `-m` 38건만 돌려 **새 셋이 한 번도 안 돌았다** | 오늘 전체를 돌렸더니 `KeyError` |

★ ⑤를 채우자 해설서에서 바로 하나 더 걸렸다 — 레지스트리 문언에 「대표자명」이라는 **낱말**은
있는데 뜻은 **「대표자명(person)은 끈다」**였다. **끈 것도 문언으로 증명하게** 고쳤다.
켤 때만 근거를 요구하고 끌 때는 안 하면, 실수로 꺼진 축이 조용히 통과한다.

### 새로 연 것 — 여기부터 보면 된다

**⑤ `preprocess/mfds_guide.py --sheet` 가 마스킹을 안 지난다** 🔴

    검증셋도 `data/derived/` 에 떨어지는 순간 D-17 대상이다.
    지금 파일(`mfds_guide_labelsheet.jsonl`)은 실측상 잔여 0 이지만 **표본 운이지 규칙이 아니다.**
    사례집 쪽은 오늘 고쳤다(`masked()` 를 `--dump` 와 `--sheet` 가 함께 지난다).
    고치는 법 — `mfds_guide.py` 도 같은 모양으로 바꾸고, 시트를 다시 뽑는다.
    🚨 다시 뽑으면 **사람이 이미 채운 `확정유형` 이 날아간다** — 아직 안 채웠으면 지금이 싸다.

**⑥ 사례집 4호가 D-40 에 2건 모자란다** (28 / 30)

    `mfds_sanctions` 전량과 합쳐야 선다. ③(빠진 행 확인)과 같은 일이다.

**⑦ 사례집 인용표현 349 vs 사실원장의 「24회 · 19종」** 🚨

    **세는 단위가 다르다** — 원장 쪽은 유형에 붙은 위법 문구 종수,
    새 계수는 인용부호 안 표현 전부(‘GI(혈당지수)’ 같은 것 포함).
    **합치기 전에 맞춰야 한다.** 지금 둘을 더하면 아무 뜻도 없는 수가 나온다.

**⑧ 5호 검증셋 25건** — `data/derived/mfds_casebook_labelsheet.jsonl` (seed 20260908)

    🚨 `확정유형` 은 **사람이** 채운다. AI 가 채우면 홀드아웃이 자기 채점이 된다.
    ★ 해설서 검증셋 150건(②)과 **성격이 다르다** — 저기는 3분류가 6종을 뭉친 것이고,
      여기는 5호 하나만 둘로 갈린다. 한 사람이 이어서 하면 30분이면 끝난다.
