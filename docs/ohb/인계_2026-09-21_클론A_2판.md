# 인계 2026-09-21 (2판) — 클론 A 에서 B 작업분을 검토하고 고쳤다

> 작성 Claude · 클론 A(회사 기기) 세션 · **앞 문서** `인계_2026-09-21_클론A.md` (B → A 인계)
> 🚨 **이 문서의 코드 결과는 컨테이너 실측이다** — 클로드 작업 공간에 저장소를 옮겨 pytest·ruff 를 돌렸다.
> 🔄 A 실기기 — `check` 게이트 **609 passed**(마지막 커밋 `7269b04` 기준) · `doctor`·`inventory`·`data-setup --mirror`·`doctor --env` 사용자 실행 확인.
> B 디스크는 한 번도 안 봤다. **B 에 앉으면 §0-1 만 순서대로 치면 된다.**

## §0 열 줄

1. A 가 정본과 같아졌다 — `data-sync` 42개 · `--check` 원장 최신 152 · `check` 565 passed · skip 5 → 0 [사용자 실행 · A]
2. B 의 09-19~21 작업(D-247~D-255 · 커밋 25개)을 전수로 읽었다. **런타임 결함은 셋**이었고 나머지는 문서·경계
3. 🔴 개인 피심인 실명이 조사 7꼴(로부터·만·까지·에게·씨·께서·라는) 앞에서 **안 가려졌다** — 조사 목록이 세 벌이었다 → 한 벌
4. 🔴 `endorse_scan --candidates` 가 앵커 없이 가려 상호·실명이 후보 파일에 남았다 → 앵커를 싣는다
5. 🔴 로그 가림이 쉼표·괄호에서 끊기고 `api_key=`·`access_token=` 을 못 가렸다 → 고침
6. 인용 상한 「40자」 산문 9곳(생성물 넷 포함)을 120자로 · 산문 ↔ `PARAMS` 대조 게이트
7. 데이터 경로 다섯 — 별칭 예약어 한 곳 · 보도자료 폴더 이름 표에서 · `db-reset --data` 사전검사 전부 · register 원장 sha · `data-sync` 두 단계 교체
8. 계약 표면이 화면 라우트를 **우연히** 빼던 것을 명시로 · `\s` 경고 원인 · 린트 `W605`
9. 문서 — 집행계약 게이트 색인(+ 대조 게이트) · 사실원장 09-21 절 · README 수 · 병렬·산출물 · 앞 인계 두 칸 정정
10. 판정 대기 7건은 `결정요청_2026-09-21_클론A.md` — 🔴 **round2 가 오기 전에 2번(행정심판 계보)을 정해야 `golden --write` 가 안 멈춘다**

## §0-1 🔴 클론 B 에서 할 것 — 순서대로 (여기만 보면 된다)

A 에서 할 일은 끝났다 — `7269b04` 까지 push · A 의 `.env` 에 `RAW_MIRROR` 가 적혔다(거울 폴더는 아직 비어 있다).
★ 아래 ① → ⑤. 각 단계의 **출력을 클로드에게 주고** 다음으로 간다 (특히 ③ 의 `--dry-run` — 올리기 전에 수를 본다).

**① 받고 게이트**

```
git pull origin ohb
uv run python launcher.py check
```

기대 — 게이트 609 passed(정본은 파생물이 있어 skip 없이 돈다). 🔴 가 있으면 여기서 멈추고 출력을 준다.

**② 원문 거울 붙이기** — 거울 폴더는 **비어 있어** `data-setup` 이 스스로 못 찾는다(안에 `copylane-raw-mirror` 가 생기는 것은 첫 올리기 뒤다). 경로를 준다.

```
Get-ChildItem "G:\내 드라이브"
uv run python launcher.py data-setup --mirror "G:\내 드라이브\CopyLane_raw_mirror"
uv run python launcher.py doctor --env
```

- 드라이브 글자(`G:`)는 B 기준으로 바꾼다. B 가 소유 계정(팀장 개인 계정)으로 로그인돼 있으면 `내 드라이브` 에 **폴더**로 보인다. `.lnk` 로 보이면 `G:\.shortcut-targets-by-id\<id>` 쪽이다(A 와 같다)
- 역할을 물으면 `canonical` 그대로 — `--role` 을 주지 않는다. 🚨 `replica` 로 바꾸지 않는다
- 기대 — `doctor --env` 에 `✅ DATA_ROLE canonical` · `✅ RAW_MIRROR …`

**③ 정본 결손 확인** (오늘 고친 `doctor`·`inventory` 가 정본에서는 종전처럼 보이는지)

```
uv run python launcher.py doctor
uv run python launcher.py inventory
```

기대 — 정본에서는 `legacy`·`query` 가 🟡 로 **펴져 나오고**(사본처럼 접히지 않는다) 「다시 받는다」 안내가 나온다. 🔴(`lost`) 는 0 이어야 한다.

**④ 원문 거울 올리기** — 🚨 외부 전송이다. `--dry-run` 출력을 먼저 클로드에게 준다

```
uv run python launcher.py raw-mirror-publish --dry-run
```

볼 것 — 올릴 원문 수 · 뺀 수(⬜ G2 · 재배포 제약 · 레지스트리에 없음 / 🟡 원장과 다른 원문) · 크기(MiB).
🔴 「키가 섞인 원문」이 나오면 **올리지 않는다** — 그 파일 이름을 클로드에게 준다(값은 찍히지 않는다).
확인되면:

```
uv run python launcher.py raw-mirror-publish
```

**⑤ (선택) 마스킹 고침의 실제 영향** — B 에서 바로 재도 되고, 거울을 연 뒤 A 의 미리보기로 재도 된다(A 쪽이 파생물을 안 바꿔 안전하다)

```
uv run python launcher.py extract ftc_decisions_body --preview
```

B 에서 파생물까지 새로 만들려면 §3③ 의 `data-refresh ftc_decisions_body` 순서를 따른다 — 그때는 `data-publish` · git 커밋이 뒤따른다.

**⑤ 뒤 A 에서** — `raw-mirror-sync --dry-run` → `raw-mirror-sync` → `extract ftc_decisions_body --preview` (§3-2)

**판정 (기기 무관 · round2 CSV 오기 전에)** — `결정요청_2026-09-21_클론A.md` 2번(행정심판 계보) · 1번(2인 서명 둘 — 권소라 확인)

## §1 고친 것 — 호출부 · 테스트 · 반대 대조

전부 **옛 코드에서 실패하고 새 코드에서 통과**하는 것을 확인했다 (`git stash` 로 되돌려 돌림 · 컨테이너).

| 묶음 | 고친 자리 | 새 테스트 (게이트 여부) | 옛 코드 |
|---|---|---|---|
| 마스킹 | `preprocess/mask.py` `PARTICLES`·`PERSON_TAIL`·`_mask_people` | `test_mask.py` 개인 피심인 7꼴 · 조사 목록 포함 · ftc 호출부 앵커 AST + 반대 대조 (비게이트 — 파일 머리말) | 6 실패 |
| 추천·보증 후보 | `preprocess/endorse_scan.py` `candidates()` | `test_endorse_scan.py` 후보 파일이 상호·실명을 가린다 (게이트) | 실패 |
| 로그 | `app/logging_conf.py` `_QS`·`_SENSITIVE_IN_QS` | `test_logging_redaction.py` 새는 꼴 +5 · 비밀 3 · 지우면 안 되는 것 +1 (게이트) | 8 실패 |
| 인용 상한 | `gen_registry.py:444` · `data.js:133` · `mfds_board.py` 2곳 · `전처리_사양.md:201` → `rebuild` 생성물 4 | `test_settings.py` 산문 = `PARAMS` (게이트) + 반대 대조 | 9곳 걸림 |
| 기기 별칭 | `collect/store.py` `device_problem()` 신설 ← `device_id` · `data_store.setup` · `doctor._check_data_env` | `test_raw_inbox.py` 예약어 · 판정 한 곳 / `test_misc_fix_d.py` doctor | 3 실패 |
| 보도자료 폴더 | `collect/mfds_press.py` `FAMILY, PDF_FAMILY = store.families(…)` · 격리 폴더 mkdir 안 함 | `test_raw_dirs_fix_d.py` (게이트) | 실패 |
| DB 재구축 | `scripts/load_db.py` `LOAD_INPUTS` 신설 → `db_reset.py` `LOAD_CHECKS`·`REPLICA_CHECKS`·`CANONICAL_CHECKS` | `test_db_reset_role.py` 사본·정본 각 3 · 적재 입력 ⊂ 표 (게이트) | 7 실패 |
| register | `collect/ingest.py` ledger 판정이면 `digest_of(dest)` | `test_ingest_register_fix_d.py` 유동값만 다른 파일 | 실패 |
| 파생물 받기 | `scripts/data_store.py` `_stage()` → 전부 확인 뒤 교체 · dry-run 이 저장소 준비 확인 · MiB · 「복사」 · 안 쓰던 `_get` 삭제 · `launcher.py` docstring | `test_data_store.py` 중간 파일 오염 · dry-run · 단위 | 3 실패 |
| 계약 표면 | `tests/test_contract_surface.py` `_CORE = "app.api"` | 코어만 · 화면 안 섞임 (게이트) | — (의도 명시) |
| 경고 | `preprocess/ftc_reason_probe.py` docstring `r"""` · `pyproject.toml` `W605` | ruff 가 잡는다 | ruff 1건 |
| 집행계약 색인 | `docs/00_거버넌스_집행계약.md` §1-3 · 머리글 43 → 45 | 🆕 `tests/test_contract_index.py` 양방향 (게이트) | 1 실패 |

**컨테이너 전체** — ruff format·check 통과 · `pytest` 1,114 passed · 12 skipped · 6 xfailed · **3 failed**
(스테이징 누락 — `assets/diagrams/README.md`·README 트리 항목. 고침 전 기준선과 같은 셋) · 모은 수 1,135 / gate 591.

**철회한 것** — 수집기 dry-run·건너뜀에서 `sleep` 이 빠진다는 지적: `collect/http.fetch` 가 `MIN_INTERVAL` 을
스스로 지킨다(규약 5 는 지켜진다). README 에 낡은 맨 명령 이름: 0건. `evalset_by_statute` 사람 칸: 생성물이라 해당 없음.

## §2 판정 대기 — `결정요청_2026-09-21_클론A.md`

1 🔴 클로드가 옮겨 적은 2인 서명 둘 · 2 🔴 행정심판 계보(round2 전에) · 3 `/search` 재배포 축 · 4 프로젝트 사본
내보내기 검사 · 5 DISCLAIMER 문안 · 6 사전 구분자 정규화 · 7 보기 `extract` 멈춤. 앞 인계 §5-1 의 8건은 그대로 남아 있다.

## §3 사용자가 칠 것

**① A 에서 게이트** (쓰기 되받아 대조 뒤)

```
uv run python launcher.py check
uv run python launcher.py rebuild
git status --short
```

`rebuild` 뒤 `git status` 에 **생성물 넷이 안 뜨면** 컨테이너에서 다시 만든 판과 A 에서 만든 판이 바이트까지 같다는 뜻이다.

**② A 의 🟡 넷 — 레포 밖 백업으로 옮긴다** (원장에 없는 옛 파일 · 지우지 않는다)

```
mkdir C:\Users\playdata2\Desktop\CopyLane_backup\20260921-stale
move data\derived\mfds_casebook\evasion_scan.json C:\Users\playdata2\Desktop\CopyLane_backup\20260921-stale\mfds_casebook_evasion_scan.json
move data\derived\mfds_casebook\quotes.json C:\Users\playdata2\Desktop\CopyLane_backup\20260921-stale\mfds_casebook_quotes.json
move data\derived\mfds_press\evasion_scan.json C:\Users\playdata2\Desktop\CopyLane_backup\20260921-stale\mfds_press_evasion_scan.json
move data\derived\mfds_press\quotes.json C:\Users\playdata2\Desktop\CopyLane_backup\20260921-stale\mfds_press_quotes.json
uv run python launcher.py derived-manifest --check
```

🚨 경로는 `--check` 출력의 🟡 줄 그대로 옮긴 것이다 — 출력과 다르면 출력을 따른다.

**③ B 에서 — 마스킹 고침이 실제 파생물을 몇 행 바꾸는지 (D-142 판 대조)**. 정본 전용이라 B 에서만 된다.

```
git pull origin ohb
uv run python launcher.py check
uv run python -m preprocess.endorse_scan --candidates
uv run python launcher.py data-refresh ftc_decisions_body --dry-run
uv run python launcher.py data-refresh ftc_decisions_body
git diff --stat -- data/derived_manifest.jsonl
```

개인 실명 가림은 공정위 추출(`ftc_decisions_body`)에서 앵커가 사람을 실을 때만 걸린다 — 그래서 그 원천을 다시 뽑는다.
`data-refresh` 가 바뀐 생성물을 원장에 적으므로 **원장에서 sha 가 바뀐 줄이 이번 고침의 실제 영향**이다. 없으면 B 의 코퍼스에
그 조사 꼴이 없었던 것이고, 있으면 `data-publish` 전에 바뀐 파일 하나를 열어 이름이 가려졌는지 눈으로 본다.
`endorse_scan --candidates` 는 `build/` 에만 쓴다(커밋 안 됨) — 후보 파일에 `[대표]` 가 보이면 된다.

## §3-1 같은 날 이어서 — 런처 자동화 점검 (A 실행 → 고침)

A 에서 읽기만 하는 명령 다섯을 돌렸다 [사용자 실행 · A] — `db-reset --data` 미리보기(사본 사전검사 통과 · 오늘 넓힌 검사가
실제 디스크에서 돈 첫 기록) · `data-refresh --dry-run`(정본 문지기가 막음) · `data-sync --dry-run`(받을 것 0) 은 정상이었다.
⛔ 제가 드린 `doctor --data` 는 틀린 명령이었다 — 런처는 옵션 없는 `doctor` 가 데이터 검사다.

| 무엇 | 실측 [사용자 실행 · A] | 고친 자리 | 테스트 · 반대 대조 |
|---|---|---|---|
| 사본에서 원문 결손을 「볼 것」·🔴 로 세고 **수집을 권했다** | `doctor` 「사람이 볼 것 5,545」(legacy 5,266 · query 279) · `inventory` 가 같은 줄을 🔴 로 「이 기기에서 쓰려면 다시 받는다」 — D-226 과 정면으로 어긋난다 | `collect/missing.py` `REPLICA_FOLDS`·`level()`·`levels()`·`role()` — 사본은 `legacy`·`query` 를 접는다(`lost` 는 안 접는다) · `scripts/doctor.py` `_report_missing` · `--hash` 낡음 안내 · `preprocess/inventory.py` | `test_missing.py` 역할별 수준 · doctor 사본/정본 · inventory 사본 (게이트) — 옛 코드 5 실패 |
| 같은 결손을 inventory 🔴 · doctor 🟡 | D-253 맥락 4 가 고칠 이유로 든 불일치가 **색에서** 남아 있었다(분류만 한 벌) | 수준을 `missing.level()` 한 곳에서 | `test_missing.py` 정본에서 둘 다 🟡 · 수준을 정하는 자리 한 곳 |
| `data-setup` 이 바로가기로 붙인 공유 폴더를 못 찾았다 | `G:\내 드라이브` 에 `CopyLane_store` 가 **폴더로 안 보이고**, 내용은 `G:\.shortcut-targets-by-id\<id>\copylane-derived` [기기 폴더 목록 · 클로드 조회] | `scripts/data_store.py` `SHORTCUT_TARGETS`·`SIGNATURE`·`_shortcut_targets()` — id 폴더를 **내용(배치 폴더)으로** 알아본다 · 받은편지함 배치 이름 `INBOX_LAYOUT` 을 data_store 로 (raw_inbox 가 읽는다) | `test_data_store.py` 두 모양 · 남의 폴더 안 집음 · 받은편지함과 안 섞음 (게이트) |

컨테이너 — 1,121 passed · 3 failed(스테이징 누락 · 기준선과 같은 셋). 사본·정본으로 `inventory`·`doctor --data` 를 돌려 출력을 봤다 —
사본은 「사람이 볼 것 0 · 사본 15,536 접음」, 정본은 둘 다 🟡.
🚨 빈 저장소(한 번도 올린 적 없음)는 내용으로 못 알아본다 — 그때는 `--store` 로 준다(안내문에 적었다).

**A 에서 확인할 것**

```
uv run python launcher.py doctor
uv run python launcher.py inventory
uv run python -c "from scripts import data_store as ds; print(ds.candidates())"
```

`candidates()` 가 `G:\.shortcut-targets-by-id\1ZPf6ut4STqiUNTGG6SXtaAL-ybsZEOGp` 하나를 내면 바로가기 탐색이 된 것이다
(`.env` 는 안 건드린다 — 찾기만 한다).

## §3-2 같은 날 이어서 — 원문 거울 (D-256 · 팀장 판정 (나))

팀장 — *「원문이 있어야 프로젝트 작업에 이해도가 올라가는 것 아닌가」*. ⛔ 제가 「사본은 원문을 쓰지 않는다」고 한 것은 과했다 —
D-226 이 막는 것은 **원문으로 파생물을 만드는 것**이다. 대안 넷 중 (나) — **팀장 기기에만 읽기용 원문 거울** + 임시 폴더 미리보기.

| 무엇 | 자리 | 테스트 |
|---|---|---|
| 정본 → 거울 올리기 · 사본 ← 거울 받기 | 🆕 `scripts/raw_mirror.py` · 런처 `raw-mirror-publish`(50) · `raw-mirror-sync`(51) | 🆕 `tests/test_raw_mirror.py` 9 (게이트) |
| 추출 미리보기 — 임시 폴더에만 · 저장소 파생물을 건드리면 🔴 | 🆕 `preprocess/preview.py` · `launcher.py extract <원천> --preview` | 🆕 `tests/test_preview.py` 3 (게이트) |
| 거울 폴더 찾기 · `.env` `RAW_MIRROR` | `data_store` `MIRROR_NAME`·`SIGNATURE`·`setup --mirror` · `collect/env.py` · `.env.example` | `test_raw_mirror.py` |
| 안내 문구 정정 · 거울 안내 | `collect/missing.py` `mirror_hint()` · `doctor` · `inventory` | `test_missing.py` |

반대 대조 일곱(각각 빼면 실패) · 컨테이너 1,133 passed(실패 셋은 기준선) · 모은 수 1,154 / gate 609. ⬜ **실기기 미실행.**

**사람이 할 것 (Drive · 한 번)** — 🚨 코드는 공유 범위를 못 본다
1. **팀장 개인 계정**(`CopyLane_store` 소유 계정)으로 drive.google.com 에서 `CopyLane_raw_mirror` 폴더를 만든다 — **일반 액세스 「제한됨」** · 어느 기기의 브라우저에서 해도 된다
2. A 의 Drive for desktop 계정이 소유 계정과 다르면 **그 계정만** 편집자로 공유한다 — 팀원 계정은 넣지 않는다 (원문은 마스킹 전이다).
   소유 계정으로 로그인된 기기(B 로 보인다)는 공유·바로가기 없이 `내 드라이브` 에 폴더로 보인다
3. 공유받은 기기(A)는 `내 드라이브` 에 바로가기 추가 → A·B 각각 `uv run python launcher.py data-setup` (거울을 찾아 `RAW_MIRROR` 를 적는다)

**그다음 순서**

```
B:  uv run python launcher.py raw-mirror-publish --dry-run
B:  uv run python launcher.py raw-mirror-publish
A:  uv run python launcher.py raw-mirror-sync --dry-run
A:  uv run python launcher.py raw-mirror-sync
A:  uv run python launcher.py extract ftc_decisions_body --preview
```

마지막 줄이 오늘 마스킹 고침이 공정위 파생물을 몇 줄 바꾸는지 A 에서 잰다(§3③ 의 B 측정을 A 에서 대신한다).

## §3-3 같은 날 이어서 — 소성민 코드 리뷰 10건 고침

리뷰(ssm 브랜치 · main 최신화 뒤) 10건 중 **9건을 A 의 현재 코드에서 재확인**했다 — 전부 남아 있었다. 6번(관리자 오류 화면)은 ssm 브랜치 코드라 안 봤다(소성민 몫).

| # | 무엇 | 고친 자리 | 테스트 (반대 대조) |
|:-:|---|---|---|
| 2 | 🔴 합성 직함(부사장·부회장·사내이사·이사장·전무이사·상무이사) 뒤 실명이 안 가려짐 — **09-19 경계 고침의 회귀**. 반출 검사도 같은 정규식을 빌려 **같이 못 봤다** | `preprocess/mask.py` `_TITLES` · `_SHORT_TITLE` | `test_mask.py` 직함 두 표 · `test_derived_manifest.py` 개인식별 (고침 전 코드로 돌리면 낙방) |
| 4 | `collect --dry-run` 이 law_api 밖에서 **실제 수집** + 별칭·겹침 검사 건너뜀 | `collect/__init__.py` `COLLECTOR_OPTIONS` · `launcher.py collect` | `test_launcher_automation.py` 넘김 · 거부 · 표↔argparse 대조 |
| 1 | `sync_project` 가 부를 때마다 TypeError (`subprocess.run(newline=)`) | `scripts/sync_project.py` `last_commit` | `test_review_fix_ssm.py` — 레포 전체 subprocess 호출에 `newline=` 없음 |
| 8 | `PASS_RISK_MAX_PROVISIONAL = R2` — D-227 대로 **R1** · 이름 `PASS_RISK_MAX` | `app/contracts.py` | `test_review_fix_ssm.py` R2 · 위험도 없음은 통과 아님 |
| 3 | 이 기기가 받은 원문을 잃으면 다시 수집해도 조용히 skip — doctor `lost` 안내와 모순 | `collect/store.py` `plan_raw` → `restore` · `restore_needs_row` · `collect/ingest.py` | `test_raw_inbox.py` 되살림 · 판 경로 · 다른 기기/옛 행 · G2 는 안 되살림 · `test_ingest_register_fix_d.py` |
| 5 | 바이트만 같은 **다른 이름**을 `moved` 로 봐 팀원 원문이 `raw-import` 에서 조용히 빠짐 | `collect/missing.py` `_name_key` | `test_missing.py` 폴더 옮김은 moved · 다른 이름은 아님 |
| 9 | `embed --check` 가 동기화에 막힘 | `launcher.py` `needs_data(unless=)` | `test_launcher_automation.py` embed 두 개 |
| 7 | `put_setting` 이 `.env` 와 `os.environ` 에 다른 값 | `collect/setkey.py` | `test_data_store.py` 따옴표 경로 |
| 10 | `MLFLOW_TRACKING_URI` 를 가린 입력창으로 받음 · 원문 키 검사가 기본값을 키로 찾음 → `SETTINGS` 로 · `DATABASE_URL` 은 비밀번호라 `KEYS` 에 남김 | `collect/env.py` | `test_review_fix_ssm.py` |

⛔ **고치지 않은 것** — `_clean` 이 빈 값을 거부해 설정을 지울 수 없는 것(#7 뒷부분 · 지금 지우는 경로가 없다) · 「전무」「상무」「감사」 **단독** 직함(09-19 전에도 없었다 · 「감사 결과」 오탐 — `--survey` 잔여를 보고 정한다) ·
「피심인부사장김철수」처럼 **한 글자 앞붙이를 붙여 쓴 것**(단독 「사장」과 같은 경계 · 낱말 안쪽 오탐을 피한다).
🟡 **곁가지로 본 것** — `tests/test_contracts.py` 뒤에 `tests/test_contract_surface.py` 만 따로 돌리면 스냅샷이 갈린다(`ForwardRef` 문자열이 앞 테스트의 `model_rebuild` 에 따라 바뀐다). 전량 순서로는 통과 · **고치기 전부터** 그랬다 — 안 고쳤다.

**🔴 B 에서 할 것 (마스킹 #2 — 09-19 이후 파생물)**

```
git pull origin ohb
uv run python launcher.py check
uv run python scripts/derived_manifest.py --export-check
```

`--export-check` 가 이제 합성 직함 잔여를 잡는다 — 🔴 로 멈추면 **어느 파일에 몇 건**인지(이름은 첫 글자만)를 클로드에게 준다.
그 파일의 원천을 `data-refresh <원천>` 으로 다시 만들고 → `--export-check` 0 → `data-publish`. Drive 에 올라간 09-19 이후 판을 새 판으로 덮는 것이다.
🚨 doctor 의 `moved` 분류(#5)가 좁아졌다 — B 의 `doctor` 에서 결손 분류 수가 **바뀌는지** 같이 본다(바뀌면 그 목록을 준다).

## §3-4 같은 날 이어서 — 전수 재검토(에이전트 다섯 · 영역별) → 고침

재검토 판정은 **조건부**였다(마스킹 치명 2). 아래는 전부 고쳤고, 각 줄의 테스트는 **고치기 전 코드로 돌리면 낙방**하는 것을 확인했다(표시 없는 것은 코드 판독 + 새 테스트).

| # | 무엇 | 고친 자리 | 테스트 |
|:-:|---|---|---|
| C1 | 🔴 「소송대리인 **변호사** 김철수」 — 「변」이 성씨라 **직함을 지우고 실명을 남겼다** · 「이사 전무 김철수」도 | `mask.py` `_NOT_TITLE`(이름 자리가 또 직함이면 이름 아님) · `_SHORT_TITLE` 에 전무·상무 | `test_mask.py` 직함 표 |
| C2 | 🔴 「대표자 : 홍길동」·「대표이사(홍길동)」·「대표이사, 홍길동」·「… 및 박영희」가 그대로 · 반출 검사도 0건 | `mask.py` `_TITLE_SEP` · `_NAME_JOIN` · 반출 검사에 **마스킹과 다른 규칙의 넓은 거름**(`derived_manifest.people_loose` · 🟡 알림만 · `[임의]`) | `test_mask.py` · `test_derived_manifest.py` 넓은 거름 둘 |
| C2' | 과잉 삭제 — 「대표자 **표시광고**」·「대표이사 **고발**」·「이상의」가 `[대표]` · 그것을 지킨다던 테스트가 사람 규칙을 안 부르는 `mask()` 를 불러 **아무것도 안 쟀다** | `_NOT_NAME` 판정 어휘 · 조사 뗀 첫 이름으로도 대조(`_name_head` · 반출 검사도 같은 함수) | `test_mask.py` 두 테스트를 `mask_person` 으로 |
| I1·I2 | 라우터가 위험도를 안 봐 **확정 + 위반 + R3 → pass** · 계약도 받아 줌 · 옛 테스트가 그것을 지켰다 | `graph.route_after_judge` 가 `is_pass` · `JudgeResponse` 에 「attempt 0 의 pass ⇒ 전부 통과」 · `RiskAssessment` 는 하한 아래 금지 | `test_graph_routers.py` · `test_contracts.py` |
| I3 | 재생성 거부가 누적 키라 한 번 거부되면 뒤 시도가 통과해도 `search_failed` | 상태에 `rejected`(이번 시도) · 라우터가 그것을 본다 | `test_graph_routers.py` |
| I4 | `data-publish` 가 원장 칸의 부류를 믿어 원문캐시가 올라갈 수 있었다 | 부류를 지금 규칙으로 다시 세고 다르면 멈춤 | `test_data_store.py` |
| I5 | 받은편지함 올리기가 G2 를 안 걸렀다 | `_held_back` 하나로(g2 · noredist · unknown) | `test_raw_mirror.py` |
| I6·I7 | 바뀐 원천은 수집마다 **같은 판**이 또 생김 · 되살리기가 치운 판을 되살림 · 되살릴 자리의 다른 바이트를 덮을 수 있었다 | `store._same_elsewhere`(디스크·원장·옛 판을 한 곳에서) · 원본 이름 우선 · 자리에 다른 바이트면 새 판 | `test_raw_inbox.py` 셋 |
| I8 | `--limit` 으로 일부만 받아도 수집 완료 표시(오늘 #4 가 넓혔다) · `collected_at:` 빈 값이면 다음 줄을 먹는 정규식 | `registry.mark_if_complete` 한 곳 · 수집기 다섯이 부름 · 정규식 한 줄 안 | `test_review_fix_0921.py` |
| I9 | 다른 원천의 흔한 이름(`page_0001.json`)이 옮겨짐 | 같은 원천 요구 | `test_missing.py` |
| I10 | `label_merge --merge` 가 첫 파일 줄만 씀 · **범위밖 ↔ 유형이 「1인」으로 세여 분할에 들어감** | `labels.verdict`(범위밖도 판단) · 파일 전부에서 키마다 한 줄 | `test_label_merge.py` 둘 |
| I12 | 분할 뒤 새 라벨 파일이 입력 검사를 통과 · 낡은 주입본이 봉인 문서의 변형을 train 에 | `split.verify_inputs` 가 새 입력도 봄 · `golden` 이 주입 원본의 분할을 대조 | `test_review_fix_0921.py` |
| I13 | 저장소의 0바이트·잘린 객체가 영영 안 고쳐짐 · 복사가 끊기면 `.part` 가 남음 | `data_store.object_ok`(크기) — 올리기 셋·받기 둘 · `_stage` 정리 | `test_data_store.py` · `test_raw_mirror.py` |
| — | 팀원 기기가 정본이 받은 것을 **다시 불러** 조회수 원천이 새 판으로 깔림 | `store.already_have` — 수집기 넷 | `test_raw_inbox.py` |
| — | 파생물 원장이 없으면 사본이 「같다」로 통과 · db-reset 이 옛 판 사본에서 볼륨을 지움 · 0바이트 입력을 성공으로 적재 · 2인 확인 전 소스를 「레지스트리에 없는 소스」로 지움 | `data_store.ledger_missing` · `db_reset` 원장 대조 · `load_db._jsonl_at` 하나로 · 거두는 집합 | `test_data_store.py` · `test_db_reset_role.py` · `test_review_fix_0921.py` |
| — | doctor `--env --data` 가 데이터를 건너뛰고 0 · 깨진 원장 줄을 🔴 로 찍고 0 | `doctor.main` · `_BROKEN` | `test_review_fix_0921.py` |
| — | 런처 — `--use` 가 수집기 넷에서 버려짐 · `setkey`/`admin-add` 뒤 인자를 Click 이 **되비춤** · 명령 줄 Rich 표기 · cp949 콘솔 · `doctor --env --hash` · `extract --preview` 가 옵션 버림 · 사본의 `data-refresh --dry-run` 거부 · `chunk --dump` 가 받은 뒤 거부 · 메뉴 수집에 미리보기 질문 | `launcher.py` (게이트 `test_키_입력_경로…` 는 `extra` 를 **어디에도 넘기지 않는지** 보게 넓혔다) | `test_launcher_automation.py` 셋 |
| — | 생성 스크립트 셋이 `--help`·`--check` 에도 **다시 씀** · `scripts/collect.py`·`eval_rule.py` 가 안내대로 돌리면 `app` 을 못 찾음 · `collect.py validate` 가 깨진 줄에 0 · 뒷광고를 「편입 가능」 | `docmeta.refuse_args` · `gen_registry`(옮겨 적음) · `sys.path` · `_BROKEN` · `OUT_OF_SCOPE_TYPES` | 정적 + 직접 실행 |
| — | app — 없는 계정의 로그인이 4 ms(있는 계정 40 ms) · `postgres://` 가 alembic 에서 죽음 · `pool` 이 늘 50 · `rd1` 주석이 D-127 과 다름 · 계약 스냅샷이 실행 순서에 따라 갈림 | `auth.verify_account` · `sqlalchemy_url` · `graph` · 스냅샷 전에 `model_rebuild`(표면 한 줄 · 계약 변경 아님) | `test_contract_surface.py` |

⛔ **고치지 않은 것 — 판정 · 측정 · 규모가 필요한 자리**
- **I11 (판정)** — 문구 겹침 제외가 **라벨 있는 test 행에만** 걸린다(`golden.py`). 적법(음성) 문장이 train·test 양쪽에 남는다. 막으면 Precision 측정이 정직해지고 test 음성 수가 준다 — 결정요청 대상
- 마스킹 — 붙여 쓴 「와·과」 목록(`김철수와 박영희`) · 드문 성씨(제갈·독고·편·방·왕) · 원천 가림 표기 `O`·`*`·`△` · NFD 한글 · 한 글자 앞붙이를 붙여 쓴 「피심인부사장김철수」 — 넓은 거름이 일부를 알린다
- `mfds_board._slug` 120자 자르기가 확장자를 떨굼 · `mfds_press` 첨부 이름의 `__c` — 고치면 **기존 파일 이름이 바뀌어** 다시 받는다. B 에서 해당 파일이 있는지 먼저 센다
- `drop_raw_for_g2` 를 부르는 곳이 없다 — G2 원문 삭제가 코드로 집행되지 않는다(지금 G2 수집 소스 0). 어디서 부를지는 판정
- app — 요청 본문 상한을 다 읽은 뒤 검사 · 비활성 계정 세션 유지 · `/search` 카테고리 오타 0건(계약 타입 변경) · 벡터 오류 문구 노출 · 사전형 로그 미가림
- 생성물 최신성 — `data_sources.yaml` 말고는 대조가 없다(`build_matrix --check` 를 게이트로 올릴지)
- 페이지네이션 — `openapi` 가 받은 행 수를 total 과 안 맞춤 등(네트워크 없이 못 잰다)

**🔴 B 에서 할 것 (마스킹 C1·C2 + §3-3 과 합쳐서)**

```
git pull origin ohb
uv run python launcher.py check
uv run python scripts/derived_manifest.py --export-check
uv run python launcher.py doctor
uv run python -m preprocess.labels
```

- `--export-check` — 🔴(엄격)과 🟡(넓은 거름) 수·예를 클로드에게 준다. 🔴 파일의 원천을 `data-refresh` → 다시 `--export-check` → `data-publish`
- `doctor` — `moved` 가 좁아졌다(같은 이름 · 같은 원천). 분류 수가 바뀌면 그 목록을 준다
- `preprocess.labels` — 범위밖 ↔ 유형 갈림이 이제 「갈림」이다. `유형 붙은 것` 수가 줄면 분할 봉인이 줄 수 있다 — `golden --write` 가 멈추면(봉인 감소) 출력을 준다. **`--allow-shrink` 를 먼저 쓰지 않는다**

## §3-5 같은 날 이어서 — 판정 둘(I11 · G2) 반영 + 남은 것 중 지금 할 수 있는 것

**팀장 판정** — I11 → **(나)** 적법 평가 문장과 겹치는 **학습 행을 뺀다**(평가는 그대로) · G2 → **(나)** 삭제 경로가 붙기 전에는 G2 를 **받지 않는다**.

| # | 무엇 | 고친 자리 | 테스트 |
|:-:|---|---|---|
| I11 | 적법(음성) 문장이 train·test 양쪽에 남았다 — 문구 겹침 제외가 라벨 있는 test 행에만 걸렸다 | `golden.py` — 음성 test 문장과 정규화 문구가 같은 train 행을 뺀다 · `stat["음성겹침_학습제외"]` · `--write` 가 늘 그 수를 찍는다 | `test_review_fix_0921.py` |
| G2 | `drop_raw_for_g2` 를 부르는 곳이 없어 G2 를 받으면 원문이 영구히 남는다(지금은 G2 10개가 전부 hold/manual · 용도 deny 라 **우연히** 막힘) | `registry.require` 가 `G2_DROP_WIRED` 가 비면 G2 를 거부(D-92) · 게이트가 목록 ↔ 실제 호출부를 양방향 대조 | 게이트 `test_G2_는_삭제_경로가_붙기_전에는_받지_않는다` · `test_review_fix_0921.py` 셋 |
| G2' | 🔴 **내 오류 정정** — 오늘 단 주석이 G2 원문 삭제를 **D-17** 로 인용했다. 삭제 규칙은 **D-92** 다(D-17 은 마스킹 · 「원문 미보관」의 출처) | store · raw_inbox · raw_mirror · missing · registry · openapi · 테스트 셋의 G2 줄 20곳(앞서 있던 것 포함 · `docs/` 는 그대로) | — |
| P1 | `openapi` — 받은 행 < 원천 신고여도 완료 · 전체 건수를 못 읽으면 끝을 「200 B 미만」으로만 알아 **끝나지 않을 수 있었다** | 부족·모름이면 `collected_at` 안 찍음(부족은 종료코드 1) · 행 없는 장도 끝 · `MAX_PAGES` 20,000 `[임의]` 에서 🔴 멈춤 | `test_pagination_guard.py` 넷 |
| P2 | `ftc_body` — `totalCnt` 없음을 0 으로 읽어 「받을 것 없음」· 중간 빈 장에서 조용히 끝 | 없음은 멈춤 · 목록이 덜 준 수를 돌려주고 그때는 완료 안 찍음 · 종료코드 1 | 같은 파일 셋 |
| P3 | `law_api._search_hits` — `totalCnt` 없음을 0 으로 · 깨진 장만큼 적게 받고 「실패 0」 | 없음은 멈춤 · 못 받은 수를 돌려주고 `collect_cases` 가 **실패로 센다** | 같은 파일 둘 |
| A1 | 비활성(`disabled_at`) 계정의 쿠키가 만료(8시간)까지 관리자 화면을 열었다 | `routers/auth.account_active`(로그인과 같은 조회) · `admin.require_governor` | `test_review_fix_0921.py` |
| A2 | `/search` 의 `vector` 가 예외 문장을 그대로 냈다(하위 예외 · 모델 경로) | 응답엔 클래스 설명 첫 줄만(`retrieve.public_reason`) · 원문은 서버 로그 | 같은 파일 |
| A3 | dict·JSON 모양 로그(`{"q": "…"}`)와 dict 로그 인자가 가림을 통째로 지났다 | `logging_conf._QS` 가 따옴표 열쇠도 · `RedactFilter` 가 문자열 아닌 인자도 | 같은 파일 넷 |
| A4 | 폼 본문을 다 읽은 뒤 상한을 쟀다 | `app/formbody.read_capped` 하나(선언 길이 먼저 · 흘려 읽다 넘으면 413) — 로그인 · 검수 폼 | 같은 파일 |
| CI | 판정매트릭스 대조가 런처에만 있었다 | `gate.yml` 에 `build_matrix.py --check` 단계(데이터 불필요 · `PYTHONUTF8`) | ⬜ CI 실행은 push 뒤 |

**행동이 바뀐 것 — B 에서 처음 보일 수 있다**
- `openapi` 수집이 **종료코드 1** 로 끝나고 `collected_at` 을 안 찍을 수 있다 — 원천이 신고보다 적게 준 것이다. 출력의 「받은 행 · 원천 신고」 두 수를 준다
- `openapi` 에서 전체 건수를 못 읽는 원천은 이제 **완료로 안 찍힌다** — 그런 원천이 실제로 있는지는 안 쟀다
- `ftc_body` 도 목록 부족이면 종료코드 1 · 본문 실패는 종전대로(찍고 1 · `law_api` 와 같은 규칙)

⛔ **여전히 안 한 것**
- `/search` 카테고리 오타 0건 — 계약 타입을 바꾸는 일이라 팀원 화면과 맞춰야 한다(계약 변경 · 판정)
- 마스킹 잔여(붙여 쓴 와·과 · 드문 성씨 · `O`·`*`·`△` · NFD · 붙여 쓴 한 글자 직함) — B 의 실제 수를 본 뒤
- `_slug`·`__c` 파일 이름 — B 에서 해당 파일 수를 본 뒤
- JSON 값 안의 이스케이프 따옴표(`"a\"b"`)는 가림이 앞에서 끊긴다 — 필터는 그물이다(`logging_conf` 머리말)

**🔴 B 에서 할 것 — §3-4 의 목록을 이것으로 갈음한다 (순서대로 · 각 출력을 클로드에게)**

```
git pull origin ohb
uv run python launcher.py check
uv run python scripts/derived_manifest.py --export-check
uv run python launcher.py doctor
uv run python -m preprocess.labels
uv run python -m preprocess.golden --write
```

1. `check` — 게이트 기대치는 **A 의 `check` 출력을 받은 뒤** 적는다(컨테이너 모은 수 게이트 645 · 파생물 skip 차이가 있다)
2. `--export-check` — 🔴(엄격)·🟡(넓은 거름) 수와 예 → **넓은 거름을 엄격으로 올릴지**가 이 수로 정해진다. 🔴 원천은 `data-refresh` → 다시 `--export-check` → `data-publish`
3. `doctor` — `moved` 가 좁아졌다(같은 이름 · 같은 원천). 분류 수가 바뀌면 목록
4. `preprocess.labels` — 범위밖 ↔ 유형 갈림이 이제 「갈림」이다. `유형 붙은 것` 이 줄 수 있다
5. `golden --write` — 새로 「**적법 평가 문장과 겹쳐 학습에서 뺀 행 N개**」가 찍힌다(I11). N 과 train·test 수를 준다.
   봉인 감소로 멈추면 출력을 준다 — 🚨 **`--allow-shrink` 를 먼저 쓰지 않는다**
6. 파일 이름 두 건 — 아래 출력을 준다. ① `mfds_board._slug` 가 120자에서 잘라 확장자를 떨군 파일(이름 123자 이상) ② 이름에 `__c`(판 표시 · `store.EDITION_MARK`)가 든 파일 — 진짜 판과 원천 이름이 우연히 `__c` 를 품은 것을 가른다. 0 이면 고친다 · 있으면 이름이 바뀌어 다시 받게 되므로 판정

```
Get-ChildItem data\raw -Recurse -File | Where-Object { $_.Name.Length -ge 123 } | Select-Object @{n="dir";e={$_.Directory.Name}}, Name
Get-ChildItem data\raw -Recurse -File -Filter "*__c*" | Group-Object { $_.Directory.Name } | Select-Object Name, Count
```

7. (선택) 마스킹 드문 성씨 · NFD — `uv run python -m preprocess.mask --target ftc --survey` 출력을 주면 붙여 쓴 와·과 · 드문 성씨 규모를 잰다

## §4 안 본 것 (D-188)

- ~~A 기기의 게이트 결과~~ ✅ 609 passed (사용자 실행)
- 원문 거울 · 미리보기의 **실기기 실행** — §0-1 ④ 이후
- B 디스크 · B 의 파생물 — 마스킹 영향은 추정도 안 했다
- CI — 이번 커밋의 워크플로 결과 · 🆕 `build_matrix --check` 단계가 windows runner 에서 도는지
- `mfds_cgm_expc` 5,109 / 5,129 — 사실원장 09-21 ② (B 원문을 봐야 갈린다)
- DB 의 `source_use` — 결정요청 3번의 「0건」은 생성물 기준

## §5 깔고 간 가정

1. A 의 작업 트리는 오늘 오전 스테이징 뒤로 **바뀌지 않았다** — 쓰기 전에 기기 판을 다시 받아 기준선과 바이트로 대조했다.
2. 컨테이너의 핀 판(fastapi 0.141.1 · ruff 0.16.5 …)이 A 의 `uv.lock` 과 같다.
3. `data_store.setup` 이 정본 역할로 `--device canonical` 을 받게 된 것(종전은 늘 거부)은 `device_id()` 규칙과 맞춘 것이다 — 막을 이유가 있으면 알려 달라.
