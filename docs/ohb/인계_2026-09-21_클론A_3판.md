# 인계 2026-09-21 (3판) — 클론 A → 클론 B · B 에서 할 일 한 장

> 작성 Claude · 클론 A(회사 기기) 세션 · **앞 문서** `인계_2026-09-21_클론A_2판.md`
> 🚨 **B 에 앉으면 §0 만 순서대로 친다.** 2판의 §0-1 · §3 · §3-3 · §3-4 · §3-5 · §3-6 에 흩어져 있던 「B 에서 할 것」을 여기 한 줄로 모았다 —
> 2판의 B 목록은 이것으로 **갈음**한다. 무엇을 왜 고쳤는지(표 · 테스트 · 반대 대조)는 2판이 정본이다.
> 기준 커밋 `9cfa51e` (ohb · push 됨 · A `git status` 깨끗 [사용자 실행]).

## 지금 상태 — 한 줄씩

- A 실기기 `check` — **게이트 645 passed · 612 deselected · skip 0** · `build_matrix --check` 「판정 85건」 [사용자 실행 · A]
- 코드 — 소성민 리뷰 10건 · 전수 재검토 · 판정 둘(I11 (나) · G2 (나)) · 페이지 가드 · 앱 네 건 · CI 매트릭스 대조까지 **커밋·push 끝** (2판 §3-3~§3-5)
- 문서 — 전수 최신화 26개 + **D-257**(D-17 개정 · 팀장 판정 (가) — 마스킹은 파생물에, 원문은 저장소·공유 저장소·배포물에 없다) · DISCLAIMER §4 · README (2판 §3-6)
- claude.ai 프로젝트 — 사본 22개 갱신(`sync` 20 + 병렬작업 계약 · 2판 인계) · 옛 판 24건 내림 · 지식 129만 / 200만
- 🔴 **B 는 09-21 에 한 번도 안 돌았다** — 오늘 고친 마스킹·라벨·골든셋이 **B 의 파생물에 아직 안 닿았다.** 그게 §0 이다

## §0 🔴 B 에서 할 것 — 순서대로 · 각 단계 출력을 클로드에게

**① 받고 게이트**

```
git pull origin ohb
uv run python launcher.py check
```

기대 — **게이트 645 passed · skip 0**. 🔴 가 있으면 여기서 멈추고 출력을 준다.

**② 데이터 상태 — 읽기만**

```
uv run python launcher.py doctor --env
uv run python launcher.py doctor
uv run python launcher.py inventory
uv run python scripts/derived_manifest.py --export-check
```

- `doctor --env` — `✅ DATA_ROLE canonical`. `RAW_MIRROR` 가 없으면 ⑥ 에서 붙인다
- `doctor` · `inventory` — 정본에서는 `legacy`·`query` 가 🟡 로 **펴져** 나온다(사본처럼 안 접힌다) · `lost` 🔴 는 0 이어야 한다 ·
  `moved` 가 좁아졌다(같은 이름 · 같은 원천) — 분류 수가 전과 다르면 그 목록을 준다
- `--export-check` — 🔴(엄격 · 마스킹과 같은 규칙) · 🟡(넓은 거름 · 다른 규칙 · 알림만 · `[임의]`) 의 **수와 예**를 준다.
  ★ 🟡 수가 **넓은 거름을 엄격으로 올릴지**(판정)의 근거다

**③ 마스킹 고침을 파생물에 반영** — 🔴 가 난 원천만. 기본은 공정위 본문이다(합성 직함 · 「변호사」 · 「대표자 :」 · 조사 7꼴이 여기서 걸린다)

```
uv run python launcher.py data-refresh ftc_decisions_body --dry-run
uv run python launcher.py data-refresh ftc_decisions_body
uv run python scripts/derived_manifest.py --export-check
uv run python -m preprocess.endorse_scan --candidates
git diff --stat -- data/derived_manifest.jsonl
```

- `--export-check` 🔴 가 **0** 이 될 때까지 🔴 원천을 같은 순서로 다시 만든다
- `git diff` 에서 sha 가 바뀐 줄이 이번 고침의 **실제 영향**이다 — 바뀐 파일 하나를 열어 이름이 가려졌는지 눈으로 본다
- `endorse_scan --candidates` 는 `build/` 에만 쓴다 — 후보 파일에 `[대표]` 가 보이면 된다

**④ 라벨 · 골든셋 다시 만들기**

```
uv run python -m preprocess.labels
uv run python -m preprocess.golden --write
```

- `labels` — 범위밖 ↔ 유형 갈림이 이제 「갈림」이다(I10) — `유형 붙은 것` 이 줄 수 있다
- `golden --write` — 새 줄 「**적법 평가 문장과 겹쳐 학습에서 뺀 행 N개**」(I11). **N · train · test 수**를 준다
- 🚨 봉인 감소로 멈추면 출력을 준다 — **`--allow-shrink` 를 먼저 쓰지 않는다**

**⑤ 파생물 올리기 · 원장 커밋**

```
uv run python launcher.py data-publish --dry-run
uv run python launcher.py data-publish
git status --short
```

`--dry-run` 출력(올릴 파일 · 부류 · 크기)을 먼저 준다. 원문캐시 부류가 섞이면 멈춘다(정상 — I4). 커밋 명령은 `git status` 를 보고 드린다.

**⑥ 원문 거울 올리기** (D-256) — 🚨 **외부 전송이다. `--dry-run` 출력을 먼저 준다**

```
Get-ChildItem "G:\내 드라이브"
uv run python launcher.py data-setup --mirror "G:\내 드라이브\CopyLane_raw_mirror"
uv run python launcher.py raw-mirror-publish --dry-run
```

- 드라이브 글자는 B 기준으로 바꾼다 · 역할을 물으면 `canonical` 그대로(`--role` 을 주지 않는다)
- 볼 것 — 올릴 원문 수 · 뺀 수(⬜ G2 · 재배포 제약 · 레지스트리에 없음 / 🟡 원장과 다른 원문) · 크기(MiB)
- 🔴 「키가 섞인 원문」이 나오면 **올리지 않는다** — 파일 이름만 준다
- 확인되면 `uv run python launcher.py raw-mirror-publish`

**⑦ 셈 두 개 — 코드를 고칠지 판정할 근거** (읽기만)

```
Get-ChildItem data\raw -Recurse -File | Where-Object { $_.Name.Length -ge 123 } | Select-Object @{n="dir";e={$_.Directory.Name}}, Name
Get-ChildItem data\raw -Recurse -File -Filter "*__c*" | Group-Object { $_.Directory.Name } | Select-Object Name, Count
uv run python -m preprocess.mask --target ftc --survey
```

- 첫 줄 — `mfds_board._slug` 가 120자에서 잘라 확장자를 떨군 파일 · 둘째 — 이름에 `__c`(판 표시)가 든 파일
  → 0 이면 이름 규칙을 고친다 · 있으면 이름이 바뀌어 **다시 받게** 되므로 판정
- `--survey` — 붙여 쓴 「와·과」 · 드문 성씨 · `O`·`*`·`△` 가림 · NFD 한글 잔여의 규모

**⑧ ⑥ 뒤 A 에서** — `raw-mirror-sync --dry-run` → `raw-mirror-sync` → `extract ftc_decisions_body --preview`

## §1 판정 대기 — 기기 무관

| # | 무엇 | 어디 | 비고 |
|:-:|---|---|---|
| 1 | 🔴 **행정심판 계보** — round2 CSV 가 오기 전에 정해야 `golden --write` 가 안 멈춘다 | `결정요청_2026-09-21_클론A.md` 2번 | 시한 있음 |
| 2 | 🔴 클로드가 옮겨 적은 **2인 서명 둘** — 권소라 확인 | 같은 문서 1번 | 사람만 채운다 (D-66) |
| 3 | `/search` 재배포 축 · 프로젝트 사본 내보내기 검사 · 사전 구분자 정규화 · 보기 `extract` 멈춤 | 같은 문서 3 · 4 · 6 · 7번 | |
| 4 | DISCLAIMER 나머지 세 줄 — R0~R3(77) · 커밋되는 것(199) · 화면 꼬리말이 **안 그려진다**(213) | 같은 문서 5번 · 초안은 2판 §3-6 | 153행은 D-257 로 고쳤다 · 공개 문안 |
| 5 | 레지스트리 `masking:` 문언 10곳 「즉시 마스킹, 원문 미보관 (D-17)」 → D-257 뜻으로 | D-257 결정 7 · `scripts/gen_registry.py` `EXTRA` | **2인 확인 대상** — 초안까지 |
| 6 | `--export-check` 넓은 거름(🟡)을 엄격으로 올릴지 | §0 ② 의 수를 본 뒤 | |
| 7 | `/search` 카테고리 오타 0건 → 계약 타입 변경 | 팀원 화면과 맞춘다 | 계약 변경 |
| 8 | 병렬작업 계약 담당 표 — 표에 없던 여섯(`auth.py`·`routers/auth.py`·`logging_conf.py`·`templating.py`·`formbody.py`·`collect/**`)을 **팀장**으로 적었다 | `docs/00_병렬작업_계약.md` §5 | 확인만 |

## §2 클로드 몫 — B 에서 이어서 (§0 출력을 받은 뒤)

- 마스킹 잔여 고침 — §0 ⑦ `--survey` 의 수를 보고(붙여 쓴 와·과 · 드문 성씨 · 가림 기호 · NFD · 붙여 쓴 한 글자 직함)
- `_slug`·`__c` 이름 규칙 — §0 ⑦ 의 수가 0 이면
- **도면 A-04 · S-01 · W-01** — `retrieve` 배선(09-14)을 아직 안 그린다. 원천 `assets/diagrams/src/*.html` 을 고치고 `launcher.py diagram` (D-217 · 기기에서)
- `S0-14` 검토표 「이견」 절 — G2 는 `reviewed_by` 를 채워도 안 열린다는 한 줄(생성기 `scripts/review_sheet.py` · 2인 확인 절차 문서라 초안까지)
- `전처리_결과서.md` §2 괄호 수(보류 15 · 안 씀 13 · 배제 10)가 16 과 안 맞는다 — 원장 09-17 절로 모수를 가른다
- 담당자 알림(병렬작업 계약 등급 2) — `routers/user.py` → ksr·lse · `routers/admin.py` → ssm·psj · `gate.yml`·`docs/05_배포/` → ssm
- CI — `gate.yml` 의 「판정매트릭스 대조」 단계가 windows runner 에서 도는지(push 뒤 Actions 결과)

## §3 B 에서 처음 보일 수 있는 것 — 결함이 아니다

- `openapi` 수집이 **종료코드 1** · `collected_at` 안 찍음 — 원천이 신고보다 덜 줬다(「받은 행 · 원천 신고」 두 수를 준다) · 전체 건수를 못 읽는 원천도 이제 완료로 안 찍힌다
- `ftc_body` 목록 부족이면 종료코드 1 · `law_api` 는 목록에서 못 받은 수를 **실패로** 센다
- G2 원천은 `registry.require` 가 **거부**한다(D-92 — 삭제 경로 전). 지금 G2 10개는 전부 hold/manual 이라 실제로 부딪칠 일은 없다
- `data-publish` 가 원문캐시 부류를 만나면 멈춘다 — D-257 결정 3 의 집행이다

## §4 안 본 것 · 깔고 간 가정

- **B 디스크 · B 파생물** — 한 번도 안 봤다. §0 의 수는 전부 처음 재는 것이다 (D-188)
- 거울·미리보기 — **실기기 미실행** (D-146)
- `data-publish --dry-run` 이 부류·크기를 찍는지는 코드 판독이다 — 출력이 다르면 출력을 따른다
- ⑦ 의 PowerShell 두 줄은 A 에서도 안 돌렸다 — 경로가 다르면 알려 달라
- 프로젝트 사본 운영 — 레포 원본이 바뀌면 **그때 옛 사본을 내리고 새 판을 올린다**(팀장 지시 · `sync` 목록에 없는 문서도 같다)
