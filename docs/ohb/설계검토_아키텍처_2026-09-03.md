# 설계 논리 검토 — 아키텍트 관점 (2026-09-03 저녁)

> 오한빈 · 브랜치 `ohb` · 짝 문서 `정합성검토_2026-09-03.md`
> 관점 — 「문서가 서로 맞는가」가 아니라 **「설계 흐름이 끝까지 닫히는가 · 엣지에서 무엇이 새는가」**.
> 범위 — 기획서 v4.0 2·3·4·5·6·7·8·9장 · 설계 3종(LangGraph 상태·DB·청크) · DDL · `app/models.py` · 전처리 사양 · 수집전처리 기획 · 집행계약 · 배포계획 · 보안점검 · D-119~124 · 관련 D-번호.
> 이미 `아키텍처검토_2026-09-03.md`(위험도/상태 축 · U-USER 집행 · xlsx 원본 · REQ ID) 와 `설계가정검증_2026-09-03.md` 에 있는 지적은 **뺐다.**
> 표기 — **상** = 구현에 들어가면 되돌리기 비싼 것 · **중** = 문서 한 곳을 고치면 닫히는 것 · **하** = 표현.

---

## 0. 한 줄

**결정은 대부분 옳고, 새는 자리는 「결정과 결정 사이」다.** 특히 ① 재판정 루프의 **「보류」 처리**, ② **D-96 동의 ② ↔ D-122 NOTRAIN 의 정면 충돌**, ③ **실사례 홀드아웃이 사실상 비어 있음**, ④ **9/11 게이트가 판정 불가능한 조건**이라는 넷은 **데이터를 기다릴 이유가 없는 결정 미결**이라 지금 정해야 harness(9/10)·픽스처(D-124)가 빈 채로 넘어가지 않는다.

---

## 1. 🚨 지금 결정해야 하는 것 여덟 — 데이터 무관 · 9/10 전

| # | 결정할 것 | 왜 지금인가 | 권고 |
|:-:|---|---|---|
| **R1** ✅ **D-125** | **재판정 루프에서 「보류」는 통과인가 실패인가** (3-2·3-3 분기는 이진 · 상태는 3종) | 보류=통과면 생성기가 경계 어휘로 수렴, 보류=실패면 경계 클래스가 K 소진 → 「합법화 불가」 증명서(거짓) | 통과 = `상태==확정 ∧ 위험도 ≤ 주의`. 보류는 실패로 세되 K 소진 시 증명서가 아니라 **제3 종착 「보류 — 전문가 검토」** |
| **R2** ✅ **D-125** (outcome 축) | **K 소진 종착 문구** — 루프에 들어가는 문장은 B(실증형)뿐인데 종착 문구는 C(절대형) 것 | D-59 가 금지한 「A/B 를 C 처럼 답하기」를 루프가 재현 | 종착을 가른다: A·C → 판정 직후 증명서 직행(루프 미진입) / B 루프 K 소진 → 「표현 탐색 실패 · 원문 유지 + 실증 자료 안내」. 🔄 **생성 실패는 D-59 의 불가 사유(A/B/C · 주장의 성질)가 아니라 루프의 결과 상태**이므로 A/B/C 에 넷째를 더하지 말고 `outcome` 축(통과 / 증명서 / 탐색실패 / 보류)으로 따로 둔다 |
| **R3** | **위험도 순서형에 「적법/없음」 값** — 4단계(형사·과징금·업무정지·주의)에 0 이 없다 | `max(코드 하한, 인코더)` 의 적법 하한이 없고, 골든셋·QWK 는 R0~R4 5값, `judgment.risk_*` 는 무제약 Integer | **R0 포함 5값**을 사실원장에 고정하고 화면 배지는 4단계로 접는다. `CHECK (risk BETWEEN 0 AND 4)`. 프론티어 잔여 리스크는 별도 연속 산식 |
| **R4** ✅ **D-127** | **「근거 불일치」의 뜻** — 7-2(코드↔모델 2등급 차 = 보류 트리거) vs 상태 스키마(인용 검증 실패) vs 5-6(차단·재생성) | D-124 픽스처 #3 을 두 팀이 다르게 구현한다. 실제로 필요한 상태(**조문 검색 실패**)는 정의가 없다 | 상태 = `확정 / 보류(사유코드) / 근거없음(조문 검색 실패 — 위험도·유형 유지 + 검토 권고)`. 7-2 표와 사실원장을 같은 문장으로 |
| **R5** ✅ **D-128** (b안) | **D-96 동의 ② ↔ D-122 NOTRAIN** — 「품질 개선 활용 → 학습 데이터로 쓸 수 있다 (`consent_train`)」 vs 「사용자 입력 문구는 학습·색인에 들어갈 수 없다」 | 게이트 35 를 구현하면 `consent_train` 이 죽은 필드, 약관 문구 두 벌 | 하나를 고른다. NOTRAIN 유지 → 동의 ②·`consent_train`·D-103 ④ 학습 행 삭제 / 동의 ② 유지 → D-122 를 「기본 NOTRAIN · 동의 ② 에서만 해제」로 개정 |
| **R6** ✅ **D-126** | **K 의 의미** — 5-8 「K회 재시도」(초기+2=총 9회) vs 6-2 「3×2=6회」 vs 스키마 `attempt ≤ K` vs `models.py attempt` 0-base. 주장 원장 즉시 거부·인용 차단·사후 대조 재생성에 **상한·카운터 없음** | sLLM 호출이 예산을 넘거나 서술 재생성이 무한 루프. D-124 라우터 단독 테스트의 종료 규칙이 없다 | `attempt` 0-base · 총 라운드 = K+1 을 사실원장에 고정. 세 거부를 **한 카운터**로 합산. 서술 재생성은 1회 후 「슬롯만 렌더」 폴백 |
| **R7** | **9/11 Phase 1 게이트 조건** — 「유형별 Recall ≥ B+0.15」인데 harness 마감 9/10 · 인코더 학습 3W 시작 · D-124 skeleton 은 「모델도 판정 로직도 없이」 | 9/11 에 인코더도 룰 baseline 도 없어 **판정 불가** → 8-3 「예외 없음」 그대로면 Phase 2 착수 데드락. 전환 데드라인(9/11)도 근거 없이 지나간다 | 두 안 중 하나 — **(a)** 9/11 게이트를 「skeleton e2e + **룰 매칭 baseline(B) 측정** + 30건 검증」으로 재정의하고 인코더 Recall(B+0.15)은 별도 게이트로(⚠️ 9/28 에 얹으면 Phase 2 게이트와 겹친다 — 9/23 추석 전이 후보) / **(b)** 8-3 의미를 지키고 Phase 1 게이트 날짜 자체를 옮긴다(9/11 → 9/23, 전환 데드라인은 9/11 유지 · 판정 입력은 30건 검증). 어느 쪽이든 skeleton 정의에 「룰 매칭 노드만 실물」을 넣어야 B 가 나온다 |
| **R8** | **실사례 홀드아웃 출처** — 4-8 `test_holdout = [kfia_approved, mfds_press, mfds_sanctions]` 인데 `kfia` 는 **G1 배제**(D-110), `mfds_press` **본문 0**, `mfds_sanctions` 는 U1 학습·4층 검증·경제성 산출에 **삼중 사용** | 홀드아웃이 비거나 누수. 유형별 30건(D-40)이 성립 안 해 발표 지표 1·2 가 「측정 불가」 | 🔄 **출처 분리(기관이 달라 편향이 독립) 원칙은 지킨다** — 1순위 홀드아웃 = `mfds_sanctions` 전량(광고 유형만) + Meta 수기 100~200(S4-04) + `mfds_press`(공공누리 판정 후). `mfds_sanctions` 는 **평가 전용**으로 못 박고 학습·경제성 산출에서 뺀다(경제성은 `ftc_decisions_body` 로 충분). **`doc_id` 분리는 폴백**이며 그때는 「출처 분리 F1」이 아니라 「문서 분리 F1」이라고 지표 이름을 바꿔 적는다. `train ∩ test_holdout = ∅` 게이트 |

---

## 2. 판정·생성 파이프라인

| # | 심각도 | 종류 | 위치 | 문제 | 실패 시나리오 | 제안 |
|:-:|:-:|:-:|---|---|---|---|
| P1 | 상 | 논리오류 | 3-2·3-3 · 사실원장 「판정 축」 · D-123 | → **R1** | | |
| P2 | 상 | 논리오류 | 5-8 ④ · D-32 · D-59 · 상태 스키마 | → **R2** | | |
| P3 | 상 | 누락 | 사실원장 「판정 축」 · 7-2 · 5-8 ③ · `models.py risk_floor` | → **R3** | | |
| P4 | 상 | 위험 | 2-6 · D-103 ① · D-120 · D-83 층 3 | 「템플릿 생성기는 판정을 아예 모른다 — 객체인지만 본다」 + 공존 규칙 R-D1 은 「문서 안에서만」. 층 3 예시 세 문장(「위암 진단」·「매일 양파즙」·「검사 결과 좋아짐」)은 **각각 단독 승인**되고 C 에서 슬롯 셋에 꽂히면 R-D1 이 어디서도 안 돈다 | 승인 배지 셋을 단 질병 치료 표방 상세페이지가 나간다 — D-103 이 막으려 한 「배지가 위반을 세탁」 그 형태. **승인 시점과 조립 시점의 문서 경계가 다르다** | 조립 산출(원고)을 문서 1개로 보고 **판정 코어의 층 3 을 조립 완료 시점에 한 번 더 통과**시킨다. 🔄 이것은 「템플릿은 판정을 모른다」(D-103 ①)의 **예외**이지 우회가 아니다 — R-D1 은 판정 논리이므로 「코드 연산이라 판정이 아니다」라고 말하면 같은 규칙이 두 벌이 된다(D-90). D-103 ① 을 「슬롯 채움은 판정을 모르고, **조립 완료는 문서 판정을 받는다**」로 개정. 발동 시 제거 불가 「문서 수준 경고」. 승인은 `(sentence_id, sha256)` 바인딩 |
| P5 | 상 | 모순 | 7-2 · 6-3 · 3-5 ③ · 상태 스키마 · 5-6 · D-124 | → **R4** | | |
| P6 | 중 | 논리오류 | 2-3 · D-82 · D-61 | 「카테고리 확신이 낮으면 가장 넓은 법령 집합을 걸고 Abstention」 — 보류가 **결과 전체**인지 자격 의존 판정에만 붙는지 없다 | 결과 전체가 보류면 「위암 치료」같은 C 절대형이 「보류」 뒤에 숨는다(안전 방향 역전). 최댓값 하나로 접으면 비식품에 형사 배지 | D-61 대로 **카테고리별 분기를 나란히 출력** · 상태=보류 · 위험도·유형은 지우지 않는다. 교정 UI 는 좁히는 방향만 |
| P7 | 중 | 논리오류 | D-120 개정분 ① · 결정표 「단순 광고문구」 | 「긴 글을 고르면 검사가 는다 → 자기 신고가 미탐을 만들지 않는다」는 **한 방향만** 논증. 「단일문구 여러 개」를 고르면 층 3 이 꺼진다 = 자기 신고가 통과 근거(D-61 금지선). 「단순」 2문장 이상에 R-D1 적용 여부 불명 | 후기형 긴 글을 「추가」로 3항목에 나눠 넣으면 R-D1 미탐 | 「여러 개」 모드에서도 각 항목 **내부**는 문서 1개로 R-D1 적용, 항목 **간** 발동 시 「경계 선언 재확인」 경고. 단순 = 긴 글과 처리 동일로 명시 |
| P8 | 중 | 모순·누락 | 5-8 ④ · 6-2 · 상태 스키마 · `attempt` · 3-3 · 5-6 · 3-5 ② | → **R6** | | |
| P9 | 중 | 모순 | D-122 ↔ D-96 ② · D-103 ④ | → **R5** | | |
| P10 ✅ D-127 | 중 | 누락 | D-120 「대량」 · D-105 ③ · 상태 3종 | 배치 = 「for-loop + 집계」인데 항목 57번이 타임아웃·예외로 판정을 못 마쳤을 때의 값이 상태 3종에 없다 | 오류 행이 엑셀 리포트에 「특이사항 없음」 또는 「보류」로 세탁. 보류(모른다)와 오류(안 봤다)가 같은 칸 | 상태 enum 에 `미판정(오류·타임아웃)` — 통과로 집계 금지. 리포트에 미판정 건수 열 필수 |
| P11 | 중 | 논리오류 | D-44 · D-10 · 5-8 ① · 6-1 | 「숫자 필드가 스키마에 없으므로 모델이 생성할 수 없다」 — JSON 문법은 **필드**를 막지 문자열 안의 숫자를 못 막는다. 대체 문구 본문은 원문 수치를 보존해야 한다(「197,500원」) | 「문법으로 막았다」가 심사 질문 하나에 무너진다. 사후 대조를 「보험」으로 두면 완화 수치 변조(「약 5천만원」)가 통과 | D-44 주장을 「슬롯 밖 숫자 **필드** 없음」으로 좁히고 3-5 ② 대조 게이트를 **주 방어**로 승격. 주장 원장에 수치 항목(생성 수치 ⊆ 원문 수치 ∪ 제품사실) |
| P12 | 중 | 위험 | D-124 · 상태 스키마 TODO | 「의미 검증은 골든 픽스처 회귀」 + 「픽스처를 실제 출력으로 갱신하는 규칙」 → 출력이 픽스처를 갱신하면 **자기참조**라 드리프트를 못 잡는다. 픽스처 6~8건은 R1·R2·R3·R4 가 정해져야 쓸 수 있다 | 픽스처가 빈 채로 9/10 을 넘긴다 | R1~R6 은 데이터 무관 → **오늘 결정**해 `contracts.py` 에 반영. 픽스처 갱신은 「골든셋 정답 대조 통과 + 2인 확인」 조건부만 |
| P13 | 하 | 누락 | 전처리 사양 1-3 `offset_map` | 시작 오프셋만 준다 — NFKC 확장(`㈜`→`(주)`)·결합 문자에서 스팬 **끝** 좌표가 어긋나 P2-9 `(offset, length)` 를 못 채운다 | | 끝 오프셋 배열 추가 |
| P14 | 하 | 모순 | D-123·D-124 가 인용하는 「D-100(근거 조문 필수)」 | 실제 D-100 은 매트릭스↔레지스트리 ALIAS·게이트 19(소스 이용조건 근거)라 **축이 다르다** — D-99·D-105 가 경계한 「같은 단어 다른 뜻」 | | 인용을 D-30(주장 원장)·5-6(인용 검증)으로 |
| P15 | 하 | 모순 | 사양 1-5 `evasion_flags` · 3-4 | 「인코더의 입력 피처」인데 화면은 「한 단계 올렸습니다」로 **코드 규칙처럼** 귀속. 인코더 피처면 감사 불가(「블랙박스 실격」), 코드 규칙이면 +1 상한·합산 규칙 미정 | | 코드 하한 쪽 규칙으로 두고 +1 상한 명시 |
| P16 | 하 | 모순 | D-83 층 1 「인정 기능성 보유 여부 — 우리가 판별」 vs D-61 | 텍스트에서 판별 불가한 값 | | 층 1 문구를 D-61(식별자 입력 또는 분기 출력)에 맞춘다 |
| P17 | 하 | 확인 | 위험도 순서 「과징금 > 업무정지」 근거 | 식품표시광고법에서 과징금은 영업정지 **갈음**이라 순서가 반대일 수 있다 | | 검증 ② 에서 함께 확정 |

---

## 3. 데이터 · 거버넌스 · DB

| # | 심각도 | 종류 | 위치 | 문제 | 실패 시나리오 | 제안 |
|:-:|:-:|:-:|---|---|---|---|
| D1 | 상 | 논리오류 | 수집전처리 §4-8 · 6-2(a) · D-110 · 사실원장 | → **R8** | | |
| D2 | 상 | 논리오류 | DDL `ck_golden_injected_not_holdout` · 사양 규약 9·10 · 2-4 · D-91 | 표면 변형(S1~S5)은 합성이라 `injected` → holdout 불가 → 규약 10(같은 split)에 의해 **S0 원형도 holdout 에 못 들어간다** | 「정규화 층 이득 = Recall(S0) − Recall(S1..S5)」를 **train/dev 에서만** 잴 수 있다. 규약 10 은 DB 제약이 없어(CHECK 는 다른 행을 못 본다) 어기면 조용히 누수 | `origin` 에 `surface_synth` 추가 + 「S0 가 `real` 이면 그 표면 변형은 holdout 허용」. `surface_of` split 일치는 트리거로 |
| D3 ✅ D-128 | 상 | 모순 | DDL §3-6 vs 부록 SQL · `models.py consent_train` · D-96 · D-122 | ① DDL 본문은 골든셋 `consent` 열을 서술하나 **SQL 에 컬럼이 없다** ② `golden_sample.fragment_id NOT NULL` → 사용자 입력은 소스·프래그먼트가 없어 **적재 자체가 불가** ③ = R5 ④ `NOTRAIN` 은 `source_constraint.flag` 인데 붙일 `source_id` 가 없어 **죽은 열거값** | `consent_train=true` 사용자가 있어도 학습 경로가 없고, 게이트 35 가 구현되면 consent 행을 막는다 | R5 로 정리한 뒤: 사용자 유래가 필요하면 `provenance='user'` 가상 source + `CHECK (fragment_id IS NOT NULL OR provenance='user')`. NOTRAIN 은 소스 플래그가 아니라 **저장 경로 상수** — 게이트 35 가 그 상수를 본다 |
| D4 ✅ D-129 | 상 | 누락 | `WorkDoc`·`Judgment`·`BatchRun`·`SlotAssignment` · DB_스키마 런타임 층 · D-96 · D-68 | ① `work_doc` 에 소유자·세션·만료 열이 없다 — 「세션 종료와 함께 지운다」의 **지울 키가 없다** ② `judgment` 는 `subject_id` 만 갖고 `doc_id` FK 가 없어 문장 삭제 시 **고아 행** ③ 업로드 CSV·이미지 **저장 경로가 어디에도 정의되지 않아** 게이트 35 가 검사할 경로가 없다 ④ DB_스키마 W1 필수 `judgment.is_public`·`screened_at`(D-68) 이 ORM 에 없다 | 동의 없는 사용자의 원문은 지워져도 판정 행이 남아 D-96 수명 규칙이 반쪽 | `work_doc(owner_id, session_id, expires_at)` · `judgment.doc_id FK CASCADE` · `upload_blob(doc_id, path, kind, notrain)` · `is_public`·`screened_at` |
| D5 | 중 | 논리오류 | 집행계약 §5 · 사양 §5 · `v_publishable_golden` · D-71 | 「공개 판정은 행이 만들어진 시점의 사실로」— 근거가 「캐스케이드 뒤 조인이 달라진다」인데 골든셋 행도 CASCADE 라 **함께 사라져 조인할 행이 없다**. 남는 근거(등급 변경)는 방향이 위험 | D-102 처럼 **뒤늦게 제약이 강화**되면 행은 `redistributable=true` 로 고정 → `v_publishable_golden` 누출. 안전 방향 단조성 역행 | 복사는 유지하되 뷰를 `g.redistributable AND NOT EXISTS(NOREDIST via fragment→source)` **AND 결합**. 불일치 행 게이트 |
| D6 | 중 | 모순 | 사양 §0 vs 수집전처리 §4-0 · §4-4 | 사양 「[P3] 마스킹이 [P2] 정규화보다 먼저 — 뒤에 오면 맵이 어긋난다」 vs 기획 4-0 `[P2] 정규화 → [P3] 마스킹`. 토큰도 `<ORG>`·`<BRAND>` vs `[업체]`·`[상표]` | 구현자가 어느 문서를 따르느냐로 `offset_map` 기준 텍스트가 달라진다 | 기획 4-0 을 사양에 맞춰 정정 · 토큰 1종 확정 · DDL `sentence.raw` 주석에 「마스킹 후」 |
| D7 | 중 | 모순 | D-17 · 규약 2·4 · D-92 · 집행계약 「G2 raw 삭제」 · 사실원장 | D-17 「등급 무관 원문 미보관」 vs 규약 2 「raw 무손상 저장·덮어쓰기 금지」 vs D-92(미보관은 G2 만). 실제로 `ftc_decisions_body` 1,087건 **미마스킹 원문이 `data/raw/` 상주**. G2 「재실행은 재수집」인데 규약 4 「sha256 같으면 스킵」 → 재수집이 스킵돼 **재추출 불가** | 업체명·대표자명이 영구 보관돼 D-17 이 문서에서만 성립 | 🔄 **선택지 둘** — (a) 마스킹을 raw 착지 전으로: D-17 을 글자대로 지키지만 raw 의 sha256 이 원천과 달라져 **재현 검증(규약 4)이 약해지고 마스킹 오류는 재수집 없이 복구 불가** · 규약 2 「수집기는 파싱하지 않는다」와 충돌 / (b) D-92 대로 G2 만 미보관, G3 raw 는 **마스킹본을 별도 경로**에 두고 raw 접근을 `RAW_READERS` + 보관 기간으로 제한 — 문서 쪽(D-17 「등급 무관 원문 미보관」)을 D-92 에 맞춰 개정. **(b) 가 비용이 낮다.** 스킵 조건 = `sha256 동일 AND 파생물 존재` · `event=delete` 뒤 첫 fetch 강제는 어느 쪽이든 |
| D8 | 중 | 누락 | DDL `source_use` · `v_current_chunk` · 청크_스키마 `source_grade` | 「G2 는 U2·U3 deny — 정의다」인데 DB 에 `chunk`↔`fragment.grade` 제약이 없고 `v_current_chunk` 는 `excluded=false` 만 필터. `source_grade` 필드는 DDL 에 없다 | G2·G0 청크가 적재되면 검색·인용에 그대로 노출 | `v_current_chunk` 에 `grade='G3' AND U2 allow` · 삽입 트리거로 G3 외 거부 |
| D9 | 중 | 누락 | DDL `golden_sample` · §4-6 ⑥ · §4-8 규칙 1 | 주입본과 V0 원본을 잇는 `base_sample_id` 가 없다. 주입 원본은 `product_fact` 유래라 `sent_id NULL` → 분할 키 `doc_id` 규칙을 적용할 수 없다 | V0(holdout 허용)과 그 주입본(train)이 다른 split 으로 — 한 스팬만 다른 근사 중복 누수 | `base_sample_id FK` · 분할 키 `COALESCE(doc_id, base_sample_id)` · 짝 split 일치 트리거 |
| D10 | 중 | 모순 | 사실원장 「판정 축」 · DDL `risk_t` R0~R4 · §4-6 ④ QWK · `risk_floor Integer` | → **R3** | | |
| D11 | 중 | 누락 | `Judgment.law_version String(40)` · `v_current_chunk`·`v_risk_lookup` · 청크 TODO · `segment.source_set_version` | `law_version` 은 단일 문자열인데 evidence 는 다법령·다 `effective_date`. 뷰는 전부 **현행만** — as-of 조회가 없다. `source_set_version` 은 기획서·DB_스키마가 세그먼트 열로 요구하나 DDL 은 `dataset_manifest` 에만 | 「언제 무엇으로 판정했나」는 남지만 **그 버전으로 재실행**할 수 없다 → D-103 「재검증 대기」 비교 불능 | `law_version` → JSONB `{law_id: effective_date}` · `v_chunk_asof(d)` · `segment.manifest_id FK` |
| D12 | 하 | 위험 | `ck_segment_k_anon`(상수 20) · 사실원장 `K_MIN (1W 확정)` | `member_count` 는 빌드 코드의 **자기 신고 정수**라 CHECK 가 실제 인원을 검증 못 함. K_MIN 「1W 확정」 시한이 지났는데 미확정 | | 산출 코드를 게이트로 고정 · K_MIN 확정 티켓 |
| D13 | 하 | 위험 | `dict_entry UNIQUE(dict_kind, term)` + `fragment_id NOT NULL` | 고시(G3)와 협회 해설서(G2)에 모두 있는 어휘가 **한 프래그먼트에만** 귀속 → 협회 회수 시 캐스케이드가 **고시가 정한 사실까지 지운다** (D-16 역행) | | `dict_entry_source(entry_id, fragment_id)` 다대다 |

---

## 4. 운영 · 배포 · 보안 · 평가 · 일정

| # | 심각도 | 종류 | 위치 | 문제 | 실패 시나리오 | 제안 |
|:-:|:-:|:-:|---|---|---|---|
| O1 | 상 | 논리오류 | 8-3 · 사실원장 「일정」 · D-62 · D-65 · D-94 · D-124 · D-77 ⑥ | → **R7** | | |
| O2 | 상 | 모순 | D-96 · D-103/D-119 · 7-1 | D-96 「기본 보관 안 함 · 거부해도 잃는 기능 없다」 vs 진입점 C 「승인 문구가 문서로 쌓이고 템플릿이 조립」 — C 는 텍스트의 **영속 보관을 전제** | 동의 ① 거부 사용자에게 C 는 빈 슬롯(「미검수」)만 남는다 → 「동의가 기능의 대가가 되면 안 된다」 위반 | C 진입을 「문서 저장 = 사용자의 명시적 저장 행위(동의 ① 과 동일 의미)」로 재정의 · D-96 형태도에 C 분기 · 발표의 「거부해도 잃는 기능 없음」은 A·B 한정 |
| O3 ✅ D-129 | 상 | 누락 | 7-3 `langgraph-checkpoint-postgres` · 작업 테이블 · D-96 · 보안점검 P1-4/P1-5/P1-6 · D-66 | 체크포인터가 그래프 상태(원문·생성문)를 Postgres 에 영속, 생성은 작업 테이블에 저장 — **어디에도 checkpoint·job 행 파기가 없다.** D-66 「A 는 가입 없음」인데 P1-5 IDOR 대책은 `WHERE user_id = :me` | 미동의 원문이 checkpoints 에 무기한 잔류 · SQLAdmin 에서 열람 → 「기본 미보관」이 DB 층에서 무너진다. 비회원 결과는 소유자가 없어 쿼리 검증 불성립 | 판정 종료 시 thread checkpoint 삭제(또는 TTL) · job 원문 컬럼 분리 후 완료 즉시 파기 · doctor 검사 · 비회원 결과는 서명 토큰 + 세션 바인딩 + 짧은 TTL |
| O4 | 상 | 논리오류 | 6-2 ① · 6-3 「교차 불일치율」 · D-36 · D-09 · D-123 | 「룰 매칭은 인코더와 오류가 독립 → 상관 오류가 구조적으로 깨진다」 — 그러나 룰 사전은 판정 코어의 **코드 하한**이고 생성기는 그 코어(룰 포함)를 통과하도록 최적화된다 | 통과분에서 A/B 불일치율은 구조적으로 0 에 수렴 — 판정기 B 는 리워드 모델의 일부라 독립 판정기가 아니다. 실질 방어는 사각지대율(사람)뿐 | 교차 불일치율은 **실사례 홀드아웃(판정 평가)에서만** 보고 — 거기서는 룰↔인코더 독립 논거가 성립한다. 🔄 「구조적으로 깨진다」는 **판정 평가에 한정해 유지**하고, 생성물 평가에서는 「사각지대율 + GPT 제3 판정기(D-78 ②)」가 방어라고 나눠 적는다 |
| O5 | 상 | 위험 | 사실원장 「수집 실측」 · 6-2(a) · 6-3 지표 1·2 · D-65 · D-110 · D-118 ⑤ | 1층 737건은 공정위 = 「부동산·통신의 문장형 광고」. 절대 기준 유형 「질병 표방」은 식약처 영역인데 `mfds_sanctions` 첫 장만 · `mfds_press` 본문 0 | 9/10 검증 ③ 이 737건만 세면 질병 표방 < 30 → D-65 「기존 유형 미달 시 확장 금지」 발동 + 발표 지표 1 단독 표기 유형이 「측정 불가」 | → R8 + `mfds_sanctions` 전량 수집 |
| O6 | 중 | 모순 | 6-3 L1 「위법 검출 Recall — 결함 주입 정답과 대조」 · `ck_golden_injected_not_holdout` · 8-4 ① | 게이트(B+0.15)를 어느 셋으로 재는지 미정. 주입셋이면 CHECK 위반·자기 채점, 실사례면 O5 | | 주입셋 = 개발·회귀·Span F1 전용 / 실사례 = 보고 지표·게이트 로 6-3 표 수정 |
| O7 | 중 | 누락 | 6-3 L3 예산표 · P2-11 · D-120 · D-105 · 7-3 | 「판정 소계 1.1초 / p95 3초」가 **문장 1건인지 요청 1건인지** 미명시. 7-3 은 리랭커가 매핑·사례 2회인데 예산표엔 1회 | 긴 글 10문장이면 4 vCPU 직렬 11초+ · 배치 CSV 는 수백 배 → p95 3초는 단문 데모에서만 성립 | 예산을 「문장당」으로 명시 · 문장 배치 추론 · 긴 글·배치는 작업 테이블 비동기 · W2 실측에 「10문장 요청 p95」 |
| O8 | 중 | 누락 | 7-6 · D-79 · D-85 ② 「판정 스택 ~1.5GB」 · 배포계획 §1·§6 | 1.5GB = Postgres 512 + 인코더·리랭커 int8 700 + OS 300 — **KURE-v1(0.57B) 임베딩이 빠져 있다.** 「생성까지 올린다」에서 llama.cpp CPU 점유와 판정 p95 동시 보장 규칙 없음. 데모 노트북 사양·예산 없음 | 생성 1건이 4코어를 다 쓰면 동시 판정 p95 붕괴 · 노트북 동시 구동이 발표 당일 처음 검증 | W2 실측에 KURE 포함 메모리 피크 · 추론·sLLM 컨테이너 `cpuset` 분리 · 배포계획 §6 에 노트북 최소 사양·실측 시점(7W) |
| O9 | 중 | 위험 | 배포계획 §2·§5 「리허설 9/22~9/26」 · 공휴일 · 8W+9W 작업일 3일 · D-47 「GGUF 는 W9」 · 8-5 공동작업 3 | 9/22~26 중 9/24·25 공휴일 · 9/26 토 → 실 작업일 2일. 동결 뒤 3일에 최종 배포 2회·GGUF 변환·적대 표본 100건 검수·직전 재학습·PT·시연영상 | 발표 지표 2 의 「실질 통과율」 문장을 못 만든다 | 리허설 9/21~9/23 · 재학습 마감 10/9 · GGUF·사각지대율 검수를 7W 이전으로 |
| O10 | 중 | 모순 | 7-1 관리자 #4 「배치 검수」 · D-76 ①③ · D-105 | D-76 「관리자는 구조화 값 집계만 · 행 단위 나열 금지」인데 배치 검수(CSV → 문장별 판정 → 엑셀 = 원문 행)가 **관리자 화면**에 있다. D-105 스스로 「URL·CSV 는 진입점 A 의 입력 취득 방식」 | 클라우드 에디션에서 운영자가 사용자 원문 행 단위 리포트를 생성·열람 → 차별점 ④ 위반 | 🔄 **결정 사항이지 정정이 아니다** — D-105 가 배치를 관리자 UI 담당(사람)에게 배정했고 팀원 산출물이 그 위에 있다. 선택지: (a) 배치 검수 UI 를 사용자 화면(A)으로 옮기고 관리자에는 `batch_run.summary` 집계만 / (b) 관리자 화면에 두되 **온프레미스 에디션 전용**으로 표시하고 클라우드 에디션에서는 비활성(D-107 「비활성 + 사유」). 열람 경계(D-76)를 지키는 것은 둘 다이고, 팀원 작업량은 (b) 가 작다 |
| O11 | 중 | 위험 | 사실원장 「발급 현황」(4종 미신청 · 점검일 9/1 경과) · RunPod 슬롯 「정한다」 · D-94 · D-78 ④ | 학습은 3W(9/11)부터인데 미신청. 슬롯 규칙 내용이 없다 | 발급 지연 시 T2·T3 전체 대기 · 동시 실행 충돌·볼륨 유휴 과금으로 $300 조기 소진 | 발급일·만료일 즉시 기입 · 1슬롯 순번 큐(팀원당 N GPU-h · 야간 배치) · 파드 종료 시 볼륨 삭제를 D 번호로 |
| O12 | 중 | 모순 | 9장 철수 조건 2·5·7 · D-110 · D-65 ②③ · 설계가정검증 | 철수 2 「심의 통과 문구 100건」 소스가 G1 배제로 **이미 미달**인데 발동 기록 없음. 철수 5(시정 페어 50건)·D-65 ②③ 이 9/10 검증 5건에 없다. 철수 7 「5주차」는 폐기된 W 표기 | 전환 데드라인 9/11 이 입력값 없이 지나간다 | 9/10 검증에 ⑥ 문구 인용 비율 · ⑦ 시정 페어 비율 추가 · 철수 조건 판정일 9/11 명시 · 철수 2 발동 처리(또는 `mfds_hf_*` 대체 지정) |

---

## 5. 검토했지만 문제 없음 (재검토 불필요)

- 3-5 단조성 6지점(키워드 선별·주장 원장·완화 어휘·인용 검증) — 「차단·삭제만 가능, 되살리는 경로 없음」으로 일관 · D-123 정합.
- 진입점 B 주장 원장 — 원문이 없어 허용 집합이 제품사실뿐 → A 보다 엄격. 별도 규칙 불필요.
- 전처리 사양의 P3→P2→P4 순서와 비파괴 원칙, N4 를 사전 어휘 구간에만 적용 — 정합 (단 수집전처리 기획 4-0 이 반대로 적혀 있다 · D6).
- D-119·D-103·D-104·2-4 ④ — 이미지는 C 배치용 · 판정 경로 외부 호출 0 · OCR 미채택, 네 문서가 같은 말 (P4 의 조립 시점 층 3 만 빠짐).
- `ck_surface_pairing` 이 규약 9 를 정확히 구현 · `judgment.subject_type` 값 집합이 DB_스키마·ORM·게이트 21 에서 일치 · 캐스케이드 경로 11테이블 전부 CASCADE · DDL 18테이블 실물 일치 · 층 수 6 일치.
- GPT API 제3 판정기(D-78 ②) — 입력이 마스킹된 공개 의결서 문구이고 산출물이 모델에 안 들어가므로 QUERYLOG·doctor 16 과 경계 성립.
- AWS 비용 $99/예산 $210 세 문서 일치 · 노출 경계(Caddy 80/443 · postgres 호스트 포트 없음 · admin 127.0.0.1) 세 문서 일치 · 과금 경계 D-69 서술 정합.
- D-120 입력 형태 선언 — 기본값을 검사가 늘어나는 「긴 글」로 둔 논리는 D-09 와 일관 (P7 의 반대 방향만 보강).

---

## 6. 처리 순서 제안

```
오늘~9/5   R1~R6 결정 → 사실원장 「판정 축」·상태 enum·K 정의 → contracts.py · D-124 픽스처
9/5~9/8    R7 게이트 재정의 (8-3 · 사실원장) · R8 홀드아웃 재정의 (4-8 · 6-2) → 게이트 train∩holdout=∅
9/8~9/10   D3·D4 (ORM: work_doc 소유·만료 · judgment doc_id · upload_blob) · O3 (checkpoint 파기) · D6 (4-0 순서)
9/10 검증  O12 항목 ⑥⑦ 추가 · O5 mfds_sanctions 전량 · mfds_press 2인 판정
이후       D5·D8·D9·D11 (DDL 뷰·트리거) · O7·O8 (W2 실측 항목) · O9 (리허설·재학습 일정) · O10 (배치 UI 위치)
```

---

## 7. 재검토 — 이 문서의 권고가 어디까지 「정정」이고 어디부터 「선택」인가

> 🔄 **2026-09-03 저녁 반영** — 「정정에 가깝다」 열의 R1·R2(outcome)·R4·R5·R6·D3·D4·O3·P10 을 **D-125~D-129** 로 등재하고 사실원장·상태 스키마·`models.py`·DB_스키마·DDL 주석·집행계약·보안점검·기획서에 전파했다. R3(위험도 값 개수)·R7·R8·P4·D7·O10 은 **선택**으로 남긴다 — `models.py` 의 위험도 범위 CHECK 도 그래서 걸지 않았다.
>
> **2차 재검토 (정정한 권고 자체를 다시 봄)** — ① KcELECTRA 미등재 지적: D-84 ③ 이 비교군으로 넣었고 `models` 섹션에 없음을 재확인 → 유지 ② 배포 담당 미배정: 사실원장·기획서 8-5 어디에도 배포 담당이 없음 재확인 → 유지 ③ R8 폴백 강등: 기획서 6-2(a)·9장 리스크표가 「출처 분리」를 상관 오류 방어 ①로 명시 → 유지 ④ P4 D-103 예외: R-D1 은 D-83 층 3 의 판정 규칙이므로 「코드 연산」으로 부르면 규칙 두 벌(D-90) → 유지 ⑤ D7 선택지: 규약 2·4 와의 충돌은 실재 → 유지 ⑥ R7 (b)안 9/23: 추석 전 마지막 작업일이라는 것만 근거이고 **3W 중간 발표(9/15) 뒤 8일**이 팀에 맞는지는 팀장 판단 ⑦ 기획서 8-5 점검일 9/17: 「범위 확정일과 같은 날」 이상의 근거는 없다 — 대안은 날짜를 비우고 「3W 말」로만 두는 것.

| 구분 | 항목 | 비고 |
|---|---|---|
| **정정에 가깝다** — 문서가 서로 반대라 어느 쪽이든 골라야 하고, 고르지 않으면 구현이 갈린다 | R1 · R4 · R5 · R6 · D3 · D4 · D6 · O3 · P10 | 단, R4 는 9/3 아키텍처검토가 같은 날 정한 「확정/보류/근거 불일치」를 **다시 여는 것**이라 팀장 재결정 사항 |
| **선택** — 원칙끼리의 트레이드오프라 팀장이 값을 정한다 | R2(outcome 축) · R3(5값 vs 이진+4단계) · R7(a/b) · R8(홀드아웃 소스) · P4(D-103 예외) · D7(a/b) · O10(a/b) | 표에 선택지를 나란히 적었다. 한쪽을 「권고」라 부른 곳은 **비용이 낮은 쪽**이지 옳은 쪽이 아니다 |
| **표현 정리** | P13~P17 · D12 · D13 · O4 | 구현에는 영향 없음 |
| **이 문서가 확인하지 못한 것** | R3 의 「과징금 > 업무정지」 순서(P17) · O8 의 KURE 메모리 실측 · D7(a) 의 마스킹 오류율 | 실측 없이는 방향만 말할 수 있다 |

🚨 **권고를 그대로 적용하면 안 되는 자리 셋** — ① R8 의 초안은 `doc_id` 분리를 1순위로 적었다가 **출처 분리 원칙을 약화**시켜서 폴백으로 내렸다 ② P4 의 초안은 「코드 연산이라 판정이 아니다」라고 적었다가 **같은 규칙 두 벌**이 되어 D-103 개정으로 바꿨다 ③ D7 의 초안은 raw 착지 전 마스킹을 단정했다가 **재현성·규약 2 충돌**이 있어 선택지로 바꿨다. 같은 종류의 오류가 남아 있을 수 있으므로, 채택 시 각 항목의 「왜 문제인가」를 원문에서 다시 확인한다.

> 🚨 **이 문서의 항목은 채택 전까지 결정이 아니다.** 채택하면 D-125 이후 번호로 결정기록에 적고, 「영향」 칸의 문서를 같은 커밋에서 고친다 — 오늘 정합성 정정 17건의 대부분이 「결정은 있는데 영향 문서가 안 따라간」 형태였다.
