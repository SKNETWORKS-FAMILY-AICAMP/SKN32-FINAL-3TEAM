# 결정요청 2026-09-22 — 사람 마스킹의 범위 (제3자 개인 · 공무원)

> 작성 Claude · **판정 대기** — 레지스트리 문언·2인 확인이 걸린 것만 모았다. 코드는 **안 바꿨다**(아래 ⓪ 제외).
> 근거 실측은 전부 클론 B · 사용자 실행 — `derived_manifest.py --pii-triage`(09-22 두 번) · `build/probe_person_0922.py`.
> 🚨 이 문서에는 실명을 적지 않는다. 사람은 「의사 3명」처럼 수로만 적는다.

## ⓪ 판정 없이 한 것 (선언 안의 일)

| 무엇 | 근거 | 커밋 |
|---|---|---|
| 앵커가 놓친 피심인 — 본문 「피심인 X」 + 피심정보내용 이름 자리의 X 를 가린다 | ftc 선언 「업체명」= 피심인 (D-233) · D-248 (a) 와 같은 「원천이 적은 것」 방식 | `b5ce5c9` |
| `law_go_kr` 에서 대표자 밖의 사람(조리실장·점장 …)과 부분 가림(「조○우」)을 가린다 | 레지스트리 문언이 **「대표자명 등 개인 실명」** — 코드가 선언을 못 따라가던 자리 | 이번 커밋 |

## ① 판정 — `ftc_decisions_body` 의 제3자 개인 실명을 가릴까

**상황** — 공정위 의결서 「이유」가 인용한 **광고 문구 안에** 의사 3명의 실명이 「이름 + 원장」 꼴로 있다. 그 인용이
`ftc_stage` · `ftc_layer1_phrases` · `ftc_reason_sample` · **골든셋**(train)에 들어가 공유 저장소로 나갔다(팀 내부 · 인용 문구는 재배포 불가 · D-249).
원문 전체로는 「이름 + 직함」 후보가 원장 16 · 교수 10 · 박사 4 · 약사 7 · 회장 38 · 사장 34 · 실장 22 · 팀장 48 · 변호사 12(서로 다른 값 · 드문 것)이다 —
대부분 **피심인이 아닌** 임직원·광고 속 인물이다(탐침 B절).

**용어** — *제3자 개인*: 피심인(처분받은 사업자)이 아닌 사람. *선언*: 레지스트리 `masking:` 문언 — 무엇을 가리는지의 정본이다(D-54).
지금 ftc 의 선언은 「업체명·상표·피심인 주소」(+ 원천이 가려 주는 대표자명)이고, D-248 ⬜ 는 「제3자 개인 실명은 D-233 상 대상 밖」이라 적었다.
🚨 그런데 코드는 이미 「대표이사 X」·「변호사 X」를 **누구든** 가린다 — 선언과 실물이 어긋나 있다.

| 대안 | 얻는 것 | 떠안는 것 |
|---|---|---|
| **(가) ★ 넣는다** — 선언을 「개인 실명은 피심인이 아니어도 가린다(직함과 함께 나온 사람)」로 개정 · `PERSON_ALL_NAMES` 에 ftc 추가 · 「이름 + 원장·교수·박사·한의사·약사·회장·사장·실장·팀장·변호사」 규칙(「의사」=뜻·「대표」·「직원」 제외) | 광고 속 의사·이유의 임직원이 닫힌다 · 선언과 코드가 한 모양 · 「개인 특정이 먼저」(팀장 09-19)와 맞는다 | **2인 확인**(선언 개정 · 게이트 15) · 오탐 어휘(「계정별 원장」 류)를 막아 가야 한다 · 과잉삭제는 `--stage` 판 대조로 잰다 |
| (나) 인용 광고 문구 **안에서만** | 오탐 최소 · 의사 3명은 닫힌다 | 이유 본문의 임직원은 남는다 · 규칙이 「인용 안/밖」을 알아야 한다 |
| (다) 그대로 (D-248 유지) | 일이 없다 | 의사 3명이 골든셋에 남는다 · 선언과 코드의 어긋남이 그대로다 |

**권고 (가)**. ⛔ 틀릴 수 있는 지점 — 「대표 172」는 뺐다. 그 안의 실명은 남는다(다음 측정으로 따로 본다).

**선언 문안 초안** (`scripts/gen_registry.py` `EXTRA` · 2인 확인 대상 — 클로드는 안 고친다)

> 업체명·상표·피심인 주소 즉시 마스킹 (D-17 · D-257). 🔄 2026-09-2x — **개인 실명은 피심인이 아니어도 가린다**
> (직함과 함께 나온 임직원·광고에 나온 의사 등). 원천이 대표자명을 가려 주지만 우리 쪽 마스킹을 끄지 않는다.

## ② 판정 — 보도자료 제목의 공무원 실명

**상황** — `mfds_press_index` 의 보도자료 **제목**에 「식약처 차장 ○○○」 류 공무원 실명 2건. 넓은 거름(🟡 · 막지 않는다)에만 걸렸다.
`mfds_press` 는 사람 축을 **끈** 원천이다(2026-09-09 판정 · 2인 확인 · 상표명을 사람으로 먹는 오탐 · D-157).

| 대안 | 얻는 것 | 떠안는 것 |
|---|---|---|
| **(가) ★ 남긴다** — 공적 직무 수행 중인 공무원 · 정부가 공개한 제목 | 코드·선언 변경 없음. 반출 검사는 이미 막지 않는다(🟡) | 「개인이 특정되는 자리」 원칙의 예외를 **기록으로** 둔다(이 문서 · D 번호) |
| (나) 가린다 | 원칙이 한 줄 | 사람 축을 켜야 한다 — 09-09 판정을 뒤집고 2인 확인 · 상표명 오탐이 돌아온다 |

**권고 (가)**. ⛔ 틀릴 수 있는 지점 — D-17 · D-257 의 「대표자명」을 「모든 개인」으로 읽으면 (나)다.

## 판정 뒤 할 일

- ① (가) — 권소라 2인 확인 → `gen_registry.py` 문언 → `rebuild` → 클로드가 ftc 규칙 + 반대 대조 → B 에서 추출 → `data-refresh` → `data-publish` → 옛 객체 삭제
- ② (가) — D 번호 한 줄 (예외 기록) · 코드 없음
