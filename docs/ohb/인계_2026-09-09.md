# 인계 — 2026-09-09 · 라벨의 출처를 다시 세우고 첫 지표를 낸 하루 (D-168 ~ D-174)

> 클론 B(개인 PC) · 브랜치 `ohb` · 마지막 커밋 `d68be68`
> 앞 문서: `docs/ohb/인계_2026-09-08.md`
> 🚨 **이 문서를 안 읽으면 헛돌 자리가 셋 있다** — §0.5 를 먼저 읽는다.

---

## 0. 한 줄

**「사람이냐 LLM 이냐」로 이틀을 썼는데, 답은 「조문이 붙인다」였다.**

오늘 절반은 「사례집으로 홀드아웃을 만들자」에 썼다. **D-155 가 09-08 에 이미 같은 결론을
내려 두었고**(사례집=금지 표현 사전), 사실원장 782줄에 수치까지 적혀 있었다. 확인을 먼저
하자는 말 한마디가 그 왕복을 끊었다.

★ 남은 절반이 실질이었다 — **재는 쪽이 고장 나 있던 자리 셋**을 찾아 고쳤다.
셋 다 「초록불인데 아무것도 안 보고 있었다」는 같은 모양이다.

---

## 0.5 🚨 어제와 달라진 것 — 이것부터

### ① `--sheet` 는 이제 **사람이 채운 라벨을 지우지 않는다**

    종전   uv run python launcher.py extract mfds_casebook --dump --sheet 30
           → 검증셋 파일을 **조건 없이 덮어썼다.** 절반 채운 사람이 이 줄을 다시
             돌리면 그 자리에서 사라지고, data/derived 는 커밋이 안 되니 못 되돌린다.

    지금   겹치는 행의 라벨은 **이어받는다.** 잃을 것이 하나라도 있으면 **멈춘다**
           (종료코드 1 · 파일 그대로). 무엇을 잃는지와 어떻게 하는지를 찍는다.

🚨 **인계 문서가 다음 사람에게 시키던 표준 명령이 바로 그 줄이었다.** 겁내지 말고 돌려도 된다.

### ② 검증셋은 **150이 아니라 248**이다

    data/derived/mfds_guide_labelsheet.jsonl      248건  ← 🔄 바뀌었다
    data/derived/mfds_casebook_labelsheet.jsonl    25건

종전 150건은 **중앙 14자 · 10자 미만 47건(31%)** 이었다. 「배앓이」(3자)와
「가스가 차고 복부 팽만감을 느껴 배앓이를 겪습니다」(25자)가 같은 표본에 있었다.
**낱말은 맥락이 없어 사람도 유형을 못 고른다** — 다 붙이고 나서 3분의 1이 못 쓰는 것이었다.

    uv run python launcher.py extract mfds_special_use_guide --sheet 90 --min-len 20
      → 248건 · 중앙 29자 · 10자 미만 0건 · seed 20260908

### ③ 🆕 **골든셋 파이프라인이 섰다 — 순서가 중요하다**

    uv run python launcher.py golden --write
      [P12] 분할 → [P6] 사전 → [P10] 주입 → 물질화

    uv run python scripts/eval_rule.py     # 판정기 B · 첫 실측 지표

🔴 **분할이 맨 앞이다.** 종전 순서(사전 먼저)에서는 사전이 **평가 문구로** 만들어졌다 —
봉인된 평가 문구 118개 중 **118개**가 사전에 있었다. 그 사전으로 매칭기를 재면
외운 것을 맞힌다. 게이트가 이제 그것을 막는다 (D-174).

    data/derived/golden/split_manifest.json   분할 (train / test_sentence)
    data/derived/banned_terms.jsonl           사전 536종
    data/derived/injected_golden.jsonl        주입 902건
    data/derived/golden/golden.jsonl          🆕 **골든셋 1,910행**

### ④ `mfds_press --dump` 가 **열렸다** (열린 항목 H 닫힘)

    uv run python launcher.py extract mfds_press --dump
      → data/derived/mfds_press_labels.jsonl  135줄 · 문구 고유 359종

### 그리고 변하지 않은 것 — `data/` 는 여전히 커밋되지 않는다 (D-19)

새 기기라면 09-08 인계 §0.5 의 ①②③ 을 그대로 따른다. 오늘 추가된 것은 없다.
🚨 `uv sync` · `launcher.py keys` · 원본 수집 · `launcher.py doctor` 순서.

---

## 1. 🔴 지금 열려 있는 것

### A. 🔴 문장 단위 평가 홀드아웃 — **2/8** 이다 (D-174 정정)

여전히 최상위 리스크다. 그러나 오늘 **어디서 오는지가 확정됐다.**

| 원천 | 단위 | 중앙 길이 | 확정 라벨 | |
|---|---|---:|---:|---|
| `mfds_casebook` 인용표현 | **낱말** | 4자 | 232 | 🔴 사전이지 문장이 아니다 (D-155) |
| `mfds_casebook` 글 | 식약처 **서술문** | 34자 | 139 | 🔴 광고가 아니다 |
| **`mfds_special_use_guide` 위반문구** | **광고 문구** | 16자 | 0 | ⬜ 20자↑ **729** · 사람이 붙인다 |
| **`mfds_press` 광고문구** | **광고 문구** | 11자 | 49 | 🚨 유형별 30 미달 |

⛔ **위 표를 보고 「사람이 붙이는 것이 유일한 경로」라고 적었는데 틀렸다.**
`ftc_extract` 가 이미 `근거절`에서 조문을 읽어 라벨을 붙이고 있었다 — 사람도 모델도 아니다.

    ftc 조문 라벨 — 거짓_과장 168 · 소비자_기만 40 · 비방광고 1 · 부당_비교광고 2

★ 그래서 `preprocess/split.py` 로 `ftc` 를 **문서 단위로 봉인**해 평가로 돌렸다 (D-171).

    train 192문서 · test_holdout 203문서

    거짓_과장 72 ✅ · 질병_예방치료_표방 41 ✅ · 소비자_기만 40 ✅
    의약품_오인 38 ✅ · 건강기능식품_오인 32 ✅
    🔴 비방광고 6 · 부당_비교광고 3 (측정 불가) · 후기_체험기_기만 0

**8종 중 5종이 D-40 을 넘겼고 사람 손은 0 이었다.**

⛔ **그 「5/8」이 틀렸다** (D-174 · 같은 날 저녁). **단위를 섞은 수**다 —
사례집은 낱말(중앙 4자)이고 의결서는 문장(중앙 12자)이다. 한 시험지로 세면
두 과제를 평균한 값이 된다. **D-155·D-172 가 경고한 혼동을 그 D 를 쓰면서 다시 밟았다.**

    문장 단위 시험지 (`golden.jsonl` · test_sentence 206행)
      거짓_과장  73 ✅ · 소비자_기만 67 ✅ · 적법(음성) 60
      🔴 부당_비교광고 4 · 비방광고 2 — 측정 불가
      🔴 **어느 단위로도 평가 데이터가 없는 유형 4종** —
         질병_예방치료_표방 · 의약품_오인 · 건강기능식품_오인 · 후기_체험기_기만

**사례집은 시험지가 아니라 사전이다** (D-155). 갈라 쓰려 해도 어느 쪽이든 D-40 미달이라
(질병 41 → 20/21) 데이터의 한계다. **이것이 A 의 실제 크기이고, 오늘 작아진 것이 아니라
정확해졌다.**

★ **첫 실측 지표가 나왔다** — `uv run python scripts/eval_rule.py`

    판정기 B · 사전 411종(단독판정) · 시험지 206행
      거짓_과장   P 0.500  R 0.014  F1 0.027
      소비자_기만  P 0.000  R 0.000  F1 0.000
      🔴 적법 60행 오탐 **0건**

🚨 **낮은 것이 정상이다** (D-156). 이 수가 「그래서 모델이 필요하다」의 근거다.
누수 8행을 빼기 전에는 F1 0.161 이었다 — **여덟 행이 점수의 대부분이었다.**

🚨 남은 셋은 **라벨링으로 안 풀린다** — 원천에 사건 자체가 드물다. 「측정 불가」로
보고한다 (`split_manifest.json` 의 `unmeasurable`).
🚨 그리고 `ftc` 슬라이스는 학습과 **같은 기관**이라 원천 편향은 못 잰다 (`same_source`).

⛔ **사례집으로 홀드아웃을 만들 수 있다고 두 번 착각했다.** 확정유형별 고유 문구가
60·61·58·39 로 D-40 을 넘기지만, **수가 아니라 단위가 문제다.** 같은 문구가 두 유형에
동시에 붙은 것이 13종이다(「다이어트」 = 거짓_과장 이자 건강기능식품_오인) — 다중 라벨이
풍부해서가 아니라 **낱말이 라벨을 감당할 만큼 크지 않아서다.**

### B. ⬜ 검증셋 채우기 — 🔄 **두 파일의 지위가 다르다** (D-172)

    data/derived/mfds_guide_labelsheet.jsonl     248건  확정유형·붙인이·붙인날 비어 있음
    data/derived/mfds_casebook_labelsheet.jsonl   25건  5호만

| 파일 | 용도 | 사람 필수? |
|---|---|---|
| `mfds_casebook_labelsheet.jsonl` 25건 | **평가셋** (`U1: deny`) | 🔴 **필수** — 최우선 |
| `mfds_guide_labelsheet.jsonl` 248건 | **학습 라벨** (`U1: allow`) | 🟡 기준 표본 + 확장 |

⛔ **두 파일이 같은 경고 문구를 찍고 있어서 이틀을 같은 것으로 다뤘다** (D-172).
🔴 **248 은 D-40(평가 규칙)에서 역산한 수다** — 해설서는 학습 원천이라 자를 잘못 댔다.
수 자체는 기준 표본으로 쓸 만해서 그대로 두었지만, 근거는 아래가 아니라 D-172 다.

지시서는 `docs/ohb/라벨링_지시서_2026-09-09.md` 에 있다. 취합·κ 는
`uv run python scripts/label_merge.py data/derived/labels/*.jsonl`.

참고 — 종전 역산 근거:

| 원천라벨 | 후보 | 필요 | 20자↑ 모집단 | 뽑은 수 |
|---|:-:|---:|---:|---:|
| 거짓ㆍ과장ㆍ기만 | 3종 | 90 | 415 | 90 |
| 질병의 예방 치료, 의약품 혼동, 건강기능식품 혼동 | 3종 | 90 | 246 | 90 |
| 부당한 비교ㆍ비방 | 2종 | 60 | 68 | **68 (전량)** |

🔴 세 번째는 **여유가 8** 이다 — 두 유형으로 균등하게 안 갈리면 미달한다. 그래서 표본이
아니라 전량을 넣었다. 붙이고 나서 한쪽이 30 미만이면 **그 유형은 「측정 불가」로 보고한다** (D-40).

🚨 `확정유형` 은 **사람이** 채운다. 추출기가 채우면 홀드아웃이 자기 채점이 된다.
★ 이제 `--sheet` 를 다시 돌려도 채운 것은 이어받는다 (§0.5 ①).

### C. ⬜ `mfds_sanctions` 못 받은 259행 (D-161) — 우선순위 낮음. 09-08 §1.C 그대로.

### D. ⬜ 19시 이후 수집 — `mfds_hf_ingredient` 8 · `mfds_hf_individual` 5. 09-08 §1.D 그대로.

### E. ⬜ AI Hub — `AIHUB_KEY` 비어 있음, 받는 경로 미정. 09-08 §1.E 그대로.

### F. ⬜ 해설서 전화번호 1건 (`033-332-4000`) — `POLICY` 축에 전화번호가 없다.
🔄 `mfds_press` 에서는 **실측 0건**이라 이 원천이 그 결정을 기다릴 이유는 없다.

### G. ⬜ 「AI 광고 생성」 화면 문구 (D-164). 09-08 §1.G 그대로.

### 🆕 N. ⬜ **받아만 두고 아무도 안 읽는 데이터 1,220건**

    mfds_hf_ingredient   773건   FNCLTY_CN      "…콜레스테롤 수치 개선에 도움을 줄 수 있습니다"
    mfds_hf_individual   447건   PRIMARY_FNCLTY "갱년기 남성의 건강에 도움을 줄 수 있음"

`EXTRACTORS` 에 `mfds_hf_ingredient_board` 만 있어서 **이 둘은 읽는 코드가 없다.**
골든셋의 **음성(적법) 표본**과 같은 종류이고 지금 쓰는 465건의 **2.6배**다.
★ DDL 의 `product_fact` 테이블이 이 데이터의 자리다 — DB 를 세운 뒤 그리로 넣는다.
🚨 `mfds_hf_ingredient` 는 `BSSH_NM`(업체명)·`ADDR`(주소)를 담고 있다 — **마스킹 정책 선언이 선행**이다.
⬜ `DAY_INTK_CN` 에 「2.5？3.4 g/일」 — 물음표가 인코딩 깨짐이다. 파싱 때 확인한다.

### 🆕 O. ⬜ 🚨 **판(`__c`)이 생겼는데 「어느 것을 읽나」가 아무 데도 없다**

2026-09-09 밤 재수집에서 처음으로 판이 생겼다 —

    data/raw/mfds_hf_ingredient/page_0007.json  +  page_0007__c20260909.json
    data/raw/mfds_hf_individual/page_0003.json  +  page_0003__c20260909.json

`store.save_raw` 는 「원본은 그대로 둔다(규약 2)」고만 하고 **읽는 쪽 규칙은 없다.**
지금은 이 두 디렉터리를 읽는 추출기가 없어서 **터지지 않았을 뿐**이다.
🔴 누가 `*.json` 으로 glob 하는 추출기를 쓰는 순간 **같은 페이지를 두 번 읽는다.**
⬜ 규칙을 정해야 한다 — 「가장 최신 판만」인지 「원본만」인지. **다음 사람이 밟을 함정이다.**

### 🆕 L. ⬜ `casebook` 25건의 **우선순위가 바뀌었다**

저녁에 사례집이 **사전 쪽**으로 확정되면서(D-155·D-174), 5호 25건을 붙여도
**평가가 아니라 사전 항목**이 는다. 종전 인계는 「`후기_체험기_기만` 의 유일한 평가
씨앗」이라 적었는데 **그 전제가 바뀌었다.** 우선순위를 다시 정해야 한다.

### 🆕 M. ⬜ `부당_비교광고`·`비방광고` 의 평가 원천

`ftc` 에 4건·2건뿐이다. **라벨링으로 안 풀린다** — 원천에 사건 자체가 드물다.
새 원천을 찾거나 발표에서 「측정 불가」로 확정한다.

### 🆕 K. ⬜ 주입본의 **틀 다양성**이 낮다 (D-173)

T7a·T7b 는 171건이 **고유 틀 3종**이다. 이대로 학습하면 모델이 「타사 제품보다」라는
말버릇을 배우지 부당 비교를 배우지 않는다. 실사례(ftc 근거절)에서 표현을 뽑아 틀을 늘린다.
⬜ T3(제품유형 오인)도 미구현이라 `건강기능식품_오인` 이 주입에서 0 이다.

### I. 🔄 `mfds_press` 원천유형 — **레코드 24 / 고유 문구 49**

09-08 의 「24건」은 레코드 기준이다. 고유 문구로는 49종이다.

    16 질병 치료 효능·효과 표방   11 건강기능식품으로 오인   17 신체조직(의) 기능에 효과
     2 의약품으로 오인            3 의약품 또는 건강기능식품으로 오인

🚨 **어느 유형도 30 을 못 넘긴다.** 홀드아웃에 넣으면 우리 규칙이 「못 잰다」고 말한
데이터를 시험지에 넣는 것이 된다.
⬜ 「신체조직(의) 기능에 효과」 17종을 우리 축 어디로 보낼지 미정.

### J. ⬜ 🆕 세션 토큰이 raw 에 210개 있다

`data/raw/mfds_press/` 105 파일 × 2개. **D-159(담지 않기가 가리기보다 앞선다)와
D-92(원본 무손상)가 정면으로 부딪히는 자리**라 정하지 않았다. 만료된 토큰이고
`data/` 는 커밋되지 않아 유출 경로는 없다. 「없는 것이 낫다」는 참이다.

### ✅ 닫힌 것

- **H** — `mfds_press` 마스킹. 2인 확인 기입 완료(2026-09-09) → `--dump` 열림
- `mfds_casebook` 파일명 갈림 — 안 생겼다. 고유 path 1개, 원장 2줄 아니다
- `.gitignore` 의 `.env~` 구멍 — `.env*` 로 덮었고 게이트가 동작으로 검사한다

---

## 2. 오늘의 수치

    ftc          627 문구 / 854 후보 / 283 문서        (09-08 재현)
    mfds_hf      465 레코드 · 인정번호 465 · 훼손 0     (09-08 재현)
    guide      2,728 (위반문구 1,834 · 표시내용 619 · 수정쌍 245)
    casebook     190 · 인용표현 349회 · 원문 250종 · 🔄 정규화 234종
    press        135 · 문구 410회 · 고유 359종 · 원천유형 레코드 24 / 고유 49

🔄 **234 가 [P6] 사전의 항목 수다** (D-117 — 매칭은 정규화문). 250 은 보관 원문의 수다.
「탈모 예방」/「탈모예방」처럼 **띄어쓰기만 다른 쌍 16개**가 접힌다.
🚨 250 으로 사전을 만들면 16개가 중복이다.

---

## 3. 🚨 오늘 나온 「초록불인데 안 보고 있던 것」 셋

### ① `mfds_press` — **문구 표 안**에서 버려지는 열을 아무도 안 셌다

    문구 표 **안**   버린 열 69회 / 11종 · 🚨 모르는 열 16회 / 6종   ← 안 셌다
    문구 표 **밖**   모르는 열 40종                                  ← 세고 있었다

계수기가 `if "문구" not in cols:` 안에만 있었다. `DROP` 바로 위 주석이
「버린 것을 세지 않는 것이 제일 나쁘다」인데 그 일을 하고 있었다.
★ **세는 것만 넣었다. `ROLES` 는 안 바꾼다** — `광고 방법` 2 · `광고기간` 1 은
근거가 아니다 (D-40). 늘어나는지만 본다.
🚨 **문구 표 밖의 40종은 넣어도 레코드가 한 줄도 안 는다** — 처분 4종은
「업무정지│등록취소│시정명령│과징금│경고│합계」 한 줄짜리 집계표다.

### ② `store` — 규약 4 가 영원히 안 걸리고 있었다 (D-168)

    jsessionid 있는 파일 105 / 105 · 파일당 2회 · 길이 64자 (예외 없음)
    토큰 제거로 줄어드는 바이트 126 (전 파일 동일)

**길이가 고정된 세션 토큰**이라 바이트 수는 같고 해시만 갈린다.
⛔ D-149(offset 밀림)로 읽을 뻔했다 — **그건 크기가 변한다.**
→ 저장은 원문 그대로, 「같은가?」만 정규화문으로 (`store.VOLATILE`).

### ③ 게이트 — **실패할 수 없는 단언**이 둘 있었다 (D-170)

`.gitignore` 게이트가 **패턴 문자열**을 보고 있었다. docstring 에 `.env~` 를 이름까지
적어 놓고도 그것이 안 걸리는 채로 초록이었다 — `.gitignore` 가 저지른 실수를
그것을 지키는 게이트가 똑같이 저지르고 있었다.

🔴 **고친 뒤에도 반쪽이 죽어 있었다.** `git check-ignore` 는 추적 중인 파일을
「무시되지 않음」으로 답한다 → `.env.example` 단언은 어떤 경우에도 실패할 수 없었다.
`--no-index` 로 고쳤다.

🔴 **그리고 같은 날 한 번 더 밟았다.** `POLICY` 대조 게이트의 특례를 일반화하면서
「끈다 문언이 있고 꺼져 있으면 통과」로 썼다 — `person` 을 실수로 켜 두어도 낱말
「대표자명」이 문언에 있어 통과한다. **D-170 을 쓴 지 한 시간 만이다.**

🚨 **규칙을 아는 것과 그 순간에 적용하는 것은 다른 일이다.** 아는 것은 세 번 다 실패했고
**반대 대조라는 절차는 세 번 다 잡았다.** 게이트를 고치면 일부러 깨뜨려 보고 커밋한다.

---

## 4. 명령 — 오늘 바뀐 것만

    uv run python launcher.py check                     # 게이트 38
    uv run python launcher.py doctor                    # 원장 ↔ 디스크
    uv run python launcher.py sync                      # → build/project_sync/ (업로드는 Claude)

    uv run python launcher.py extract mfds_press --verify   # 🔄 문구 표 안 계수가 먼저 찍힌다
    uv run python launcher.py extract mfds_press --dump     # 🔄 열렸다
    uv run python launcher.py extract mfds_special_use_guide --sheet 90 --min-len 20
    uv run python launcher.py extract mfds_casebook --dump --sheet 30   # 🔄 이제 안 지운다

    uv run python launcher.py golden --write            # 🆕 분할 → 사전 → 주입 → 물질화
    uv run python scripts/label_merge.py data/derived/labels/*.jsonl   # 🆕 취합 · κ

🚨 `--min-len` 기본값은 **0** 이다. 하한을 기본으로 걸면 이미 넘긴 표본이 조용히 달라진다
(같은 seed, 다른 내용). **거를지는 사람이 정한다** (D-54).

---

## 5. 결정 D-168 ~ D-170

| | |
|---|---|
| **D-168** | 동일성 판정은 **정규화문**으로 한다 — 저장은 원문. D-117·D-152 의 세 번째 적용 |
| **D-169** | 검증셋 표본은 **길이를 함께 본다** — 낱말은 사람도 못 붙인다. 기본값은 0 |
| **D-170** | **실패할 수 없는 단언은 단언이 아니다** — 게이트에도 반대 대조를 돌린다 |
| **D-171** | **라벨의 출처는 셋이다** — 조문 · 구성 · 판단. ①②를 먼저 쓴다 |
| **D-172** | **평가 규칙으로 학습 표본을 재지 않는다** — 같은 모양의 파일이 같은 지위가 아니다 |
| **D-173** | 합성 데이터는 **틀 다양성을 함께 잰다** — 안 재면 말버릇을 배운다 |
| **D-174** | **분할이 사전보다 먼저다** — 시험지로 만든 사전은 자기를 맞힌다 |

전문은 `docs/00_설계결정기록.md`.

---

## 6. ⛔ 오늘 내가 틀린 것

1. **사흘 전 결정을 다시 결정하려 했다.** D-155 가 「사례집=금지 표현 사전」을 이미 정했고
   사실원장에 수치까지 있었는데, 「홀드아웃 v0 를 만들자」로 이틀을 밀었다.
   🚨 **원장을 먼저 읽는 절차가 없었다.** 새 세션은 `00_사실원장.md` 와
   `00_설계결정기록.md` 의 최근 D 를 먼저 본다.
2. **28을 유형별 수로 읽었다** — 사실원장 784줄이 그 오류를 이미 정정해 두었는데 또 밟았다.
3. **`.gitignore` 를 고치면서 그것을 검사하는 게이트가 있는 걸 안 봤다.** 게이트가 깨진 채
   커밋·푸시됐다(`pre-commit` 에는 pytest 가 없다). 다음 커밋에서 고쳤다.
4. **실패할 수 없는 단언을 두 번 썼다.** §3 ③.
5. **「사람이냐 LLM 이냐」로 이틀을 물었다.** 둘 다 ③판단이고, ①조문·②구성을 안 세고
   시작했다. `ftc` 의 조문 라벨은 **이미 코드가 뽑고 있었다.**
6. **평가 자로 학습 표본을 쟀다** — 248 의 근거가 D-40 이었다 (D-172).
7. **비문을 만들어 놓고 수치만 봤다** — 7규칙 × 171건, 6유형 30 통과, 전부 초록.
   표본을 눈으로 보고서야 「갱년기 남성의 건강았어요」가 나왔다 (D-173).
8. 🔴 **사전을 시험지로 만들었다** — 봉인된 평가 문구 118개 중 118개가 사전에
   들어가 있었다. 물질화 직전 검토에서 잡았다 (D-174).
9. 🔴 **「5/8 이 섰다」로 단위를 섞었다** — D-155·D-172 가 경고한 혼동을 그 D 를
   쓰면서 다시 밟았다. 문장 단위로는 2/8 이다.
10. 🔴 **`doc_id` 를 두 곳에서 만들어 어긋났다** — 봉인 60개를 빼려다 449개를 뺐다.
    바로 위 주석에 「같은 규칙이어야 한다」고 적어 놓고 그 줄에서 어겼다.
11. 🔴 **원장에 재지 않은 수를 적었다.** 사전의 `거짓_과장` 을 **407** 로 사실원장에
   써 넣었는데 실제 출력은 **466** 이었다. 도구 출력이 잘려서 못 본 자리를
   **기억으로 메웠다.** 원장은 수치의 단일 출처인데(D-54) 거기에 안 잰 수를 넣었다.
   ★ **원장의 수는 실행 출력에서 복사한다 — 요약에서 쓰지 않는다.**
12. **레코드 수와 고유 문구 수를 섞어 말했다** — press 원천유형 24 vs 49, 사례집 41/38/32/32 vs
   60/61/58/39. **무엇을 세는지 먼저 적는다** (D-160).

---

## 7. 하드 제약 (변함없음)

- AI Hub 원본을 클라우드 컨테이너에 올리지 않는다
- `reviewed_by`·`확정유형` 은 **사람이** 채운다
- API 키 값을 대화·셸 인자에 넣지 않는다 (`launcher.py setkey <이름>`)
- `git` 은 사람이 직접 돌린다
- 원장 3종(사실원장·산출물현황·설계결정기록)은 팀장 전담

---

# 🌙 2026-09-09 **밤** 증보 — 여기부터가 새 대화가 읽을 자리

> 위쪽(낮)은 골든셋·attribution 초안까지다. 아래가 그 뒤에 일어난 전부다.

## 0. 새 기기에서 이어 붙이는 법

```
uv run python launcher.py onboard
```

되는 것을 하고 **안 되는 자리를 이름으로 낸다.** 구조상 안 따라오는 것 셋 —
`.env` 키(D-111) · `data/raw` 원문(D-19) · `git`(팀 규칙상 사람이 돌린다).

🚨 **원장(`data/manifest.jsonl`)은 git 으로 따라온다.** 그래서 「팀이 무엇을 받았나」는
새 기기에서도 맞고, 「이 기기에 있나」만 다르다 — `launcher.py inventory` 가 그 차이를 낸다.

## 1. 오늘 밤 선 것

| 층 | 상태 |
|---|---|
| 3층 판단규범 | 조문 본문 **2,216 노드** + 별표 산문 **300 노드** — 그전엔 **0** 이었다 |
| 4층 위험도 | 별표 파싱 **243행** + 자동 감사(D-98 개정) |
| 2층 적법라벨 | `product_fact` **1,250** (건기식 API 2종 · 중복 제거 후 신규 316) |
| 5층 반례 | 판례 84 · 재결례 120 |
| 제품 사실 | 화장품 원료 **53,088** (🚨 판정 경로 없음 — 산출만) |
| **RAG** | 청크 **2,585** · 임베딩 **2,585** · `vector(1024)` 실측 확인 |
| DB | source 30 · document 32 · dict_entry 536 · product_fact 1,250 · manifest 10,849 |
| API | FastAPI 기동 — `/` `/health` `/search`(부분일치) `/judge`(**501**) |

## 2. 🔴 지금 막고 있는 것 — 판정 둘

**골든셋 1,910행이 DB 밖에 서 있다.** `docs/ohb/결정요청_2026-09-09_밤_5건.md` ⑤.

- `split_t` 가 `('train','dev','test_holdout')` 인데 골든셋은 **`test_sentence`** 를 쓴다.
  `test_holdout` 은 `mfds_press` 홀드아웃이라 뜻이 다르다 — 접으면 두 평가가 한 칸에 섞인다.
- `violation_t` 는 `V0~V8` 인데 **뜻이 어디에도 없다.** 오늘 013453 [별표 1] 을 파싱해
  **조문이 대응표를 준다**는 것이 드러났다(제1~8호). 그런데 `후기_체험기_기만` 은 법에서
  **제5호의 목**이고 제8호(사행심·음란)는 우리 목록에 없다.

## 3. 소스 판정이 바뀐 것

| | |
|---|---|
| `foodsafety_admin_measure` | ❌ **미채택** — 웹 스크레이핑을 하려고 개인정보 예외를 만들려던 참에, 오픈API 가 같은 축을 **개인정보 없이** 준다는 것이 드러났다 |
| `kcia_guideline` | ⏸ **hold** — 「이용조건 없음 = 허락 없음」으로 판정해 놓고 `U1·U4 allow` 였다 |
| `foodsafety_ad_monitor` (I0500) | ⏸ **hold** — 우리 키·sample 키 둘 다 `ERROR-310`. **안내 페이지가 있다고 서비스가 있는 게 아니다** |
| `foodsafety_penalty_std` (I1660) | ✅ 520행. **가치 A → C** — `VILT_TYPE_NM` 이 우리 8종과 무관한 축이었다 |

**1층 실사례는 `ftc` 8,253 + `mfds_sanctions` 제8조 30 이 전부다.** 대체 경로 셋이 다 막혔고,
레지스트리가 이미 「1층의 실질 중심은 ftc 하나로 좁아진다」고 적어 둔 것이 확인됐다.

## 4. 게이트가 잡은 사고 여덟

마스킹 정책 10줄 소실(생성물 손편집) · 청크 297행 조용히 덮임(키 4중 충돌) ·
레지스트리 142ms/건(캐시 없음) · `.venv` 로 게이트 3분 · `require()` 가 `status` 미검사 ·
robots 를 낱말로 판단(두 번째) · 3층이 별표만으로 서 있었음 · 생성기 ↔ 훅 충돌

게이트 **42건** · 전체 스위트 **9초**.

## 5. 다음에 값이 큰 것 셋

1. **판정 둘**(§2) — 골든셋 1,910행이 여기 걸려 있다
2. **`/search` 벡터 전환** — 임베딩이 있으니 `embedding <=> %s::vector` SQL 한 줄
3. **판정 엔진**(LangGraph) — `/judge` 가 501 로 자리만 잡아 뒀다

## 6. 미수집 — 판정을 막는 것은 없다

`launcher.py status`(팀 축)와 `launcher.py inventory`(기기 축)가 낸다.
남은 것은 5·6층(코퍼스·페르소나) 13건과 판정 대기 2건이고 **판정 파이프라인을 막지 않는다.**
