-- 0020 · flag_t 에 `ND`(변경금지)를 더한다 (2026-09-25 · 팀장 판정 (가))
--
-- 왜 — 「2025 자주하는 질문집(화장품)」(mfds_cosmetic_faq_2025) PDF 표지에 공공누리 **제3유형(출처표시 · 변경금지)**
--      마크가 있다. 원문 그대로 색인 · 인용은 되지만 라벨 · 사전 · 학습 · 평가셋 같은 **파생 데이터셋은 안 된다**(D-110).
--      🚨 지금까지 이 금지를 적을 칸이 `U1: deny` 뿐이었고, 그 칸은 「평가 전용」 뜻으로도 쓰였다(D-155 사례집 —
--         deny 인데 금지 표현 사전으로 학습에 들어간다). 같은 표기가 두 뜻이면 다음 사람이 선례를 따라 틀린다.
--      ★ 그래서 법적 금지를 **플래그로** 따로 적는다. 집행은 `collect.registry.assert_derivable` (파생 산출물 쓰기 직전).
--
-- 🚨 0005 와 같이 **더하기만** 한다 — 같은 트랜잭션에서 이 값을 쓰지 않는다(적재는 `load_db` 가 다음 실행에서 한다).
--    값의 순서는 `db/schema.sql` 과 같아야 한다(`ADD VALUE` 는 끝에 붙는다) — `db-drift` 가 대조한다.

ALTER TYPE flag_t ADD VALUE IF NOT EXISTS 'ND';
